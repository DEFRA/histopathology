trigger:
  branches:
    include:
      - develop
      - main

parameters:
  - name: deployToDev
    displayName: Deploy to DEV
    type: boolean
    default: true

  - name: deployToTest
    displayName: Deploy to TST
    type: boolean
    default: false

  - name: deployToPreProd
    displayName: Deploy to Pre-Prod
    type: boolean
    default: false

  - name: runSonar
    displayName: Run SonarCloud analysis
    type: boolean
    default: false

  - name: sonarServiceConnection
    type: string
    default: 'SonarCloud-Histo'

  # - name: deployToProd
  #   displayName: Deploy to PROD
  #   type: boolean
  #   default: false

variables:
  - name: buildConfiguration
    value: 'Release'
    variables:
  - name: isDevelop
    value: $[eq(variables['Build.SourceBranch'], 'refs/heads/develop')]
  - name: isMain
    value: $[eq(variables['Build.SourceBranch'], 'refs/heads/main')]
  - name: solution
    value: 'HistopathologySystem.slnx'
  - name: webProject
    value: 'src/Histo.Web/Histo.Web.csproj'
  - name: databaseScriptsPath
    value: 'src/Database'

stages:
  - stage: Build
    displayName: Build and Test
    jobs:
      - job: build
        pool:
          vmImage: 'ubuntu-24.04'
        steps:
          - checkout: self
            fetchDepth: 0
            persistCredentials: false

          - task: UseDotNet@2
            displayName: 'Install .NET 10 SDK'
            inputs:
              version: '10.0.x'
              includePreviewVersions: true

          - ${{ if eq(parameters.runSonar, true) }}:
            - task: SonarCloudPrepare@4
              displayName: 'Prepare Sonar analysis'
              condition: ne(variables['Build.Reason'], 'PullRequest')
              inputs:
                SonarCloud: ${{ parameters.sonarServiceConnection }}
                organization: 'defra'
                scannerMode: 'dotnet'
                projectKey: 'DEFRA_histopathology'
                projectName: 'Histopathology'
                extraProperties: |
                  sonar.cs.opencover.reportsPaths=$(Agent.TempDirectory)/**/coverage.opencover.xml

          - script: |
              dotnet --info
              echo "--- files in working directory ---"
              ls -la
            displayName: 'Diagnostics'

          - script: dotnet restore $(solution)
            displayName: 'Restore dependencies'

          - script: dotnet build $(solution) --configuration $(buildConfiguration) --no-restore
            displayName: 'Build'

          - script: dotnet test $(solution) --configuration $(buildConfiguration) --no-build --logger trx --collect:"XPlat Code Coverage;Format=opencover" --results-directory $(Agent.TempDirectory)
            displayName: 'Run tests'

          - task: PublishTestResults@2
            displayName: 'Publish test results'
            condition: succeededOrFailed()
            inputs:
              testResultsFormat: VSTest
              testResultsFiles: '**/*.trx'
              searchFolder: '$(Agent.TempDirectory)'

          - task: DotNetCoreCLI@2
            displayName: 'Publish Histo.Web'
            inputs:
              command: publish
              publishWebProjects: false
              projects: '$(webProject)'
              arguments: '--configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)'
              zipAfterPublish: true

          - task: CopyFiles@2
            displayName: 'Stage SQL scripts'
            inputs:
              sourceFolder: 'src/Database'
              contents: '**/*.sql'
              targetFolder: '$(Build.ArtifactStagingDirectory)/sql'

          - bash: |
              set -euo pipefail

              # Detect whether any file under the database scripts path (src/Database —
              # covering Deploy/, Migrations/, StoredProcedures/) changed, so downstream
              # deployment stages can skip applying database scripts when there are no
              # script changes to deploy.
              if [ "$(Build.Reason)" = "PullRequest" ]; then
                DIFF_RANGE="origin/$(System.PullRequest.TargetBranch)...HEAD"
              else
                DIFF_RANGE="HEAD~1...HEAD"
              fi

              echo "Checking for database script changes using range: $DIFF_RANGE"

              if git diff --name-only $DIFF_RANGE -- $(databaseScriptsPath) | grep -q .; then
                echo "Database script changes detected under $(databaseScriptsPath):"
                git diff --name-only $DIFF_RANGE -- $(databaseScriptsPath)
                echo "##vso[task.setvariable variable=dbScriptsChanged;isOutput=true]true"
              else
                echo "No database script changes detected under $(databaseScriptsPath)."
                echo "##vso[task.setvariable variable=dbScriptsChanged;isOutput=true]false"
              fi
            name: detectDbChanges
            displayName: 'Detect database script changes'

          - publish: '$(Build.ArtifactStagingDirectory)'
            artifact: drop
            displayName: 'Publish artifact'

          - ${{ if eq(parameters.runSonar, true) }}:
            - task: SonarCloudAnalyze@4
              displayName: 'Run Sonar analysis'
              condition: and(succeededOrFailed(), ne(variables['Build.Reason'], 'PullRequest'))

            - task: SonarCloudPublish@4
              displayName: 'Publish quality gate result'
              condition: and(succeededOrFailed(), ne(variables['Build.Reason'], 'PullRequest'))
              inputs:
                pollingTimeoutSec: '300'

  - stage: DEV
    dependsOn: Build
    condition: and(succeeded(), eq(variables.isDevelop, true), eq('${{ parameters.deployToDev }}', true))
    variables:
    - name: appName
      value: 'DEVCDEWEBAW1401'
    - name: tenantId
      value: '770a2450-0227-4c62-90c7-4e38537f1102'
    - name: appId
      value: '89060d89-195d-47f4-a900-2f66fba81a75'
    - name: sqlServer
      value: 'devcdeinfsq1401.database.windows.net,1433'
    - name: sqlServerHost
      value: 'devcdeinfsq1401.database.windows.net'
    - name: dbName
      value: 'histopathology'      
    - name: dbScriptsChanged
      value: $[ stageDependencies.Build.build.outputs['detectDbChanges.dbScriptsChanged'] ]
    jobs:
      - deployment: deploy_dev
        environment: 'dev'
        pool:
          name: 'DEFRA-COMMON-UBUNTU-SSV3'
        strategy:
          runOnce:
            deploy:
              steps:
                - download: current
                  artifact: drop

                - script: |
                    if [ "$(dbScriptsChanged)" = "true" ]; then
                      echo "Database script changes detected — database deployment WILL be applied for DEV."
                    else
                      echo "No database script changes detected — database deployment WILL BE SKIPPED for DEV."
                    fi
                  displayName: 'Log database deployment decision'

                - task: AzureCLI@2
                  displayName: 'Apply database scripts'
                  condition: eq(variables['dbScriptsChanged'], 'true')
                  inputs:
                    azureSubscription: 'AZR-CDE-DEV1'
                    scriptType: bash
                    scriptLocation: inlineScript
                    inlineScript: |
                      set -euo pipefail

                      TOOLS="$(Agent.TempDirectory)/sqlcmd"
                      mkdir -p "$TOOLS"
                      curl -sSL --fail --max-time 60 -o "$TOOLS/sqlcmd.tar.bz2" \
                        https://github.com/microsoft/go-sqlcmd/releases/download/v1.8.2/sqlcmd-linux-amd64.tar.bz2
                      tar -xjf "$TOOLS/sqlcmd.tar.bz2" -C "$TOOLS"

                      cd "$(Pipeline.Workspace)/drop/sql/Deploy"
                      echo "Applying Deploy.sql"
                      "$TOOLS/sqlcmd" -S "$(sqlServerHost)" -d "$(dbName)" \
                        --authentication-method ActiveDirectoryAzCli \
                        -i "Deploy.sql" -b -t 0

                - task: AzureWebApp@1
                  inputs:
                    azureSubscription: 'AZR-CDE-DEV1'
                    appType: 'webAppLinux'
                    appName: '$(appName)'
                    package: '$(Pipeline.Workspace)/drop/*.zip'
                    runtimeStack: 'DOTNETCORE|10.0'
                    deploymentMethod: 'zipDeploy'
                    appSettings: >-
                      -Saml2__SPEntityId "https://dev-cde.azure.defra.cloud"
                      -Saml2__PublicOrigin "https://dev-cde.azure.defra.cloud"
                      -Saml2__IdPEntityId "https://sts.windows.net/$(tenantId)/"
                      -Saml2__IdPMetadataUrl "https://login.microsoftonline.com/$(tenantId)/federationmetadata/2007-06/federationmetadata.xml?appid=$(appId)"
                      -Authentication__Bypass "false"
                      -ConnectionStrings__HistologyDb "Server=tcp:$(sqlServer);Database=$(dbName);Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;Authentication=Active Directory Managed Identity;"


  - stage: TST
    dependsOn:
      - DEV
      - Build
    condition: and(in(dependencies.Build.result, 'Succeeded'), in(dependencies.DEV.result, 'Succeeded', 'Skipped'), eq(variables.isDevelop, true), eq('${{ parameters.deployToTest }}', true))
    variables:
    - name: appName
      value: 'TSTCDEWEBAW1401'
    - name: tenantId
      value: '770a2450-0227-4c62-90c7-4e38537f1102'
    - name: appId
      value: '2f7f564e-38a4-4cc7-aa92-0ee8c8cfc974'
    - name: sqlServer
      value: 'tstcdeinfsq1401.database.windows.net,1433'
    - name: sqlServerHost
      value: 'tstcdeinfsq1401.database.windows.net'
    - name: dbName
      value: 'histopathology'      
    - name: dbScriptsChanged
      value: $[ stageDependencies.Build.build.outputs['detectDbChanges.dbScriptsChanged'] ]
    jobs:
      - deployment: deploy_tst
        environment: 'test'
        pool:
          name: 'DEFRA-COMMON-UBUNTU-SSV3'
        strategy:
          runOnce:
            deploy:
              steps:
                - download: current
                  artifact: drop

                - script: |
                    if [ "$(dbScriptsChanged)" = "true" ]; then
                      echo "Database script changes detected — database deployment WILL be applied for TST."
                    else
                      echo "No database script changes detected — database deployment WILL BE SKIPPED for TST."
                    fi
                  displayName: 'Log database deployment decision'

                - task: AzureCLI@2
                  displayName: 'Apply database scripts'
                  condition: eq(variables['dbScriptsChanged'], 'true')
                  inputs:
                    azureSubscription: 'AZR-CDE-TST1'
                    scriptType: bash
                    scriptLocation: inlineScript
                    inlineScript: |
                      set -euo pipefail

                      TOOLS="$(Agent.TempDirectory)/sqlcmd"
                      mkdir -p "$TOOLS"
                      curl -sSL --fail --max-time 60 -o "$TOOLS/sqlcmd.tar.bz2" \
                        https://github.com/microsoft/go-sqlcmd/releases/download/v1.8.2/sqlcmd-linux-amd64.tar.bz2
                      tar -xjf "$TOOLS/sqlcmd.tar.bz2" -C "$TOOLS"

                      cd "$(Pipeline.Workspace)/drop/sql/Deploy"
                      echo "Applying Deploy.sql"
                      "$TOOLS/sqlcmd" -S "$(sqlServerHost)" -d "$(dbName)" \
                        --authentication-method ActiveDirectoryAzCli \
                        -i "Deploy.sql" -b -t 0

                - task: AzureWebApp@1
                  inputs:
                    azureSubscription: 'AZR-CDE-TST1'
                    appType: 'webAppLinux'
                    appName: '$(appName)'
                    package: '$(Pipeline.Workspace)/drop/*.zip'
                    runtimeStack: 'DOTNETCORE|10.0'
                    deploymentMethod: 'zipDeploy'
                    appSettings: >-
                      -Saml2__SPEntityId "https://tst-cde.azure.defra.cloud"
                      -Saml2__PublicOrigin "https://tst-cde.azure.defra.cloud"
                      -Saml2__IdPEntityId "https://sts.windows.net/$(tenantId)/"
                      -Saml2__IdPMetadataUrl "https://login.microsoftonline.com/$(tenantId)/federationmetadata/2007-06/federationmetadata.xml?appid=$(appId)"
                      -Authentication__Bypass "false"
                      -ConnectionStrings__HistologyDb "Server=tcp:$(sqlServer);Database=$(dbName);Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;Authentication=Active Directory Managed Identity;"

  - stage: PreProd
    dependsOn: Build
    condition: and(succeeded(), eq(variables.isMain, true), eq('${{ parameters.deployToPreProd }}', true))
    variables:
    - name: appName
      value: 'PRE-CDE-Web-App'
    - name: tenantId
      value: '770a2450-0227-4c62-90c7-4e38537f1102'
    - name: appId
      value: '__PREPROD_APP_ID__'
    - name: sqlServer
      value: 'precdeinfsq1401.database.windows.net,1433'
    - name: sqlServerHost
      value: 'precdeinfsq1401.database.windows.net'
    - name: dbName
      value: 'histopathology'
    - name: dbScriptsChanged
      value: $[ stageDependencies.Build.build.outputs['detectDbChanges.dbScriptsChanged'] ]
    jobs:
      - deployment: deploy_preprod
        environment: 'preprod'
        pool:
          name: 'DEFRA-COMMON-UBUNTU-SSV5'
        strategy:
          runOnce:
            deploy:
              steps:
                - download: current
                  artifact: drop

                - script: |
                    if [ "$(dbScriptsChanged)" = "true" ]; then
                      echo "Database script changes detected — database deployment WILL be applied for Pre-Prod."
                    else
                      echo "No database script changes detected — database deployment WILL BE SKIPPED for Pre-Prod."
                    fi
                  displayName: 'Log database deployment decision'

                - task: AzureCLI@2
                  displayName: 'Apply database scripts'
                  condition: eq(variables['dbScriptsChanged'], 'true')
                  inputs:
                    azureSubscription: 'AZR-CDE-PRE1'
                    scriptType: bash
                    scriptLocation: inlineScript
                    inlineScript: |
                      set -euo pipefail

                      TOOLS="$(Agent.TempDirectory)/sqlcmd"
                      mkdir -p "$TOOLS"
                      curl -sSL --fail --max-time 60 -o "$TOOLS/sqlcmd.tar.bz2" \
                        https://github.com/microsoft/go-sqlcmd/releases/download/v1.8.2/sqlcmd-linux-amd64.tar.bz2
                      tar -xjf "$TOOLS/sqlcmd.tar.bz2" -C "$TOOLS"

                      cd "$(Pipeline.Workspace)/drop/sql/Deploy"
                      echo "Applying Deploy.sql"
                      "$TOOLS/sqlcmd" -S "$(sqlServerHost)" -d "$(dbName)" \
                        --authentication-method ActiveDirectoryAzCli \
                        -i "Deploy.sql" -b -t 0

                - task: AzureWebApp@1
                  inputs:
                    azureSubscription: 'AZR-CDE-PRE1'
                    appType: 'webAppLinux'
                    appName: '$(appName)'
                    package: '$(Pipeline.Workspace)/drop/*.zip'
                    runtimeStack: 'DOTNETCORE|10.0'
                    deploymentMethod: 'zipDeploy'
                    appSettings: >-
                      -Saml2__SPEntityId "https://pre-cde.azure.defra.cloud"
                      -Saml2__PublicOrigin "https://pre-cde.azure.defra.cloud"
                      -Saml2__IdPEntityId "https://sts.windows.net/$(tenantId)/"
                      -Saml2__IdPMetadataUrl "https://login.microsoftonline.com/$(tenantId)/federationmetadata/2007-06/federationmetadata.xml?appid=$(appId)"
                      -Authentication__Bypass "false"
                      -ConnectionStrings__HistologyDb "Server=tcp:$(sqlServer);Database=$(dbName);Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;Authentication=Active Directory Managed Identity;"

  #- stage: Prod
  #  displayName: PROD
  # dependsOn:
  #    - PreProd
  #    - Build
  #  condition: and(succeeded(), eq(variables.isMain, true), eq('${{ parameters.deployToProd }}', true)))
  #   variables:
  #   - name: appName
  #     value: 'PRD-CDE-Web-App'
  #   - name: tenantId
  #     value: '770a2450-0227-4c62-90c7-4e38537f1102'
  #   - name: appId
  #     value: '__PROD_APP_ID__'
  #   - name: sqlServer
  #     value: 'prdcdeinfsq1401.database.windows.net,1433'
  #   - name: sqlServerHost
  #     value: 'prdcdeinfsq1401.database.windows.net'
  #   - name: dbName
  #     value: 'histopathology'
  #   - name: dbScriptsChanged
  #     value: $[ stageDependencies.Build.build.outputs['detectDbChanges.dbScriptsChanged'] ]
  #   jobs:
  #     - deployment: deploy_prod
  #       environment: 'prod'
  #       pool:
  #         name: 'DEFRA-COMMON-UBUNTU-SSV5'
  #       strategy:
  #         runOnce:
  #           deploy:
  #             steps:
  #               - download: current
  #                 artifact: drop
  #
  #               - script: |
  #                   if [ "$(dbScriptsChanged)" = "true" ]; then
  #                     echo "Database script changes detected — database deployment WILL be applied for PROD."
  #                   else
  #                     echo "No database script changes detected — database deployment WILL BE SKIPPED for PROD."
  #                   fi
  #                 displayName: 'Log database deployment decision'
  #
  #               - task: AzureCLI@2
  #                 displayName: 'Apply database scripts'
  #                 condition: eq(variables['dbScriptsChanged'], 'true')
  #                 inputs:
  #                   azureSubscription: 'AZR-CDE-PRD1'
  #                   scriptType: bash
  #                   scriptLocation: inlineScript
  #                   inlineScript: |
  #                     set -euo pipefail
  #
  #                     TOOLS="$(Agent.TempDirectory)/sqlcmd"
  #                     mkdir -p "$TOOLS"
  #                     curl -sSL --fail --max-time 60 -o "$TOOLS/sqlcmd.tar.bz2" \
  #                       https://github.com/microsoft/go-sqlcmd/releases/download/v1.8.2/sqlcmd-linux-amd64.tar.bz2
  #                     tar -xjf "$TOOLS/sqlcmd.tar.bz2" -C "$TOOLS"
  #
  #                     cd "$(Pipeline.Workspace)/drop/sql/Deploy"
  #                     echo "Applying Deploy.sql"
  #                     "$TOOLS/sqlcmd" -S "$(sqlServerHost)" -d "$(dbName)" \
  #                       --authentication-method ActiveDirectoryAzCli \
  #                       -i "Deploy.sql" -b -t 0
  #
  #               - task: AzureWebApp@1
  #                 inputs:
  #                   azureSubscription: 'AZR-CDE-PRD1'
  #                   appType: 'webAppLinux'
  #                   appName: '$(appName)'
  #                   package: '$(Pipeline.Workspace)/drop/*.zip'
  #                   runtimeStack: 'DOTNETCORE|10.0'
  #                   deploymentMethod: 'zipDeploy'
  #                   appSettings: >-
  #                     -Saml2__SPEntityId "https://cde.azure.defra.cloud"
  #                     -Saml2__PublicOrigin "https://cde.azure.defra.cloud"
  #                     -Saml2__IdPEntityId "https://sts.windows.net/$(tenantId)/"
  #                     -Saml2__IdPMetadataUrl "https://login.microsoftonline.com/$(tenantId)/federationmetadata/2007-06/federationmetadata.xml?appid=$(appId)"
  #                     -Authentication__Bypass "false"
  #                     -ConnectionStrings__HistologyDb "Server=tcp:$(sqlServer);Database=$(dbName);Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;Authentication=Active Directory Managed Identity;"
