@page
@model BatchesForDispatchModel
@{
    ViewData["Title"] = "Batches for Dispatch";
    ViewData["PageTitle"] = "Batches for Dispatch";
}

@if (!Model.Batches.Any())
{
    <p>No batches awaiting dispatch.</p>
}
else
{
    <table class="data-table">
        <thead>
            <tr><th>Batch No</th><th>Customer Ref</th><th>Completed Date</th><th>Action</th></tr>
        </thead>
        <tbody>
            @foreach (var b in Model.Batches)
            {
                <tr>
                    <td>@b.ID</td>
                    <td>@b.CustomerRef</td>
                    <td>@b.CompletedDate?.ToShortDateString()</td>
                    <td>
                        <form method="post">
                            <input type="hidden" name="batchId" value="@b.ID" />
                            <button type="submit" asp-page-handler="Select">Select</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
