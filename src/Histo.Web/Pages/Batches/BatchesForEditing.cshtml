@page
@model BatchesForEditingModel
@{
    ViewData["Title"] = "Batches for Editing";
    ViewData["PageTitle"] = "Batches for Editing";
}

@if (!Model.Batches.Any())
{
    <p>No batches currently in progress.</p>
}
else
{
    <table class="data-table">
        <thead>
            <tr><th>Batch No</th><th>Customer Ref</th><th>Received Date</th><th>Action</th></tr>
        </thead>
        <tbody>
            @foreach (var b in Model.Batches)
            {
                <tr>
                    <td>@b.ID</td>
                    <td>@b.CustomerRef</td>
                    <td>@b.ReceivedDate?.ToShortDateString()</td>
                    <td>
                        <form method="post">
                            <input type="hidden" name="batchId" value="@b.ID" />
                            <button type="submit" asp-page-handler="Select">Edit</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
