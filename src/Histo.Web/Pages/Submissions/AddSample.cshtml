@page
@model Histo.Web.Pages.Submissions.AddSampleModel
@{
    ViewData["Title"] = "Add sample";
    ViewData["PageTitle"] = "Add sample";
}

@section BeforeHeading {
    <a asp-page="/Submissions/BatchBlockSummary" asp-route-batchId="@Model.BatchId" class="govuk-back-link">Back</a>
}

@if (Model.ModelError is not null)
{
    <div class="govuk-error-summary" data-module="govuk-error-summary">
        <div role="alert">
            <h2 class="govuk-error-summary__title">There is a problem</h2>
            <div class="govuk-error-summary__body">
                <ul class="govuk-list govuk-error-summary__list"><li>@Model.ModelError</li></ul>
            </div>
        </div>
    </div>
}

<form method="post">
    <input type="hidden" asp-for="BatchId" />
    <input type="hidden" asp-for="BatchSubmissionId" />
    <div class="govuk-form-group">
        <label class="govuk-label" for="SenderRef">Sender reference</label>
        <input class="govuk-input" asp-for="SenderRef" required />
    </div>
@*     <div class="govuk-form-group">
        <div class="govuk-checkboxes" data-module="govuk-checkboxes">
            <div class="govuk-checkboxes__item">
                <input class="govuk-checkboxes__input" asp-for="IsNeuropath" type="checkbox" />
                <label class="govuk-label govuk-checkboxes__label" for="IsNeuropath">Neuropath</label>
            </div>
        </div>
    </div> *@
    <button type="submit" class="govuk-button" data-module="govuk-button">Add sample</button>
    <a asp-page="/Submissions/BatchBlockSummary" asp-route-batchId="@Model.BatchId" class="govuk-button govuk-button--secondary" data-module="govuk-button">Cancel</a>
</form>

@* Contextual lookup, pre-filled with the sender ref entered above, replacing the need to leave
   this task and navigate to the standalone Search menu. *@
<form method="get" asp-page="/Search/ViewImportedData"
      onsubmit="document.getElementById('icc-filter').value = document.getElementById('SenderRef').value;">
    <input type="hidden" id="icc-filter" name="Filter" value="" />
    <button type="submit" class="govuk-button govuk-button--secondary" data-module="govuk-button">Check historical data for this sender ref</button>
</form>
