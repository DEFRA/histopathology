# Histopathology System — Migration Agent Run Journal

**Project:** Histopathology System — VB.NET ASP.NET WebForms → C# .NET 10 + Razor Pages  
**Started:** 2026-07-27  
**Assessment ref:** [migration-assessment-net10.md](../.github/docs/migration-assessment-net10.md)

---

## How to Use This Journal

1. **Before each agent run** — note the current time, create a new row in [Run Log](#run-log) with `In Progress` status, and record the **Start Time** in [Session Metrics](#session-metrics).
2. **After each agent run** — note the finish time, update Run Log status, fill in **End Time** and **Duration** in Session Metrics, and link to the agent's output report.
3. **Any issue that is not auto-fixed** — add a row to [Open Issues](#open-issues).
4. **Any architectural or config decision made** — add a row to [Decision Log](#decision-log).

> **Duration tip:** Use the PowerShell one-liner in [Session Metrics](#session-metrics) — run it before starting the agent, then re-run after it finishes to print elapsed minutes automatically.

---

## Phase Tracker

| Phase | Description | Agents | Status | Started | Completed |
|-------|-------------|--------|--------|---------|-----------|
| 0 | Foundation & Safety Net | `testing`, `documentation`, `intelligent-migration`, `requirements-to-scrum-board`, `azure-infra-analyser` | In Progress | 2026-07-27 | — |
| 1 | Authentication Migration | `identity-migration` | **In Progress** | 2026-07-27 | — |
| 2 | Reporting Migration | `pdf-report-modernizer`, `pdf-reference-generator`, `pdf-validation` | Not Started | — | — |
| 3 | Platform Migration (VB→C#/.NET 10) | `dotnet-code-refactor`, `modernise-to-modular-monolith` | In Progress | 2026-07-27 | — |
| 4 | **Domain + Repository Modules** | `implementation` | **Complete** | 2026-07-29 | 2026-07-29 |
| 5 | UI Migration (Phase 5) | `ui-implementation` | **Complete** (58/58 non-reporting-blocked pages; 3 pages blocked on Phase 2) | 2026-07-30 | 2026-08-03 |
| 6 | Testing & Cutover | `dotnet-test-automation-and-quality-agent`, `playwright-tester-agent`, `testing` | Not Started | — | — |

---

## Run Log

> One row per agent invocation. Add rows chronologically.

| # | Date | Phase | Agent | Scope / Input | Status | Issues Found | Fixes Applied | Output / Linked Report |
|---|------|-------|-------|---------------|--------|--------------|---------------|------------------------|
| 1 | _(yyyy-MM-dd)_ | 0 | `pdf-reference-generator` | Baseline Crystal Reports PDFs before any code changes | Not Started | — | — | — |
| 2 | _(yyyy-MM-dd)_ | 0 | `dotnet-code-refactor` | Pre-conversion cleanup across all 185 VB files | Not Started | — | — | — |
| 3 | 2025-07-27 | 0 | `testing` | Baseline xUnit tests for `Histo.Core` domain layer — 8 testable logic areas discovered in `clsAnimal`, `clsBatch`, `clsBlock`, `Common.vb` | Done | ISS-008 resolved; test coverage limited to pure-logic layer (no DB until Phase 1; no auth until Phase 2) | Created `src/Histo.Core/`, `tests/Histo.Tests/`, `.github/workflows/ci.yml` — see outputs | [docs/Test-Strategy.md](/docs/Test-Strategy.md), [tests/Histo.Tests/](/tests/Histo.Tests/), [.github/workflows/ci.yml](/.github/workflows/ci.yml) |
| 4 | 2026-07-27 | 0 | `documentation` | HLD, LLD, 4 ADRs, Runbook — full codebase discovery across 3 projects, 185 VB files | Done | Plaintext SQL credential in Web.config (see ISS-006); no automated tests exist; Crystal Reports GAC-only dependency confirmed | — (documentation only — no code changes) | [docs/HLD.md](/docs/HLD.md), [docs/LLD.md](/docs/LLD.md), [docs/ADR/](/docs/ADR/), [docs/Runbook.md](/docs/Runbook.md) |
| 5 | 2026-07-27 | 0 | `intelligent-migration` | Programme governance — Intelligent-Migration-Plan, Intelligent-Team-Model, Risk-and-Governance, ROI-and-Budget; anchored to Chaos Report failure patterns; R-001–R-014 risk register; £498K indicative cost model with 42% AI uplift assumption | Done | R-001 (ISS-009 severity confirmed High/High); R-010 key-person risk raised; R-011 Azure admin dependency as critical path blocker | — (documentation only — no code changes) | [docs/Intelligent-Migration-Plan.md](/docs/Intelligent-Migration-Plan.md), [docs/Intelligent-Team-Model.md](/docs/Intelligent-Team-Model.md), [docs/Risk-and-Governance.md](/docs/Risk-and-Governance.md), [docs/ROI-and-Budget.md](/docs/ROI-and-Budget.md) |
| 6 | _(yyyy-MM-dd)_ | 0 | `requirements-to-scrum-board` | Convert assessment → Azure DevOps / Jira backlog CSV | Not Started | — | — | — |
| 7 | _(yyyy-MM-dd)_ | 0 | `azure-infra-analyser` | Config analysis → `infra-analysis.json` | Not Started | — | — | — |
| 8 | 2026-07-27 | 1 | `implementation` | Phase 1 — Solution Scaffold + Infrastructure Module: `HistopathologySystem.slnx`, `src/Histo.Infrastructure/` (IDbConnectionFactory, SqlConnectionFactory, IAppLogger, AppLogger, AppOptions), `src/Histo.Web/` stub (Program.cs, health endpoint, _Layout.cshtml, appsettings.json), 6 domain module stubs (Submissions, Histology, QC, Reporting, AuditLog, Administration), 8 new tests (SqlConnectionFactoryTests, AppOptionsTests) | Done | No issues — all 78 tests pass; `Histo.Web` and `Histo.Infrastructure` build green | Created all `src/` project stubs under existing `src/` folder per user instruction | [src/Histo.Infrastructure/](/src/Histo.Infrastructure/), [src/Histo.Web/](/src/Histo.Web/), [HistopathologySystem.slnx](/HistopathologySystem.slnx) |
| 25 | 2026-07-29 | 4 | `implementation` | **Phase 4 — Domain and Repository Modules (all 5 sub-phases):** Sub-phase 4.1 `Histo.Administration` (User model, IUserRepository/LookupRepository + Dapper impls, UserService/LookupService); Sub-phase 4.2 `Histo.AuditLog` (AuditLogEntry, IAuditLogRepository + multi-SP fan-out impl, AuditLogService); Sub-phase 4.3 `Histo.QualityControl` (QCNote + rowstamp concurrency, IQCNoteRepository + return-value concurrency detection, QCNoteService with QCNoteConcurrencyException propagation); Sub-phase 4.4 `Histo.Histology` (Block + BlockStatus, HistologyRef, IBlockRepository/IHistologyRepository + Dapper impls, BlockService/HistologyRefService with BlockHelpers delegation); Sub-phase 4.5 `Histo.Submissions` (Batch, Animal, BatchSubmission, Tissue + TissueOwner, DomainExceptions, IBatchRepository/ISubmissionRepository + Dapper impls, BatchService/SubmissionService with PG-number auto-reversal via AnimalHelpers); 10 new unit tests added (SubmissionServiceAnimalTests, QCNoteServiceTests, DomainExceptionTests); Moq added to test project | **Done** | No issues — build succeeded 0 warnings/errors; 88 tests pass (up from 78), 0 failures, 1 skipped (integration) | All 5 module `.csproj` files upgraded with Dapper + Microsoft.Data.SqlClient; test project wired to all 5 new modules | [src/Histo.Administration/](/src/Histo.Administration/), [src/Histo.AuditLog/](/src/Histo.AuditLog/), [src/Histo.QualityControl/](/src/Histo.QualityControl/), [src/Histo.Histology/](/src/Histo.Histology/), [src/Histo.Submissions/](/src/Histo.Submissions/) |
| 26 | 2026-07-30 | 5 | `ui-implementation` | **Phase 5 — UI Migration (WebForms → Razor Pages), Session 2 (resumed):** Completed Batch 3 CRUD pages (EditQCNote, BatchDetails, EditBatch, ReceiveBatch, BatchesForArchiving), Batch 4 complex workflow pages (AddSubmission, ViewSamples, BlockDetails, ArchiveBlocks, ArchiveTissues, BookHistologyRef, BookBlockRef), Batch 5 admin pages (UserMaintenance, PickListMaintenance, Error/Shared); wired all domain service + repository DI registrations and `AddSession`/`UseSession` in `Program.cs`; fixed 26 build errors (nullable session IDs, BatchStatus namespace, HistologyRef.Ref property name, Activity import, non-record Batch `with` expression) | **Done** | 26 build errors fixed (all resolved); build succeeded 0 warnings/errors; 88 tests pass, 0 failures | `Program.cs` updated with full DI wiring; all 5 Phase 4 project references active; `app.UseSession()` added to middleware pipeline | [src/Histo.Web/Pages/](/src/Histo.Web/Pages/), [src/Histo.Web/Program.cs](/src/Histo.Web/Program.cs) |
| 64 | 2026-08-06 | 5 | `GitHub Copilot` | **Search/navigation behaviour audit — Receive Submission, Edit Submission Status, Entry Quality Data, QC Notes (4 modules):** Full systematic comparison of grid loading, search field wiring, Go/Check button event handling, navigation targets, and record retrieval for all four modules. Read all legacy ASPX/VB code-behind files (`BatchesNotReceived.aspx.vb`, `BatchesForEditing.aspx.vb`, `BatchesForDispatch.aspx.vb`, `QCNotes.aspx.vb`, `QCNotes.aspx`) and all modern Razor Pages counterparts (`BatchesNotReceived.cshtml(.cs)`, `BatchesForEditing.cshtml(.cs)`, `BatchesForDispatch.cshtml(.cs)`, `QCNotes.cshtml(.cs)`, `EditQCNote.cshtml`, `QCNote.cs`, `BatchService.cs`, `QCNoteService.cs`). **Findings: 4 issues raised (ISS-042 through ISS-045).** **(1) ISS-042 — Quick-Go handler missing status/existence validation (all 3 batch-selection modules):** Legacy `btnGo_Click` calls `CheckBatchExists` with a specific status before navigating; modern `OnPostGoAsync` unconditionally sets `Session.BatchID` and redirects with no database check and no inline error message. **(2) ISS-043 — QCNotes Edit button passes wrong key (n.ID = Submission Number instead of n.QCNoteRef):** Every grid-row Edit click navigates to the wrong QC note record or silently redirects back. **(3) ISS-044 — BatchesForEditing grid scope mismatch:** Legacy loads all-status batches (`GetBatchesWithStatus(0)`); modern loads only in-progress (`GetInProgressAsync()`). **(4) ISS-045 — QCNotes page scope changed from global to batch-scoped:** Legacy `QCNotes.aspx` loaded all QC notes system-wide; modern requires `Session.BatchID`. No code changes — analysis and issue registration only. | **Done** | ISS-042 raised (Quick-Go no validation — 3 modules); ISS-043 raised (QCNotes Edit button wrong key — critical bug); ISS-044 raised (BatchesForEditing scope too narrow); ISS-045 raised (QCNotes global → batch-scoped change) | — (analysis only — no code changes in this run) | [src/Histo.Web/Pages/Batches/BatchesNotReceived.cshtml.cs](../src/Histo.Web/Pages/Batches/BatchesNotReceived.cshtml.cs), [src/Histo.Web/Pages/Batches/BatchesForEditing.cshtml.cs](../src/Histo.Web/Pages/Batches/BatchesForEditing.cshtml.cs), [src/Histo.Web/Pages/Batches/BatchesForDispatch.cshtml.cs](../src/Histo.Web/Pages/Batches/BatchesForDispatch.cshtml.cs), [src/Histo.Web/Pages/QC/QCNotes.cshtml](../src/Histo.Web/Pages/QC/QCNotes.cshtml) |
| 65 | 2026-08-06 | 5 | `GitHub Copilot` | **ISS-042, ISS-043, ISS-044, ISS-045 resolved — Quick-Go validation, QCNotes Edit key, BatchesForEditing scope, QCNotes global fallback:** **(1) ISS-043 (Critical):** Fixed `QCNotes.cshtml` `value="@n.ID"` → `value="@n.QCNoteRef"` in per-row Edit form — single-line fix, all QC note editing now navigates to the correct note. **(2) ISS-042 (High):** Rewrote `OnPostGoAsync` in all 3 batch page models as `async Task<IActionResult>` with DB-backed validation: `BatchesNotReceived` checks `BatchStatus.Submitted`; `BatchesForEditing` checks existence only; `BatchesForDispatch` checks `InProgress` OR `Received`. On failure, sets `GoError` property and returns `Page()`; added `govuk-error-summary` panel to all 3 Razor templates. **(3) ISS-044 (High):** Added `GetAllBatchesAsync()` full-stack (`IBatchRepository` → `BatchRepository` calling `GetAllBatches` SP → `BatchService` passthrough); changed `BatchesForEditingModel.OnGetAsync()` from `GetInProgressAsync()` to `GetAllBatchesAsync()` — now shows all submissions of any status, matching legacy `GetBatchesWithStatus(0)` behaviour. **(4) ISS-045 (Medium):** Added `GetAllAsync()` full-stack (`IQCNoteRepository` → `QCNoteRepository` calling `GetBatchQCNotes` with no ID param → `QCNoteService` passthrough); added `IsGlobalView` bool to `QCNotesModel` (`true` when `Session.BatchID` is null/0); `OnGetAsync` routes to `GetAllAsync()` (global) or `GetBySubmissionAsync()` (batch) accordingly; `QCNotes.cshtml` shows contextual heading ("Batch No: X" or "Showing all QC notes (no batch selected)"). Build: 0 errors, 0 warnings. Tests: 90 pass, 1 skipped, 0 fail. | **Done** | ISS-042, ISS-043, ISS-044, ISS-045 all resolved | `IQCNoteRepository.cs` (+`GetAllAsync`), `QCNoteRepository.cs` (+implementation), `QCNoteService.cs` (+`GetAllAsync` passthrough), `IBatchRepository.cs` (+`GetAllBatchesAsync`), `BatchRepository.cs` (+implementation), `BatchService.cs` (+`GetAllBatchesAsync` passthrough), `BatchesNotReceived.cshtml.cs` (async `OnPostGoAsync` + `GoError`), `BatchesForEditing.cshtml.cs` (async `OnPostGoAsync` + `GoError` + `GetAllBatchesAsync`), `BatchesForDispatch.cshtml.cs` (async `OnPostGoAsync` + status check + `GoError`), `QCNotes.cshtml.cs` (`IsGlobalView` + `GetAllAsync` fallback), `BatchesNotReceived.cshtml` (error summary), `BatchesForEditing.cshtml` (error summary), `BatchesForDispatch.cshtml` (error summary), `QCNotes.cshtml` (Edit key fix + global/batch heading). | [src/Histo.Web/Pages/QC/QCNotes.cshtml](../src/Histo.Web/Pages/QC/QCNotes.cshtml), [src/Histo.Web/Pages/Batches/BatchesForEditing.cshtml.cs](../src/Histo.Web/Pages/Batches/BatchesForEditing.cshtml.cs) |
| 67 | 2026-08-07 | 5 | `GitHub Copilot` | **Help Pages investigated and implemented (GDS-compliant) — root-cause analysis + 3 new files + nav update:** Legacy help was delivered as two static `.htm` files (`HistoHelp_CustomerGroup.htm` and `HistoHelp_HistoGroup.htm`) linked from `VLAHeader.ascx` via `lnkHelp` (Target="_blank"). These were outside the `.aspx` page-by-page migration scope and the help link was never ported to `_Layout.cshtml` or `_NavPartial.cshtml`. **Root cause:** `.htm` files are not `.aspx` pages — invisible to the migration page inventory; header Help link dropped when `VLAHeader.ascx` was replaced by the GDS service navigation partial. **Files created:** (1) `src/Histo.Web/Pages/Help/Index.cshtml` — single GDS-compliant help page serving role-sensitive content: Customer group sees 15 sections (General, Saving Data, Home Screen, Submission workflow, View Submissions, Copy Submission, Search PM Dates, View Samples, Submission Status); Histopathology User/Maintenance group sees all 37 sections including Receive Submission, Quality Data, Archive, QC Notes, Search Submissions, Booking, Edit Submission Status, User/Audit/Pick List Maintenance, View Old ICC_Sub Data. All legacy VLA image-based toolbar icon descriptions modernised to GDS plain-English equivalents. Anchor IDs match legacy `#PageName` pattern for context-sensitive deep-linking. Table of contents with numbered anchors. GDS `govuk-heading-l`, `govuk-heading-m`, `govuk-body`, `govuk-list`, `govuk-summary-list` throughout. (2) `src/Histo.Web/Pages/Help/Index.cshtml.cs` — `HelpModel : HistoPageModel` (session-gated; `OnGet` only). **File modified:** (3) `src/Histo.Web/Pages/Shared/_NavPartial.cshtml` — added `Help` link at the bottom of the nav bar, available to all groups. Build: 0 errors, 0 warnings. Tests: 90 pass, 1 skipped, 0 fail. | **Done** | No new ISS raised — Help pages were a confirmed migration gap, not a regression. | `src/Histo.Web/Pages/Help/Index.cshtml` (created), `src/Histo.Web/Pages/Help/Index.cshtml.cs` (created), `src/Histo.Web/Pages/Shared/_NavPartial.cshtml` (Help link added). | [src/Histo.Web/Pages/Help/Index.cshtml](../src/Histo.Web/Pages/Help/Index.cshtml), [src/Histo.Web/Pages/Shared/_NavPartial.cshtml](../src/Histo.Web/Pages/Shared/_NavPartial.cshtml) |
| 68 | 2026-08-10 | 3 | `GitHub Copilot` | **Modular monolith architecture — IService interface boundaries and DI module registrations:** Implemented the recommended modular monolith pattern across all 5 feature modules. Created 9 public `IService` interface contracts: `IUserService`, `ILookupService` (`Histo.Administration`); `IBatchService`, `ISubmissionService` (`Histo.Submissions`); `IBlockService`, `IHistologyRefService`, `IBlockTestService` (`Histo.Histology`); `IAuditLogService` (`Histo.AuditLog`); `IQCNoteService` (`Histo.QualityControl`). Created 5 `XxxModule.cs` DI extension files — each exposing a single `AddXxxModule(IServiceCollection)` extension method wiring all internal repositories and services for that module: `AdministrationModule`, `SubmissionsModule`, `HistologyModule`, `AuditLogModule`, `QualityControlModule`. All 9 service classes updated to implement their respective `IXxxService` interface. Added `Microsoft.Extensions.DependencyInjection.Abstractions v9.0.0` to all 5 feature module `.csproj` files. Refactored `Histo.Web/Program.cs` from 25 individual `AddScoped` calls (with 15 module-internal `using` imports) to 5 module extension method calls (with 5 namespace-level `using` imports). Updated 40+ Razor Page model constructors and field declarations across all 10 `Pages/` subfolders to inject `IXxxService` interfaces instead of concrete service types. Build: 0 errors. Tests: 90 pass, 1 skipped. | **Done** | CS0535 — `UpdateSubmissionAsync` declared in `ISubmissionService` but not implemented in `SubmissionService` (method exists only on repository; no Razor Page calls it) | Removed `UpdateSubmissionAsync` from `ISubmissionService.cs` to resolve CS0535 | `Histo.Administration/Interfaces/IUserService.cs` (new), `ILookupService.cs` (new), `AdministrationModule.cs` (new); `Histo.Submissions/Interfaces/IBatchService.cs` (new), `ISubmissionService.cs` (new), `SubmissionsModule.cs` (new); `Histo.Histology/Interfaces/IBlockService.cs` (new), `IHistologyRefService.cs` (new), `IBlockTestService.cs` (new), `HistologyModule.cs` (new); `Histo.AuditLog/Interfaces/IAuditLogService.cs` (new), `AuditLogModule.cs` (new); `Histo.QualityControl/Interfaces/IQCNoteService.cs` (new), `QualityControlModule.cs` (new); all 9 service classes (`: IXxxService` added); all 5 feature `.csproj` files (`+DI.Abstractions v9.0.0`); `Histo.Web/Program.cs` (25→5 module calls); 40+ Razor Pages (concrete → interface injection). |
| 66 | 2026-08-07 | 5 | `GitHub Copilot` | **Manual NTLogin login bridge implemented (ADR-006) — pre-Entra-ID authentication gate:** Root cause: `Environment.UserDomainName` returns `WORKGROUP` or machine name on non-domain-joined developer machines; `HttpContext.User.Identity.Name` is always empty under Kestrel because Windows Authentication is not active until Entra ID Phase 2. **ADR-006 created** (`docs/ADR/ADR-006-manual-login-page-bridge.md`) recording the bridge decision, decommission trigger (Phase B SAML ACS endpoint), files to delete, and `// BRIDGE (ADR-006):` marker strategy for `grep -r "BRIDGE (ADR-006)" src/` discovery. **HistoPageModel.cs simplified** — removed all auto-NT-detection logic (`Environment.UserDomainName`, domain-prefix stripping, `UserService` call, `AccessDenied` redirect); replaced with a pure 4-line session gate: `if (string.IsNullOrEmpty(Session.GroupName)) → RedirectToPage("/Login")`. **Login.cshtml created** — GDS-compliant NTLogin entry form with `govuk-error-summary` and inline `govuk-input--error` state; `Layout = null` (standalone, no HistoPageModel gate); `BRIDGE (ADR-006):` comment in template. **Login.cshtml.cs created** — `LoginModel : PageModel` (not HistoPageModel — prevents redirect loop); `OnGet` redirects to `/Index` if already signed in; `OnPostAsync` strips domain prefix, calls `UserService.ResolveUserAsync`, on success calls `Session.PopulateFromUser` + redirects to `/Index`, on failure redisplays form with error message. Build: 0 errors, 0 warnings. Tests: 90 pass, 1 skipped, 0 fail. | **Done** | No new ISS raised. | `docs/ADR/ADR-006-manual-login-page-bridge.md` (created), `src/Histo.Web/Pages/HistoPageModel.cs` (simplified to session gate only), `src/Histo.Web/Pages/Login.cshtml` (created), `src/Histo.Web/Pages/Login.cshtml.cs` (created). | [docs/ADR/ADR-006-manual-login-page-bridge.md](../docs/ADR/ADR-006-manual-login-page-bridge.md), [src/Histo.Web/Pages/Login.cshtml](../src/Histo.Web/Pages/Login.cshtml) |
| 63 | 2026-08-06 | 5 | `GitHub Copilot` | **Booking/Archive/nav naming parity fixes — 5 files corrected to match legacy labels:** **(1) BookingMenu.cshtml:** Restored "Book Non-PG histology ref" (was "Book histology reference" — "Non-PG" qualifier was silently dropped by the migration agent; the qualifier is functional, not cosmetic, as it signals that PG-numbered sender refs are excluded from a booking range); restored "Book blocks" (was "Book block reference"); removed "Edit histology references" link (not present in legacy `BookingMenu.aspx` — the pool-level histology ref editor was added to the menu in Run #21 but had no legacy booking-menu precedent). **(2) BookHistologyRef.cshtml:** Page title corrected from "Book histology reference" to "Book Non-PG histology ref" to match the restored menu label and the legacy internal title ("Block Book Histology Refs"). **(3) BookBlockRef.cshtml:** Page title corrected from "Book block reference" to "Book blocks" matching the restored menu label and the legacy internal title ("Book Blocks"). **(4) ArchiveMenu.cshtml:** Removed "Batches for archiving" link — `BatchesForArchiving.aspx` was never under the legacy `ArchiveMenu.aspx`; it was a direct home page link labelled "Archive Submission". The page remains accessible from the home page "Archive batches" link in the Laboratory panel. **(5) _NavPartial.cshtml:** Three nav bar labels aligned with legacy home page labels and the home page (`Index.cshtml`): "Edit batches" → "Edit submission status" (legacy: "Edit Submission Status"); "Dispatch batches" → "Enter quality data" (legacy: "Enter Quality Data"); "Quality data" → "QC notes" (was pointing to `QCNotes` while the home page uses "QC notes" for the same page — nav and home page were inconsistent within the new app). Build: 0 errors, 0 warnings. Tests: 90 pass, 1 skipped, 0 fail. | **Done** | No new ISS raised — all changes are label/link parity corrections, not functional gaps. "Edit histology references" removed from booking menu (was never a legacy menu item — ISS-021 added it without menu precedent; the page itself at `/Bookings/EditHistologyRef` is retained). | `BookingMenu.cshtml` (2 label restores + 1 link removed), `BookHistologyRef.cshtml` (title), `BookBlockRef.cshtml` (title), `ArchiveMenu.cshtml` (1 link removed), `_NavPartial.cshtml` (3 label fixes). | [src/Histo.Web/Pages/Bookings/BookingMenu.cshtml](../src/Histo.Web/Pages/Bookings/BookingMenu.cshtml), [src/Histo.Web/Pages/Archive/ArchiveMenu.cshtml](../src/Histo.Web/Pages/Archive/ArchiveMenu.cshtml), [src/Histo.Web/Pages/Shared/_NavPartial.cshtml](../src/Histo.Web/Pages/Shared/_NavPartial.cshtml) |
| 62 | 2026-08-05 | 5 | `GitHub Copilot` | **Full page review — ISS-038, ISS-039, ISS-040 resolved (field mapping, dropdown values, archiving list):** **(1) ISS-038 — EditQualityDataTest Code-keyed dropdown values:** Tables 14 (QC Codes), 15 (Remedial Actions), 16 (Archive Locations) use a `Code` string column as the value key, not integer `ID`. Added `Code string?` property to `LookupItem`; extended `MapLookupItem` to read the `Code` column; changed `EditQualityDataTest.cshtml` dropdown `value` from `@c.ID` (always 0 for Code-keyed tables) to `@(c.Code ?? c.Name)`. **(2) ISS-039 — BatchDetails missing legacy display fields:** `Batch` model lacked `BatchDate`, `ProjectDescription`, `ContactDescription`, `Species`, `FixationDescription` — Dapper auto-maps from `GetCommonBatchTablesByID` SP result. Added all 5 properties; added Date Submitted, Project/contract code, Pathologist, Species, Fixation, and Status Comments rows to `BatchDetails.cshtml` summary list. **(3) ISS-040 — BatchesForArchiving always empty:** Page model had a TODO placeholder `Batches = []` with no service call. Added `GetCompletedAsync()` → `GetCompletedBatches` SP chain through `IBatchRepository`/`BatchRepository`/`BatchService`; changed page model property type from `IReadOnlyList<Batch>` to `IReadOnlyList<BatchListResult>`; updated `BatchesForArchiving.cshtml` columns from 4 (ID, CustomerRef, CompletedDate, Actions) to 7 (ID, Project, Pathologist, Species, CustomerRef, CompletedDate, Actions). Also persisted `StatusComments` in `BatchRepository.UpdateAsync`. Build: 0 errors, 0 warnings. Tests: 90 pass, 1 skipped, 0 fail. | **Done** | ISS-038, ISS-039, ISS-040 resolved | `Histo.Administration/Models/LookupItem.cs` (+`Code`), `LookupRepository.cs` (`MapLookupItem` +Code), `EditQualityDataTest.cshtml` (dropdown value fix), `BatchRepository.cs` (`UpdateAsync` +StatusComments, +`GetCompletedAsync`), `Histo.Submissions/Models/Batch.cs` (+5 fields), `BatchDetails.cshtml` (+6 summary rows), `Histo.Submissions/Interfaces/IBatchRepository.cs` (+`GetCompletedAsync`), `BatchService.cs` (+`GetCompletedAsync`), `BatchesForArchiving.cshtml.cs` (type + service wiring), `BatchesForArchiving.cshtml` (+3 columns). | [src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml](../src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml), [src/Histo.Web/Pages/Batches/BatchesForArchiving.cshtml](../src/Histo.Web/Pages/Batches/BatchesForArchiving.cshtml) |
| 61 | 2026-08-05 | 5 | `GitHub Copilot` | **ISS-036, ISS-037, ISS-041 resolved — ViewSubmissions Contact/Project dropdowns, EditBatch Status field, home page navigation alignment:** **(1) ISS-036 — ViewSubmissions Contact/Project/Fixation dropdowns blank:** ISS-030 converted fields to `govuk-select` but options rendered blank. `GetContactsByAreaAsync`/`GetProjectsByAreaAsync`/`GetImportedTablesAsync` used direct `QueryAsync<LookupItem>` — the SPs for Contacts (table 18), Projects (table 19), Fixation (table 10) return `Description`/`IsActive` column names, not `Name`/`Active`. Extended `MapLookupItem(dynamic r)` helper to handle both `Name`/`Description` and both `IsActive`/`Active` column name variants; applied to all three methods. **(2) ISS-037 — EditBatch Status field missing:** `EditBatch.cshtml` only had CustomerRef, Comments, IsPreCassetted — the primary legacy purpose (changing submission status) was entirely absent. Fully rewrote `EditBatch.cshtml(.cs)`: Status `govuk-select` (all 6 statuses), StatusComments `govuk-textarea`, OriginalStatus hidden field for server-side validation rules (can't change to Received via this page; can't set In Progress if currently Submitted), saves via both `UpdateAsync` and `UpdateStatusAsync`. Added `BatchService.UpdateAsync(Batch, int)`. Added `StatusComments string?` property to `Batch` model. **(3) ISS-041 — Home page navigation misaligned:** Legacy had "Assign Tissues to Blocks", "Edit Submission Status", "Enter Quality Data" in the Laboratory panel. New app had only "Edit batches" and "Dispatch batches" with no "Assign tissues" link. Updated `Index.cshtml`: renamed "Edit batches" → "Edit submission status", "Dispatch batches" → "Enter quality data", added "Assign tissues to blocks" → `/Batches/BatchesReceived`. Renamed headings in `BatchesForEditing.cshtml` and `BatchesForDispatch.cshtml`; added "Assign blocks" button and renamed links in `BatchDetails.cshtml`. Build: 0 errors, 0 warnings. Tests: 90 pass, 1 skipped, 0 fail. | **Done** | ISS-036, ISS-037, ISS-041 resolved | `LookupRepository.cs` (`MapLookupItem` extended), `EditBatch.cshtml.cs` (full rewrite — Status, StatusComments, OriginalStatus, validation), `EditBatch.cshtml` (Status dropdown + StatusComments), `BatchService.cs` (+`UpdateAsync`), `Histo.Submissions/Models/Batch.cs` (+`StatusComments`), `Index.cshtml` (nav labels + "Assign tissues" link), `BatchDetails.cshtml` (+Assign blocks button, renamed links), `BatchesForEditing.cshtml` (title), `BatchesForDispatch.cshtml` (title). | [src/Histo.Web/Pages/Batches/EditBatch.cshtml](../src/Histo.Web/Pages/Batches/EditBatch.cshtml), [src/Histo.Web/Pages/Index.cshtml](../src/Histo.Web/Pages/Index.cshtml) |
| 57 | 2026-08-04 | 5 | `ui-implementation` | **ISS-030 resolved — ViewSubmissions: 4 lookup dropdowns, Clear Search, remove SubmittedArea, 3 missing grid columns:** (1) Added `GetSpeciesLookupAsync()` to `ILookupRepository`/`LookupRepository`/`LookupService` — calls `GetSpeciesLookup` SP with custom `dynamic` mapper (`SpeciesID`→`ID`, `Species`→`Name`). (2) Added `DateReceived`, `DateCompleted`, `CustomerReceivedDate` (all `DateTime?`) to `BatchSearchResult`. (3) Rewrote `ViewSubmissions.cshtml.cs` — added `LookupService` dependency; added 4 lookup collections (`Projects`, `Contacts`, `SpeciesList`, `Fixations`) loaded via `LoadLookupsAsync()`; removed `SubmittedArea` bind property and criteria assignment; extracted `LoadLookupsAsync()` helper to avoid duplication between GET and POST handlers. (4) Rewrote `ViewSubmissions.cshtml` — converted Project/Pathologist/Species/Fixation from `govuk-input` to `govuk-select` dropdowns populated from lookup collections; added "Clear search" GET anchor in `govuk-button-group` alongside the Search button; removed `SubmittedArea` field; expanded results grid from 6 to 9 data columns + Select action (10 total) — now matches legacy `grdviewResults` (Sub. Number, Project Code, Pathologist, Species, Date Submitted, Date Received/Rejected, Date Completed, Customer Received Date, Status). Build: 0 errors, 0 warnings. Tests: 90 pass, 1 skipped, 0 fail. | **Done** | ISS-030 resolved | `ILookupRepository.cs` (+`GetSpeciesLookupAsync`), `LookupRepository.cs` (+implementation), `LookupService.cs` (+passthrough), `SearchModels.cs` (+3 date columns on `BatchSearchResult`), `ViewSubmissions.cshtml.cs` (rewritten), `ViewSubmissions.cshtml` (rewritten). | [src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml](../src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml) |
| 56 | 2026-08-04 | 5 | `ui-implementation` | **ISS-027, ISS-028, ISS-029 resolved — Audit Log columns, QCNotes columns/Quick-Go, UserMaintenance Show Deactivated (Modules 3–5):** **ISS-027 (Audit Log — 3 pages):** Added 6 missing legacy granular columns to `AuditLogEntry` model: `TableName`, `FieldName`, `BeforeValue`, `AfterValue`, `Reason`, `KeyID` (all nullable — Dapper maps them from `GetAuditLogByDate`/`GetAuditLogByUser`/`GetAuditLogBySubmission`/`GetAuditLogTissue` SPs). Rewrote all 3 Razor templates (`AuditLogByDate`, `AuditLogByUser`, `AuditLogBySubmission`) to display 8-column table matching legacy (Table, Field, Date/Time, User, Before, After, Reason, Key). Updated all 3 CSV export handlers to use the new column set. Added `UserService` dependency to `AuditLogByUserModel`, added `Users` property, replaced plain `UserID` number textbox with a user dropdown. **ISS-028 (QCNotes — 4 columns + Quick-Go):** Added `QCNoteRef` (int?), `StainRef` (string?), `ProjectDescription` (string?), `Species` (string?) to `QCNote` model (mapped from `GetBatchQCNotes` SP columns). Added `QuickGoRef` bind property + `OnPostGoAsync` handler to `QCNotesModel` (navigates to `EditQCNote` by note ref). Rewrote `QCNotes.cshtml` to 8-column table (QC Note Ref, Submission No, Stain Ref, Project, Species, Created By, Date, Edit button) + Quick-Go form above table — matching legacy grid column set. Page scope kept as per-batch (deliberate simplification consistent with app's batch-session model). **ISS-029 (UserMaintenance — Show Deactivated):** Added `[BindProperty(SupportsGet = true)] public bool ShowInactive` to `UserMaintenanceModel`; default behaviour (unchecked) filters to `Active == true` using in-memory LINQ — no SP change required. Added `govuk-checkboxes--small` toggle form with `onchange="this.form.submit()"` above the user table, matching legacy `cbActive` AutoPostBack checkbox behaviour. Build: 0 errors, 0 warnings. Tests: 90 pass, 1 skipped, 0 fail. | **Done** | ISS-027, ISS-028, ISS-029 all resolved | `AuditLogEntry.cs` (+6 nullable columns), `AuditLogByDate/User/Submission.cshtml.cs` (CSV headers + `UserService` for ByUser), `AuditLogByDate/User/Submission.cshtml` (8-column table + user dropdown on ByUser), `QCNote.cs` (+4 columns), `QCNotes.cshtml.cs` (+`QuickGoRef` + `OnPostGoAsync`), `QCNotes.cshtml` (8-column + Quick-Go form), `UserMaintenance.cshtml.cs` (+`ShowInactive` with in-memory filter), `UserMaintenance.cshtml` (govuk-checkboxes toggle). | [src/Histo.AuditLog/Models/AuditLogEntry.cs](../src/Histo.AuditLog/Models/AuditLogEntry.cs), [src/Histo.QualityControl/Models/QCNote.cs](../src/Histo.QualityControl/Models/QCNote.cs), [src/Histo.Web/Pages/Admin/UserMaintenance.cshtml](../src/Histo.Web/Pages/Admin/UserMaintenance.cshtml) |
| 55 | 2026-08-04 | 5 | `ui-implementation` | **ISS-026 resolved — ViewSubmissions: full filter form and correct service (Module 2):** Rewrote `ViewSubmissions.cshtml.cs` to use `BatchService.SearchAsync(BatchSearchCriteria)` (was incorrectly calling fixed-status `GetReceivedAsync`/`GetNotReceivedAsync` etc. based on a single `StatusFilter` dropdown). Added `UserService` dependency for Submitted By / Entered By user dropdowns. Replaced the single `StatusFilter` property with all 15 `BatchSearchCriteria` filter fields as `[BindProperty]`. Changed `Results` type from `IReadOnlyList<BatchListResult>` to `IReadOnlyList<BatchSearchResult>`. Renamed handler from `OnGetAsync` (GET-based) to `OnPostSearchAsync` (POST-based), added `Searched` flag to suppress empty results panel on initial load. Kept `OnPostSelect` for batch-session-load and redirect to BatchDetails — the key functional distinction from `SearchSubmissions` (which is read-only). Rewrote `ViewSubmissions.cshtml`: replaced single Status dropdown with 15-field filter form matching `SearchSubmissions.cshtml` (Submission No, Status, Project Code, Pathologist, Species, Fixation, Submitted Area, Submitted By dropdown, Entered By dropdown, Histology Ref, Sender Ref, Submitted Date From/To, Received Date From/To); updated results table to show `BatchSearchResult` columns (Submission No, Project, Pathologist, Species, Batch date, Status) + Select button; added `@using Histo.Core.Domain` for `BatchStatus` constants. Build: 0 errors, 0 warnings. Tests: 90 pass, 1 skipped, 0 fail. | **Done** | ISS-026 resolved | `ViewSubmissions.cshtml.cs` (full rewrite — `UserService` dependency + 15 bind properties + `BatchSearchResult` result type + `OnPostSearchAsync` handler), `ViewSubmissions.cshtml` (full rewrite — 15-field filter form + 6-column results table + Select action). | [src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml](../src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml) |
| 54 | 2026-08-04 | 5 | `ui-implementation` | **ISS-025 resolved — Batch list pages: missing legacy columns and Quick-Go (Module 1):** Full-stack implementation of all 5 batch list page gaps identified in Run #53. *Backend layer:* Added `BatchListResult` model to `Histo.Submissions/Models/SearchModels.cs` (10 properties: `ID`, `ProjectDescription`, `ContactDescription`, `Species`, `BatchDate`, `ReceivedDate`, `CompletedDate`, `CustomerRef`, `Status`, `AllTissuesAssigned`); changed 4 existing `IBatchRepository` methods (`GetReceivedAsync`, `GetInProgressAsync`, `GetNotReceivedAsync`, `GetOnHoldAsync`) to return `IReadOnlyList<BatchListResult>` instead of `IReadOnlyList<Batch>`; added new `GetForDispatchAsync()` method calling `GetBatchesForDispatch` SP (fixes the `BatchesForDispatch` page which was incorrectly calling `GetReceivedBatches` SP); applied same changes to `BatchRepository.cs` and `BatchService.cs` (4 Dapper target types + 1 new implementation + 5 service passthroughs). *Page model layer:* Updated all 5 page models (`BatchesReceived`, `BatchesNotReceived`, `BatchesForEditing`, `BatchesForDispatch`, `SubmissionsOnHold`) and `ViewSubmissions.cshtml.cs` to use `IReadOnlyList<BatchListResult>`; added `[BindProperty] QuickGoId` and `OnPostGoAsync` handler to `BatchesReceived`, `BatchesNotReceived`, `BatchesForEditing`, `BatchesForDispatch` (Quick-Go not added to `SubmissionsOnHold` — legacy page had no direct number entry); fixed `BatchesForDispatch.cshtml.cs` service call from `GetReceivedAsync()` to `GetForDispatchAsync()`; updated page title strings to sentence case. *Razor template layer:* Rewrote all 5 Razor templates adding Project, Pathologist, Species, and Batch date columns; added `AllTissuesAssigned` govuk-tag badge to `BatchesReceived`; added Status column to `BatchesForEditing`; added Quick-Go form (govuk-form-group, govuk-input, govuk-button--secondary) above the table on the 4 pages where applicable. Build typo fix: `BatchListResultListResult` typo in `BatchRepository.GetOnHoldAsync` return type corrected to `BatchListResult`. Build: 0 errors, 0 warnings. Tests: 90 pass, 1 skipped, 0 fail. | **Done** | ISS-025 resolved | `Histo.Submissions/Models/SearchModels.cs` (+`BatchListResult`), `Histo.Submissions/Interfaces/IBatchRepository.cs` (4 types changed + `GetForDispatchAsync` added), `Histo.Submissions/Repositories/BatchRepository.cs` (same), `Histo.Submissions/Services/BatchService.cs` (same), `BatchesReceived.cshtml(.cs)`, `BatchesNotReceived.cshtml(.cs)`, `BatchesForEditing.cshtml(.cs)`, `BatchesForDispatch.cshtml(.cs)`, `SubmissionsOnHold.cshtml(.cs)`, `ViewSubmissions.cshtml.cs`. | [src/Histo.Submissions/Models/SearchModels.cs](../src/Histo.Submissions/Models/SearchModels.cs), [src/Histo.Web/Pages/Batches/BatchesReceived.cshtml](../src/Histo.Web/Pages/Batches/BatchesReceived.cshtml) |
| 53 | 2026-08-04 | 5 | `GitHub Copilot (manual investigation)` | **Cross-module column and filter gap audit — all 57 migrated pages:** Full systematic comparison of every legacy `.aspx` page that contains a DataGrid or filter panel against its new Razor Page equivalent. Read all 5 affected legacy ASPX files, all 5 new Razor Pages, and all relevant service/repository/model files in parallel. Findings: 5 separate issue categories identified across 4 modules (Batch list pages, ViewSubmissions, Audit Log, QC Notes, User Maintenance). Root cause pattern: pages that are simple status-filtered list grids (BatchesReceived, BatchesNotReceived, BatchesForEditing, BatchesForDispatch) were all wired to fixed-status repository methods returning only `Batch` header records (`ID`, `CustomerRef`, `ReceivedDate`/`CompletedDate`) rather than the richer lookup-joined result set (`ProjectDescription`, `ContactDescription`, `Species`, `BatchDate`) returned by the legacy stored procedures. Same pattern applies to `ViewSubmissions`, which uses `BatchService.GetReceivedAsync()` instead of `BatchService.SearchAsync(BatchSearchCriteria)`. The audit log simplification (Action/Entity/Detail schema vs granular Table/Field/Before/After/Reason/Key) is a deliberate architectural decision but removes the field-level before/after values that are critical for audit trail compliance. Duration: ~45 minutes investigation. | **Done** | ISS-025 raised (Batch list pages — 4 pages missing 4 columns each + Quick-Go); ISS-026 raised (ViewSubmissions — 7/9 columns + 11/12 filters missing, wrong service); ISS-027 raised (Audit Log all 3 pages — 5 columns missing, schema gap); ISS-028 raised (QCNotes — 4 columns missing, wrong scope); ISS-029 raised (UserMaintenance — Show Deactivated filter missing) | Investigation only — no code changes | [docs/migration-run-journal.md](migration-run-journal.md) |
| 52 | 2026-08-03 | 5 | `gds-ui` | **Remove “Batches received” from home page and nav:** User confirmed “Batches received” was not a link on the legacy `Home.aspx` and should be removed. Removed the `<li>` link from the Laboratory panel in `Index.cshtml` and from `_NavPartial.cshtml`. The page itself (`Batches/BatchesReceived.cshtml`) is unchanged and remains accessible via normal batch workflow navigation. Build 0 errors; 90 tests pass, 1 skipped. | **Done** | No ISS raised. Parity with legacy: page exists but is not a direct home/nav link. | `Index.cshtml` (removed “Batches received” from Laboratory panel), `_NavPartial.cshtml` (removed “Batches received”). | [src/Histo.Web/Pages/Index.cshtml](../src/Histo.Web/Pages/Index.cshtml), [src/Histo.Web/Pages/Shared/_NavPartial.cshtml](../src/Histo.Web/Pages/Shared/_NavPartial.cshtml) |
| 51 | 2026-08-03 | 5 | `gds-ui` | **Remove “Fix completed dates” from home page (preserve legacy URL-only behaviour):** User confirmed they want to restore the legacy behaviour where `FixCompletedDates` is not linked from any navigation or home panel — accessible only by typing `/Admin/FixCompletedDates` directly in the browser address bar, exactly as `FixCompletedDates.aspx` was in the legacy application. Removed the `<li>` link from the Administration panel in `Index.cshtml`. The page itself (`Admin/FixCompletedDates.cshtml`) is unchanged and remains fully functional at its URL. Build 0 errors; 90 tests pass, 1 skipped. | **Done** | No ISS raised. Parity with legacy: page exists but is unlinked from home, matching the original deployment. | `Index.cshtml` (removed “Fix completed dates” link from Administration panel). | [src/Histo.Web/Pages/Index.cshtml](../src/Histo.Web/Pages/Index.cshtml) |
| 50 | 2026-08-03 | 5 | `gds-ui` | **Search menu label revert + FixCompletedDates legacy provenance investigation:** (1) User requested “Search test totals” in `SearchMenu.cshtml` be reverted to the legacy label “Search outputs” — applied. `SearchMenu.cshtml` link for `SearchTest` updated from “Search test totals” to **“Search outputs”**. (2) User queried why “Fix completed dates” appears on the new home Administration panel when they never saw it in the legacy application as a Maintenance user. Investigation of legacy codebase confirmed: `FixCompletedDates.aspx` is **never linked from any page in the legacy application** — it does not appear in `Home.aspx`, any menu page, or any navigation control. It was a completely hidden utility, accessible only by typing the URL directly into the browser address bar. This explains why Maintenance users never saw it. The decision to add it to the new Administration panel (Run #41, Batch E3) was a deliberate discoverability improvement. User can remove it from `Index.cshtml` if they wish to preserve the legacy “hidden by URL” behaviour. Build 0 errors; 90 tests pass, 1 skipped. | **Done** | No ISS raised. `SearchMenu.cshtml` label reverted. FixCompletedDates legacy provenance confirmed as URL-only hidden utility — not a home link in legacy. | `SearchMenu.cshtml` (“Search outputs” label). | [src/Histo.Web/Pages/Search/SearchMenu.cshtml](../src/Histo.Web/Pages/Search/SearchMenu.cshtml) |
| 49 | 2026-08-03 | 5 | `gds-ui` | **Home page and nav label reverts + label analysis:** User queried why labels changed from legacy and requested 3 reverts. Analysis performed across all 22 legacy home links vs new `Index.cshtml` and `_NavPartial.cshtml`. Three labels reverted to legacy names: (1) “Receive batches” → **“Receive submissions”** (legacy term; “batches” is more accurate to the data model but “submissions” is what users call them); (2) “Book histology refs” → **“Booking”** (exact legacy label, shorter and more familiar); (3) “Pick lists” → **“Pick list maintenance”** (legacy label, retains the maintenance context). One label **cannot** be reverted: “Create New TSE Submission” / “Create New Non-TSE Submission” (two legacy home buttons) remains as **“New submission”** — ISS-023 (Run #44) consolidated both types into a single Cassetted page with govuk-radios type selection; splitting back into two home links would bypass the type-selection step and violate the GDS “one thing per page” principle. Two labels are more accurate than legacy and were not reverted: “Archive” / “Archive batches” (the Archive module covers blocks, tissues, and batches — not just submissions; legacy “Archive Submission” was misleading); “Search” in nav (covers all 10 search types, not submissions only). User also queried why “Batches received”, “Dispatch batches”, and “Fix completed dates” appear as home/nav links when they were not on the legacy `Home.aspx`: (a) “Batches received” — added to surface the lab workflow step explicitly; in legacy users navigated via Batch Details to find received batches; (b) “Dispatch batches” — similarly, a core lab step promoted to top-level for workflow clarity; (c) “Fix completed dates” — an admin maintenance utility that existed but was buried; promoted to the Administration panel for discoverability (Maintenance group only). User also asked about “Search Outputs” — confirmed present in the Search menu as “Search test totals” (SearchTest.cshtml); it was never a top-level home link in legacy and remains accessible via Search menu. Build 0 errors; 90 tests pass, 1 skipped. | **Done** | No ISS raised. 3 labels reverted; 2 labels intentionally kept as new (more accurate); 3 new home links explained and retained; “Search Outputs” confirmed in Search menu as “Search test totals”. | `Index.cshtml` (3 label changes: “Receive submissions”, “Booking”, “Pick list maintenance”), `_NavPartial.cshtml` (2 label changes: “Receive submissions”, “Booking”). Build 0 errors; 90 tests pass, 1 skipped. | [src/Histo.Web/Pages/Index.cshtml](../src/Histo.Web/Pages/Index.cshtml), [src/Histo.Web/Pages/Shared/_NavPartial.cshtml](../src/Histo.Web/Pages/Shared/_NavPartial.cshtml) |
| 48 | 2026-08-03 | 5 | `gds-ui` | **GDS compliance pass — Batches, Submissions, QC, Admin, and Blocks modules (34 pages):** User requested a full GDS sweep of all remaining modules not covered in Runs #45–#46. Full audit of 34 pages across 5 modules found 4 categories of violation applying consistently across all pages: (1) **Title Case in `ViewData["Title"]` and `ViewData["PageTitle"]`** — all 34 pages had Title Case headings (e.g., `"Batch Details"`, `"View Submissions"`, `"Edit Batch"`, `"Copy Blocks"`) — corrected to sentence case on all 34 pages; (2) **No `govuk-back-link`** on any of the 34 pages — added `@section BeforeHeading` back links to all 34 pages with contextually correct destinations: home-entry pages back to `/Index`, detail pages back to their parent list, action pages back to their source detail page (e.g., EditBatch → BatchDetails, ReceiveBatch → BatchesNotReceived, AddSubmission → ViewSamples, EditQCNote → QCNotes, EditQualityDataTest → QualityData, CopyBlocks → SubmissionDetailsBlock, CopySamples → BlockDetails); (3) **Missing `@{ ViewData }` block entirely** on 5 pages — `AddQCNote.cshtml`, `EditQCNote.cshtml`, `QualityData.cshtml`, `EditQualityDataTest.cshtml`, `FixCompletedDates.cshtml` had no ViewData block at all, meaning the layout’s `@if (ViewData["PageTitle"] is string pt)` guard rendered no `<h1>` for these pages — added correct ViewData blocks with sentence-case titles to all 5; (4) **Inline `<p class="govuk-error-message">` on `Cassetted.cshtml`** — replaced with `govuk-error-summary` component including `role="alert"` and `data-module` wiring. Confirmation pages `CopyBatchSummary` and `CopySamplesSummary` received sentence-case title fixes only (no back link appropriate on confirmation screens). Build 0 errors; 90 tests pass, 1 skipped. | **Done** | No new ISS raised. All 34 pages now compliant. GDS compliance sweep is now complete across all 56 migrated pages (Runs #45, #46, #48 combined). Remaining 4 pages blocked on Phase 2 (Reporting) are excluded from scope. | Batches (11): `Cassetted`, `BatchDetails`, `BatchesForEditing`, `BatchesForDispatch`, `BatchesNotReceived`, `BatchesReceived`, `EditBatch`, `ReceiveBatch`, `SubmissionsOnHold`, `CopyBatch`, `CopyBatchSummary`. Submissions (6): `ViewSubmissions`, `ViewSamples`, `AddSubmission`, `AddSample`, `SubmissionDetails`, `SubmissionDetailsBlock`. QC (5): `QCNotes`, `AddQCNote`, `EditQCNote`, `QualityData`, `EditQualityDataTest`. Admin (8): `UserMaintenance`, `AddUser`, `EditUser`, `PickListMaintenance`, `EditLookupItem`, `PickListUserArea`, `FixCompletedDates`, `EditAnimalRef`. Blocks (4): `BlockDetails`, `CopyBlocks`, `CopySamples`, `CopySamplesSummary`. | [src/Histo.Web/Pages/Batches/](../src/Histo.Web/Pages/Batches/), [src/Histo.Web/Pages/Submissions/](../src/Histo.Web/Pages/Submissions/), [src/Histo.Web/Pages/QC/](../src/Histo.Web/Pages/QC/), [src/Histo.Web/Pages/Admin/](../src/Histo.Web/Pages/Admin/), [src/Histo.Web/Pages/Blocks/](../src/Histo.Web/Pages/Blocks/) |
| 47 | 2026-08-03 | 5 | `gds-ui` | **Functionality Traceability Matrix created (`docs/Functionality-Traceability-Matrix.md`):** User requested a new Markdown document mapping all legacy home-page functions to their new .NET 10 Razor Pages equivalents, including migration status, notes on changes/enhancements, and identified gaps. Document sources: full read of `docs/Parity-Audit-Report.md` (all 11 sections), `src/Histo.Web/Pages/Index.cshtml` (new home page links), and the 63-file Razor Pages listing. Document structure: §1 Legacy Home Page → New Application Traceability Matrix (22 rows, one per named legacy function including TSE/NonTSE, View Submissions, Search PM Dates, View Samples, Receive Submissions, Assign Tissues, Quality Data, Archive, Edit QC Notes, View Old ICC_Sub data, Search Submissions, Search Outputs, Search Block Refs, Search Archive Location, Search Un-used Histology Refs, Booking, Edit Submission Status, User Maintenance, Pick List Maintenance, Audit Logs, Edit Sender/Histology Ref); §2 New Home Page Navigation → Legacy origin (19 rows, all current home links with role-panel context); §3 Additional migrated pages not on home (24 rows); §4 Gaps (4 sub-sections: Phase 2 reporting blockers, critical security gaps, documented functional simplifications, superseded/N/A items); §5 Overall completion summary. Status legend: Completed / Partial / Pending / Auth gap (🔒 — applies to all migrated pages due to ISS-001). | **Done** | No new ISS raised. Document intentionally cross-references but does not replace `Parity-Audit-Report.md` or `migration-run-journal.md`. | `docs/Functionality-Traceability-Matrix.md` (new file, 22-row legacy traceability matrix + 19-row home nav map + 24-row additional pages + gap register). | [docs/Functionality-Traceability-Matrix.md](../docs/Functionality-Traceability-Matrix.md) |
| 46 | 2026-08-03 | 5 | `gds-ui` | **GDS compliance pass — Bookings, Archive, and Audit Log modules (12 pages):** Full GDS audit of three modules requested by user. **Bookings (4 pages):** `BookingMenu` used bespoke `menu-links` CSS class, missing `govuk-link` on all 3 anchors, Title Case link text, PageTitle Title Case — fixed to `govuk-list govuk-list--spaced`, `govuk-link`, sentence case. `BookHistologyRef`, `BookBlockRef`, `EditHistologyRef`: no `govuk-back-link`, Title Case page titles — added `@section BeforeHeading` with `govuk-back-link` to `/Bookings/BookingMenu` on all 3, corrected all titles to sentence case. **Archive (4 pages):** `ArchiveMenu` same menu-links/govuk-link/Title Case violations as BookingMenu — fixed. Additionally, `ArchiveMenu` link to `BatchesForArchiving` pointed to `/Archive/BatchesForArchiving` but the file lives under `Pages/Batches/` — corrected to `/Batches/BatchesForArchiving`. `ArchiveBlocks`, `ArchiveTissues`, `BatchesForArchiving`: no `govuk-back-link`, Title Case titles — added `@section BeforeHeading` with back link to `/Archive/ArchiveMenu` on all 3, sentence case applied. **Audit Log (4 pages):** `AuditLogMenu` same menu-links/govuk-link/Title Case violations — fixed. `AuditLogByDate`, `AuditLogByUser`, `AuditLogBySubmission`: no `govuk-back-link`, Title Case in both `ViewData["Title"]` and `ViewData["PageTitle"]` (including em-dash separator form e.g. `Audit Log — Search by Date`) — added `@section BeforeHeading` with back link to `/AuditLog/AuditLogMenu`, corrected all titles to sentence case. Build 0 errors; 90 tests pass, 1 skipped. | **Done** | No new ISS raised — all findings were GDS deviations fully resolved in-session. Bonus: fixed broken `/Archive/BatchesForArchiving` navigation route (was 404, now correct). | `BookingMenu.cshtml` (govuk-list + govuk-link + sentence case), `BookHistologyRef.cshtml`, `BookBlockRef.cshtml`, `EditHistologyRef.cshtml` (back link + sentence case), `ArchiveMenu.cshtml` (govuk-list + govuk-link + sentence case + route fix), `ArchiveBlocks.cshtml`, `ArchiveTissues.cshtml`, `BatchesForArchiving.cshtml` (back link + sentence case), `AuditLogMenu.cshtml` (govuk-list + govuk-link + sentence case), `AuditLogByDate.cshtml`, `AuditLogByUser.cshtml`, `AuditLogBySubmission.cshtml` (back link + sentence case). Build 0 errors; 90 tests pass, 1 skipped. | [src/Histo.Web/Pages/Bookings/](../src/Histo.Web/Pages/Bookings/), [src/Histo.Web/Pages/Archive/](../src/Histo.Web/Pages/Archive/), [src/Histo.Web/Pages/Batches/BatchesForArchiving.cshtml](../src/Histo.Web/Pages/Batches/BatchesForArchiving.cshtml), [src/Histo.Web/Pages/AuditLog/](../src/Histo.Web/Pages/AuditLog/) |
| 45 | 2026-08-03 | 5 | `gds-ui` | **GDS compliance pass — Search module (all 10 pages):** User requested a full GDS review of the Search module covering links, components, layout, styling, content, and interactions. Full audit of all 10 Search pages (`SearchMenu`, `SearchPMDates`, `SearchBlockRefs`, `SearchArchiveLocation`, `SearchSample`, `SearchSender`, `SearchSubmissions`, `SearchTest`, `SearchUnUsedHistologyRefs`, `ViewImportedData`) found 9 categories of violation: (1) `SearchMenu` used bespoke `menu-links` CSS class with unstyled anchors instead of `govuk-list govuk-list--spaced` + `govuk-link`; (2) All 8 link texts in `SearchMenu` were Title Case instead of sentence case; (3) 8 of 9 results pages had no `govuk-back-link` navigation to the menu; (4) 8 pages had Title Case in `ViewData["Title"]`/`ViewData["PageTitle"]` (becomes `<h1>` and `<title>`); (5) `SearchPMDates` had mismatched Title vs PageTitle strings; (6) `SearchBlockRefs` used `<p class="govuk-error-message">` for validation errors instead of `govuk-error-summary` component above the form; (7) `SearchBlockRefs` had no empty-state paragraph when search returns 0 results; (8) `SearchArchiveLocation` had the same inline error paragraph pattern as SearchBlockRefs; (9) Back links could not be positioned before the `<h1>` because the layout had no section slot before the heading render. Fixes: rewrote `SearchMenu` with `govuk-list`/`govuk-link`/sentence-case; added `@RenderSection("BeforeHeading", required: false)` slot to `_Layout.cshtml` before the `<h1>` render; added `@section BeforeHeading` with `govuk-back-link` to all 9 results pages; standardised all `ViewData` titles to sentence case; replaced both inline error paragraphs with `govuk-error-summary` components; added empty-state message to `SearchBlockRefs`. Build 0 errors; tests 90 pass, 1 skipped, 0 fail. | **Done** | No new ISS issues — all findings were GDS deviations, not functional gaps. Remaining recommendations noted (date input component, SearchSubmissions form width, CSV export parity) but outside current scope. | `_Layout.cshtml` (`BeforeHeading` section slot), `SearchMenu.cshtml` (govuk-list + govuk-link + sentence case), `SearchBlockRefs.cshtml(.cs)` (error-summary, empty state, sentence case), `SearchArchiveLocation.cshtml` (error-summary, sentence case), `SearchPMDates.cshtml` (sentence case + back link), `SearchSubmissions.cshtml` (sentence case + back link), `SearchSample.cshtml` (sentence case + back link), `SearchSender.cshtml` (sentence case + back link), `SearchTest.cshtml` (sentence case + back link), `SearchUnUsedHistologyRefs.cshtml` (sentence case + back link), `ViewImportedData.cshtml` (sentence case + back link). | [src/Histo.Web/Pages/Search/SearchMenu.cshtml](../src/Histo.Web/Pages/Search/SearchMenu.cshtml), [src/Histo.Web/Pages/Shared/_Layout.cshtml](../src/Histo.Web/Pages/Shared/_Layout.cshtml) |
| 44 | 2026-08-03 | 5 | `ui-implementation` | **ISS-023 resolved (BatchType) + ISS-024 nav link fix:** Two related fixes triggered by user noticing the "Add Submission" link still showed Sender Reference instead of the TSE/Non-TSE selection step. (1) **ISS-023 BatchType across the stack:** Added `BatchType` property and `BatchTypeConstants` static class (TSE=0, NonTSE=1) to the `Batch` model; added `BatchType` SP parameter to `BatchRepository.AddAsync`; added `BatchType` session property (`KeyBatchType`) to `ISessionService`/`SessionService`; added TSE/Non-TSE `govuk-radios` group to `Cassetted.cshtml(.cs)`, binding `BatchType` and persisting it to session and to the `AddBatch` SP on creation; added `Session.BatchType = Batch.BatchType` restore in `BatchDetails.cshtml.cs` and `EditBatch.cshtml.cs` so opening an existing batch restores the correct type from DB; added "Submission category" row (TSE/Non-TSE) to `BatchDetails.cshtml` summary list. (2) **ISS-024 nav link wrong target:** `_NavPartial.cshtml`'s "New submission" link pointed to `/Submissions/AddSubmission` (the "add an animal to an existing batch" page — correctly asks for Sender Ref in that context) instead of `/Batches/Cassetted` (the correct first step for creating a new submission). One-line fix to the `asp-page` attribute. Build 0 errors/warnings; tests 90 pass, 1 skipped, 0 fail. | **Done** | ISS-023 resolved; ISS-024 raised and resolved (see Open Issues) | `Histo.Submissions/Models/Batch.cs` (+`BatchType`, `BatchTypeConstants`), `BatchRepository.cs` (`AddAsync` +`BatchType` param), `ISessionService.cs` (+`BatchType`), `SessionService.cs` (+`KeyBatchType`, `BatchType` prop), `Cassetted.cshtml(.cs)` (TSE/NonTSE radios + session write), `BatchDetails.cshtml.cs` + `EditBatch.cshtml.cs` (session restore), `BatchDetails.cshtml` (summary row), `_NavPartial.cshtml` (nav fix). Build 0 errors; 90 tests pass, 1 skipped. | [src/Histo.Web/Pages/Batches/Cassetted.cshtml](../src/Histo.Web/Pages/Batches/Cassetted.cshtml), [src/Histo.Web/Pages/Shared/_NavPartial.cshtml](../src/Histo.Web/Pages/Shared/_NavPartial.cshtml), [src/Histo.Submissions/Models/Batch.cs](../src/Histo.Submissions/Models/Batch.cs) |
| 43 | 2026-08-03 | 5 | `ui-implementation` | **ISS-022 resolved — per-animal Sender/Histology Ref rename workflow (final Phase 5 gap):** User asked to re-scan `docs/Parity-Audit-Report.md` and the run journal for missing/partial pages and migrate them. Re-verified the full legacy `.aspx` list (64 pages) against `src/Histo.Web/Pages/` and confirmed every page from Batches A–F (Runs #35–#42) is genuinely present, leaving only ISS-022 (the true per-animal `EditHistologyRef.aspx` renamer, distinct from the pool-counter page already built at `Bookings/EditHistologyRef`) as an open gap — `FinalPrintBatch`/`SubmissionForm`/`SubmissionNotes` remain correctly deferred to Phase 2. Ported `clsAnimal.UpdateAnimalSenderRef`/`UpdateAnimalHistologyRef` (SPs `EditAnimalSenderRef`/`EditAnimalHistologyRef`, return-value validation for not-found/duplicate/already-used) to `ISubmissionRepository`/`SubmissionRepository`, added a `SubmissionService` passthrough and a new `AnimalRefUpdateException`, and built `Admin/EditAnimalRef.cshtml(.cs)` — a Sample Ref lookup + Sender Ref rename + Histology Ref rename page, reusing `AnimalHelpers.ComputePgAutoHistologyRef` for the PG-number reverse-format check and `ValidationHelpers.ValidateHistoRef` for format validation. Linked from `Index.cshtml`'s Maintenance-only Administration panel as "Edit Sender/Histology Ref", matching legacy `Home.aspx` visibility. | **Done** | ISS-022 resolved (see Open Issues) | `Histo.Submissions/Models/DomainExceptions.cs` (+`AnimalRefUpdateException`), `Histo.Submissions/Interfaces/ISubmissionRepository.cs`, `Histo.Submissions/Repositories/SubmissionRepository.cs`, `Histo.Submissions/Services/SubmissionService.cs`, `Admin/EditAnimalRef.cshtml(.cs)`, `Index.cshtml` nav link. Build 0 errors/warnings; tests 90 pass, 1 skipped, 0 fail. | [src/Histo.Web/Pages/Admin/EditAnimalRef.cshtml](../src/Histo.Web/Pages/Admin/EditAnimalRef.cshtml), [src/Histo.Submissions/Repositories/SubmissionRepository.cs](../src/Histo.Submissions/Repositories/SubmissionRepository.cs) |
| 42 | 2026-08-01 | 5 | `ui-implementation` | **Batch F — ExcelExport (cross-cutting CSV export) and ViewImportedData; FinalPrintBatch confirmed blocked on Phase 2:** Investigation corrected the parity audit's assumption — neither `ExcelExport.aspx` nor `ViewImportedData.aspx` depends on Crystal Reports. `ExcelExport.aspx` is a generic "render current results as Excel" utility invoked from 4 already-migrated pages (`AuditLogByDate`, `AuditLogBySubmission`, `AuditLogByUser`, `SearchArchiveLocation`). Added a new `CsvExportHelper` (`src/Histo.Web/CsvExportHelper.cs`, plain CSV — no new NuGet dependency, faithful equivalent to "opens in Excel") and an `OnPostExportCsvAsync`/"Export to CSV" govuk-button on all 4 pages, reusing each page's already-held in-memory result set (no requery). Built `Search/ViewImportedData.cshtml(.cs)` ("View Old ICC_Sub data" table selector + filtered results + its own CSV export), added 1 new small `LookupService` unit test. Wired a new nav link from `Index.cshtml`. Confirmed `FinalPrintBatch.aspx` genuinely IS blocked on Phase 2 — its only two actions (`btnPrintBatch`, `btnSubmissionNotes`) launch `SubmissionForm.aspx`/`SubmissionNotes.aspx` popups, both pure Crystal Reports PDF generators (already correctly identified in Batch C, Run #36) with no interactive content of their own; building a shell page whose only two buttons are non-functional was judged not to add real value and was intentionally deferred to Phase 2. | **Done** | ISS-019 — 2 more of the 34 originally-missing pages resolved (with corrected classification); `FinalPrintBatch` reclassified from "missing UI page" to "blocked on Phase 2 reporting", consistent with `SubmissionForm`/`SubmissionNotes` | `src/Histo.Web/CsvExportHelper.cs`, CSV export wiring on 4 pages, `Search/ViewImportedData.cshtml(.cs)` | [src/Histo.Web/CsvExportHelper.cs](../src/Histo.Web/CsvExportHelper.cs), [src/Histo.Web/Pages/Search/ViewImportedData.cshtml](../src/Histo.Web/Pages/Search/ViewImportedData.cshtml) |
| 41 | 2026-08-01 | 5 | `ui-implementation` | **Batch E3 — QualityData and FixCompletedDates:** Built `QC/QualityData.cshtml(.cs)` (batch-wide QC/dispatch worklist across histology/antibody/special-stain block tests, deliberately simplified to edit one test at a time via new `QC/EditQualityDataTest.cshtml(.cs)` rather than the legacy multi-select batch-save) and `Admin/FixCompletedDates.cshtml(.cs)` (admin utility recomputing `CompletedDate` for cassetted batches with fully-dispatched tests, via new `BatchService.FixCompletedDatesAsync`, ported to per-batch independent error handling instead of the legacy single-transaction approach). **Hot-fix applied by calling agent:** the generated `BatchService.FixCompletedDatesAsync`/`TryGetLatestDispatchDate` helper had two overloads differing only by `ref`/`out` parameter modifier (CS0663, does not compile in C#) — fixed by removing the `out` overload and initializing the accumulator inline before the first `ref` call. Verified `dotnet build` (0 errors) and `dotnet test` (88 pass, 1 skipped) after the fix. | **Done** | ISS-019 — 2 more of the 34 originally-missing pages resolved; CS0663 build break introduced by the subagent run was caught and fixed before commit | `QC/QualityData.cshtml(.cs)`, `QC/EditQualityDataTest.cshtml(.cs)`, `Admin/FixCompletedDates.cshtml(.cs)`, `Histo.Submissions/Services/BatchService.cs` (build fix) | [src/Histo.Web/Pages/QC/QualityData.cshtml](../src/Histo.Web/Pages/QC/QualityData.cshtml), [src/Histo.Web/Pages/Admin/FixCompletedDates.cshtml](../src/Histo.Web/Pages/Admin/FixCompletedDates.cshtml) |
| 40 | 2026-08-01 | 5 | `ui-implementation` | **Batch E2 — BatchBlocks, BatchSummary, BatchBlockSummary determined to be already covered:** Verified all 3 pages are functionally superseded by pages built in earlier runs: `BatchBlocks.aspx` by `Blocks/BlockDetails.cshtml` (already documented in its own doc comment); `BatchSummary.aspx`/`BatchBlockSummary.aspx` by `Submissions/ViewSamples.cshtml` + `Submissions/SubmissionDetails.cshtml`/`SubmissionDetailsBlock.cshtml` (Batch C). Found and closed a real gap while verifying this: `SubmissionDetails.cshtml` was orphaned (no page linked to it) and `SubmissionService.DeleteAnimalAsync` was unused — added `OnPostEditAsync`/`OnPostDeleteAsync` handlers plus Tissues/Delete buttons to `Submissions/ViewSamples.cshtml(.cs)`, reusing existing repository/service methods (no new backend code). Flagged that `docs/Parity-Audit-Report.md`'s page-status table is now stale following Batches A–D and should be treated as a point-in-time snapshot, not a live source of truth. | **Done** | ISS-019 — 3 more of the 34 originally-missing pages confirmed as false positives (already covered); 1 navigation gap closed (ViewSamples Edit/Delete wiring) | `Submissions/ViewSamples.cshtml(.cs)` | [src/Histo.Web/Pages/Submissions/ViewSamples.cshtml](../src/Histo.Web/Pages/Submissions/ViewSamples.cshtml) |
| 39 | 2026-08-01 | 5 | `ui-implementation` | **Batch E1 — PickListMaintenanceID and PickListUserArea (resolves ISS-018):** Built `Admin/EditLookupItem.cshtml(.cs)` (unrestricted per-table Add/Edit editor, replacing `PickListMaintenanceID.aspx`, linked from `Admin/PickListMaintenance.cshtml`'s new per-row Edit links) and `Admin/PickListUserArea.cshtml(.cs)` (area-scoped single-table editor for Projects/Contacts, replacing `PickListUserArea.aspx` — confirmed as a genuinely distinct page from `PickListMaintenanceID`, not a sub-flow of it; its legacy entry point is `BatchDetails.aspx`'s New Project/Contact buttons, which the migrated `Batches/BatchDetails.cshtml` doesn't have yet, so it's reachable by direct route for now). Added `CreateLookupItemAsync`/`UpdateLookupItemAsync` to `ILookupRepository`/`LookupRepository`/`LookupService`, refactoring `ResolveSelectProcAsync` into a shared `ResolveLookupProcsAsync` that resolves Select/Update/Insert/Delete SP names together via `GetEditableLookupProcs`; Insert uses Dapper `DynamicParameters` since the Area parameter is conditional per-table. Deliberate simplification: uniform Description/IsActive/UserID(+Area) parameter shape used for all tables rather than guessing at unverifiable legacy per-table SP signatures (no `.sql` source available in-repo to confirm exact Code-keyed vs ID-keyed parameter names). | **Done** | ISS-018 resolved | `Admin/EditLookupItem.cshtml(.cs)`, `Admin/PickListUserArea.cshtml(.cs)`, `ILookupRepository`/`LookupRepository`/`LookupService` Create/Update methods | [src/Histo.Web/Pages/Admin/EditLookupItem.cshtml](../src/Histo.Web/Pages/Admin/EditLookupItem.cshtml), [src/Histo.Web/Pages/Admin/PickListUserArea.cshtml](../src/Histo.Web/Pages/Admin/PickListUserArea.cshtml) |
| 38 | 2026-08-01 | 5 | `ui-implementation` | **Batch D2 — Copy workflow family (7 legacy pages, all resolved):** `CopyBatch.aspx`/`CopyBatchBlocks.aspx` consolidated into `Batches/CopyBatch.cshtml(.cs)` + `Batches/CopyBatchSummary.cshtml(.cs)` (submission-level copy: batch header, submissions, animals, tissues via existing `BatchService`/`SubmissionService`). `CopyBlocks.aspx` → `Blocks/CopyBlocks.cshtml(.cs)` (block+tissue copy across samples in the same batch). `CopySamples.aspx`/`CopySamplesBlocks.aspx`/`CopySamplesSummary.aspx` consolidated into `Blocks/CopySamples.cshtml(.cs)` + `Blocks/CopySamplesSummary.cshtml(.cs)` (copies blocks/tissues from a sample in a *different* submission onto an existing sample in the current batch — corrected understanding: this does not create new animal rows via `CopyAnimalAsync` as initially hypothesised; it reuses the same `BlockService.CopyBlockAsync`/`SubmissionService.CopyTissueAsync` methods already used by `CopyBlocksModel`). No new repository/service code was needed for any of the 7 pages — all copy operations were already supported by Phase 4 methods. Wired dead-link entry points on `BlockDetails.cshtml` (Copy samples) and `BatchDetails.cshtml` (Copy batch, from prior sub-run). Documented simplifications: TSE/Non-TSE batch-type matching not reproduced (no batch-type field in migrated model); auto-generate-histology-ref option on block copy not reproduced (kept target's existing ref); legacy `CopySamplesSummary.aspx`'s independent read-only grid feature not reproduced as a separate capability (superseded by `SubmissionDetailsBlock`/`BlockDetails`). | **Done** | ISS-019 resolved for these 7 pages | `Batches/CopyBatch.cshtml(.cs)`, `Batches/CopyBatchSummary.cshtml(.cs)`, `Blocks/CopyBlocks.cshtml(.cs)`, `Blocks/CopySamples.cshtml(.cs)`, `Blocks/CopySamplesSummary.cshtml(.cs)` | [src/Histo.Web/Pages/Batches/CopyBatch.cshtml](../src/Histo.Web/Pages/Batches/CopyBatch.cshtml), [src/Histo.Web/Pages/Blocks/CopyBlocks.cshtml](../src/Histo.Web/Pages/Blocks/CopyBlocks.cshtml), [src/Histo.Web/Pages/Blocks/CopySamples.cshtml](../src/Histo.Web/Pages/Blocks/CopySamples.cshtml) |
| 37 | 2026-08-01 | 5 | `ui-implementation` | **Batch D1 — AddSample and Cassetted pages, with corrected functional understanding:** Parity audit's initial descriptions of these two pages were wrong and corrected during this run. `AddSample.aspx` is not a separate sample-creation workflow — it's the landing page for adding an *existing* animal (found via `Search/SearchSample`) to the current batch, mirroring the already-migrated `Submissions/AddSubmission.cshtml`. Built `Submissions/AddSample.cshtml(.cs)` accordingly, using existing `SubmissionService.AddAnimalAsync` (no new repository code). `Cassetted.aspx` is not a block-status transition — it's the "Submission Type" step shown after clicking "New submission" on Home, which creates the batch header and routes to Batch Details. Built `Batches/Cassetted.cshtml(.cs)` using existing `BatchService.AddAsync`/`LookupService.GetLookupDataAsync` (no new repository code). Wired two previously-dead links: `Search/SearchSample.cshtml` now has an "Add to batch" action, and `Index.cshtml`'s "New submission" link now correctly points to `/Batches/Cassetted` instead of skipping to `/Submissions/AddSubmission`. | **Done** | ISS-019 partial (2 more of the 34 missing pages resolved, with corrected classification vs. the original audit description) | `Submissions/AddSample.cshtml(.cs)`, `Batches/Cassetted.cshtml(.cs)`, dead-link fixes in `Search/SearchSample.cshtml` and `Index.cshtml` | [src/Histo.Web/Pages/Submissions/AddSample.cshtml](../src/Histo.Web/Pages/Submissions/AddSample.cshtml), [src/Histo.Web/Pages/Batches/Cassetted.cshtml](../src/Histo.Web/Pages/Batches/Cassetted.cshtml) |
| 36 | 2026-08-01 | 5 | `ui-implementation` | **Batch C — Submission detail pages, plus SubmissionForm/SubmissionNotes disposition determined:** Built `Submissions/SubmissionDetails.cshtml(.cs)` (sender ref, editable PM date/histology ref, per-animal tissues add/delete) and `Submissions/SubmissionDetailsBlock.cshtml(.cs)` (per-animal blocks list/delete), both using existing `ISubmissionRepository`/`SubmissionService` and `IBlockRepository`/`BlockService` methods — no new repository code needed, confirming Phase 4 repository layer is comprehensive for this module. **Determined `SubmissionForm.aspx` and `SubmissionNotes.aspx` are NOT missing UI pages** — both are pure Crystal Reports PDF-export popups (invoked from `FinalPrintBatch.aspx`/`ViewSubmissions.aspx`) with no interactive markup; they belong to the Phase 2 reporting migration, not Phase 5 UI implementation, and were correctly excluded from this batch's scope. | **Done** | ISS-019 partial (2 of the 34 missing pages resolved; 2 more reclassified as reporting-phase work, not UI-phase work) | `Submissions/SubmissionDetails.cshtml(.cs)`, `Submissions/SubmissionDetailsBlock.cshtml(.cs)` | [src/Histo.Web/Pages/Submissions/](../src/Histo.Web/Pages/Submissions/) |
| 35 | 2026-08-01 | 5 | `ui-implementation` | **ISS-020 resolved — Search module, remaining 4 of 8 pages:** Built `SearchSender`, `SearchSubmissions`, `SearchTest`, `SearchUnUsedHistologyRefs` Razor Pages under `src/Histo.Web/Pages/Search/`, completing the Search module (all 8 pages now exist). Wired to existing `SubmissionService.GetAnimalsBySenderRefAsync`, `BatchService.SearchAsync`/`GetTestItemRowsAsync`, `LookupService.GetLookupDataAsync`, `UserService.GetAllUsersAsync`; added one missing service passthrough (`HistologyRefService.GetAllUnusedRefsAsync`, repository method already existed); added the missing "Search Test Totals" link to `SearchMenu.cshtml` (menu only listed 7 of 8). `SearchTest` deliberately follows the existing SIMPLIFIED scope decision on `IBatchRepository.GetTestItemRowsAsync` (test-item counts only, not the full legacy premium cross-tab analytics engine). `SearchSender` reimplemented as a standalone search (legacy page was a session-state picker for the not-yet-migrated `AddSample.aspx` workflow), consistent with how `SearchSample` handled the same situation. | **Done** | ISS-020 fully resolved | 8 new Razor Pages total across this and Run #27; 1 new service passthrough; 1 menu link fix | [src/Histo.Web/Pages/Search/](../src/Histo.Web/Pages/Search/) |
| 27 | 2026-07-31 | 5 | `ui-implementation` | **ISS-020 (partial) — Search module, first 4 of 8 pages:** Built `SearchPMDates`, `SearchBlockRefs`, `SearchArchiveLocation`, `SearchSample` Razor Pages under `src/Histo.Web/Pages/Search/`, wired to existing `SubmissionService`/`BlockService` methods (`GetByPmDateRangeAsync`, `GetAnimalsBySenderRefAsync`, `GetTissueArchiveAsync`) and `Histo.Core.Domain.BlockRefRangeHelpers`; added 4 missing read-only passthrough methods to `BlockService` (`GetUsedBlockRefsByHistologyRefAsync`, `GetUsedBlockRefsBySenderRefAsync`, `GetBlockArchiveAsync`, `GetSlideArchiveAsync`) — the Dapper repository methods already existed, only the service-layer passthrough was missing | **Done** | No repository/SP gaps found — all data access pre-existed; `SearchArchiveLocation` legacy logic (hierarchical expand/collapse grids across 3 modes) was significantly more complex than the reference pattern, reproduced here as flat GOV.UK tables per already-documented `SIMPLIFIED` model notes; `SearchSample` legacy page was a session-dependent picker (populated by `AddSubmission.aspx` lookup, redirects to non-existent `AddSample` page) — reimplemented as a standalone read-only Sender Ref search | No new repository/SP methods required; only `BlockService` passthroughs added | Build succeeded 0 errors; 88 tests pass, 1 skipped, 0 failures | [src/Histo.Web/Pages/Search/](/src/Histo.Web/Pages/Search/), [src/Histo.Histology/Services/BlockService.cs](/src/Histo.Histology/Services/BlockService.cs) | All 9 Crystal Reports (.rpt) → Razor HTML + Dapper wiring (3-stage ReportDefinition.json pipeline): Stage 1 parse, Stage 2 templates, Stage 3 wire-up | Not Started | — | — | — |
| 10 | _(yyyy-MM-dd)_ | 2 | `pdf-validation` | RMSE pixel diff + structural checks for all 9 reports vs Phase 0 reference PDFs | Not Started | — | — | — |
| 15 | 2026-07-27 | 3 | `modernise-to-modular-monolith` | Target Architecture, Migration Plan, ADR-005 — Dapper/SP decision gate (Option A confirmed by user) | Done | GetUserByNTLogin SP called with Entra UPN may need NT login format mapping (see ISS-009) | — (planning only — no code changes) | [docs/Target-Architecture.md](/docs/Target-Architecture.md), [docs/Migration-Plan.md](/docs/Migration-Plan.md), [docs/ADR/ADR-005-data-access-dapper-stored-procedures.md](/docs/ADR/ADR-005-data-access-dapper-stored-procedures.md) |
| 16 | _(pre-plan placeholder)_ | 5 | `ui-implementation` | ASCX controls → `_Layout.cshtml` + Partial Views | **Superseded** | — | Work completed under Runs #26, #31, #32, #33 — `_Layout.cshtml`, `_NavPartial.cshtml`, `_UserContextPartial.cshtml` migrated; GOV.UK Design System layout shell implemented. Phase 5 is Complete. | — |
| 17 | _(pre-plan placeholder)_ | 5 | `ui-implementation` | 18 simple display pages → Razor Pages | **Superseded** | — | Work completed across Runs #26, #27, #33, #35 and others — all simple display/list pages migrated as part of Phase 5 batches A–F. Phase 5 is Complete. | — |
| 18 | _(pre-plan placeholder)_ | 5 | `ui-implementation` | 28 CRUD pages → Razor Pages | **Superseded** | — | Work completed across Runs #26, #33, #35–#43 — all CRUD pages migrated across Batches A–F. Phase 5 is Complete. | — |
| 19 | _(pre-plan placeholder)_ | 5 | `ui-implementation` | 12 complex multi-step workflow pages → Razor Pages | **Superseded** | — | Work completed across Runs #36–#43 — Copy workflow family (7 pages, Run #38), QualityData (Run #41), ExcelExport/ViewImportedData (Run #42), ISS-022 EditAnimalRef (Run #43) and others. Phase 5 is Complete. | — |
| 20 | _(yyyy-MM-dd)_ | 5 | `azure-infra-planner` | Interview → `infra-plan.json` | Not Started | — | — | — |
| 21 | _(yyyy-MM-dd)_ | 5 | `azure-infra-implementer` | Generate all Bicep modules + CI/CD pipeline | Not Started | — | — | — |
| 22 | _(yyyy-MM-dd)_ | 5 | `devops-pipeline-modernizer` | CI/CD YAML, branching strategy | Not Started | — | — | — |
| 23 | _(yyyy-MM-dd)_ | 6 | `dotnet-test-automation-and-quality-agent` | C# xUnit domain tests + integration tests | Not Started | — | — | — |
| 24 | _(yyyy-MM-dd)_ | 6 | `playwright-tester-agent` | E2E Playwright tests for critical user journeys | Not Started | — | — | — |
| 27 | 2026-07-30 | 5 | `ui-implementation` | **Hot-fix — `IAppLogger` not registered in DI container:** All 8 domain services (`UserService`, `LookupService`, `AuditLogService`, `BlockService`, `HistologyRefService`, `QCNoteService`, `BatchService`, `SubmissionService`) threw `AggregateException` on startup because `IAppLogger` was missing from the DI container. Added `AddTransient<IAppLogger>` factory registration in `Program.cs` using `ILoggerFactory` to create an `AppLogger<IAppLogger>` backed by the Serilog pipeline. | **Done** | ISS-012: `IAppLogger` missing from DI — caused `System.AggregateException` on `Histo.Web` startup; all 8 domain services unresolvable | Added `IAppLogger` transient factory registration in `Program.cs` | [src/Histo.Web/Program.cs](../src/Histo.Web/Program.cs) |
| 28 | 2026-07-30 | 5 | `ui-implementation` | **Hot-fix — Home page shows no modules (all panels hidden):** `GroupName` was always empty string on every request because `SessionService.Populate()` only set `UserName`, `PopulateFromUser()` didn’t exist, and `getUserDetails()` equivalent was never called. Added `PopulateFromUser(User)` to `ISessionService`/`SessionService`; added `OnPageHandlerExecutionAsync` override to `HistoPageModel` that calls `UserService.ResolveUserAsync` when `GroupName` is uninitialised, with a dev-mode stub. | **Done** | ISS-013: all Home page panels hidden; `IsCustomer`/`IsHistoUser`/`IsMaintenance` always `false` | Added `PopulateFromUser`, `OnPageHandlerExecutionAsync` with DB resolve + dev stub | [src/Histo.Web/Pages/HistoPageModel.cs](../src/Histo.Web/Pages/HistoPageModel.cs), [src/Histo.Web/Services/SessionService.cs](../src/Histo.Web/Services/SessionService.cs) |
| 29 | 2026-07-30 | 5 | `ui-implementation` | **Investigation — Header, footer, and page styling not fully migrated:** User reported Home page modules display correctly but header/footer/CSS do not match the legacy application. Investigation found: (1) Bootstrap 5 utility classes used across all migrated CRUD pages but never referenced in `_Layout.cshtml`; (2) `wwwroot/css/Styles.css` is an explicit placeholder stub, never migrated from the actual production theme `HistopathologySystem/Style/vla-ie.css`; (3) no `wwwroot/images/` folder — legacy logo (`vlalogoExtended.gif`) never copied; (4) footer/header markup does not replicate `VLAHeader.ascx`/`VLAFooter.ascx` structure (bordered blue box, Top anchor, Copyright/Department lines). Root cause: `ui-implementation` agent migrated markup and C# logic correctly but never migrated the CSS asset pipeline. Recommended fix documented in ISS-014; not yet applied — pending confirmation. | **Investigated** | ISS-014: header/footer/styling gap — Bootstrap missing, `Styles.css` stub not replaced, no logo asset | — (analysis only, fix pending) | [docs/migration-run-journal.md](migration-run-journal.md) |
| 30 | 2026-07-30 | 5 | `ui-implementation` | **Hot-fix — Applied ISS-014 Bootstrap + VLA theme migration:** Added Bootstrap 5.3.3 CDN `<link>` (with SRI integrity hash) and bundle `<script>` to `_Layout.cshtml`; added logo `<img>` reference (`~/images/vlalogoExtended.gif`) inside `.site-title`; replaced footer markup with `#top` anchor link, Copyright line, and Department line matching `VLAFooter.ascx`; added `<a name="top"></a>` after `<body>` for the footer link target. Replaced `wwwroot/css/Styles.css` placeholder stub with the full VLA theme (blue `#003399` / accent `#33ccff` palette, `.site-header`/`.site-nav`/`.nav-links`, `.site-context`/`.ctx-batch`, `.page-title-bar`, `.home-panels`/`.panel`, `.site-footer`/`.footer-top-link`, `.site-logo`, `table-dark` override, `.text-error`/`.text-warning-legacy`) ported from `HistopathologySystem/Style/vla-ie.css`. Created `wwwroot/images/` and copied `HistopathologySystem/Images/vlalogoExtended.gif` into it. | **Done** | ISS-014: header/footer/styling gap — Bootstrap missing, `Styles.css` stub not replaced, no logo asset | Bootstrap CDN wired, VLA theme CSS migrated, logo asset copied, header/footer markup replaced | [src/Histo.Web/Pages/Shared/_Layout.cshtml](../src/Histo.Web/Pages/Shared/_Layout.cshtml), [src/Histo.Web/wwwroot/css/Styles.css](../src/Histo.Web/wwwroot/css/Styles.css) |
| 31 | 2026-07-31 | 5 | `gds-ui` | **GOV.UK Design System compliance pass — superseded Bootstrap/VLA theme (ISS-014) with real govuk-frontend v6.2.0:** Downloaded and vendored govuk-frontend CSS/JS, GDS Transport fonts (4 files), and all required favicon/crown/icon-mask assets into `wwwroot/govuk/` and `wwwroot/assets/` (unpkg was blocked by the corporate Zscaler proxy; jsDelivr CDN used instead — confirmed working, matches CSS `url()` root-relative asset paths). Rewrote `_Layout.cshtml` with the govuk-frontend v6 page template: skip link, GOV.UK header with inline Tudor Crown SVG logo, `govuk-service-navigation` component (service name + role-based nav), `govuk-main-wrapper`, and full `govuk-footer` (crown SVG, OGL licence SVG, Crown copyright link, Support links heading) — fetched exact reference markup from `alphagov/govuk-frontend` GitHub raw (v6.2.0 tag) rather than reconstructing from memory. Converted `_NavPartial.cshtml` to emit `govuk-service-navigation__item` list items and `_UserContextPartial.cshtml` to plain text (no bespoke CSS). Removed the Bootstrap 5.3.3 CDN entirely. Rewrote `Index.cshtml` home panels using `govuk-grid-row`/`govuk-grid-column-one-third` + `govuk-list` (replacing bespoke `.home-panels`/`.panel`). Converted 15 pages with Bootstrap-dependent markup (tables, buttons, forms, alerts) to real `govuk-table`, `govuk-button`, `govuk-form-group`/`govuk-input`/`govuk-textarea`/`govuk-select`/`govuk-checkboxes`, `govuk-error-summary`, `govuk-warning-text`, and `govuk-summary-list` markup so no page regresses once Bootstrap CSS was removed. Trimmed `Styles.css` down to only the two non-GDS helper classes (`.data-table`, `.filter-form`) still used by pages not yet converted (see ISS-015). | **Done** | ISS-015 raised: 10 pages (audit log search/list pages, `BatchesForDispatch`, `BatchesForEditing`, `BatchesNotReceived`, `BatchesReceived`, `SubmissionsOnHold`, `ViewSubmissions`, `QCNotes`) still use the legacy `.data-table`/`.filter-form`/unstyled `<button>` markup rather than real `govuk-table`/`govuk-fieldset` — not a regression (no Bootstrap dependency), but not yet full GDS component compliance | Vendored govuk-frontend v6.2.0 assets; rewrote layout, nav partial, user-context partial, home page, and 15 CRUD/detail pages to real GDS markup; build succeeded 0 warnings/errors; 88 tests pass, 1 skipped, 0 fail | [src/Histo.Web/Pages/Shared/_Layout.cshtml](../src/Histo.Web/Pages/Shared/_Layout.cshtml), [src/Histo.Web/Pages/Shared/_NavPartial.cshtml](../src/Histo.Web/Pages/Shared/_NavPartial.cshtml), [src/Histo.Web/Pages/Index.cshtml](../src/Histo.Web/Pages/Index.cshtml), [src/Histo.Web/wwwroot/css/Styles.css](../src/Histo.Web/wwwroot/css/Styles.css), [src/Histo.Web/wwwroot/govuk/](../src/Histo.Web/wwwroot/govuk/) |
| 32 | 2026-07-31 | 5 | `gds-ui` | **Hot-fix — Runtime crash on launch: `InvalidOperationException` on service navigation link (regression from Run #31):** User launched the application after the GDS compliance pass and hit `System.InvalidOperationException: 'Cannot override the 'href' attribute for <a>. An <a> with a specified 'href' must not have attributes starting with 'asp-route-' or an 'asp-action', 'asp-controller', 'asp-area', 'asp-route', 'asp-protocol', 'asp-host', 'asp-fragment', 'asp-page' or 'asp-page-handler' attribute.'` on every page load. Root cause: the service-name link in `_Layout.cshtml` was written with **both** a literal `href="/"` **and** `asp-page="/Index"` on the same `<a>` tag — the ASP.NET Core Anchor Tag Helper throws at render time whenever a literal `href` is combined with any `asp-*` navigation attribute, because it cannot determine which one should win. Removed the redundant literal `href="/"`, keeping only `asp-page="/Index"` (which generates the equivalent `href="/"` via the tag helper). Verified with `dotnet build` (0 errors), `dotnet test` (88 pass, 1 skipped), and a live `dotnet run` smoke test confirming the home page returns HTTP 200 and the link renders as `<a class="govuk-service-navigation__link" href="/">Histopathology System</a>` with no exception. | **Done** | ISS-016: `<a>` tag combined literal `href` with `asp-page` tag helper attribute, causing `InvalidOperationException` on every page render | Removed the literal `href="/"` from the service navigation link in `_Layout.cshtml`, keeping only `asp-page="/Index"`. Confirmed no other `.cshtml` file in the project combines a literal `href`/`asp-route-*` with any `asp-page`/`asp-action`/`asp-controller`/`asp-area`/`asp-route`/`asp-protocol`/`asp-host`/`asp-fragment` attribute (regex-searched entire `Pages/` tree). | [src/Histo.Web/Pages/Shared/_Layout.cshtml](../src/Histo.Web/Pages/Shared/_Layout.cshtml) |
| 34 | 2026-08-01 | 5 | `implementation` | **Full legacy-vs-current parity audit (read-only, no code changes):** User requested a full-scope comparison of every module, screen, feature, workflow, business rule, and CRUD operation between the legacy WebForms app and the current Razor/GDS app. Enumerated all 64 legacy `.aspx` pages, 8 `.ascx` controls, 9 Crystal Reports, and all current Razor Pages across 10 subfolders; performed 1:1 name/functional mapping; inspected all 5 domain module repository interfaces (`Histo.Administration`, `Histo.Submissions`, `Histo.Histology`, `Histo.QualityControl`, `Histo.AuditLog`) for Create/Update/Delete completeness; grepped legacy `CheckPermissions()` usage and current `Histo.Web` auth wiring. Findings: only 30/64 pages (47%) migrated; Reporting at 0%; domain/repository CRUD layer is materially ahead of the UI layer (most entities already have Create/Update/Delete methods with no consuming page); authentication/authorization is 0% implemented in code (only a dev-only stub identity exists, no `[Authorize]`/Entra ID wiring anywhere). Produced [docs/Parity-Audit-Report.md](Parity-Audit-Report.md) with a full page-by-page mapping table, CRUD parity table, and 10 prioritized findings (F-01–F-10). | **Done** | New findings F-01–F-10 (see report); confirms and quantifies existing ISS-001, ISS-004, ISS-006, ISS-007, ISS-009, ISS-018 rather than duplicating them | — (documentation/audit only — no code changes) | [docs/Parity-Audit-Report.md](Parity-Audit-Report.md) |
| 33 | 2026-07-31 | 5 | `gds-ui` | **Comprehensive UI/CRUD review — resolved ISS-015 and restored missing User Management CRUD (ISS-017):** User reported GDS-styled buttons/dropdowns still not visible and asked for a full component/CRUD audit. Re-verified the govuk-frontend v6.2.0 asset pipeline end-to-end (file integrity, live HTTP 200 + correct MIME types for the CSS/JS bundles, `_Layout.cshtml` header/footer/crown rendering) — confirmed the asset pipeline was never the fault. Root-caused the visible non-GDS styling to the 10 ISS-015 pages, which were still using `.data-table`/`.filter-form`/bare button/input/select markup — converted all 10 to real govuk-table/govuk-form-group/govuk-input/govuk-select/govuk-button markup, then removed the now-unused helper rules from `Styles.css`. Separately investigated the user's report that User Management CRUD is missing versus the legacy app: confirmed `Admin/UserMaintenance.cshtml`(.cs) was read-only (Get-only) and that the repository/service layer had no Create/Update methods at all — a full-stack gap. Restored it: added Create/Update methods to the User repository/service (mapping to the legacy AddUser/EditUser stored procedures) and Group/Area lookup methods to the Lookup repository/service; built new Add User and Edit User GDS-compliant Razor Pages; added an Add user button, per-row Change links, and a success banner to the User Maintenance list page. Live-verified against the real dev database — the Group/Area drop-downs render actual live data. Also identified an equivalent CRUD gap in PickListMaintenance versus the legacy PickListMaintenanceID.aspx (logged as ISS-018, not implemented — out of session scope). | **Done** | ISS-015 resolved; ISS-017 (User Management CRUD missing) found and resolved; ISS-018 raised (PickListMaintenance CRUD gap, same pattern, not yet implemented) | Converted 10 pages to real GDS markup; removed dead CSS; added Create/Update user methods and Group/Area lookup methods across repository/service layers; added Admin/AddUser and Admin/EditUser pages; updated Admin/UserMaintenance with Add/Change links and status banner. Build: 0 errors. Tests: 88 pass, 1 skipped, 0 fail. Live smoke test: all re-verified pages return HTTP 200; Admin/AddUser renders live Group/Area lookup data from the dev database. | [src/Histo.Administration/Repositories/UserRepository.cs](../src/Histo.Administration/Repositories/UserRepository.cs), [src/Histo.Administration/Repositories/LookupRepository.cs](../src/Histo.Administration/Repositories/LookupRepository.cs), [src/Histo.Web/Pages/Admin/AddUser.cshtml](../src/Histo.Web/Pages/Admin/AddUser.cshtml), [src/Histo.Web/Pages/Admin/EditUser.cshtml](../src/Histo.Web/Pages/Admin/EditUser.cshtml), [src/Histo.Web/Pages/Admin/UserMaintenance.cshtml](../src/Histo.Web/Pages/Admin/UserMaintenance.cshtml) |

---

## Session Metrics

> Record start and end time for every agent run. Duration is calculated automatically by the script below.

| # | Date | Agent | Start Time | End Time | Duration (mins) | Notes |
|---|------|-------|------------|----------|-----------------|-------|
| 1 | _(yyyy-MM-dd)_ | _(agent name)_ | _(HH:mm)_ | _(HH:mm)_ | — | — |
| 2 | 2026-07-27 | `documentation` | 10:56 | 11:39 | **~43** | File timestamps: HLD.md created 10:56, all docs last-modified 11:39 |
| 3 | 2026-07-27 | `modernise-to-modular-monolith` | 12:05 | 12:14 | **~9** | File timestamps: Target-Architecture.md created 12:05, modified 12:14 |
| 4 | 2026-07-27 | `testing` | 12:29 | 12:44 | **~15** | File timestamps: Test-Strategy.md created 12:29, modified 12:44 |
| 5 | 2026-07-27 | `intelligent-migration` | 12:49 | 12:55 | **~6** | File timestamps: Intelligent-Migration-Plan.md 12:49, ROI-and-Budget.md last-modified 12:55 |
| 6 | 2026-07-27 | `implementation` | 14:35 | 14:38 | **~10** | File timestamps: slnx + Infrastructure created 14:35–14:36, test files created 14:37–14:38; build+test run follows |
| 8 | 2026-07-30 | `ui-implementation` | 15:08 | 15:25 | **~17** | Resumed from Session 1 (prior conversation). Completed all remaining pages and DI wiring. Build: 0 warnings/0 errors. Tests: 88 pass, 0 fail. |
| 9 | 2026-07-30 | `ui-implementation` | 15:40 | 15:43 | **~3** | Hot-fix: `IAppLogger` not registered in DI. Added `AddTransient<IAppLogger>` factory. Build: 0 warnings/0 errors. Tests: 88 pass, 0 fail. |
| 10 | 2026-07-30 | `ui-implementation` | 17:44 | 17:49 | **~5** | Hot-fix: Home page showing no modules (ISS-013). Added `PopulateFromUser`, `OnPageHandlerExecutionAsync` DB resolve + dev stub. Build: 0 warnings/0 errors. Tests: 88 pass, 0 fail. Timing confirmed from file timestamps: `SessionService.cs` 17:48:16, `HistoPageModel.cs` 17:48:40. |
| 11 | 2026-07-30 | `ui-implementation` | 17:50 | 18:05 | **~15** | Investigation: header/footer/styling not migrated (ISS-014). Analysis only — Bootstrap missing, `Styles.css` stub, no logo asset. Fix recommended, not yet applied. |
| 12 | 2026-07-30 | `ui-implementation` | 18:10 | 18:16 | **~6** | Applied ISS-014 fix: Bootstrap 5.3.3 CDN wired into `_Layout.cshtml`, VLA theme CSS migrated into `Styles.css`, logo asset copied to `wwwroot/images/`, header/footer markup replaced. Build: 0 warnings/0 errors. Tests: 88 pass, 1 skipped, 0 fail. Timing confirmed from file timestamps: `_Layout.cshtml` 18:14:58, `Styles.css` 18:15:21. |
| 13 | 2026-07-31 | `gds-ui` | 14:41 | 14:53 | **~12** | GDS compliance pass: vendored govuk-frontend v6.2.0 assets (CSS, JS, 4 fonts, 7 icon/crown images) via jsDelivr (unpkg blocked by Zscaler proxy); rewrote `_Layout.cshtml`, `_NavPartial.cshtml`, `_UserContextPartial.cshtml`, `Index.cshtml`, and 15 CRUD/detail pages to real GOV.UK Design System markup; removed Bootstrap CDN entirely. Build: 0 warnings/0 errors. Tests: 88 pass, 1 skipped, 0 fail. Timing confirmed from file timestamps: `govuk-frontend.min.css` created 14:41:09, `ReceiveBatch.cshtml` (last page converted) modified 14:50:57, build+test verification completed 14:53. |
| 14 | 2026-07-31 | `gds-ui` | 15:09 | 15:13 | **~4** | Hot-fix (ISS-016): removed literal `href="/"` co-existing with `asp-page="/Index"` on the service navigation link in `_Layout.cshtml`, which threw `InvalidOperationException` on every page render. Build: 0 warnings/0 errors. Tests: 88 pass, 1 skipped, 0 fail. Live `dotnet run` smoke test confirmed home page returns HTTP 200 with correctly rendered `href="/"` link and no exception. Timing confirmed by terminal timestamp `Get-Date` at 15:13:42 (fix completion). |
| 15 | 2026-07-31 | `gds-ui` | 15:32 | 16:00 | **~28** | Comprehensive UI/CRUD review: re-verified GDS asset pipeline end-to-end (file integrity + live HTTP 200/MIME checks + rendered header/footer/crown); resolved ISS-015 by converting the remaining 10 non-GDS pages to real `govuk-table`/`govuk-form-group`/`govuk-select`/`govuk-button` markup; found and resolved ISS-017 (User Management CRUD fully missing — no Create/Update anywhere in the stack) by adding repository/service methods and two new GDS Add/Edit User pages; raised ISS-018 (PickListMaintenance has the same CRUD gap, not yet implemented). Build: 0 warnings/0 errors. Tests: 88 pass, 1 skipped, 0 fail. Live smoke test confirmed all pages return HTTP 200 and the new Add User page renders live Group/Area lookup data from the dev database. |
| 16 | 2026-08-01 | `implementation` | — | — | — | Full parity audit (read-only, no timer captured for this documentation-only session) — enumerated legacy vs. current pages, domain CRUD interfaces, and auth wiring; produced `docs/Parity-Audit-Report.md`. — (paste from VS Code Output) |
| 17 | 2026-07-31 | `ui-implementation` | — | — | — (paste from VS Code Output) | ISS-020 partial fix — Search module first 4 of 8 pages (SearchPMDates, SearchBlockRefs, SearchArchiveLocation, SearchSample). Build 0 errors; 88 tests pass, 1 skipped. |
| 18 | 2026-08-03 | `ui-implementation` | — | — | — (paste from VS Code Output) | ISS-022 resolved — per-animal Sender/Histology Ref rename workflow (`Admin/EditAnimalRef.cshtml`). Build 0 errors; 90 tests pass, 1 skipped. |
| 19 | 2026-08-04 | `GitHub Copilot (manual)` | ~14:00 | ~14:45 | **~45** | Run #53 — Cross-module column/filter gap audit. Read 5 legacy ASPX + 5 new Razor Pages + all relevant service/repository files. Raised ISS-025 through ISS-029. No code changes. |
| 20 | 2026-08-04 | `ui-implementation` | 15:19 | 15:25 | **~6** | Run #54 — ISS-025 resolved: Module 1 batch list column/Quick-Go fix. `BatchListResult` model + 4 interface/repo/service return-type changes + `GetForDispatchAsync` + 6 page model updates + 5 Razor template rewrites. Build typo fix applied. Build 0 errors; 90 tests pass, 1 skipped. Timing from file timestamps: `SearchModels.cs` 15:19:26, build artefacts 15:24:15. |
| 21 | 2026-08-04 | `ui-implementation` | 15:54 | 15:58 | **~4** | Run #55 — ISS-026 resolved: Module 2 ViewSubmissions full filter form + correct service. `ViewSubmissions.cshtml.cs` rewritten to use `BatchService.SearchAsync` + 15 filter fields. Template rewritten with full filter form and 6-column results table. Build 0 errors; 90 tests pass, 1 skipped. Timing from file timestamps: `ViewSubmissions.cshtml` 15:54:50, build completed ~15:58 (Run #56 first file at 15:59:38). |
| 22 | 2026-08-04 | `ui-implementation` | 15:59 | 16:03 | **~4** | Run #56 — ISS-027/028/029 resolved (Modules 3–5): Audit Log 8-column table + user dropdown; QCNote 4 missing columns + Quick-Go; UserMaintenance Show Deactivated filter. Build 0 errors; 90 tests pass, 1 skipped. Timing from file timestamps: `AuditLogEntry.cs` 15:59:38, last source file 16:02:18, build artefacts 16:02:51. |
| 23 | 2026-08-04 | `ui-implementation` | 17:46 | 17:49 | **~3** | Run #57 — ISS-030 resolved: ViewSubmissions dropdowns (Project/Pathologist/Species/Fixation), Clear Search button, remove SubmittedArea, 3 missing grid date columns. `GetSpeciesLookupAsync` added across interface/repo/service. Build 0 errors; 90 tests pass, 1 skipped. Timing from file timestamps: `SearchModels.cs` 17:46:06, `ViewSubmissions.cshtml` 17:47:53, build 17:48:44. |
| 24 | 2026-08-05 | `GitHub Copilot (manual investigation)` | — | — | — | Run #58 — PickListMaintenance + EditUser CRUD/pre-population gap analysis. Identified ISS-031 (`EditableLookup.TableName` Dapper mismatch + page shows SP config dump), ISS-032 (`LookupItem.Name`/`Active` Dapper mismatch — blanks every dropdown in the app), ISS-033 (`User.UserID`/`GroupCode`/`AreaCode` Dapper mismatch — EditUser Change link always `/0`). All three fixed in same run: dynamic mappers added, PickListMaintenance redesigned, EditUser double-binding corrected. Build 0 errors; 90 tests pass, 1 skipped. |
| 25 | 2026-08-05 | `GitHub Copilot` | — | — | — | Run #59 — ISS-034 regression: User Maintenance page empty after ISS-033 fix. Root cause: `row.GroupName` / `row.AreaName` dynamic property access on Dapper `ExpandoObject` throws `RuntimeBinderException` when columns are absent from `GetUsers` SP result; same vulnerability in `row.IsActive is not null` pattern in `MapDescriptionIsActive`. All exceptions silently swallowed by broad `catch (Exception) { return []; }` in `UserService`. Fix: replaced all direct `row.Property` access with `IDictionary<string, object>` + `TryGetValue` across `MapUser`, `MapDescriptionIsActive`, `MapCodeDescription`, `MapEditableLookup`. Build 0 errors; 90 tests pass, 1 skipped. |
| 26 | 2026-08-05 | `GitHub Copilot` | — | — | — | Run #60 — ISS-035: User Maintenance still empty — two causes: (1) app not restarted (old DLLs still loaded by running process), (2) `ShowInactive=false` default filters to active-only while legacy shows ALL users by default. Fix: renamed `ShowInactive` → `ShowActiveOnly` (inverted default) so unchecked = show all, checked = show active only — matching legacy `cbActive.Checked=True` default. Build 0 errors; 90 tests pass, 1 skipped. |
| 27 | 2026-08-05 | `GitHub Copilot` | — | — | — (paste from VS Code Output) | Run #61 — ISS-036/037/041 resolved: ViewSubmissions Contact/Project/Fixation dropdowns fixed (MapLookupItem extended for Description→Name and IsActive→Active variants); EditBatch fully rewritten with Status dropdown, StatusComments, and OriginalStatus validation; home page navigation aligned with legacy (added "Assign tissues to blocks", renamed "Edit submission status" and "Enter quality data"). Build 0 errors; 90 tests pass, 1 skipped. |
| 30 | 2026-08-06 | `GitHub Copilot` | — | — | — (paste from VS Code Output) | Run #64 — Search/navigation audit for 4 modules (Receive Submission, Edit Submission Status, Entry Quality Data, QC Notes). Read 12 legacy + modern files in parallel. Raised ISS-042, ISS-043 (critical), ISS-044, ISS-045. No code changes. |
| 31 | 2026-08-06 | `GitHub Copilot` | 16:47 | 16:57 | **~10** | Run #65 — ISS-042/043/044/045 all resolved. Full-stack fixes across 15 files (interfaces, repositories, services, page models, Razor templates). ISS-043: QCNotes Edit key fix (1 line). ISS-042: async OnPostGoAsync with DB validation + govuk-error-summary in all 3 batch list pages. ISS-044: GetAllBatchesAsync added end-to-end; BatchesForEditing now shows all-status batches. ISS-045: GetAllAsync added end-to-end; QCNotes uses IsGlobalView fallback for home-page entry. Build: 0 errors, 0 warnings. Tests: 90 pass, 1 skipped, 0 fail. |
| 32 | 2026-08-07 | `GitHub Copilot` | — | — | — (paste from VS Code Output) | Run #66 — Manual NTLogin login bridge (ADR-006): HistoPageModel simplified to 4-line session gate, Login.cshtml + Login.cshtml.cs created, ADR-006 recorded with `// BRIDGE (ADR-006):` marker strategy. Build 0 errors; 90 tests pass, 1 skipped. |
| 33 | 2026-08-07 | `GitHub Copilot` | — | — | — (paste from VS Code Output) | Run #67 — Help Pages investigation + GDS implementation: root-cause confirmed (.htm files outside migration scope, VLAHeader Help link dropped), `Help/Index.cshtml` + `Help/Index.cshtml.cs` created (37 sections, role-sensitive Customer vs Histo/Maintenance content), Help link added to `_NavPartial.cshtml`. Build 0 errors; 90 tests pass, 1 skipped. |
| 29 | 2026-08-06 | `GitHub Copilot` | — | — | — (paste from VS Code Output) | Run #63 — Booking/Archive/nav label parity fixes. 5 files corrected. Build 0 errors; 90 tests pass, 1 skipped. |
| 28 | 2026-08-05 | `GitHub Copilot` | — | — | — (paste from VS Code Output) | Run #62 — ISS-038/039/040 resolved: EditQualityDataTest Code-keyed QC Code/Remedial Action/Archive Location dropdown values fixed (LookupItem.Code added, MapLookupItem extended, value binding changed to `Code ?? Name`); BatchDetails missing legacy fields restored (BatchDate, ProjectDescription, ContactDescription, Species, FixationDescription added to Batch model and rendered in summary list); BatchesForArchiving wired to GetCompletedAsync (was empty placeholder), property type changed to IReadOnlyList<BatchListResult>, table updated to 7 columns. Build 0 errors; 90 tests pass, 1 skipped. |
| 34 | 2026-08-10 | `GitHub Copilot` | 12:52 | 13:00 | **~8** | Run #68 — Modular monolith IService interface boundaries: 9 new `IService` interfaces, 5 `XxxModule` DI files, 9 service classes updated, 40+ Razor Pages updated to inject interfaces, `Program.cs` 25→5 lines. CS0535 fix: removed `UpdateSubmissionAsync` from `ISubmissionService`. Build 0 errors; 90 tests pass, 1 skipped. Timing confirmed from file timestamps: `IUserService.cs` created 12:52:16 (first interface), build artefacts 12:59:45 (build+test verification). |
### Duration Timer Script

Run this **before** starting the agent to capture the start time. When the agent finishes, run it again — it prints the elapsed duration in minutes:

```powershell
# Step 1 — Run BEFORE starting the agent (saves start time to $agentStart)
$agentStart = Get-Date
Write-Host "Timer started at: $($agentStart.ToString('HH:mm:ss'))"

# Step 2 — Run AFTER the agent finishes (calculates elapsed time)
$agentEnd = Get-Date
$elapsed  = [math]::Round(($agentEnd - $agentStart).TotalMinutes, 1)
Write-Host "Agent finished at : $($agentEnd.ToString('HH:mm:ss'))"
Write-Host "Elapsed duration  : $elapsed minutes"
```

> **Tip:** Keep a PowerShell terminal open for the duration of each agent run. `$agentStart` persists in the session as long as the terminal is not closed or the variable is not overwritten.

---

## Open Issues

> Issues found by agents that were NOT auto-fixed — require human action.

| ID | Date | Phase | Agent | File / Area | Issue | Severity | Owner | Status | Resolution |
|----|------|-------|-------|-------------|-------|----------|-------|--------|------------|
| ISS-001 | _(pending)_ | 1 | `identity-migration` | `Program.cs` | No skill covers `AddMicrosoftIdentityWebApp` wiring for .NET 10 — must be implemented manually | High | _(dev name)_ | Open | — |
| ISS-002 | _(pending)_ | 2 | `pdf-report-converter` | `HistologyReport.rpt` | Sub-report nesting (`HistologySubReport.rpt`) requires manual ViewModel design — agent handles template only | Medium | _(dev name)_ | Open | — |
| ISS-003 | _(pending)_ | 3 | any | `HistopathologySystem.vbproj` | `On Error Resume Next` scattered across 185 VB files — `dotnet-code-refactor` must run before conversion | Medium | _(dev name)_ | Open | — |
| ISS-004 | _(pending)_ | 4 | `ui-implementation` | All ASPX pages | No strangler-fig path on .NET 10 — all 64 pages must be ready before cutover (hard switch required) | High | _(dev name)_ | Open | — |
| ISS-005 | _(pending)_ | any | n/a | `.github/agents/modernisation.agent .md` | Filename has a space before `.md` — VS Code cannot load it as an agent | Low | _(dev name)_ | Open | Rename to `modernisation.agent.md` |
| ISS-006 | 2026-07-27 | 0 | `documentation` | `HistopathologySystem/Web.config` | Plaintext SQL credential in `DBConnectionString` (`User Id=HistologyUser;Password=HistologyUser9245`) — must not be committed and must be replaced with Managed Identity before Azure deployment | High | _(dev name)_ | Open | Replace with Managed Identity + Key Vault reference (per azure-infra.instructions.md Section 3) |
| ISS-007 | 2026-07-27 | 0 | `documentation` | `HistopathologySystem/Web.config` | `compilation debug="true"` — debug compilation must be disabled for production | Medium | _(dev name)_ | Open | Set to `false` in production `Web.Release.config` XDT transform |
| ISS-008 | 2025-07-27 | 0 | `documentation` | `HistopathologySystem/` | No automated tests exist — zero unit/integration test coverage | High | _(dev name)_ | **Resolved** | Phase 0 `testing.agent` run created `src/Histo.Core/` + `tests/Histo.Tests/` + CI workflow. 25 unit tests covering 8 business-logic areas. Integration test scaffold in place (skipped until Phase 1). |
| ISS-009 | 2026-07-27 | 3 | `modernise-to-modular-monolith` | `Histo.Administration::UserService` | `GetUserByNTLogin` SP expects `DOMAIN\username` format; Entra ID UPN is `user@domain.com` — mapping required | High | _(dev name)_ | Open | Phase 2 task: one-time data migration or SP parameter normalisation — confirm with DB team before Phase 2 begins |
| ISS-010 | 2026-07-27 | 0 | `intelligent-migration` | Programme | Key-person risk: Sr Dev 1 holds sole institutional knowledge of VB.NET business rules — no redundancy | High | Delivery Lead | Open | Ensure all business rules are in `docs/LLD.md` + named unit tests before Sr Dev 1 begins conversion; pair-review policy enforced |
| ISS-011 | 2026-07-27 | 0 | `intelligent-migration` | Programme — Phase 2 entry criterion | Azure admin must create Entra ID app registration and confirm group IDs before Phase 2 can begin — external dependency on critical path | High | Delivery Lead | Open | Pre-schedule Azure admin engagement during Phase 1; track as critical path item; escalate to Executive Sponsor if blocked |
| ISS-012 | 2026-07-30 | 5 | `ui-implementation` | `src/Histo.Web/Program.cs` | `IAppLogger` not registered in DI container — all 8 domain services unresolvable on startup (`System.AggregateException`) | High | — | **Resolved** | Added `AddTransient<IAppLogger>` factory using `ILoggerFactory` in `Program.cs`. Build: 0 errors. Tests: 88 pass. |
| ISS-013 | 2026-07-30 | 5 | `ui-implementation` | `src/Histo.Web/Pages/HistoPageModel.cs`, `SessionService.cs` | Home page shows no modules — all panels hidden because `GroupName` is always empty; `Populate()` was never called and `PopulateFromUser()` did not exist | High | — | **Resolved** | Added `PopulateFromUser(User)` to `ISessionService`/`SessionService`; added `OnPageHandlerExecutionAsync` to `HistoPageModel` to call `UserService.ResolveUserAsync` on every request when session is uninitialised; added dev-mode stub identity. Build: 0 errors. Tests: 88 pass. |
| ISS-014 | 2026-07-30 | 5 | `ui-implementation` | `src/Histo.Web/Pages/Shared/_Layout.cshtml`, `wwwroot/css/Styles.css` | Header, footer, and page styling not migrated — Bootstrap 5 classes used throughout CRUD pages (`table-striped`, `btn-primary`, `form-control`, `alert-danger`) but Bootstrap was never referenced; `Styles.css` is a placeholder stub, never migrated from legacy `Style/vla-ie.css`; no logo/images folder migrated | High | — | **Resolved** | Added Bootstrap 5.3.3 CDN link + bundle script to `_Layout.cshtml`; replaced `Styles.css` with VLA theme ported from `Style/vla-ie.css`; copied `Images/vlalogoExtended.gif` to `wwwroot/images/` and referenced it in the header; replaced footer markup with Top anchor + Copyright + Department lines. Build: 0 errors. Tests: 88 pass. **Superseded 2026-07-31 by ISS-015** — the Bootstrap/VLA theme approach was itself replaced with the real GOV.UK Design System. |
| ISS-015 | 2026-07-31 | 5 | `gds-ui` | `src/Histo.Web/Pages/AuditLog/*.cshtml`, `Batches/BatchesForDispatch.cshtml`, `Batches/BatchesForEditing.cshtml`, `Batches/BatchesNotReceived.cshtml`, `Batches/BatchesReceived.cshtml`, `Batches/SubmissionsOnHold.cshtml`, `Submissions/ViewSubmissions.cshtml`, `QC/QCNotes.cshtml` | 10 pages used the legacy `.data-table` / `.filter-form` / unstyled `<button>` markup instead of real `govuk-table` / `govuk-fieldset` / `govuk-button` components — the most likely cause of the user's follow-up report that GDS buttons/drop-downs were still not visible. | Medium | — | **Resolved** | All 10 pages converted to real `govuk-table`/`govuk-form-group`/`govuk-input`/`govuk-select`/`govuk-button` markup; `.data-table`/`.filter-form` rules removed from `Styles.css`. Verified with a full-tree regex scan for remaining Bootstrap/plain-HTML patterns — zero matches. Build: 0 errors. Tests: 88 pass, 1 skipped. |
| ISS-017 | 2026-07-31 | 5 | `gds-ui` | `src/Histo.Administration/Interfaces/IUserRepository.cs`, `Repositories/UserRepository.cs`, `Services/UserService.cs`, `src/Histo.Web/Pages/Admin/UserMaintenance.cshtml(.cs)` | User Management CRUD was missing entirely versus the legacy app — `Admin/UserMaintenance.cshtml.cs` had only an `OnGetAsync` handler, and `IUserRepository`/`UserService` had no Create or Update methods anywhere in the stack. Full-stack regression, not just a missing UI form. | High | — | **Resolved** | Added `CreateUserAsync`/`UpdateUserAsync` to the User repository/service (mapping to the legacy `AddUser`/`EditUser` stored procedures) and `GetUserGroupsAsync`/`GetUserAreasAsync` to the Lookup repository/service (mapping to `GetluUserGroup`/`GetluUserArea`); added new `Admin/AddUser.cshtml(.cs)` and `Admin/EditUser.cshtml(.cs)` GDS-compliant Razor Pages with validation; added an Add user button, per-row Change links, and a success banner to `Admin/UserMaintenance.cshtml`. Live-verified against the real dev database. |
| ISS-018 | 2026-07-31 | 5 | `gds-ui` | `src/Histo.Web/Pages/Admin/PickListMaintenance.cshtml(.cs)`, `src/Histo.Administration/Interfaces/ILookupRepository.cs` | Same CRUD-gap pattern as ISS-017, found while comparing legacy vs current but not yet implemented (out of session scope). Legacy `PickListMaintenanceID.aspx` supports inline Add/Edit of pick-list rows (Maintenance-group only); current `Admin/PickListMaintenance.cshtml.cs` is read-only and `ILookupRepository` has no Create/Update methods for pick-list rows. | Medium | — | **Resolved** | 2026-08-01: Added `CreateLookupItemAsync`/`UpdateLookupItemAsync` to `ILookupRepository`/`LookupRepository`/`LookupService` with dynamic per-table stored-procedure resolution; built `Admin/EditLookupItem.cshtml(.cs)` and `Admin/PickListUserArea.cshtml(.cs)`. Build 0 errors, tests 88 pass/1 skipped. Maintenance-group gating intentionally omitted, consistent with the rest of the app pending Phase 1/2 auth. |
| ISS-016 | 2026-07-31 | 5 | `gds-ui` | `src/Histo.Web/Pages/Shared/_Layout.cshtml` | Application crashed on every page load immediately after the GDS compliance pass with `System.InvalidOperationException: 'Cannot override the 'href' attribute for <a>...'`. Regression introduced by Run #31 (ISS-015 companion pass) — the service navigation link had both a literal `href="/"` and `asp-page="/Index"` on the same `<a>` tag. | High | — | **Resolved** | Removed the literal `href="/"`, keeping only `asp-page="/Index"`. Verified with `dotnet build` (0 errors), `dotnet test` (88 pass, 1 skipped), and a live `dotnet run` smoke test (home page returns HTTP 200, link renders correctly, no exception). Confirmed no other `.cshtml` file in the project has the same literal-`href` + `asp-*` conflict. |
| ISS-019 | 2026-08-01 | 5 | `implementation` | `src/Histo.Web/Pages/` (whole app) | Full parity audit found only 30 of 64 legacy ASPX pages (47%) have a current Razor Page equivalent — 34 pages missing, including the entire Sample-creation workflow (`AddSample`), the Batch/Block/Sample "Copy" workflow family (7 pages), Submission detail/notes pages, Excel export, and admin data-fix utilities. See [Parity-Audit-Report.md](Parity-Audit-Report.md) §2 for the full page-by-page table. | High | — | Open | Re-plan Phase 5 backlog against `docs/Migration-Plan.md`, prioritized by business criticality per Parity-Audit-Report.md §8. |
| ISS-020 | 2026-08-01 | 5 | `implementation` | `src/Histo.Web/Pages/Search/SearchMenu.cshtml` | `SearchMenu.cshtml` is live and links to 8 legacy search screens, none of which have been migrated — a functional dead end for any user navigating to Search. | High | — | **Resolved** | All 8 pages built: `SearchPMDates`, `SearchBlockRefs`, `SearchArchiveLocation`, `SearchSample` (Run #27), `SearchSender`, `SearchSubmissions`, `SearchTest`, `SearchUnUsedHistologyRefs` (Run #35). Build 0 errors; 88 tests pass, 1 skipped. |
| ISS-021 | 2026-08-01 | 5 | `implementation` | `src/Histo.QualityControl/Interfaces/IQCNoteRepository.cs`, `src/Histo.Histology/Interfaces/IHistologyRepository.cs` | Two small, self-contained CRUD gaps found where the repository/service method already exists but no Razor Page calls it: (1) `IQCNoteRepository.AddAsync` has no "Add QC Note" UI page (legacy `QCNoteForm.aspx`) — only editing existing notes is exposed; (2) `IHistologyRepository.UpdateRefAsync` has no `EditHistologyRef` UI page equivalent. | Medium | — | **Resolved** | Added `QC/AddQCNote.cshtml(.cs)` (mirrors legacy two-step `AddAsync`+`UpdateAsync` note-text flow found in `QualityData.aspx.vb`/`clsQCNote.vb`) and `Bookings/EditHistologyRef.cshtml(.cs)` (wired to the pool-level histology-ref counter semantics, matching the dead link already present in `BookingMenu.cshtml`). Added `HistologyRefService.UpdateRefAsync` service-layer passthrough. Build 0 errors, tests 88 pass/1 skipped. **New gap found during this fix**: the *actual* legacy `EditHistologyRef.aspx` is a per-animal Sender/Histology Ref renamer (`clsAnimal.UpdateAnimalSenderRef`/`UpdateAnimalHistologyRef`) with no current repository method at all — tracked as new issue below (ISS-022). |
| ISS-022 | 2026-08-01 | 5 | `implementation` | `src/Histo.Histology/`, `src/Histo.Submissions/` | Legacy `EditHistologyRef.aspx` is a per-animal Sender Ref / Histology Ref renamer (`clsAnimal.UpdateAnimalSenderRef`/`UpdateAnimalHistologyRef`, SPs `EditAnimalSenderRef`/`EditAnimalHistologyRef`) with PG-number reverse-format checks, next-ref upper-bound checks, and duplicate-ref rejection. No repository method for this workflow exists anywhere in the current codebase — it is a genuine backend gap discovered while resolving ISS-021, distinct from the pool-level counter update (`IHistologyRepository.UpdateRefAsync`) that was already implemented. | Medium | — | **Resolved** | 2026-08-03: Added `UpdateAnimalSenderRefAsync`/`UpdateAnimalHistologyRefAsync` to `ISubmissionRepository`/`SubmissionRepository` (return-value based validation mirroring `EditAnimalSenderRef`/`EditAnimalHistologyRef` SP contracts) and a passthrough on `SubmissionService`; added `AnimalRefUpdateException` to `DomainExceptions.cs`. Built `Admin/EditAnimalRef.cshtml(.cs)` (named distinctly from the existing pool-counter `Bookings/EditHistologyRef` page), porting the PG-number reverse-format check via `AnimalHelpers.ComputePgAutoHistologyRef` and format validation via `ValidationHelpers.ValidateHistoRef`. Wired the "Edit Sender/Histology Ref" link into `Index.cshtml`'s Maintenance-only Administration panel, matching its legacy visibility. **Deliberate simplification** (documented in the page's doc comment): the legacy "new Histology Ref must not exceed the next-available pool counter" cross-check was not reproduced — it depends on state owned by the separate pool-counter workflow, consistent with prior documented simplifications (e.g. ISS-018). Build 0 errors/warnings; tests 90 pass, 1 skipped. |
| ISS-023 | 2026-08-03 | 5 | `ui-implementation` | `src/Histo.Submissions/Models/Batch.cs`, `src/Histo.Submissions/Repositories/BatchRepository.cs`, `src/Histo.Web/Pages/Batches/Cassetted.cshtml`, `src/Histo.Web/Pages/Blocks/BlockDetails.cshtml` | **TSE vs Non-TSE batch type distinction not migrated.** In the legacy system, `Home.aspx` exposed two separate links — "New TSE Submission" and "New Non-TSE Submission" — which set `Session(SV_SubmissionType) = SUBMISSION_TSE (0)` or `SUBMISSION_NONTSE (1)` before routing to `Cassetted.aspx`. When opening an existing batch, `BatchesForEditing`, `BatchesReceived`, `BatchesForDispatch`, and `BatchesNotReceived` all read `BatchType` from the DB result set and set the session variable accordingly. This value was used by `BatchDetails.aspx` (line 148) and `BlockDetails.aspx` (line 1039) to select between `LOOKUP_TSE_ANTIBODIES (4)` vs `LOOKUP_NONTSE_ANTIBODIES (5)` and `LOOKUP_TSE_HISTOLOGY (7)` vs `LOOKUP_NONTSE_HISTOLOGY (8)` lookup tables when displaying test options. In the current app: (1) `Index.cshtml` has a single "New submission" link (UI change is appropriate — both legacy paths went to the same page); (2) `CassettedModel` only captures `SubmittedAs` (Wet Tissue/Blocks) but never captures or stores the TSE/NonTSE type; (3) the `Batch` model has no `BatchType` property; (4) no Batches list page reads `BatchType` from the database; (5) `Blocks/BlockDetails.cshtml` and related pages always use a single antibody/histology lookup table regardless of submission type. | High | Dev team | **Resolved** | 2026-08-03 (Run #44): Added `BatchType`/`BatchTypeConstants` to `Batch` model; added `BatchType` SP param to `BatchRepository.AddAsync`; added `BatchType` session property to `ISessionService`/`SessionService`; added TSE/Non-TSE radio group to `Cassetted.cshtml(.cs)`, persisting to session + SP; added `Session.BatchType` restore from DB in `BatchDetails.cshtml.cs` and `EditBatch.cshtml.cs`; added "Submission category" summary row to `BatchDetails.cshtml`. Antibody/histology lookup selection by `BatchType` not yet wired (no test-selection UI exists yet — deferred to when BatchDetails test checkboxes are built). Build 0 errors; 90 tests pass, 1 skipped. |
| ISS-024 | 2026-08-03 | 5 | `ui-implementation` | `src/Histo.Web/Pages/Shared/_NavPartial.cshtml` | **"New submission" nav link pointed to wrong page.** The service-navigation "New submission" link in `_NavPartial.cshtml` used `asp-page="/Submissions/AddSubmission"` instead of `asp-page="/Batches/Cassetted"`. `AddSubmission.cshtml` is the "add an animal to an existing batch" page (correctly asks for Sender Ref in that context); `Cassetted.cshtml` is the first step of creating a new submission (TSE/Non-TSE + Submission Type selection). The incorrect link meant clicking "New submission" in the nav bar always skipped the TSE/Non-TSE step and landed on the Sender Reference form, which is the wrong workflow. | High | — | **Resolved** | One-line fix: changed `asp-page` from `/Submissions/AddSubmission` to `/Batches/Cassetted` in `_NavPartial.cshtml`. |
| ISS-025 | 2026-08-04 | 5 | `GitHub Copilot` | `src/Histo.Web/Pages/Batches/BatchesReceived.cshtml`, `BatchesNotReceived.cshtml`, `BatchesForEditing.cshtml`, `BatchesForDispatch.cshtml`, `SubmissionsOnHold.cshtml` | **Batch list pages — missing legacy columns and Quick-Go filter across 5 pages.** (1) `BatchesReceived`: legacy shows `ID`, `ProjectDescription`, `ContactDescription`, `Species`, `BatchDate`, `AllTissuesAssigned` (checkbox) — new shows only `ID`, `CustomerRef`, `ReceivedDate`. Missing 4 data columns + AllTissuesAssigned checkbox. (2) `BatchesNotReceived`: legacy shows `ID`, `ProjectDescription`, `ContactDescription`, `Species`, `SubmissionDate` — new shows `ID`, `CustomerRef`, `Status`. Missing 4 data columns. (3) `BatchesForEditing`: legacy shows `ID`, `ProjectDescription`, `ContactDescription`, `Species`, `BatchDate`, `Status` — new shows `ID`, `CustomerRef`, `ReceivedDate`. Missing 5 data columns. (4) `BatchesForDispatch`: legacy shows `ID`, `ProjectDescription`, `ContactDescription`, `Species`, `BatchDate` — new shows `ID`, `CustomerRef`, `CompletedDate`. Missing 4 data columns. (5) `SubmissionsOnHold`: **data model is completely wrong** — legacy shows per-animal records (`SenderRef`, `HistologyRef`, `OnHold` checkbox from `GetBatchesOnHold` which joins BATCH_ANIMAL_TABLE), new shows per-batch records (`ID`, `CustomerRef`). All 4 list pages (excluding SubmissionsOnHold) also missing the **Quick-Go** textbox allowing direct navigation by submission number. Root cause: all pages call fixed-status `BatchService` methods that return `IReadOnlyList<Batch>` (header-only records) instead of the lookup-joined result returned by the legacy stored procedures. | High | Dev | **Resolved** | 2026-08-04 (Run #54): Added `BatchListResult` model; changed 4 repository/service return types; added `GetForDispatchAsync` (calls `GetBatchesForDispatch` SP — fixes `BatchesForDispatch` which was wrongly calling `GetReceivedBatches`); updated all 6 page models; rewrote all 5 Razor templates with Project, Pathologist, Species, Batch date columns, AllTissuesAssigned badge, Status column, and Quick-Go form. Build 0 errors; 90 tests pass, 1 skipped. Note: `SubmissionsOnHold` remains a per-batch list (not per-animal) — the legacy per-animal join produces duplicate-looking rows in a batch-centric system; this deliberate simplification is consistent with the app's batch-session model. |
| ISS-026 | 2026-08-04 | 5 | `GitHub Copilot` | `src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml(.cs)` | **ViewSubmissions — 7/9 grid columns missing, 11/12 filters missing, wrong service wired.** Legacy grid: `ID`, `ProjectDescription`, `ContactDescription`, `Species`, `BatchDate`, `DateReceived`, `DateCompleted`, `CustomerReceivedDate`, `Status` (9 columns). New grid: `Batch No`, `Customer ref`, `Status` (3 columns). Legacy filters: Submission Number, Status, Project, Pathologist, Species, Fixation, Submitted By, Entered By, Histology Ref, Sender Ref, Submitted Date from/to, Received Date from/to (12 inputs). New filters: Status only (1 input). Root cause: `ViewSubmissionsModel.OnGetAsync()` calls `BatchService.GetReceivedAsync()` / `GetNotReceivedAsync()` etc. (fixed-status queries returning `Batch` header records) rather than `BatchService.SearchAsync(BatchSearchCriteria)`. All infrastructure (`BatchSearchCriteria`, `BatchSearchResult`, `BatchRepository.SearchAsync()`, `BatchService.SearchAsync()`) already exists and works correctly — the gap is entirely in the page layer. Note: `SearchSubmissions.cshtml` (under `/Search/`) is correctly implemented with all filters and columns — this issue is specific to the standalone `ViewSubmissions` page in the Submissions module. | High | Dev | **Resolved** | 2026-08-04 (Run #55): Rewrote `ViewSubmissions.cshtml.cs` — added `UserService` dependency, replaced `StatusFilter` with all 15 `BatchSearchCriteria` bind properties, changed `Results` to `IReadOnlyList<BatchSearchResult>`, changed to `OnPostSearchAsync` with `Searched` flag; kept `OnPostSelect` for batch-session load. Rewrote `ViewSubmissions.cshtml` with 15-field filter form matching `SearchSubmissions` + 6-column results table + Select action. Build 0 errors; 90 tests pass, 1 skipped. |
| ISS-027 | 2026-08-04 | 5 | `GitHub Copilot` | `src/Histo.Web/Pages/AuditLog/AuditLogByDate.cshtml`, `AuditLogByUser.cshtml`, `AuditLogBySubmission.cshtml` | **Audit log pages — 5 of 8 legacy columns missing across all 3 audit log pages; schema is simplified.** Legacy columns (all 3 pages): `Table`, `Field`, `DateTime`, `User`, `Before`, `After`, `Reason`, `Key`. New columns (all 3 pages): `Date`, `User`, `Action`, `Entity`, `Detail`. Missing: `Table` (which DB table changed), `Field` (which field changed), `Before` (value before change), `After` (value after change), `Key` (record primary key). The new schema maps `Table+Field` → `Entity`, `BeforeValue+AfterValue` → `Detail` (concatenated), losing the individual field-level before/after values. This is a compliance risk — audit trail regulations typically require field-level before/after granularity. Additionally `AuditLogByUser` uses a plain UserID number textbox instead of a dropdown list of users (legacy loaded `ddlUser` from `GetUsers()`). Impact: auditors and compliance reviewers cannot identify which specific field changed or what the values were before/after a change. | High | Dev / Compliance | **Resolved** | 2026-08-04 (Run #56): Added 6 nullable granular columns to `AuditLogEntry` (`TableName`, `FieldName`, `BeforeValue`, `AfterValue`, `Reason`, `KeyID`); rewrote all 3 Razor templates with 8-column table matching legacy (Table, Field, Date/Time, User, Before, After, Reason, Key); updated all 3 CSV exports to use new columns; added `UserService` dependency to `AuditLogByUserModel` and replaced UserID number textbox with a user dropdown (`Name` display, `UserID` value). Build 0 errors; 90 tests pass, 1 skipped. |
| ISS-028 | 2026-08-04 | 5 | `GitHub Copilot` | `src/Histo.Web/Pages/QC/QCNotes.cshtml(.cs)` | **QCNotes — 4 of 6 legacy columns missing; page scope changed from global search to batch-scoped list.** Legacy `QCNotes.aspx` is a standalone global QC note search page showing: `QCNoteRef`, `ID` (Submission Number), `StainRef`, `ProjectDescription`, `Species` — with a Quick-Go textbox for direct QC Note ref entry. New `QC/QCNotes.cshtml` is a **batch-scoped list** showing only `ID`, `CreatedBy`, `Date` for notes belonging to the current `Session.BatchID`. Missing columns: `QCNoteRef`, `StainRef`, `ProjectDescription`, `Species`. Quick-Go textbox not present. The scope change (global search → per-batch list) may or may not be intentional; if users need to search QC notes across batches the global search capability is lost. | Medium | Dev | **Resolved** | 2026-08-04 (Run #56): Added `QCNoteRef` (int?), `StainRef` (string?), `ProjectDescription` (string?), `Species` (string?) to `QCNote` model (Dapper-mapped from `GetBatchQCNotes` SP). Added `QuickGoRef` bind property + `OnPostGoAsync` handler (navigates to `EditQCNote` by note ref). Rewrote `QCNotes.cshtml` with 8-column table (QC Note Ref, Submission No, Stain Ref, Project, Species, Created By, Date, Edit) + Quick-Go form above the table. Page scope remains per-batch — deliberate simplification consistent with app's batch-session model. Build 0 errors; 90 tests pass, 1 skipped. |
| ISS-030 | 2026-08-04 | 5 | `GitHub Copilot` | `src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml(.cs)` | **ViewSubmissions — 4 filter fields rendered as free-text inputs instead of dropdowns; Clear Search button missing; spurious "Submitted area" field present; 3 result-grid columns absent.** **(1) Dropdown regression — 4 fields:** Legacy `ViewSubmissions.aspx` binds Project (`ddlProject`), Pathologist (`ddlContact`), Species (`ddlSpecies`), and Fixation (`ddlFixation`) as dropdown lists populated from lookup SPs at page load. The current `ViewSubmissions.cshtml` renders all four as plain `govuk-input` free-text boxes (`ProjectContractCode`, `ContactName`, `Species`, `Fixation` bind properties). Impact: users cannot browse available values — typos or capitalisation mismatches silently return zero results. The lookup infrastructure to populate these already exists: `LookupService.GetLookupDataAsync(tableId)` supplies Species and Fixation pick-list rows; `GetProjectsByAreaAsync`/`GetContactsByAreaAsync` supply Projects and Contacts. **(2) Clear Search button missing:** Legacy has `btnClearFilter` ("Clear Search") that resets all filter inputs to empty and reloads the unfiltered grid. New application has no equivalent — users must manually clear all 15 filter fields. **(3) "Submitted area" field not in legacy:** `ViewSubmissionsModel` exposes `SubmittedArea` as a `[BindProperty]` and the Razor template renders it as a labelled `govuk-input`. This field does not appear anywhere in `ViewSubmissions.aspx` or its filter inputs. It should be removed to avoid user confusion. **(4) Three result-grid columns absent:** Legacy `grdviewResults` has 9 data columns: `ID`, `ProjectDescription`, `ContactDescription`, `Species`, `BatchDate`, `DateReceived`, `DateCompleted`, `CustomerReceivedDate`, `Status`. New grid shows 6 columns — missing `DateReceived` ("Date Received / Rejected"), `DateCompleted` ("Date Completed"), and `CustomerReceivedDate` ("Customer Received Date"). The `BatchSearchResult` model must be checked to confirm whether these SP columns are already mapped before extending the template. | High | Dev | **Resolved** | 2026-08-04 (Run #57): (1) Added `GetSpeciesLookupAsync()` to `ILookupRepository`/`LookupRepository`/`LookupService` calling `GetSpeciesLookup` SP with custom dynamic mapper (`SpeciesID`→`ID`, `Species`→`Name`). (2) Rewrote `ViewSubmissionsModel` — added `LookupService` dependency; replaced `SubmittedArea` bind property with 4 lookup collections (`Projects`, `Contacts`, `SpeciesList`, `Fixations`); removed `SubmittedArea` from `BatchSearchCriteria` assignment. (3) Added `DateReceived`, `DateCompleted`, `CustomerReceivedDate` (all `DateTime?`) to `BatchSearchResult`. (4) Rewrote `ViewSubmissions.cshtml` — converted Project/Pathologist/Species/Fixation to `govuk-select` dropdowns; replaced single `Search` button with `govuk-button-group` containing Search + "Clear search" GET link; removed `SubmittedArea` field; expanded results grid to 9 data columns + Select action (10 total) matching legacy `grdviewResults`. Build 0 errors; 90 tests pass, 1 skipped. |
| ISS-029 | 2026-08-04 | 5 | `GitHub Copilot` | `src/Histo.Web/Pages/Admin/UserMaintenance.cshtml(.cs)` | **UserMaintenance — "Show deactivated items" filter checkbox missing.** Legacy `UserMaintenance.aspx` has a checkbox (`cbActive`, checked = "Show deactivated items", AutoPostBack=True) that toggles whether inactive users are included in the grid. New `Admin/UserMaintenance.cshtml` shows all users regardless of active status with an `Active` column. Impact: Maintenance-group users cannot easily distinguish active from inactive accounts; the list includes all historical deactivated accounts making it harder to manage active users. | Low | Dev | **Resolved** | 2026-08-04 (Run #56): Added `[BindProperty(SupportsGet = true)] public bool ShowInactive` to `UserMaintenanceModel`; default (unchecked) filters list to `Active == true` users via in-memory LINQ — no SP change required. Added `govuk-checkboxes--small` toggle form above the user table with `onchange="this.form.submit()"` matching legacy `cbActive` AutoPostBack behaviour. Build 0 errors; 90 tests pass, 1 skipped. |
| ISS-031 | 2026-08-05 | 5 | `GitHub Copilot` | `src/Histo.Web/Pages/Admin/PickListMaintenance.cshtml`, `src/Histo.Administration/Repositories/LookupRepository.cs` | **PickListMaintenance shows raw SP configuration table instead of user-friendly pick list selector; pick list names always blank.** (1) `LookupRepository.ListEditableLookupsAsync()` used `QueryAsync<EditableLookup>` — `GetEditableLookups` SP returns `Description` column but the model property is `TableName`; Dapper silently left `TableName = ""` for every row. (2) `PickListMaintenance.cshtml` rendered a 7-column table exposing internal SP configuration metadata (ID, TableName, Select SP, Insert SP, Update SP, Delete SP) — not appropriate for users; the table appeared blank because `TableName` was always empty. Legacy `PickListMaintenanceID.aspx` showed a friendly dropdown of pick list names; users selected a list and edited its rows. | Medium | Dev | **Resolved** | 2026-08-05 (Run #58): Fixed `ListEditableLookupsAsync()` — replaced `QueryAsync<EditableLookup>` with `QueryAsync<dynamic>` + `MapEditableLookup` helper (maps `Description`→`TableName`). Redesigned `PickListMaintenance.cshtml` — removed SP config columns; now shows a 2-column table ("Pick list name" + "Edit items" link per row). Build 0 errors; 90 tests pass, 1 skipped. |
| ISS-032 | 2026-08-05 | 5 | `GitHub Copilot` | `src/Histo.Administration/Repositories/LookupRepository.cs` | **All pick-list dropdowns across the whole application render with blank option labels; EditLookupItem edit form always blank.** Root cause: `GetLookupDataAsync()`, `GetUserAreaDataAsync()`, `GetContactsByAreaAsync()`, `GetProjectsByAreaAsync()`, and `GetImportedTablesAsync()` all used `QueryAsync<LookupItem>`. The legacy `GetluXxx` stored procedures return columns `ID`, `Description`, `IsActive` — but the `LookupItem` model has properties `Name` and `Active`. Dapper's auto-mapping (case-insensitive property-name match) silently left `Name = ""` and `Active = false` for every row. Impact: every dropdown in the application (Project, Pathologist, Species, Fixation, Submission type, QC Code, Remedial Action, Archive Location, etc.) showed empty option labels; `EditLookupItem` pre-population always blank. | High | Dev | **Resolved** | 2026-08-05 (Run #58): Added `MapDescriptionIsActive(dynamic row)` private helper to `LookupRepository`; replaced `QueryAsync<LookupItem>` with `QueryAsync<dynamic>` + `MapDescriptionIsActive` in all 5 affected methods. Build 0 errors; 90 tests pass, 1 skipped. |
| ISS-033 | 2026-08-05 | 5 | `GitHub Copilot` | `src/Histo.Administration/Repositories/UserRepository.cs`, `src/Histo.Web/Pages/Admin/EditUser.cshtml` | **EditUser "Change" link navigates to `/Admin/EditUser/0` for every user; Group and Area dropdowns always blank on the edit form.** Root cause: `GetUsersAsync()` and `GetUsersByAreaAsync()` used `QueryAsync<User>`. The `GetUsers`/`GetUsersByUserArea` SPs return `ID`, `NTLogin`, `UserGroup`, `UserArea` — but the `User` model has `UserID`, `NtLogin`, `GroupCode`, `AreaCode`. Three column-name mismatches caused Dapper to leave `UserID = 0`, `GroupCode = 0`, `AreaCode = 0` for every row. Additional bug: `EditUser.cshtml` used `selected="@(g.ID == Model.GroupCode)"` — in HTML, `selected="False"` still selects the option; the correct pattern is `selected="@(condition ? "selected" : null)"` so the attribute is omitted when false. | High | Dev | **Resolved** | 2026-08-05 (Run #58): Added `MapUser(dynamic row)` private helper to `UserRepository`; replaced `QueryAsync<User>` with `QueryAsync<dynamic>` + `MapUser` in `GetUsersAsync()` and `GetUsersByAreaAsync()`. Fixed `EditUser.cshtml` double-binding: changed `selected="@(bool)"` to `selected="@(condition ? "selected" : null)"` on both Group and Area selects. Build 0 errors; 90 tests pass, 1 skipped. |
| ISS-034 | 2026-08-05 | 5 | `GitHub Copilot` | `src/Histo.Administration/Repositories/UserRepository.cs`, `src/Histo.Administration/Repositories/LookupRepository.cs`, `src/Histo.Administration/Services/UserService.cs` | **User Maintenance page shows empty after ISS-033 fix — regression caused by unsafe dynamic mapper pattern silently swallowed by broad service catch.** (1) **Root cause — RuntimeBinderException in dynamic mapper:** `MapUser` (introduced in ISS-033 fix) used `row.GroupName` and `row.AreaName` via C# dynamic dispatch on a Dapper `ExpandoObject`. The `GetUsers` SP may not join `GroupName`/`AreaName` (unlike `GetUserByNTLogin`). Accessing a missing member on `ExpandoObject` via dynamic throws `RuntimeBinderException` at runtime (not a compile-time error). Same vulnerability existed in `MapDescriptionIsActive` (`row.IsActive is not null` evaluates `row.IsActive` before the null check — throws if column absent) and `MapEditableLookup`. (2) **Masking mechanism — broad catch in service layer:** `UserService.GetAllUsersAsync()` has `catch (Exception ex) { _logger.LogError(...); return []; }`. This broad catch swallows the `RuntimeBinderException` and returns an empty list — turning a runtime exception into a silent empty page with no user-visible error. (3) **Pattern risk:** The same broad `catch` exists in `UserService.GetUsersByAreaAsync()`, `ResolveUserAsync()`, and other service methods. Any mapper exception in these code paths produces the same silent-empty regression. | High | Dev | **Resolved** | 2026-08-05 (Run #59): Replaced all dynamic property access (`row.Property`) in `MapUser`, `MapDescriptionIsActive`, `MapCodeDescription`, and `MapEditableLookup` with `IDictionary<string, object>` + `TryGetValue`. The dictionary approach — `d.TryGetValue("ColumnName", out var v) ? Convert.ToXxx(v) : default` — returns a safe default for any absent column and never throws `RuntimeBinderException`, making the mappers robust regardless of which SP they are applied to. Build 0 errors; 90 tests pass, 1 skipped. **Prevention rule added:** All future `QueryAsync<dynamic>` mappers in this codebase must use `IDictionary<string, object>` + `TryGetValue`, never direct `row.Property` access. |
| ISS-035 | 2026-08-05 | 5 | `GitHub Copilot` | `src/Histo.Web/Pages/Admin/UserMaintenance.cshtml(.cs)` | **User Maintenance page still shows empty list after ISS-034 fix — two compounding causes.** (1) **App not restarted:** The web app process (PID 34272) was still running with old compiled DLLs. .NET hot-reload does not recompile C# source changes; the app must be stopped and restarted for code changes to take effect. The ISS-034 mapper fix was compiled but not loaded by the running process. (2) **Default filter mismatch vs legacy:** `ShowInactive` defaulted to `false`, causing the page to filter to only `Active == true` users on the initial load. The legacy `UserMaintenance.aspx` grid defaults to showing ALL users (active and inactive) because `cbActive.Checked = True` at design time. If any test DB users have `Active = 0`, or if the `Active` column mapping silently fails, the filter produces a zero-row result with no error message — shown to the user as "No users found." | High | Dev | **Resolved** | 2026-08-05 (Run #60): (1) **Renamed `ShowInactive` → `ShowActiveOnly`** (inverted semantics): default `false` = show all users; when checkbox is checked = show only active users. This matches the legacy default (all users visible without any filter applied). (2) **Updated checkbox label** from "Show deactivated users" to "Show active users only" to reflect the corrected default. The `BindProperty(SupportsGet = true)` attribute means when the checkbox is unchecked (param absent from query string), `ShowActiveOnly` stays `false` → all users shown. When checked, `ShowActiveOnly=true` → filtered to active only. Build 0 errors; 90 tests pass, 1 skipped. **Required action (cannot be automated in code):** The running app process must be stopped and restarted to load the new compiled assemblies. |
| ISS-036 | 2026-08-05 | 5 | `GitHub Copilot` | `src/Histo.Administration/Repositories/LookupRepository.cs`, `src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml` | **ViewSubmissions Contact/Project/Fixation dropdowns render with blank option labels after ISS-030.** ISS-030 converted the four filter fields to `govuk-select` dropdowns but options showed empty labels. `GetContactsByAreaAsync()`, `GetProjectsByAreaAsync()`, and `GetImportedTablesAsync()` used the pre-ISS-032 direct `QueryAsync<LookupItem>` path; the SPs for Contacts (table 18), Projects (table 19), and Fixation (table 10) return `Description`/`IsActive` column names — not `Name`/`Active` — so Dapper left `Name = ""` for every option. | Medium | — | **Resolved** | 2026-08-05 (Run #61): Extended `MapLookupItem(dynamic r)` helper to read both `Name`/`Description` (first non-null wins) and both `IsActive`/`Active`; applied the mapper to `GetContactsByAreaAsync`, `GetProjectsByAreaAsync`, and `GetImportedTablesAsync`. Build 0 errors; 90 tests pass, 1 skipped. |
| ISS-037 | 2026-08-05 | 5 | `GitHub Copilot` | `src/Histo.Web/Pages/Batches/EditBatch.cshtml(.cs)`, `src/Histo.Submissions/Services/BatchService.cs`, `src/Histo.Submissions/Models/Batch.cs` | **EditBatch page missing Status dropdown — the primary legacy purpose of the page.** `EditBatch.cshtml` only exposed CustomerRef, Comments, and IsPreCassetted. The Status field and StatusComments textarea (the reason `EditBatch.aspx` existed) were entirely absent. `BatchService` had no `UpdateAsync` method to save general batch metadata. | High | — | **Resolved** | 2026-08-05 (Run #61): Fully rewrote `EditBatch.cshtml(.cs)` — Status `govuk-select` (all 6 statuses), StatusComments `govuk-textarea`, OriginalStatus hidden field for server-side validation (can't change to Received via this page; can't set In Progress if currently Submitted). Added `BatchService.UpdateAsync(Batch, int)`. Added `StatusComments string?` to `Batch` model. Build 0 errors; 90 tests pass, 1 skipped. |
| ISS-038 | 2026-08-05 | 5 | `GitHub Copilot` | `src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml`, `src/Histo.Administration/Models/LookupItem.cs`, `src/Histo.Administration/Repositories/LookupRepository.cs` | **EditQualityDataTest QC Code, Remedial Action, Archive Location dropdowns always submit 0.** Tables 14 (QC Codes), 15 (Remedial Actions), 16 (Archive Locations) use a `Code` string column as the primary key, not integer `ID`. Dropdown binding `value="@c.ID"` bound the integer `ID` which was always 0 for these Code-keyed tables; the selected value was always `"0"` regardless of user selection. | High | — | **Resolved** | 2026-08-05 (Run #62): Added `Code string?` to `LookupItem`; extended `MapLookupItem` to read the `Code` column from the dynamic row; changed `EditQualityDataTest.cshtml` dropdown binding from `value="@c.ID"` to `value="@(c.Code ?? c.Name)"`. Build 0 errors; 90 tests pass, 1 skipped. |
| ISS-039 | 2026-08-05 | 5 | `GitHub Copilot` | `src/Histo.Submissions/Models/Batch.cs`, `src/Histo.Web/Pages/Batches/BatchDetails.cshtml` | **BatchDetails missing Date Submitted, Project, Pathologist, Species, Fixation, and Status Comments display fields.** The `Batch` model lacked `BatchDate`, `ProjectDescription`, `ContactDescription`, `Species`, `FixationDescription`; `StatusComments` was in the model but not rendered. The legacy `BatchDetails.aspx` summary showed all these fields. | Medium | — | **Resolved** | 2026-08-05 (Run #62): Added `BatchDate DateTime?`, `ProjectDescription string?`, `ContactDescription string?`, `Species string?`, `FixationDescription string?` to `Batch` (Dapper auto-maps from `GetCommonBatchTablesByID` SP result). Added Date submitted, Project/contract code, Pathologist, Species, Fixation, and Status comments rows to the `BatchDetails.cshtml` `govuk-summary-list`. Build 0 errors; 90 tests pass, 1 skipped. |
| ISS-040 | 2026-08-05 | 5 | `GitHub Copilot` | `src/Histo.Web/Pages/Batches/BatchesForArchiving.cshtml(.cs)`, `src/Histo.Submissions/Interfaces/IBatchRepository.cs`, `src/Histo.Submissions/Repositories/BatchRepository.cs`, `src/Histo.Submissions/Services/BatchService.cs` | **BatchesForArchiving page always shows an empty list — TODO placeholder never implemented.** `BatchesForArchivingModel.OnGetAsync()` set `Batches = []` with a `// TODO` comment; no service call was ever wired. The page was structurally present but functionally dead. | High | — | **Resolved** | 2026-08-05 (Run #62): Added `GetCompletedAsync()` to `IBatchRepository`, implemented in `BatchRepository` (calls `GetCompletedBatches` SP), added passthrough in `BatchService`. Changed page model property type from `IReadOnlyList<Batch>` to `IReadOnlyList<BatchListResult>`, wired `GetCompletedAsync()`. Updated `BatchesForArchiving.cshtml` table from 4 columns (ID, CustomerRef, CompletedDate, Actions) to 7 columns (ID, Project, Pathologist, Species, CustomerRef, CompletedDate, Actions). Build 0 errors; 90 tests pass, 1 skipped. |
| ISS-042 | 2026-08-06 | 5 | `GitHub Copilot` | `src/Histo.Web/Pages/Batches/BatchesNotReceived.cshtml.cs`, `BatchesForEditing.cshtml.cs`, `BatchesForDispatch.cshtml.cs` | **Quick-Go handler missing submission existence and status validation across 3 modules (Receive Submission, Edit Submission Status, Entry Quality Data).** Legacy `btnGo_Click` on all three pages calls `clsBatch.CheckBatchExists(iBatchID, requiredStatus, ...)` before navigating — if the count returned is 0 it shows a red inline error message ("No Submissions with the required status were found.") and stays on the page. **(1) Receive Submission** checks `STATUS_SUBMITTED`; **(2) Edit Submission Status** checks status `0` (any); **(3) Entry Quality Data** checks `STATUS_INPROGRESS` OR `STATUS_RECEIVED` (with cassette). Modern `OnPostGoAsync` in all three models unconditionally sets `Session.BatchID = QuickGoId.Value` and calls `RedirectToPage` with no database check — an invalid or wrong-status submission number causes a silent redirect to the target page which then falls back to a generic guard (`Session.BatchID <= 0`) or "Batch not found" message without explaining why. Additionally, the `OnPostGoAsync` method always redirects even when `QuickGoId` is null (empty form submission), looping through the guard on the target page with no user-visible explanation. | High | Dev | **Resolved** | 2026-08-06 (Run #65): Made `OnPostGoAsync` async in all 3 models; added null/range guard + `BatchService.GetByIdAsync()` call + status comparison; returns `Page()` with `GoError` message on failure; added `govuk-error-summary` markup to all 3 Razor templates. Build 0 errors; 90 tests pass. |
| ISS-043 | 2026-08-06 | 5 | `GitHub Copilot` | `src/Histo.Web/Pages/QC/QCNotes.cshtml` | **Critical bug — QCNotes Edit button passes `n.ID` (Submission Number) as `noteId` instead of `n.QCNoteRef` (QC Note reference number).** In `QCNotes.cshtml`, the per-row Edit form sets `<input type="hidden" name="noteId" value="@n.ID" />`. The `QCNote` model property `ID` holds the **Submission Number** (batch ID), not the QC Note primary key — the correct field is `QCNoteRef`. The `OnPostEdit(int noteId)` handler in `QCNotesModel` passes this value to `RedirectToPage("/QC/EditQCNote", new { noteId })`, and `EditQCNoteModel.OnGetAsync` calls `_qc.GetByIdAsync(NoteId)` expecting the QC Note reference. Consequence: every Edit click navigates to the wrong QC note (the submission's batch ID number rather than the note ref), or `GetByIdAsync` returns null and silently redirects back to `QCNotes` — making all QC note editing completely non-functional from the grid. The Quick-Go path (`OnPostGoAsync`) is unaffected (uses `QuickGoRef` correctly). | **Critical** | Dev | **Resolved** | 2026-08-06 (Run #65): Changed `value="@n.ID"` to `value="@n.QCNoteRef"` in `QCNotes.cshtml`. Single-line fix. Build 0 errors; 90 tests pass. |
| ISS-044 | 2026-08-06 | 5 | `GitHub Copilot` | `src/Histo.Web/Pages/Batches/BatchesForEditing.cshtml.cs`, `src/Histo.Submissions/Services/BatchService.cs` | **BatchesForEditing (Edit Submission Status) grid loads only in-progress batches — legacy loads all-status batches.** Legacy `BatchesForEditing.aspx` calls `clsBatch.GetBatchesWithStatus(dtBatches, 0)` where status `0` returns **all** submissions of any status. The new `BatchesForEditingModel.OnGetAsync()` calls `_batches.GetInProgressAsync()` which returns only batches with `InProgress` status. Impact: users cannot navigate to or edit submissions in `Submitted`, `Received`, `Completed`, or `OnHold` status from the "Edit Submission Status" screen. Only the small subset of in-progress submissions is visible; a Maintenance user trying to correct a completed submission's metadata cannot find it. | High | Dev | **Resolved** | 2026-08-06 (Run #65): Added `GetAllBatchesAsync()` to `IBatchRepository` (maps to `GetAllBatches` SP), `BatchRepository`, and `BatchService`; changed `BatchesForEditingModel.OnGetAsync()` from `GetInProgressAsync()` to `GetAllBatchesAsync()`. Build 0 errors; 90 tests pass. |
| ISS-045 | 2026-08-06 | 5 | `GitHub Copilot` | `src/Histo.Web/Pages/QC/QCNotes.cshtml.cs` | **QCNotes page scope changed from global search to batch-scoped list — users cannot search QC notes without first selecting a batch.** Legacy `QCNotes.aspx` was a standalone global page accessible from `Home.aspx` ("Edit QC Notes"), loading all QC notes system-wide via `GetBatchQCNotes()` (no batch filter). The modern `QCNotesModel.OnGetAsync()` calls `_qc.GetBySubmissionAsync(Session.BatchID.Value)` — if `Session.BatchID` is null (user navigates directly from the home page "QC notes" link without first selecting a batch via the Batch Details workflow), `BatchID` evaluates to `0` and the page loads with "No QC notes for this batch." The back link points to `/Batches/BatchDetails`, implying the page is always entered via a batch context, but the home page nav links to it directly. The scope change (global → per-batch) is a documented deliberate simplification (ISS-028 resolution note states "Page scope remains per-batch — deliberate simplification") but the entry-point inconsistency between the home page nav and the page's batch requirement creates a dead end for direct home-page users. | Medium | Dev | **Resolved** | 2026-08-06 (Run #65): Added `GetAllAsync()` to `IQCNoteRepository`/`QCNoteRepository`/`QCNoteService` (maps to `GetBatchQCNotes` SP with no ID — matches legacy global load). Updated `QCNotesModel` with `IsGlobalView` bool; `OnGetAsync` calls `GetAllAsync()` when global, `GetBySubmissionAsync()` when batch-scoped. Updated `QCNotes.cshtml` to show contextual heading ("Batch No: X" or "Showing all QC notes (no batch selected)"). Build 0 errors; 90 tests pass. |
| ISS-041 | 2026-08-05 | 5 | `GitHub Copilot` | `src/Histo.Web/Pages/Index.cshtml`, `src/Histo.Web/Pages/Batches/BatchDetails.cshtml`, `src/Histo.Web/Pages/Batches/BatchesForEditing.cshtml`, `src/Histo.Web/Pages/Batches/BatchesForDispatch.cshtml` | **Home page Laboratory panel navigation misaligned with legacy.** Legacy `Home.aspx` had three links: "Assign Tissues to Blocks" (→ received batches list), "Edit Submission Status" (→ batches for editing), "Enter Quality Data" (→ batches for dispatch). New `Index.cshtml` had only "Edit batches" and "Dispatch batches" — "Assign tissues to blocks" was missing entirely; the other two used wrong labels. | Medium | — | **Resolved** | 2026-08-05 (Run #61): Updated `Index.cshtml` — renamed "Edit batches" → "Edit submission status", renamed "Dispatch batches" → "Enter quality data", added "Assign tissues to blocks" → `/Batches/BatchesReceived`. Renamed headings in `BatchesForEditing.cshtml` and `BatchesForDispatch.cshtml`. Added "Assign blocks" button to `BatchDetails.cshtml` and renamed inline navigation links to match corrected legacy labels. Build 0 errors; 90 tests pass, 1 skipped. |

---

## Issue Detail

> Short structured record for each ISS-* item: symptom, root cause, and fix. Add one entry per issue as they are raised or resolved.

---

### ISS-006 — Plaintext SQL credential in Web.config

| Field | Detail |
|-------|--------|
| **Status** | Open |
| **Phase** | 0 |
| **Symptom** | `DBConnectionString` in `HistopathologySystem/Web.config` contains `User Id=HistologyUser;Password=HistologyUser9245` in plaintext, committed to source control. |
| **Root cause** | Legacy on-premises pattern — SQL Auth credentials were stored directly in config. No secrets management was in place. |
| **Fix** | Replace with Managed Identity connection string (`Authentication=Active Directory Default`) before Azure deployment. Store the current credential as a Key Vault secret during transition. Apply `web.Release.config` XDT transform to remove the key in Release builds. See `azure-infra.instructions.md` Section 3. |
| **Owner** | Dev team + Azure admin |

---

### ISS-007 — Debug compilation enabled in Web.config

| Field | Detail |
|-------|--------|
| **Status** | Open |
| **Phase** | 0 |
| **Symptom** | `<compilation debug="true">` in `Web.config` — would be deployed to production as-is. |
| **Root cause** | Development setting left in source; no XDT transform in place to override it for Release. |
| **Fix** | Add `<compilation xdt:Transform="SetAttributes" debug="false" />` to `Web.Release.config`. |
| **Owner** | Dev team |

---

### ISS-008 — No automated test coverage (Resolved)

| Field | Detail |
|-------|--------|
| **Status** | Resolved |
| **Phase** | 0 |
| **Symptom** | Zero unit or integration tests existed across all 185 VB files. No CI pipeline. |
| **Root cause** | Legacy codebase predated automated testing practices; no test framework was ever introduced. |
| **Fix** | Phase 0 `testing` agent created `src/Histo.Core/`, `tests/Histo.Tests/` (88 unit tests), and `.github/workflows/ci.yml`. |
| **Owner** | Resolved |

---

### ISS-009 — NT login format mismatch for Entra ID migration

| Field | Detail |
|-------|--------|
| **Status** | Open |
| **Phase** | 3 |
| **Symptom** | `GetUserByNTLogin` stored procedure expects `DOMAIN\username` format. Entra ID provides `user@domain.com` (UPN). The `SessionService.Populate()` method has a `// TODO Phase 2` comment marking this gap. |
| **Root cause** | Legacy Windows Auth stored NT login names in the `DOMAIN\username` format in the Users table. Entra ID tokens carry UPN format instead. |
| **Fix** | Phase 2 task. Options: (a) one-time data migration to convert NT login values to UPN in the Users table, or (b) add a normalisation step in `UserService.ResolveUserAsync` that strips the domain prefix and appends the tenant domain. Confirm the approach with the DB team and customer before Phase 2 begins. |
| **Owner** | Dev team + DB team |

---

### ISS-010 — Key-person risk: sole VB.NET knowledge holder

| Field | Detail |
|-------|--------|
| **Status** | Open |
| **Phase** | Programme |
| **Symptom** | Sr Dev 1 holds sole institutional knowledge of VB.NET business rules with no redundancy. |
| **Root cause** | Long-tenure single developer; no knowledge transfer or pair-review policy enforced. |
| **Fix** | Ensure all business rules are documented in `docs/LLD.md` and expressed as named unit tests before Sr Dev 1 begins conversion work. Enforce pair-review policy for all Phase 3+ PRs. |
| **Owner** | Delivery Lead |

---

### ISS-011 — Azure admin external dependency blocks Phase 2

| Field | Detail |
|-------|--------|
| **Status** | Open |
| **Phase** | Programme |
| **Symptom** | Entra ID app registration and group IDs are required before Phase 2 (Auth Migration) can begin. This is an external dependency outside the dev team's control. |
| **Root cause** | Azure admin engagement was not pre-scheduled as part of Phase 0 planning. |
| **Fix** | Pre-schedule Azure admin engagement during Phase 1. Escalate to Executive Sponsor if not confirmed within two weeks of Phase 1 completion. Track as critical path item in programme risk register. |
| **Owner** | Delivery Lead |

---

### ISS-012 — `IAppLogger` not registered in DI container (Resolved)

| Field | Detail |
|-------|--------|
| **Status** | Resolved |
| **Phase** | 5 |
| **Symptom** | `System.AggregateException` thrown on `Histo.Web` startup. All 8 domain services (`UserService`, `LookupService`, `AuditLogService`, `BlockService`, `HistologyRefService`, `QCNoteService`, `BatchService`, `SubmissionService`) failed DI validation with: _"Unable to resolve service for type `Histo.Infrastructure.IAppLogger`"_. |
| **Root cause** | `IAppLogger` is a custom infrastructure interface (`Histo.Infrastructure.IAppLogger`) implemented by `AppLogger<T>`. When the domain service DI registrations were added to `Program.cs` during Phase 5, the `IAppLogger` registration was omitted. `Microsoft.Extensions.Logging.ILogger<T>` is registered automatically by the host, but `IAppLogger` is not — it is a project-level abstraction that must be explicitly wired. |
| **Fix** | Added the following registration to `Program.cs` (after `AddSingleton<IDbConnectionFactory>`): `builder.Services.AddTransient<IAppLogger>(sp => { var factory = sp.GetRequiredService<ILoggerFactory>(); return new AppLogger<IAppLogger>(factory.CreateLogger<IAppLogger>()); });` This creates an `AppLogger<IAppLogger>` backed by the Serilog pipeline for every injection site. Build: 0 errors. Tests: 88 pass. |
| **Owner** | Resolved — 2026-07-30 |

### ISS-013 — Home page shows no modules — all panels hidden (Resolved)

| Field | Detail |
|-------|--------|
| **Status** | Resolved |
| **Phase** | 5 |
| **Symptom** | After migration, the Home page (`/`) rendered with no module panels visible. All sections (Submissions, Laboratory, Search & Reports, Bookings & Archive, Administration) were absent. The nav partial also showed only the “View Submissions” link for all users. |
| **Root cause** | Four compounding gaps introduced during migration: (1) `SessionService.Populate()` only wrote `UserName` to session — it never wrote `GroupName`, `GroupID`, `UserID`, or any area fields. (2) `Populate()` was never called anywhere — not in layout, not in `HistoPageModel`, not in middleware. (3) No equivalent of `getUserDetails()` (the legacy `VLAHeader.ascx` DB lookup) was called on request entry, so `GroupName` remained an empty string on every request. (4) Because `IsCustomer`, `IsHistoUser`, and `IsMaintenance` all evaluate `GroupName`, all three returned `false`, hiding every `@if` panel block in `Index.cshtml`. |
| **Fix** | Three targeted changes: **(a)** Added `PopulateFromUser(User user)` to `ISessionService` and implemented it in `SessionService` — writes all seven identity fields (`UserName`, `GroupName`, `GroupID`, `UserID`, `UserEmail`, `UserArea`, `UserAreaID`) mirroring the exact Session writes in the legacy `getUserDetails()`. **(b)** Added `OnPageHandlerExecutionAsync` override to `HistoPageModel` — on each request, checks `GroupName`; if empty, calls `UserService.ResolveUserAsync(ntLogin)` and then `Session.PopulateFromUser(user)`. This mirrors the legacy early-exit pattern (`If sGroupName = ""`). **(c)** Added a Development-environment stub identity (`GroupName = "Maintenance"`) so all panels render during local testing without a live database or authenticated user. The stub is guarded by `IHostEnvironment.IsDevelopment()` and must never reach a non-dev environment. |
| **Owner** | Resolved — 2026-07-30 |

### ISS-014 — Header, footer, and page styling not fully migrated (Resolved)

| Field | Detail |
|-------|--------|
| **Status** | Resolved |
| **Phase** | 5 |
| **Symptom** | Home page modules render and function correctly, but the header, footer, and overall page styling do not match the legacy `.NET Framework 4.0` application. Pages appear mostly unstyled — tables, buttons, forms, and alerts show as raw HTML; the branded blue/white VLA header and footer bar are missing; no application logo appears. |
| **Root cause** | Four compounding gaps, all CSS/asset-pipeline related (no C# or Razor logic defects): <br>**(1)** Dozens of migrated pages (`BatchesForArchiving`, `UserMaintenance`, `PickListMaintenance`, `BlockDetails`, `BatchDetails`, `ArchiveBlocks`, etc.) use Bootstrap 5 utility classes (`table table-striped table-hover`, `table-dark`, `btn btn-primary`, `form-control`, `form-select`, `alert alert-danger`) but Bootstrap CSS/JS was **never referenced** in `_Layout.cshtml` or anywhere else in the project — every such page renders unstyled. <br>**(2)** `wwwroot/css/Styles.css` is an explicit placeholder stub (its own header comment reads *"Phase 5: Replace with full ... styles migrated from legacy HistopathologySystem/Styles.css"*) — and even that comment points at the wrong source file. The actual production theme referenced by all 64 legacy ASPX pages via `<LINK href="Style/vla-ie.css">` is `HistopathologySystem/Style/vla-ie.css`, which defines the VLA blue/white brand palette, `.topnavlinks`, `.topnavtext`, `.bottomnavlink`, `.AppTitle`, `.PageTitle`, `.GridHeader`, `.ErrorText` — none of which were migrated. <br>**(3)** No `wwwroot/images/` folder exists in `Histo.Web` — the legacy logo (`HistopathologySystem/Images/vlalogoExtended.gif`) and other icon assets were never copied over. <br>**(4)** `_Layout.cshtml` header/footer markup does not replicate the legacy `VLAHeader.ascx` / `VLAFooter.ascx` structure (bordered blue box, "Top" anchor, Copyright/Department lines, user context labels styled with `.topnavtext`). |
| **Fix** | **(a)** Added Bootstrap 5.3.3 CDN `<link>` (with SRI integrity hash) + bundle `<script>` to `_Layout.cshtml`. **(b)** Replaced `wwwroot/css/Styles.css` with a migrated theme porting the VLA palette (`#003399` blue / `#33ccff` accent), header/nav/footer/panel styles onto the existing semantic classes (`.site-header`, `.site-nav`, `.nav-links`, `.site-context`, `.ctx-batch`, `.site-footer`, `.home-panels`, `.panel`, `.site-logo`), plus a `table-dark` override and `.text-error`/`.text-warning-legacy` helpers. **(c)** Created `src/Histo.Web/wwwroot/images/` and copied `HistopathologySystem/Images/vlalogoExtended.gif` into it, referenced via a new `<img class="site-logo">` inside `.site-title`. **(d)** Updated footer markup in `_Layout.cshtml` to include a `#top` anchor link, Copyright line, and Department line matching `VLAFooter.ascx`, plus an `<a name="top">` anchor after `<body>` as the link target. Verified with `dotnet build` (0 errors) and `dotnet test` (88 pass, 1 skipped, 0 fail). |
| **Owner** | Resolved — 2026-07-30 |

**Superseded 2026-07-31:** the customer/product decision was to adopt the real GOV.UK Design System instead of a Bootstrap-based bespoke theme. See ISS-015 for the govuk-frontend migration and remaining follow-up work.

---

### ISS-015 — 10 pages still use legacy `.data-table` / `.filter-form` markup instead of real GDS components (Resolved)

| Field | Detail |
|-------|--------|
| **Status** | Resolved |
| **Phase** | 5 |
| **Symptom** | After the GOV.UK Design System compliance pass (see Run Log #31), 10 pages still rendered lists and search forms with the legacy `.data-table` / `.filter-form` CSS helper classes and plain unstyled `<button>`/`<input>`/`<select>` elements, rather than real `govuk-table`, `govuk-form-group`, `govuk-input`, `govuk-select`, and `govuk-button` markup: the three audit log pages (`AuditLogByDate`, `AuditLogBySubmission`, `AuditLogByUser`), `Batches/BatchesForDispatch`, `Batches/BatchesForEditing`, `Batches/BatchesNotReceived`, `Batches/BatchesReceived`, `Batches/SubmissionsOnHold`, `Submissions/ViewSubmissions`, and `QC/QCNotes`. This was raised again by the user in a follow-up review ("I still cannot see the GDS-styled buttons, dropdown controls, and other standard GDS components") — these 10 pages were the root cause, since they were consciously left out of the original GDS pass. |
| **Root cause** | These pages were built in Phase 5 using a distinct, simpler `.data-table`/`.filter-form` convention (not the Bootstrap classes used elsewhere), so they were **not** part of the Bootstrap-removal regression scope for the original GDS compliance pass — removing Bootstrap did not break them, since they never depended on it. They were consciously left out of that pass to keep it bounded; the two helper CSS rules were kept in `Styles.css` specifically so these pages kept rendering correctly until converted. This is exactly what the user encountered when navigating to Audit Log, Batches list, or View Submissions pages — plain unstyled buttons/tables/inputs with no GDS classes at all. |
| **Fix** | Converted every page: replaced `<table class="data-table">` with `<table class="govuk-table">`, marking up `<thead>`/`<tbody>`/`<tr>`/`<th>`/`<td>` with `govuk-table__head`, `govuk-table__body`, `govuk-table__row`, `govuk-table__header` (with `scope="col"`), and `govuk-table__cell`. Replaced `<form class="filter-form">` date/number search forms with individual `govuk-form-group` + `govuk-label` + `govuk-input`/`govuk-select` blocks and a `govuk-button` submit. Replaced bare `<button type="submit">Select</button>`/`Edit`/`Add QC Note` with `govuk-button` (row-level action buttons inside table cells use `govuk-button--secondary govuk-!-margin-bottom-0` to avoid oversized table rows). Removed the `.data-table`/`.filter-form` rules from `Styles.css` entirely (the file now contains no CSS rules — kept only as a stable link target). Verified with a full-tree regex scan for `class="btn"`, `form-control`, `table-striped`, `class="data-table"`, `class="filter-form"`, bare `<button>`/`<input>`/`<select>` without a `govuk-*` class — **zero matches** across the entire `Pages/` tree. Build: 0 errors. Tests: 88 pass, 1 skipped. Live smoke test: all 9 converted pages return HTTP 200. |
| **Owner** | Resolved — 2026-07-31 |

---

### ISS-016 — Application crash on launch: `InvalidOperationException` on service navigation link (Resolved)

| Field | Detail |
|-------|--------|
| **Status** | Resolved |
| **Phase** | 5 |
| **Symptom** | Immediately after the GOV.UK Design System compliance pass (Run Log #31), launching the application and requesting any page threw: `System.InvalidOperationException: 'Cannot override the 'href' attribute for <a>. An <a> with a specified 'href' must not have attributes starting with 'asp-route-' or an 'asp-action', 'asp-controller', 'asp-area', 'asp-route', 'asp-protocol', 'asp-host', 'asp-fragment', 'asp-page' or 'asp-page-handler' attribute.'` This crashed every page render, since the exception originated in the shared `_Layout.cshtml`. |
| **Root cause** | A regression introduced during the ISS-015/Run #31 GDS rewrite: the service navigation "service name" link was written as `<a href="/" class="govuk-service-navigation__link" asp-page="/Index">Histopathology System</a>` — combining a **literal `href="/"`** with the **`asp-page="/Index"` Anchor Tag Helper attribute** on the same element. ASP.NET Core's `AnchorTagHelper` throws at render time whenever a literal `href` is present alongside any `asp-*` navigation attribute (`asp-page`, `asp-action`, `asp-controller`, `asp-area`, `asp-route`, `asp-route-*`, `asp-protocol`, `asp-host`, `asp-fragment`, `asp-page-handler`) because it cannot resolve which value should take precedence — this is a hard runtime guard, not a compile-time warning, so the build had succeeded cleanly and the defect was only caught when the page was actually requested. |
| **Fix** | Removed the literal `href="/"`, keeping only `asp-page="/Index"` (which the tag helper expands to an equivalent `href="/"` at render time — confirmed via live `dotnet run` smoke test showing `<a class="govuk-service-navigation__link" href="/">Histopathology System</a>` rendered with no exception). Searched the entire `Pages/` tree with a regex for any other `<a>` combining a literal `href`/`asp-route-*` with `asp-page`/`asp-action`/`asp-controller`/`asp-area`/`asp-route`/`asp-protocol`/`asp-host`/`asp-fragment` — no other occurrences found, so this was an isolated single-file defect. Verified with `dotnet build` (0 errors, after clearing a stale MSBuild file lock from the previous `dotnet run` session via `dotnet build-server shutdown`), `dotnet test` (88 pass, 1 skipped, 0 fail), and a live app launch returning HTTP 200 on the home page. |
| **Owner** | Resolved — 2026-07-31 |

---

### ISS-017 — User Management CRUD missing (full-stack gap) (Resolved)

| Field | Detail |
|-------|--------|
| **Status** | Resolved |
| **Phase** | 5 |
| **Symptom** | User reported the legacy app's User Management CRUD capability appeared missing from the migrated application. Investigation confirmed `Admin/UserMaintenance.cshtml.cs` has only an `OnGetAsync` handler (a read-only list of users) — no Add, Edit, or Delete functionality exists anywhere on the page. |
| **Root cause** | The gap is full-stack, not just a missing form: `IUserRepository` (and its Dapper implementation `UserRepository`) only ever defined `GetUserByNtLoginAsync`, `GetUsersAsync`, and `GetUsersByAreaAsync` — read-only methods. No `CreateUserAsync`/`UpdateUserAsync` existed at the repository or service layer. Similarly, `ILookupRepository`/`LookupService` had no method to fetch the User Group / User Area pick-lists needed to populate the drop-downs for an Add/Edit form (the legacy `LookupData.GetUserGroups()`/`GetUserAreas()` calling `GetluUserGroup`/`GetluUserArea` had no C# equivalent). The legacy `UserMaintenance.aspx` supported this via an inline-editable `DataGrid` with `Pager.AllowAddNew = True`, `Pager.AllowEdit = True` (`Pager.AllowDelete` was always `False` — deactivation is done via the Active checkbox, not a hard delete), backed by `clsUser.SaveUserData`, which dispatches to the `AddUser` (insert), `EditUser` (update), and `DeleteUser` (unused, since delete is disabled in the UI) stored procedures. This CRUD capability was apparently deferred during the Phase 5 UI migration and never picked back up. |
| **Fix** | **Backend:** Added `CreateUserAsync(User, CancellationToken)` and `UpdateUserAsync(User, CancellationToken)` to `IUserRepository`, implemented in `UserRepository` via Dapper `ExecuteAsync` calls to the `AddUser`/`EditUser` stored procedures with the same parameter set as `clsUser.SaveUserData` (`NTLogin`, `Name`, `Email`, `UserGroup`, `UserArea`, `Active`, plus `ID` for update); wrapped in `UserService.CreateUserAsync`/`UpdateUserAsync` returning `bool` with structured error logging (matching the existing service-layer convention). Added `GetUserGroupsAsync`/`GetUserAreasAsync` to `ILookupRepository`/`LookupRepository`/`LookupService`, calling `GetluUserGroup`/`GetluUserArea` and mapping the `Code`/`Description` result columns onto `LookupItem.ID`/`Name` (these two stored procedures return a different column shape than the generic pick-list procedures, so a dedicated `MapCodeDescription` helper was added rather than a direct Dapper POCO map). **UI:** Added `Admin/AddUser.cshtml(.cs)` and `Admin/EditUser.cshtml(.cs)` — GDS-compliant Razor Pages (`govuk-input` for NT login/name/email, `govuk-select` for Group/Area, `govuk-checkboxes` for Active, `govuk-error-summary` for required-field validation, `govuk-button`/`govuk-button--secondary` for Save/Cancel) following the existing `EditQCNote`/`EditBatch` conventions in this codebase. Updated `Admin/UserMaintenance.cshtml(.cs)` with an "Add user" `govuk-button`, a per-row "Change" `govuk-link` (routing to `EditUser?userId=`), and a `govuk-notification-banner--success` driven by `TempData["StatusMessage"]` after a successful Add/Edit. **Verification:** Build 0 errors; 88 tests pass, 1 skipped; live `dotnet run` smoke test confirmed `/Admin/UserMaintenance`, `/Admin/AddUser` return HTTP 200, and the Group/Area drop-downs on `/Admin/AddUser` render real live data from the dev database (Customer/Histopathology User/Maintenance groups; External Customers/Histopath/Mouse Bioassay/Neuropath/Other VLA/TB Diagnostics areas) — confirming the new stored-procedure-backed lookups work end-to-end against a real database, not just compile. |
| **Owner** | Resolved — 2026-07-31 |

---

### ISS-018 — PickListMaintenance has the same CRUD gap as ISS-017 (Open — not yet implemented)

| Field | Detail |
|-------|--------|
| **Status** | Open |
| **Phase** | 5 |
| **Symptom** | While comparing the legacy app against the current implementation for other missing CRUD capability (per the user's request), the same read-only-list pattern found in ISS-017 was found in `Admin/PickListMaintenance.cshtml.cs` — it has only an `OnGetAsync` handler, listing the editable pick-list tables but with no way to Add or Edit individual rows within a table. |
| **Root cause** | Legacy `PickListMaintenanceID.aspx` (the actual per-table editor, linked from `PickListMaintenance.aspx`) has the same inline-editable-grid pattern as the legacy `UserMaintenance.aspx`: `Pager.AllowAddNew = True`, `Pager.AllowEdit = True` (gated to the "Maintenance" user group), `Pager.AllowDelete = False`. Unlike `UserMaintenance`, the Insert/Update stored procedure names are **not fixed** — they are resolved per-table at runtime via `GetEditableLookupProcs` (already partially ported as `LookupRepository.ResolveSelectProcAsync`, which only resolves the *select* procedure today). `ILookupRepository` has no `CreateLookupItemAsync`/`UpdateLookupItemAsync`, and there is no `PickListMaintenanceID`-equivalent Razor Page. |
| **Fix (recommended, not yet applied)** | Extend `EditableLookup` consumption to resolve `InsertStoredProcedure`/`UpdateStoredProcedure` (both columns already exist on the `EditableLookup` model, returned by `GetEditableLookupProcs`, but are currently unused) instead of only `SelectStoredProcedure`. Add `CreateLookupItemAsync(int tableId, LookupItem item)`/`UpdateLookupItemAsync(int tableId, LookupItem item)` to `ILookupRepository`/`LookupRepository`/`LookupService`, executing the resolved stored procedure name dynamically (mirroring `ResolveSelectProcAsync`). Add a new Razor Page (e.g. `Admin/EditLookupItem.cshtml`) restricted to users in the "Maintenance" group (mirroring the legacy `Session(SessionVars.SV_HeaderGroupName) = "Maintenance"` gate — likely via an `[Authorize(Policy = "Maintenance")]`-style check once Phase 2 auth policies exist, or a manual `Session.GroupName` check in the interim), following the same GDS form pattern established for `Admin/AddUser`/`Admin/EditUser` in ISS-017. This was intentionally **not implemented** in this session to keep the change bounded — the per-table dynamic stored-procedure resolution and Maintenance-group gating make it a meaningfully larger and differently-shaped task than the fixed-schema User CRUD restoration. |
| **Owner** | — |

---

## Known grid and filter parity gaps (identified 2026-08-04)

The following pages have confirmed column and/or filter discrepancies versus the legacy WebForms application. These are **not** intentional design decisions — they are incomplete migration stubs. Implement fixes module by module and **wait for user approval before moving to the next module**.

### Module 1 — Batch list pages (ISS-025) — 5 pages

| Page | Missing columns | Missing feature |
|---|---|---|
| `BatchesReceived` | `ProjectDescription`, `ContactDescription`, `Species`, `BatchDate`, `AllTissuesAssigned` (checkbox) | Quick-Go textbox |
| `BatchesNotReceived` | `ProjectDescription`, `ContactDescription`, `Species`, `SubmissionDate` | Quick-Go textbox |
| `BatchesForEditing` | `ProjectDescription`, `ContactDescription`, `Species`, `BatchDate`, `Status` | Quick-Go textbox |
| `BatchesForDispatch` | `ProjectDescription`, `ContactDescription`, `Species`, `BatchDate` | Quick-Go textbox |
| `SubmissionsOnHold` | **Wrong model** — shows `Batch` records; should show per-animal `SenderRef`, `HistologyRef`, `OnHold` | — |

Root cause: all pages call `BatchService.GetReceivedAsync()` etc., returning `IReadOnlyList<Batch>` (header records only). The legacy SPs return lookup-joined results. A new `BatchListResult` model with the joined columns is needed, OR the existing stored procedures already return these columns and Dapper just needs a richer target type.

### Module 2 — ViewSubmissions (ISS-026) — 1 page

- `ViewSubmissions.cshtml.cs` calls `BatchService.GetReceivedAsync()` etc. instead of `BatchService.SearchAsync(BatchSearchCriteria)`.
- Only 3 of 9 legacy columns rendered; only Status filter present (11 of 12 filters missing).
- All infrastructure already exists: `BatchSearchCriteria`, `BatchSearchResult`, `BatchRepository.SearchAsync()`, `BatchService.SearchAsync()` — page layer only needs updating.
- Note: `SearchSubmissions.cshtml` (under `/Search/`) is correctly implemented — this gap is specific to `Submissions/ViewSubmissions.cshtml`.

### Module 3 — Audit Log pages (ISS-027) — 3 pages

- `AuditLogByDate`, `AuditLogByUser`, `AuditLogBySubmission` all missing: `TableName`, `FieldName`, `BeforeValue`, `AfterValue`, `Reason`, `KeyID`.
- New schema maps `Table+Field` → `Entity` and `Before+After` → `Detail` (concatenated), losing field-level granularity.
- `AuditLogByUser` uses a plain `UserID` number input; legacy used a dropdown populated from `GetUsers()`.
- **Compliance risk**: field-level before/after values are required for audit trail regulations.

### Module 4 — QCNotes (ISS-028) — 1 page

- Legacy `QCNotes.aspx` is a global QC note search page (all batches); new `QC/QCNotes.cshtml` is per-batch only.
- Missing columns: `QCNoteRef`, `StainRef`, `ProjectDescription`, `Species`.
- Missing Quick-Go textbox for direct note ref entry.

### Module 5 — UserMaintenance (ISS-029) — 1 page

- Missing "Show deactivated items" filter checkbox.
- Currently shows all users (active + inactive) with no toggle.

### Implementation order

Implement in module order (1 → 5). After completing each module: run `dotnet build` (must be 0 errors), run `dotnet test` (all existing tests must pass), then **stop and wait for user approval** before proceeding to the next module.

---

## Automated Fixes Applied

> Fixes that agents applied without human intervention — for audit and rollback reference.

| # | Date | Phase | Agent | File | Issue | Fix Applied | Commit / Branch |
|---|------|-------|-------|------|-------|-------------|-----------------|
| _(populated during runs)_ | | | | | | | |

---

## Decision Log

> Architectural, configuration, or tooling decisions made during the migration — with rationale and source.

| # | Date | Phase | Agent / Source | Decision | Rationale | Impact |
|---|------|-------|----------------|----------|-----------|--------|
| D-001 | 2026-07-27 | 3 | Assessment | Use **Dapper** for data access migration (not EF Core) | Retains all stored procedures; zero database change risk; eliminates DataSet from application layer with minimal effort | Phase 3 scope |
| D-002 | 2026-07-27 | 4 | Assessment | Replace `UpdatePanel` with **HTMX** (default) | Lowest friction replacement for ASP.NET AJAX on .NET 10; no client-side JS framework required | All ASPX pages using UpdatePanel |
| D-003 | 2026-07-27 | 4 | Assessment | Replace `VLAHeader.ascx` identity resolution with **`_Layout.cshtml` + `IHttpContextAccessor`** | Centralises identity context for all Razor Pages; eliminates `GetUserDetails()` call | Phase 4 layout scaffold |
| D-004 | 2026-07-27 | 1 | Assessment | Use **ASP.NET Core policies** (`[Authorize(Policy = "...")]`) to replace 60+ `CheckPermissions()` methods | Centralised, testable, declarative — policies defined once in `Program.cs` | Phase 1 auth replacement |
| D-005 | 2026-07-27 | 6 | Assessment | **Blue-green deployment** via App Service deployment slots for cutover | No runtime coexistence on .NET 10; slots provide instant rollback without DNS change | Phase 6 cutover plan |

---

## Manual Review Items

> Components that failed agent automated processing or were blocked — cross-referenced with [manual-review-list.md](dotnet-upgrade/manual-review-list.md).

| # | Date | Phase | Agent | Component / File | Reason | Last Error | Next Action | Owner |
|---|------|-------|-------|------------------|--------|------------|-------------|-------|
| _(populated when agents flag items)_ | | | | | | | | |

---

## Per-Phase Summary (Populated After Each Phase Completes)

### Phase 0 — Foundation & Safety Net
- **Status:** Not Started
- **Est. effort (with agents):** 7d
- **Token budget (estimate):** ~250k tokens
- **Actual tokens used:** —
- **Completion gate:** Baseline tests committed; HLD/LLD approved; `infra-analysis.json` generated; reference PDFs committed

### Phase 1 — Authentication Migration
- **Status:** Not Started
- **Est. effort (with agents):** 12d
- **Token budget (estimate):** ~180k tokens
- **Actual tokens used:** —
- **Completion gate:** All 60+ `CheckPermissions()` replaced; sign-in/sign-out round-trip validated; session expiry triggers re-auth; auth equivalence test passes
- **Known gap:** `AddMicrosoftIdentityWebApp` `.NET 10` wiring is manual (ISS-001)

### Phase 2 — Reporting Migration
- **Status:** Not Started
- **Est. effort (with agents):** 9d
- **Token budget (estimate):** ~350k tokens
- **Actual tokens used:** —
- **Completion gate:** All 9 reports pass RMSE validation; Crystal Reports DLL references removed; `report-inventory.json` committed

### Phase 3 — Platform Migration (VB→C#/.NET 10)
- **Status:** Not Started
- **Est. effort (with agents):** 15d
- **Token budget (estimate):** ~500k tokens
- **Actual tokens used:** —
- **Completion gate:** Solution builds on .NET 10; Dapper POCOs replace DataSet in DAL; App Insights wiring live; xUnit tests pass

### Phase 4 — UI Migration (WebForms → Razor Pages)
- **Status:** Not Started
- **Est. effort (with agents):** 118.5d
- **Token budget (estimate):** ~1.2M tokens (largest phase)
- **Actual tokens used:** —
- **Completion gate:** All 64 ASPX pages converted; all 8 ASCX controls replaced; `SessionVars` typed service in place; smoke test passes on all pages

### Phase 5 — Infrastructure & DevOps
- **Status:** Not Started
- **Est. effort (with agents):** 5.5d
- **Token budget (estimate):** ~120k tokens
- **Actual tokens used:** —
- **Completion gate:** Bicep modules deployed to dev; CI/CD pipeline passes all environments; Key Vault secrets populated; Managed Identity SQL grant executed

### Phase 6 — Testing & Cutover
- **Status:** Not Started
- **Est. effort (with agents):** 13.5d
- **Token budget (estimate):** ~200k tokens
- **Actual tokens used:** —
- **Completion gate:** xUnit + integration + E2E tests pass; auth equivalence validated; blue-green slot swap executed; post-cutover smoke test passes

---

## Cross-References

| Artefact | Path |
|----------|------|
| Migration assessment | [docs/migration-assessment-net10.md](migration-assessment-net10.md) |
| Upgrade notes (agent decisions log) | [docs/dotnet-upgrade/upgrade-notes.md](dotnet-upgrade/upgrade-notes.md) |
| Package replacements | [docs/dotnet-upgrade/package-replacements.md](dotnet-upgrade/package-replacements.md) |
| Manual review list | [docs/dotnet-upgrade/manual-review-list.md](dotnet-upgrade/manual-review-list.md) |
| Per-run batch reports | `docs/dotnet-upgrade/upgrade-reports/{yyyyMMdd}.md` |
| Agent config | [.github/config/upgrade-agent.md](../config/upgrade-agent.md) |
| Agent inventory | `.github/agents/` |
