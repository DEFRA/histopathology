# Histopathology System Migration Agent Run Journal version 2
---

## Run Log

> One row per agent invocation. Add rows chronologically.

| # | Date | Phase | Agent | Scope / Input | Status | Issues Found | Fixes Applied | Output / Linked Report |
|---|------|-------|-------|---------------|--------|--------------|---------------|------------------------|
| 1 | 2026-08-25 | 1 | `GitHub Copilot` | **Auth — Entra ID SAML scheme fix + dev environment config:** (1) `InvalidOperationException: No authentication handler for 'Cookies'` — root cause was `HistoPageModel.cs` issuing `ChallengeResult(CookieAuthenticationDefaults.AuthenticationScheme)` instead of `"saml2"`. Fixed. (2) `appsettings.Development.json` populated with real Entra ID tenant/app IDs, IdP metadata URL, ACS and SLO paths. Provided local test steps including Entra admin action to add `https://localhost:57879/Saml2/Acs` as a Reply URL. | **Done** | Entra admin must add localhost Reply URL before local testing works | `HistoPageModel.cs` ChallengeResult scheme fixed; `appsettings.Development.json` updated | [src/Histo.Web/Pages/HistoPageModel.cs](../src/Histo.Web/Pages/HistoPageModel.cs), [src/Histo.Web/appsettings.Development.json](../src/Histo.Web/appsettings.Development.json) |
| 2 | 2026-08-25 | 1 | `GitHub Copilot` | **AccessDenied page — GDS-compliant Entra ID version:** Legacy page used `Layout = null` (no GDS chrome), Windows login messaging (`Environment.UserDomainName`), and stale Windows-era model. Replaced with shared `_Layout.cshtml`, Entra ID-appropriate messaging, and email resolved from SAML claims (`ClaimTypes.Email` / `preferred_username`). | **Done** | — | Removed inline HTML boilerplate; model changed `Login` → `Email?` from SAML claims | [src/Histo.Web/Pages/AccessDenied.cshtml](../src/Histo.Web/Pages/AccessDenied.cshtml), [src/Histo.Web/Pages/AccessDenied.cshtml.cs](../src/Histo.Web/Pages/AccessDenied.cshtml.cs) |
| 3 | 2026-08-25 | 5 | `GitHub Copilot` | **GDS "There is a problem with the service" page (ServiceProblem):** Created `Pages/Errors/ServiceProblem.cshtml(.cs)` per GDS mandatory content rules (title, h1, body copy). Fixed `Program.cs` middleware from non-existent `/Error` to `UseExceptionHandler("/Errors/ServiceProblem")` + `UseStatusCodePagesWithReExecute`. `[ResponseCache(NoStore)]` applied; `TraceIdentifier` logged, never surfaced to user. | **Done** | `UseStatusCodePagesWithReExecute` currently catches 404s too — add `/Errors/PageNotFound` page when 404 pattern is needed | Created `ServiceProblem.cshtml(.cs)`; fixed `Program.cs` middleware | [src/Histo.Web/Pages/Errors/ServiceProblem.cshtml](../src/Histo.Web/Pages/Errors/ServiceProblem.cshtml), [src/Histo.Web/Program.cs](../src/Histo.Web/Program.cs) |
| 4 | 2026-08-25 | 1 | `GitHub Copilot` | **Entra ID security group analysis — authentication vs authorisation split:** Confirmed no Entra ID security group is implemented. Analysed two options: (A) security group as pre-auth gate on Enterprise Application (`Assignment required = Yes`) — no code changes; (B) Entra group claims mapped to app roles — requires backend changes. Recommended Option A. Provided step-by-step portal instructions for creating `SG-HistopathologySystem-Users` and assigning it to the Enterprise Application. | **Done** | Entra admin action required (portal-only, no code) | Analysis only — no code changes | — |
| 5 | 2026-08-25 | 5 | `GitHub Copilot` | **Quality Data session review + regression prevention (session-metrics + migration-run-journal analysis):** Reviewed documented anti-patterns. Applied fixes to `BatchesForDispatch.cshtml`, `QualityData.cshtml`, `EditQualityDataTest.cshtml`: (1) error summary `<li>` changed to `<a href="#QuickGoId">` link; (2) `"Batch No"` → `"Batch no"` sentence case; (3) `QualityData` "Done" destination corrected to `BatchesForDispatch` (was `/Index`, legacy goes back to dispatch list); (4) `EditQualityDataTest` `<form method="post">` given `asp-page` attribute (Run #83 antiforgery pattern); (5) QCNote and Dispatched checkboxes wrapped in proper `govuk-form-group > govuk-checkboxes` structure. Build: 0 errors. | **Done** | — | 5 targeted fixes applied | [src/Histo.Web/Pages/Batches/BatchesForDispatch.cshtml](../src/Histo.Web/Pages/Batches/BatchesForDispatch.cshtml), [src/Histo.Web/Pages/QC/QualityData.cshtml](../src/Histo.Web/Pages/QC/QualityData.cshtml), [src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml](../src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml) |
| 6 | 2026-08-25 | 5 | `GitHub Copilot` | **Quality Data full gap analysis (BatchesForDispatch / QualityData / EditQualityDataTest) + parity fixes:** Gap analysis of 3 pages against legacy. Implemented: (1) `BatchesForDispatch` columns renamed to legacy labels (Submission Number, Project Code, Submitted Date), Completed Date and Customer Ref removed; (2) `QualityData` batch summary expanded to all 10 legacy fields (Project/Contract Code, Pathologist, Entered By/Area, Submitted By/Area, Submission Date, Received Date, Species — all resolved via ILookupService/IUserService lookups); (3) `QualityData` grid gained dedicated On Hold column + separate Failed/Passed columns matching legacy; (4) `EditQualityDataTest` gained Not Tested radio option, QC Note Ref read-only link, Charges checkbox section (LOOKUP_PREMIUM_CHARGES = 17), JS show/hide for Dispatched fields and QC Note conditional on result, `type="number"` on Number of slides, missing validations (DispatchedBy/To required when dispatched, RemedialAction always required, archive date ↔ location cross-check). Build: 0 errors. | **Done** | TC Codes save pending at time of gap analysis (completed in Run #7) | Column renames, summary fields, validation gaps, JS conditionals, Charges UI | [src/Histo.Web/Pages/Batches/BatchesForDispatch.cshtml](../src/Histo.Web/Pages/Batches/BatchesForDispatch.cshtml), [src/Histo.Web/Pages/QC/QualityData.cshtml](../src/Histo.Web/Pages/QC/QualityData.cshtml), [src/Histo.Web/Pages/QC/QualityData.cshtml.cs](../src/Histo.Web/Pages/QC/QualityData.cshtml.cs), [src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml](../src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml), [src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml.cs](../src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml.cs) |
| 7 | 2026-08-25 | 5 | `GitHub Copilot` | **TC Codes (Premium Charges) — full stack implementation:** Investigated legacy `clsCheckBoxData.vb` and confirmed SP names: `AddHistologyTCCode` / `DeleteHistologyTCCode`, `AddAntibodiesTCCode` / `DeleteAntibodiesTCCode`, `AddSpecialStainTCCode` / `DeleteSpecialStainTCCode`. Confirmed `LOOKUP_PREMIUM_CHARGES = 17` from `Common.vb`. Confirmed TC code data is already fetched in `GetBatchBlocksByID` result sets 7–9 (previously skipped). **Full stack implemented:** (1) `BlockTest.cs` — added `TcCode(int Id, string Code)` record and `TCCodes` property; (2) `BlockTestRepository` — reads result sets 7/8/9, builds per-type TC code lookups, populates `TCCodes` on each `BlockTest`; added `SaveTCCodesAsync` delta method (delete removed → insert added); (3) `IBlockTestRepository` / `IBlockTestService` / `BlockTestService` — `SaveTCCodesAsync` added at each layer; (4) `EditQualityDataTest.cshtml.cs` — `SelectedCharges` populated from `Test.TCCodes` on GET; `SaveTCCodesAsync` called after `UpdateAsync` on POST. Build: 0 errors. | **Done** | SP existence in DB should be verified via `sys.procedures` query before testing | Full TC code read + delta save wired end to end | [src/Histo.Histology/Models/BlockTest.cs](../src/Histo.Histology/Models/BlockTest.cs), [src/Histo.Histology/Repositories/BlockTestRepository.cs](../src/Histo.Histology/Repositories/BlockTestRepository.cs), [src/Histo.Histology/Interfaces/IBlockTestRepository.cs](../src/Histo.Histology/Interfaces/IBlockTestRepository.cs), [src/Histo.Histology/Interfaces/IBlockTestService.cs](../src/Histo.Histology/Interfaces/IBlockTestService.cs), [src/Histo.Histology/Services/BlockTestService.cs](../src/Histo.Histology/Services/BlockTestService.cs), [src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml.cs](../src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml.cs) |
| 8 | 2026-08-26 | 5 | `GitHub Copilot` | **Navigation, BatchesForEditing, and EditBatch legacy parity review:** Compared `Quality data` nav link (→`BatchesForEditing`) against `EditSubmissionStatus`/`EditBatch` and legacy `BatchesForEditing.aspx`/`EditBatch.aspx.vb`. Found and fixed 3 gaps: (1) `_NavPartial.cshtml` had a duplicate link — both `Edit submission status` and `Quality data` pointed to `/Batches/BatchesForEditing` — replaced `Quality data` with `Edit QC notes` → `/QC/QCNotes`; (2) `BatchesForEditingModel.OnPostSelect` redirected to `/Batches/BatchDetails` instead of `/Batches/EditBatch` — legacy `grdBatchesForEditing_SelectedIndexChanged` always redirects to `EditBatch.aspx` — fixed; (3) `BatchesForEditing.cshtml` grid headers/columns didn't match legacy (`Batch No`/`Project`/`Batch date` instead of `Submission Number`/`Project Code`/`Submitted Date`; extra `Received date`/`Customer ref` columns not present in legacy) — corrected to match legacy exactly and the sibling `BatchesForDispatch.cshtml` pattern. `EditBatch.cshtml(.cs)` reviewed against `EditBatch.aspx.vb` — status transition validation rules already match legacy correctly, no changes needed. Build: 0 CS errors (only pre-existing file-lock copy errors from a running dev process). | **Done** | — | Nav dedup, row-select redirect target, grid column/header parity | [src/Histo.Web/Pages/Shared/_NavPartial.cshtml](../src/Histo.Web/Pages/Shared/_NavPartial.cshtml), [src/Histo.Web/Pages/Batches/BatchesForEditing.cshtml.cs](../src/Histo.Web/Pages/Batches/BatchesForEditing.cshtml.cs), [src/Histo.Web/Pages/Batches/BatchesForEditing.cshtml](../src/Histo.Web/Pages/Batches/BatchesForEditing.cshtml) |
| 9 | 2026-08-26 | 5 | `GitHub Copilot` | **EditQCNote — restored missing QC Note Ref summary and Created by/Date created sections:** Compared `EditQCNote.cshtml` against legacy `EditQCNote.aspx`/`.aspx.vb`. Legacy top summary box (`QC Note Ref`, `Submission Number`, `Project`, `Species`, `Stain Ref`) and bottom box (`CreatedBy`/`DateCreated` labels, populated from `GetQCNoteTestInformation`'s `Name`/`DateCreated` columns) were entirely absent from the migrated page — only the note textarea remained. Added `CreatedBy`/`DateCreated` to the `QCNote` model, populated them in `QCNoteRepository.GetByIdAsync` from the already-fetched `detail` result, and added a `govuk-summary-list` (QC note ref/Submission number/Project/Species/Stain ref) plus a `govuk-body` "Created by X on <date>" line to `EditQCNote.cshtml`. | **Done** | — | Restored 2 legacy sections | [src/Histo.QualityControl/Models/QCNote.cs](../src/Histo.QualityControl/Models/QCNote.cs), [src/Histo.QualityControl/Repositories/QCNoteRepository.cs](../src/Histo.QualityControl/Repositories/QCNoteRepository.cs), [src/Histo.Web/Pages/QC/EditQCNote.cshtml](../src/Histo.Web/Pages/QC/EditQCNote.cshtml) |
| 10 | 2026-08-26 | 5 | `GitHub Copilot` | **QCNotes Edit button silently non-functional — RuntimeBinderException regression from Run #9:** Reported the `Edit` button on `QCNotes.cshtml` stopped working after the previous fix. Root cause: `QCNoteRepository.GetByIdAsync` used raw `dynamic` member access (`detail?.Name`, `detail?.DateCreated`) added in Run #9 — when the `GetQCNoteHistStainTestInformation` result set lacked those exact columns for a given note, dynamic dispatch threw `RuntimeBinderException`, silently swallowed by `QCNoteService`'s broad `catch (Exception)`, returning `null`; `EditQCNoteModel.OnGetAsync` treats a null `Note` as not-found and redirects straight back to `QCNotes` — the same failure pattern as `ISS-034`. Fixed by replacing dynamic access with the established `IDictionary<string, object>` + `TryGetValue` pattern (matching `LookupRepository.MapDescriptionIsActive`/`UserRepository.MapUser`), so a missing column returns a safe default instead of throwing. | **Done** | — | Dynamic access replaced with safe dictionary lookups | [src/Histo.QualityControl/Repositories/QCNoteRepository.cs](../src/Histo.QualityControl/Repositories/QCNoteRepository.cs) |
| 11 | 2026-08-26 | 5 | `GitHub Copilot` | **EditQCNote — notes missing entirely for antibody tests + blank notes not auto-populated:** Reported that some notes present in legacy do not appear in the current system. Root cause: legacy `clsQCNote.GetQCNoteTestInformation` calls **two** SPs — `GetQCNoteHistStainTestInformation` and `GetQCNoteAntibodiesInformation` — and imports both result sets into one table before reading the note; it also builds a default padded table of Sender Ref/Histo Ref/Block Ref/Test from **all** rows when `QCText` is blank (note never edited), rather than showing nothing. `QCNoteRepository.GetByIdAsync` only called the first SP and only read the first row, so antibody-test notes were dropped entirely and blank notes showed empty instead of the legacy default text. Fixed: added the `GetQCNoteAntibodiesInformation` call (using `BatchType` read from the header row as the `SubmissionType` param, concatenated with the hist/stain rows), and added `BuildDefaultNoteText` reproducing legacy's `InsertSpaces`-padded table. Build: 0 errors. | **Done** | — | Combined both SPs' result sets; reproduced legacy default-note-text generation | [src/Histo.QualityControl/Repositories/QCNoteRepository.cs](../src/Histo.QualityControl/Repositories/QCNoteRepository.cs), [src/Histo.QualityControl/Models/QCNote.cs](../src/Histo.QualityControl/Models/QCNote.cs) |
| 12 | 2026-08-26 | 5 | `GitHub Copilot` | **EditQCNote — Created by date missing after Run #11 fix:** Reported the date next to the username was missing (username alone displayed correctly). Root cause: the `DateCreated` column is returned by the SPs as a `dd/MM/yyyy` `VARCHAR` string (same pattern documented for `NullableDateTimeTypeHandler`); that type handler only runs for Dapper's **typed** query mapping, but `QCNoteRepository` reads these rows dynamically via `IDictionary<string, object>`, bypassing type handlers entirely — the raw string failed the `created is DateTime` check and always evaluated to `null`. Fixed: added a `ParseDate` helper mirroring `NullableDateTimeTypeHandler`'s logic (native `DateTime` passthrough, `dd/MM/yyyy`/`dd/MM/yyyy HH:mm:ss` exact parse, general parse fallback). Build: 0 errors. | **Done** | — | Added dd/MM/yyyy-aware date parsing for the dynamic-read code path | [src/Histo.QualityControl/Repositories/QCNoteRepository.cs](../src/Histo.QualityControl/Repositories/QCNoteRepository.cs) |
| 13 | 2026-08-27 | 5 | `GitHub Copilot` | **TSE/NON-TSE submission workflow GDS redesign + route-based state/access-guard rollout + New Submission Create/Edit flow fixes:** Produced `docs/TSE-NonTSE-Submission-Workflow-Redesign.md` (legacy-vs-current pain points, GDS-aligned target journey, Mermaid diagram). Consolidated block management onto `SubmissionDetailsBlock` (add/edit/delete/copy, per-block tissue assignment, pre-booked block ref + mandatory histology ref for pre-cassetted submissions, bulk "number of blocks"); replaced browser `confirm()` with inline GOV.UK confirmation panels; replaced an auto-submitting checkbox with an explicit Apply button. Added `HistoPageModel.CheckBatchAccessAsync` object-level access guard backed by a pure, unit-tested `BatchAccessDecision.IsAllowed` (`Histo.Core.Domain`), and rolled out route/query-based `BatchId`/`AnimalId` (with session fallback) across `BatchBlockSummary`, `SubmissionDetailsBlock`, `BatchDetails`, `Blocks/BlockDetails`, `CopyBlocks`, `CopySamples(Summary)`. Fixed the New Submission Create/Edit flow: restored Sender Ref search/select on `AddSubmission` via the existing `SearchSender` picker, redirect after adding a sample now goes to Sample Blocks instead of back to the list, and `BatchSubmissionID` is resolved/created instead of silently redirecting to Home when missing. Fixed `Cassetted`'s Submission Type dropdown defaulting to a stale previous selection. Changed `BatchDetails` Submission date to a native `type="date"` input. Investigated and fixed `BatchDetails` button visibility across Create/View Submission journeys (added `IsViewMode`/`CanPrint`; removed the redundant QC notes button and a duplicate Samples task-list row/link). Build: 0 errors. Tests: 144 passed, 1 pre-existing skip. | **Done** | Per-block test selection (EO/H&E/IHC/Special Stain) creation still needs DBA confirmation of the correct stored procedure before implementing — not guessed; task-list overview and full route-based-state rollout to remaining wizard pages (`AddSample`, `SubmissionDetails`, `Cassetted`) deferred as lower-value/higher-risk | Full list in commit — see `docs/TSE-NonTSE-Submission-Workflow-Redesign.md` and session prompts 56–66 in `User-Prompts-Log.md` | [docs/TSE-NonTSE-Submission-Workflow-Redesign.md](TSE-NonTSE-Submission-Workflow-Redesign.md), [src/Histo.Web/Pages/HistoPageModel.cs](../src/Histo.Web/Pages/HistoPageModel.cs), [src/Histo.Core/Domain/BatchAccessDecision.cs](../src/Histo.Core/Domain/BatchAccessDecision.cs), [src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml.cs](../src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml.cs), [src/Histo.Web/Pages/Submissions/AddSubmission.cshtml.cs](../src/Histo.Web/Pages/Submissions/AddSubmission.cshtml.cs), [src/Histo.Web/Pages/Batches/Cassetted.cshtml.cs](../src/Histo.Web/Pages/Batches/Cassetted.cshtml.cs), [src/Histo.Web/Pages/Batches/BatchDetails.cshtml.cs](../src/Histo.Web/Pages/Batches/BatchDetails.cshtml.cs) |
| 14 | 2026-08-27 | 5 | `GitHub Copilot` | **Submission scenario gating, BlockDetails consolidation, and AddAnimal stored-procedure fixes (22:47–23:50):** Investigated "Add animal" button not visible on `BatchBlockSummary` despite previously working, and clarified that Add/Edit/Copy sample buttons must be gated per submission journey (Create/View/Edit/Assign Tissues) rather than always shown — confirmed Edit Submission journey edits header fields on `EditBatch`, while sample/animal/block details are edited via `BatchBlockSummary`/`SubmissionDetailsBlock`. Investigated the Add sample flow redirecting to `AddSubmission` without showing the newly added sample, cross-checked against legacy behaviour. Reviewed the create journey's navigation complexity (New TSE Submission → Submission Samples → Sample Summary → Blocking → Block Details → Search Block Refs) and, on request, implemented the agreed consolidation: merged `Blocks/BlockDetails.cshtml` into `SubmissionDetailsBlock.cshtml` (dual-mode batch-wide/animal-scoped view) with an inline "check used block refs" panel, removing a full page hop. While verifying, found newly added animals still not appearing on the sample list — traced to `BatchBlockSummary`'s either/or animal-list fallback silently dropping block-less animals whenever another animal in the batch already had blocks; fixed via a `MergeAnimals` helper. Fixed two runtime exceptions raised during testing: (1) `System.NotSupportedException: The member PMDate of type System.DBNull cannot be used as a parameter value` in `SubmissionRepository.AddAnimalAsync` — Dapper's `DynamicParameters.Add` requires an explicit `dbType` when the value can be `DBNull.Value`; fixed by adding `dbType: DbType.String` to the `PMDate` parameter; (2) `SqlException: Procedure or function AddAnimal has too many arguments specified` — the migrated call passed extra/incorrect parameters; cross-referenced legacy `clsAnimal.vb` to confirm the real `AddAnimal` signature (`SenderRef, HistologyRef, NextBlockRef, PMDate, OnHold` + `@NewID` OUTPUT) and corrected the Dapper call to match exactly. | **Done** | — | `SubmissionRepository.AddAnimalAsync` parameter/type fixes; `BatchBlockSummary` animal-list merge; `BlockDetails.cshtml` consolidated into `SubmissionDetailsBlock.cshtml` | [src/Histo.Submissions/Repositories/SubmissionRepository.cs](../src/Histo.Submissions/Repositories/SubmissionRepository.cs), [src/Histo.Web/Pages/Submissions/BatchBlockSummary.cshtml.cs](../src/Histo.Web/Pages/Submissions/BatchBlockSummary.cshtml.cs), [src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml](../src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml) |
| 15 | 2026-08-28 | 5 | `GitHub Copilot` | **Submission-journey navigation verification + GDS button-alignment fixes:** Verified back-link/cancel/redirect targets across all 4 submission journeys (Assign Tissues to Blocks, Create/View/Edit Submission) against legacy — all correct, no dead links or wrong redirect targets found. Diagnosed and fixed 4 button-alignment defects caused by missing `govuk-button-group` wrappers or missing `data-module` attributes: (1) `EditBatch.cshtml` — "Samples" link moved inside a `govuk-button-group` (was floating outside any group, misaligned from Save/Cancel); (2) `SubmissionDetailsBlock.cshtml` — added missing `data-module="govuk-button"` to the "Manage sample" link (sibling "Delete" link already had it); (3) `AddSubmission.cshtml` — wrapped "Add sample"/"Cancel" in a `govuk-button-group`, and wrapped the separately-formed "Check historical data for this sender ref" button in its own `govuk-button-group` with top margin so it no longer renders detached; (4) `BatchBlockSummary.cshtml` — "Add sample" and "Done" buttons each wrapped in a `govuk-button-group` instead of ad-hoc margin utility classes. Build: 0 errors. | **Done** | — | 4 files — `govuk-button-group` wrapping + missing `data-module` attribute | [src/Histo.Web/Pages/Batches/EditBatch.cshtml](../src/Histo.Web/Pages/Batches/EditBatch.cshtml), [src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml](../src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml), [src/Histo.Web/Pages/Submissions/AddSubmission.cshtml](../src/Histo.Web/Pages/Submissions/AddSubmission.cshtml), [src/Histo.Web/Pages/Submissions/BatchBlockSummary.cshtml](../src/Histo.Web/Pages/Submissions/BatchBlockSummary.cshtml) |
| 16 | 2026-09-01 | 5 | `GitHub Copilot` | **Receive Submission page (`ReceiveBatch`) legacy-parity rebuild + `develop` merge reconciliation:** Rebuilt `ReceiveBatch.cshtml(.cs)` from a stub "Confirm receipt?" button into the full legacy workflow (status choice Received/Rejected, date/time/received-by capture, post-fixation checkboxes with "Other" free text, rejection reason, read-only repeat-blocks indicator, read-only view mode for already-actioned submissions). Extended `Batch` model (`TimeReceived`, `ReceivedBy`, `PostFixationOther`) and `IBatchRepository`/`BatchRepository`/`IBatchService`/`BatchService` with post-fixation get/save (legacy `AddPostFixation`/`DeletePostFixation` SPs). Merged `origin/develop` into the feature branch (58 commits ahead) and resolved 8 conflicting files: additive interface/service methods kept both sides; `Batch.cs` deduped overlapping fields; `BatchRepository.UpdateAsync` — adopted develop's more complete `EditBatch` parameter mapping; `ReceiveBatch.cshtml(.cs)` — kept this session's full-featured logic rebuilt on develop's exhaustive field carry-forward (develop's own change here had regressed back to a bare "Confirm receipt" stub). Post-merge fix: `Batch.SubmittedArea`/`OtherSubmittedArea` type changed `int?`→`string?` on develop; `ReceiveBatchModel.LoadLookupsAsync` updated to match. Build: 0 errors. Tests: 144 passed, 1 skipped. | **Done** | — | Merge conflict resolution across `Batch.cs`, `IBatchRepository.cs`, `IBatchService.cs`, `BatchRepository.cs`, `BatchService.cs`, `ReceiveBatch.cshtml(.cs)`, `.gitignore` | [src/Histo.Web/Pages/Batches/ReceiveBatch.cshtml.cs](../src/Histo.Web/Pages/Batches/ReceiveBatch.cshtml.cs), [src/Histo.Web/Pages/Batches/ReceiveBatch.cshtml](../src/Histo.Web/Pages/Batches/ReceiveBatch.cshtml), [src/Histo.Submissions/Models/Batch.cs](../src/Histo.Submissions/Models/Batch.cs), [src/Histo.Submissions/Repositories/BatchRepository.cs](../src/Histo.Submissions/Repositories/BatchRepository.cs) |
| 17 | 2026-09-02 | 5 | `GitHub Copilot` | **ReceiveBatch bug fixes + Copilot PR review comment:** Fixed a `dotnet build` error in `BatchService.GetPostFixationCodesAsync` — `_logger.LogError(ex, message, args)` call had exception/message arguments reversed vs `IAppLogger.LogError(string, Exception?, params)`'s real signature. Fixed the "Received" radio button rendering invisible on `ReceiveBatch.cshtml` — its `<label>` element was missing entirely (only "Not started"/"Rejected" had one) and its `aria-controls` attribute was misspelled `data-aria-controls`; same typo fixed on the post-fixation "Other" checkbox. Addressed a GitHub Copilot PR review comment (medium severity): `ReceiveBatchModel.OnPostSaveAsync` checked `if (!await _batches.UpdateAsync(...))` expecting a `false` return on failure, but `BatchService.UpdateAsync` actually rethrows on failure — the friendly error path was dead code. Fixed by wrapping the call in `try/catch`, matching the pattern already used by `EditBatch.cshtml.cs`; corrected the misleading `UpdateAsync` doc comments on `IBatchService`/`BatchService` to state it rethrows. Also investigated and fixed a local-only dev auth bypass in `HistoPageModel.cs` (uncommitted, gated by a gitignored `appsettings.Development.json` flag, not part of this branch's merge) — root cause of "nav/user missing after login" was the bypass only re-populating `ISessionService` when the auth-cookie claim was absent, not when the in-memory session itself was empty (session is wiped on every app restart independently of the cookie). Build: 0 errors. Tests: 144 passed, 1 skipped. | **Done** | — | `LogError` argument order; missing `<label>`/`aria-controls` typo; dead error-handling branch; doc-comment correction | [src/Histo.Submissions/Services/BatchService.cs](../src/Histo.Submissions/Services/BatchService.cs), [src/Histo.Web/Pages/Batches/ReceiveBatch.cshtml](../src/Histo.Web/Pages/Batches/ReceiveBatch.cshtml), [src/Histo.Web/Pages/Batches/ReceiveBatch.cshtml.cs](../src/Histo.Web/Pages/Batches/ReceiveBatch.cshtml.cs), [src/Histo.Submissions/Interfaces/IBatchService.cs](../src/Histo.Submissions/Interfaces/IBatchService.cs) |

| 16 | 2026-08-28 | 1 | `GitHub Copilot` | **Live Dev deployment debugging — Entra ID SAML 2.0 sign-in chain (4 fixes):** Reported symptom: `https://dev-cde.azure.defra.cloud` redirected to the raw App Service hostname `devcdewebaw1401.azurewebsites.net/Saml2/login?ReturnUrl=%2F`, which then failed with `ERR_CONNECTION_CLOSED`. Root-caused and fixed in sequence via live Log stream evidence: (1) `Program.cs` — added `UseForwardedHeaders()` (trusting `X-Forwarded-For`/`Proto`/`Host`, cleared `KnownNetworks`/`KnownProxies`) since `dev-cde.azure.defra.cloud` has no App Service custom domain binding and is proxied by an external edge to the App Service's default hostname; (2) confirmed via `git log`/`git diff` that the fix wasn't reaching Dev because it was on a feature branch not yet merged to `develop` — merged via PR, redeployed; (3) since the proxy still didn't yield the correct `Request.Host` after redeploy (confirmed via captured `Location` header), added `PostConfigure<CookieAuthenticationOptions>("saml2", ...)` overriding `OnRedirectToLogin`/`OnRedirectToAccessDenied` to force the redirect to a **relative** URI (`PathAndQuery` only), removing the dependency on `Request.Host` entirely; (4) after that fix, the sign-in flow reached Entra ID and back to `/Saml2/Acs` but failed with `Microsoft.IdentityModel.Tokens.SecurityTokenInvalidAudienceException: IDX10214: Audience validation failed` — `saml2Config.AllowedAudienceUris` was never populated, so audience validation failed unconditionally regardless of the assertion; fixed by adding `saml2Config.AllowedAudienceUris.Add(saml2Config.Issuer)`. Confirmed via Log stream: SAML assertion now validates successfully (`NameIdentifier: Sabari.Katari@defra.gov.uk`). Also fixed in the same session: `_Layout.cshtml` nav/user-context chrome was rendering for anonymous requests (e.g. on the `ServiceProblem` error page after hitting the decommissioned `/Login` route) because neither `_NavPartial.cshtml` nor `_UserContextPartial.cshtml` checked authentication state — wrapped both partial renders in `@if (User.Identity?.IsAuthenticated == true)`. Remaining blocker (infra, not code): `ResolveUserByEmailAsync` fails with `SqlException: Could not open a connection to SQL Server` — `ConnectionStrings:HistologyDb` Key Vault secret/SQL firewall not yet configured for Dev; raised as a separate infra/DBA ask. Build: 0 errors. | **Done** (SAML chain); DB connectivity **Open** | Missing `UseForwardedHeaders()`; absolute Host-derived cookie challenge redirect; empty `AllowedAudienceUris`; anonymous nav/user chrome leak; `HistologyDb` DB unreachable in Dev (infra) | [src/Histo.Web/Program.cs](../src/Histo.Web/Program.cs), [src/Histo.Web/Pages/Shared/_Layout.cshtml](../src/Histo.Web/Pages/Shared/_Layout.cshtml) |
| 17 | 2026-09-01 | 5 | `GitHub Copilot` | **Dev-only auth bypass (HistoPageModel) + submission-type routing analysis across all 5 types (Create/Edit/View/Copy):** **(A) Dev bypass** — implemented a `DevAuthBypass` flag in `HistoPageModel.OnPageHandlerExecutionAsync` reading from `appsettings.Development.json` (not committed): when `true`, unconditionally bakes a hardcoded `Maintenance`-group principal into `HttpContext.User` and calls `Session.PopulateFromClaims`, bypassing the Entra ID SAML round-trip for local testing without modifying auth infrastructure. Two bugs diagnosed in the initial implementation: `SignInAsync("saml2", ...)` threw (the `"saml2"` scheme has no `IAuthenticationSignInHandler`) causing a service-error page with no nav chrome; and the inner `if (GroupName is empty)` guard was being skipped by stale cookie state, leaving `Session.GroupName` empty and all home-page panels hidden. Fixed by removing `SignInAsync` and making the principal re-application unconditional. **Why not fixed during migration:** No Entra ID endpoint was available during migration — the ADR-006 NTLogin bridge served as the dev bypass until Entra ID was wired. That bridge was decommissioned on 2026-08-28, leaving a gap for developers without an Entra ID Reply URL for localhost. **(B) Submission-type routing analysis** — deep-read of legacy `Cassetted.aspx.vb`, `BatchBlockSummary.aspx.vb`, `BatchSummary.aspx.vb`, `SubmissionDetailsBlock.aspx.vb`, `AddSample.aspx.vb`; cross-referenced all 5 submission types (Wet Tissue code `"4"`, Pre-Cassetted code `"5"`, Wax Block/Stained Section/Unstained Section codes `"1"/"2"/"3"`). Three bugs found and fixed: (1) `BatchBlockSummary.cshtml.cs` `OnPostSelect` always routed Edit sample to `SubmissionDetailsBlock` regardless of type — Wet Tissue must route to `SubmissionDetails` (tissue list + PM date/Histology ref) per legacy `BatchSummary.aspx` behaviour; added `IsWetTissue` property (`GetSubmittedAsCodeAsync` code `"4"`) and branched `OnPostSelect` accordingly; (2) `SubmissionDetails.cshtml` back-link and "Back to samples" missing `BatchId` route parameter — returning from Wet Tissue edit lost batch context; added `asp-route-BatchId`, `[BindProperty(SupportsGet=true)] BatchId`, and propagated through `LoadAnimalAsync`; (3) `SubmissionDetails.cshtml` had no view-mode awareness — PM date/Histology ref form, Delete tissue, and Add tissue all rendered in View Submission mode; added `IsViewMode => Session.IsViewSubmissionMode` property and wrapped write controls with `@if (!Model.IsViewMode)` guards; also restored "Edit sample" visibility in View mode (`BatchBlockSummary.cshtml` had hidden it, but legacy `EnableDisableButtons` kept it enabled in `SV_ViewSubmission=True` mode). **Why not fixed during migration:** The two-page consolidation (`BatchSummary.aspx` + `BatchBlockSummary.aspx` → single `BatchBlockSummary.cshtml`) was completed in Run #80–#81; however, legacy `BatchSummary.aspx` (Wet Tissue variant) was initially misidentified as already covered by `ViewSamples.cshtml` — see Run #81 root-cause note. Once the consolidation was corrected, the per-type routing divergence existed but went unnoticed until live testing with different submission types. Build: 0 errors. | **Done** | `SignInAsync` on non-sign-in scheme; unconditional bypass guard skip; Edit sample routing per submission type; `SubmissionDetails` missing `BatchId`; `SubmissionDetails` no view-mode guard | [src/Histo.Web/Pages/HistoPageModel.cs](../src/Histo.Web/Pages/HistoPageModel.cs), [src/Histo.Web/Pages/Submissions/BatchBlockSummary.cshtml.cs](../src/Histo.Web/Pages/Submissions/BatchBlockSummary.cshtml.cs), [src/Histo.Web/Pages/Submissions/SubmissionDetails.cshtml.cs](../src/Histo.Web/Pages/Submissions/SubmissionDetails.cshtml.cs), [src/Histo.Web/Pages/Submissions/SubmissionDetails.cshtml](../src/Histo.Web/Pages/Submissions/SubmissionDetails.cshtml) |
| 18 | 2026-09-01 | 5 | `GitHub Copilot` | **Full TSE/Non-TSE workflow parity audit + GDS consolidation review + route rename + Edit-sample/Add-sample routing regression fix:** **(A) Parity audit** — comprehensive gap analysis of Create/View/Edit/Copy across all 5 submission types against `Migration-Plan.md`/`Intelligent-Migration-Plan.md`/`LLD.md`; confirmed 2 defects (Wet Tissue Edit-sample POST routing depended on a stale hidden-field `IsWetTissue` instead of re-resolving server-side; `SearchSubmissions` Edit-gating inverted vs legacy) — both fixed at the time (`OnPostSelect` now re-queries `GetSubmittedAsCodeAsync` on POST; `CanEditSubmission` corrected to Received/OnHold/InProgress). Registered 7 accepted-consolidation deviations (D-1..D-7) in `Parity-Audit-Report.md` §13 (inline histology-ref grid edit, grid paging, mouse-range bulk entry, Excel mouse-number upload, TSE/NonTSE match check on Copy samples, auto-generate histology ref on Copy blocks, per-block test-type checkboxes) that were previously undocumented code comments only. **(B) Module-to-Page Mapping nav audit** — found and fixed 2 orphaned pages with zero inbound links (`Batches/SubmissionsOnHold` — restored via a "Samples on hold" button on `EditBatch`; `Bookings/EditHistologyRef` — restored via a "Edit next histology ref" link on `BookingMenu`); refreshed the stale Module-to-Page Mapping table in `Migration-Plan.md` to reflect shipped/consolidated page names. **(C) GDS route rename** — `BatchBlockSummary` renamed to `SampleSummary` (`git mv`, class rename, 301 redirect registered in `Program.cs` for the old path) because "Block" is user-facing-URL-meaningless for Wet Tissue submissions; added a `ViewData["BatchNo"]` caption ("Wet Tissue" / "Wax Block / Cassetted") to the page so users can tell submission type without inspecting the URL, closing a usability gap the rename itself exposed. **(D) D-1 disposition downgrade** — reassessed inline histology-ref grid editing: rejected as a GDS anti-pattern (no editable-table Design System component; violates "one thing per page"); downgraded Medium→Low, disposition changed to "Resolved by design", reasoning tied to fix (E). **(E) Add-sample routing regression (found via user retest)** — `AddSubmissionModel.OnPostAsync` always redirected back to `SampleSummary` after adding a sample, forcing an extra "Edit sample" click for every sample on every submission type; legacy `AddSubmission.aspx.vb::btnNext_Click` redirects straight into the per-sample detail page (`SubmissionDetailsBlock`/`SubmissionDetails`) via `SV_AddSampleNextPage`. Fixed by injecting `IBatchService` and branching the post-add redirect on `GetSubmittedAsCodeAsync` exactly like `SampleSummary`/`BatchBlockSummary`. **User-reported false negative on first fix:** the initial fix compiled correctly but a stale `Histo.Web` process (holding the previous build's `.exe` locked, confirmed via `MSB3027` in the build log) was still serving pre-fix code during the user's live retest; `dotnet test` (run to "verify") does not rebuild `Histo.Web` at all, giving a false-positive verification signal. Root-caused, confirmed no process now holds the lock, rebuilt `Histo.Web` clean (0 errors), and flagged the `dotnet test`-does-not-rebuild-Histo.Web verification gap explicitly to the user. Build: 0 errors, 0 warnings (Histo.Web + full solution). Tests: 144 pass, 1 skipped. | **Done** | D-1 through D-7 consolidation deviations (see `Parity-Audit-Report.md` §13); stale build masking a verified fix during manual retest | Edit-sample POST re-resolves submission type server-side; `SearchSubmissions.CanEditSubmission` corrected; `SubmissionsOnHold`/`EditHistologyRef` nav links restored; `BatchBlockSummary`→`SampleSummary` rename + redirect + `BatchNo` caption; `AddSubmission` post-add redirect now type-aware | [docs/Parity-Audit-Report.md](Parity-Audit-Report.md), [docs/Migration-Plan.md](Migration-Plan.md), [src/Histo.Web/Pages/Submissions/SampleSummary.cshtml.cs](../src/Histo.Web/Pages/Submissions/SampleSummary.cshtml.cs), [src/Histo.Web/Pages/Submissions/AddSubmission.cshtml.cs](../src/Histo.Web/Pages/Submissions/AddSubmission.cshtml.cs), [src/Histo.Web/Program.cs](../src/Histo.Web/Program.cs), [src/Histo.Web/Pages/Batches/EditBatch.cshtml](../src/Histo.Web/Pages/Batches/EditBatch.cshtml), [src/Histo.Web/Pages/Bookings/BookingMenu.cshtml](../src/Histo.Web/Pages/Bookings/BookingMenu.cshtml) |
| 20 | 2026-09-02 | 5 | `GitHub Copilot` | **Wet Tissue Edit sample silent-redirect bug — root-caused via user-supplied browser Network tab trace, fixed to match the already-fixed `SubmissionDetailsBlock` twin:** User reported Edit sample "does nothing" for a Wet Tissue submission (batch 29398, animal 91478) after several earlier turns misattributed it to a stale build. A Network tab trace proved otherwise: `POST /Submissions/SampleSummary?handler=Select` correctly 302'd to `SubmissionDetails` (submission-type routing is correct), but `GET /Submissions/SubmissionDetails?batchId=29398&animalId=91478` itself returned a **302** instead of 200, bouncing straight back to `/Submissions/SampleSummary?batchId=29398` — indistinguishable from "nothing happened". Root cause: `SubmissionDetailsModel.LoadAnimalAsync` only queried the plain `GetAnimalsByBatchAsync` table and silently redirected to `SampleSummary` whenever the animal wasn't found there — the identical defect class already found and fixed in `SubmissionDetailsBlockModel` earlier this session, but never ported to this twin page. `SampleSummary`'s own animal list is a merge of the plain table and the block-animal table, so an animal visible/selectable there could still be invisible to `SubmissionDetails`. Fixed `LoadAnimalAsync` to check `GetBlockAnimalsByBatchAsync` first with a fallback to `GetAnimalsByBatchAsync` (mirrors `SampleSummaryModel`/`SubmissionDetailsBlockModel`), and removed the silent redirect on not-found so `Animal` stays `null` and the view's existing (previously unreachable) "Sample not found" `govuk-warning-text` branch renders instead. Added explicit `if (Animal is null)` guards to `OnGetAsync`, `OnPostSaveDetailsAsync` and `OnPostAddTissueAsync` (all previously dereferenced `Animal!` unguarded, relying on the redirect that no longer happens). Build: 0 errors, 3 pre-existing warnings. Tests: 145 pass, 1 skipped. | **Done** | `OnPostDeleteTissueAsync` still doesn't call `LoadAnimalAsync` for batch/animal scope validation before deleting (unlike the fixed `SubmissionDetailsBlockModel` equivalent) — left as-is, not part of the reported defect | `LoadAnimalAsync` block-animal-table merge + no-redirect-on-not-found; null guards added to 3 handlers | [src/Histo.Web/Pages/Submissions/SubmissionDetails.cshtml.cs](../src/Histo.Web/Pages/Submissions/SubmissionDetails.cshtml.cs) |
| 19 | 2026-09-02 | 5 | `GitHub Copilot` | **Create / Edit / View Submission workflow review vs legacy + Pick List Management integration:** End-to-end review of the migrated submission journey (Home → Cassetted → BatchDetails create → SampleSummary → AddSubmission → SubmissionDetails / SubmissionDetailsBlock) against legacy `BatchDetails.aspx`, `BatchSummary.aspx`, `BatchBlockSummary.aspx`, `AddSample.aspx`, `AddSubmission.aspx`, `SearchSender.aspx`. Validated navigation paths, button enable/disable states, mandatory-field rules, submission-type conditional behaviour, and the sample/block/tissue workflows. **Implemented** the previously-absent Pick List Management integration: "Manage submitted by list", "Manage project / contract codes" and "Manage pathologists" buttons on both the Create Submission form (`BatchDetails?mode=create`) and the Edit Submission form (`EditBatch`), replacing legacy `btnNewSubmittedBy` / `btnNewProject` / `btnNewContact`. Each button posts the form, serialises all entered values to `TempData` and redirects to the correct maintenance page (`PickListUserArea/19` for Projects, `PickListUserArea/18` for Contacts, `UserMaintenance` for the user-backed Submitted By list) with a validated local `returnUrl`; on return the draft is restored so no entered data is lost — reproducing legacy `UpdateSessionWithBatchDetails()`-before-redirect. `ReturnUrl` passthrough added to `PickListUserArea`, `UserMaintenance`, `AddUser` and `EditUser`, and the hard-coded `Done → /Batches/BatchDetails` dead-end on `PickListUserArea` removed. All return URLs guarded with `Url.IsLocalUrl` (open-redirect prevention). | Partial — pick list integration delivered; 11 parity defects/gaps reported for decision, not fixed | **Defects:** (1) Copy sample inverted — legacy cleared Sender Ref and copied tissues/blocks, new page pre-fills Sender Ref and copies nothing; (2) submission-date rule reversed — legacy "must be today or later", new form uses `max=today` with no server check; (3) "At least one Block/Sample" Finish gate missing — batch persists as Submitted before any sample exists and "Finish" is only a link to Home; (4) `SubmissionDetailsBlock` has no `IsViewMode` gate — Save details / Add block / Add tissue / Delete all live in read-only View Submission mode; (5) Contacts and Projects no longer area-filtered (`GetLookupDataAsync(18/19)` replaced `GetContactsByArea`/`GetProjectsByArea`); (6) histology-ref validation lost (format `NN/NNNNN`, year, duplicate, next-available checks). **Missing Add sample functions:** duplicate sender-ref check, sender-ref format validation + TB Diagnostics override, mouse-number range bulk add, correlation file upload, Neuropath PG-number TSE Daybook lookup, pre-booked block validation for cassetted batches. **Lower severity:** `SafeToHandle` 2-state checkbox vs legacy mandatory 3-state dropdown; `SampleSameProjects` has no UI; no Cancel confirmation interruption; PM date unvalidated. **Verify:** `SampleSummary` routes Wet Tissue on `SubmittedAs == "4"` — could not confirm against `luSubmittedAs` seed data. | Pick List Management buttons added to Create + Edit Submission; `ReturnUrl` + draft save/restore across `BatchDetails`, `EditBatch`, `PickListUserArea`, `UserMaintenance`, `AddUser`, `EditUser`. Build 0 errors; `dotnet test` 145 passed / 0 failed. | [src/Histo.Web/Pages/Batches/BatchDetails.cshtml](../src/Histo.Web/Pages/Batches/BatchDetails.cshtml), [src/Histo.Web/Pages/Batches/BatchDetails.cshtml.cs](../src/Histo.Web/Pages/Batches/BatchDetails.cshtml.cs), [src/Histo.Web/Pages/Batches/EditBatch.cshtml](../src/Histo.Web/Pages/Batches/EditBatch.cshtml), [src/Histo.Web/Pages/Batches/EditBatch.cshtml.cs](../src/Histo.Web/Pages/Batches/EditBatch.cshtml.cs), [src/Histo.Web/Pages/Admin/PickListUserArea.cshtml](../src/Histo.Web/Pages/Admin/PickListUserArea.cshtml), [src/Histo.Web/Pages/Admin/PickListUserArea.cshtml.cs](../src/Histo.Web/Pages/Admin/PickListUserArea.cshtml.cs), [src/Histo.Web/Pages/Admin/UserMaintenance.cshtml](../src/Histo.Web/Pages/Admin/UserMaintenance.cshtml), [src/Histo.Web/Pages/Admin/UserMaintenance.cshtml.cs](../src/Histo.Web/Pages/Admin/UserMaintenance.cshtml.cs), [src/Histo.Web/Pages/Admin/AddUser.cshtml](../src/Histo.Web/Pages/Admin/AddUser.cshtml), [src/Histo.Web/Pages/Admin/AddUser.cshtml.cs](../src/Histo.Web/Pages/Admin/AddUser.cshtml.cs), [src/Histo.Web/Pages/Admin/EditUser.cshtml](../src/Histo.Web/Pages/Admin/EditUser.cshtml), [src/Histo.Web/Pages/Admin/EditUser.cshtml.cs](../src/Histo.Web/Pages/Admin/EditUser.cshtml.cs) |

---

## Open Issues
 Root-caused and fixed in sequence via live Log stream evidence: (1) `Program.cs` — added `UseForwardedHeaders()` (trusting `X-Forwarded-For`/`Proto`/`Host`, cleared `KnownNetworks`/`KnownProxies`) since `dev-cde.azure.defra.cloud` has no App Service custom domain binding and is proxied by an external edge to the App Service's default hostname; (2) confirmed via `git log`/`git diff` that the fix wasn't reaching Dev because it was on a feature branch not yet merged to `develop` — merged via PR, redeployed; (3) since the proxy still didn't yield the correct `Request.Host` after redeploy (confirmed via captured `Location` header), added `PostConfigure<CookieAuthenticationOptions>("saml2", ...)` overriding `OnRedirectToLogin`/`OnRedirectToAccessDenied` to force the redirect to a **relative** URI (`PathAndQuery` only), removing the dependency on `Request.Host` entirely; (4) after that fix, the sign-in flow reached Entra ID and back to `/Saml2/Acs` but failed with `Microsoft.IdentityModel.Tokens.SecurityTokenInvalidAudienceException: IDX10214: Audience validation failed` — `saml2Config.AllowedAudienceUris` was never populated, so audience validation failed unconditionally regardless of the assertion; fixed by adding `saml2Config.AllowedAudienceUris.Add(saml2Config.Issuer)`. Confirmed via Log stream: SAML assertion now validates successfully (`NameIdentifier: dummy@defra.gov.uk`). Also fixed in the same session: `_Layout.cshtml` nav/user-context chrome was rendering for anonymous requests (e.g. on the `ServiceProblem` error page after hitting the decommissioned `/Login` route) because neither `_NavPartial.cshtml` nor `_UserContextPartial.cshtml` checked authentication state — wrapped both partial renders in `@if (User.Identity?.IsAuthenticated == true)`. Remaining blocker (infra, not code): `ResolveUserByEmailAsync` fails with `SqlException: Could not open a connection to SQL Server` — `ConnectionStrings:HistologyDb` Key Vault secret/SQL firewall not yet configured for Dev; raised as a separate infra/DBA ask. Build: 0 errors. | **Done** (SAML chain); DB connectivity **Open** | Missing `UseForwardedHeaders()`; absolute Host-derived cookie challenge redirect; empty `AllowedAudienceUris`; anonymous nav/user chrome leak; `HistologyDb` DB unreachable in Dev (infra) | [src/Histo.Web/Program.cs](../src/Histo.Web/Program.cs), [src/Histo.Web/Pages/Shared/_Layout.cshtml](../src/Histo.Web/Pages/Shared/_Layout.cshtml) |

---

## Open Issues

| ID | Date | Phase | Agent | File / Area | Issue | Severity | Owner | Status | Resolution |
|---|------|-------|-------|-------------|-------|----------|-------|--------|------------|
| ISS-R01 | 2026-08-25 | 1 | `GitHub Copilot` | Entra ID Portal | Entra admin must add `https://localhost:57879/Saml2/Acs` as a Reply URL for local dev testing | Medium | APHA ITU | Open | — |
| ISS-R02 | 2026-08-25 | 1 | `GitHub Copilot` | Entra ID Portal | `SG-HistopathologySystem-Users` security group not yet created; `Assignment required` not yet set to Yes on Enterprise Application | High | APHA ITU | Open | — |
| ISS-R03 | 2026-08-25 | 5 | `GitHub Copilot` | `Program.cs` | `UseStatusCodePagesWithReExecute` routes both 4xx and 5xx to ServiceProblem — a dedicated 404 page is needed | Low | Dev | Open | — |
| ISS-R04 | 2026-08-25 | 5 | `GitHub Copilot` | `EditQualityDataTest.cshtml.cs` | TC code SPs (`AddHistologyTCCode` etc.) not verified to exist in DB — run `SELECT name FROM sys.procedures WHERE name IN ('AddHistologyTCCode', ...)` | High | Dev | Open | — |
| ISS-R05 | 2026-08-25 | 5 | `GitHub Copilot` | `Admin/EditUser.cshtml` | `<form method="post">` still missing `asp-page` attribute (Run #83 fix was documented but not applied to file) — edit user may fail with 400 | High | Dev | Open | — |
| ISS-R06 | 2026-08-25 | 5 | `GitHub Copilot` | `QualityData.cshtml` + `EditQualityDataTest.cshtml` | Pagination not implemented on either page (deferred by user — needed for large batches) | Medium | Dev | Open | Deferred |
| ISS-R07 | 2026-08-25 | 5 | `GitHub Copilot` | `BatchesForDispatch.cshtml.cs` | Quick-Go validation used `GetByIdAsync` + status check; batch 29395 was in the grid but rejected because `GetCommonBatchTablesByID` returns a different status than `GetBatchesForDispatch` — fixed by validating against the already-loaded dispatch list | High | Dev | **Resolved** | Fixed: removed `GetByIdAsync` + status check; replaced with `Batches.FirstOrDefault(b => b.ID == id)` |
| ISS-R08 | 2026-08-25 | 5 | `GitHub Copilot` | `EditQualityDataTest.cshtml` | Custom JS (`@section Scripts`) used for show/hide of QCNote and Dispatched fields — not GDS-recommended pattern; replaced with native `govuk-radios__conditional` and `govuk-checkboxes__conditional` | Medium | Dev | **Resolved** | Fixed: removed Scripts block; added `data-aria-controls` + GDS conditional divs |
| ISS-R09 | 2026-08-25 | 5 | `GitHub Copilot` | `EditQualityDataTest.cshtml` | Structural div errors: stray `</div>` after QCCode form-group (leftover from QCNoteRow removal) + misaligned closing tags in dispatched conditional causing rendering break | High | Dev | **Resolved** | Fixed: removed stray div; re-indented dispatched conditional to correct nesting level |
| ISS-R10 | 2026-08-25 | 5 | `GitHub Copilot` | `QualityData.cshtml` | Test column showing raw lookup codes (e.g. "3", "4") — `BlockTest.TestDetails` always null; SP returns code not description | High | Dev | **Resolved** | Fixed: added `ResolveTestNamesAsync` to `QualityDataModel`; uses `GetHistologyTypesAsync()` + `GetLookupDataAsync` for antibodies/stain; `GetTestName(t)` called in view |
| ISS-R11 | 2026-08-25 | 5 | `GitHub Copilot` | `EditQualityDataTest.cshtml` | QC Note Ref only rendered when `QCNoteRef > 0` — hidden for new tests; user expected to always see it (legacy showed empty textbox) | Medium | Dev | **Resolved** | Fixed: always renders "QC note ref: None" or clickable link; removed conditional wrapper |
| ISS-R12 | 2026-08-25 | 5 | `GitHub Copilot` | `QualityData.cshtml` | QC note column removed (showed ref link inline in grid); row CSS colour classes (`qd-row--*`) removed — not a GDS documented pattern | Low | Dev | **Resolved** | Removed column and CSS; retained `govuk-tag` in Result column as the sole GDS-compliant status indicator |
| ISS-R13 | 2026-08-26 | 5 | `GitHub Copilot` | `_NavPartial.cshtml`, `BatchesForEditing.cshtml(.cs)` | Duplicate `Quality data`/`Edit submission status` nav links both pointing to `BatchesForEditing`; row-select redirected to `BatchDetails` instead of legacy-matching `EditBatch`; grid columns/headers didn't match legacy (`BatchesForEditing.aspx`) | High | Dev | **Resolved** | Nav link replaced with `Edit QC notes` → `/QC/QCNotes`; `OnPostSelect` redirect target fixed to `/Batches/EditBatch`; grid headers/columns corrected to legacy parity |
| ISS-R14 | 2026-08-26 | 5 | `GitHub Copilot` | `EditQCNote.cshtml(.cs)`, `QCNote.cs`, `QCNoteRepository.cs` | Legacy QC Note Ref summary box and Created by/Date created footer missing from migrated `EditQCNote` page | Medium | Dev | **Resolved** | Added `CreatedBy`/`DateCreated` to `QCNote` model + repository mapping; restored both sections in the Razor view |
| ISS-R15 | 2026-08-26 | 5 | `GitHub Copilot` | `QCNoteRepository.cs` | `Edit` button on `QCNotes.cshtml` silently stopped working — `RuntimeBinderException` from dynamic access to possibly-absent `Name`/`DateCreated` columns (introduced by ISS-R14 fix), swallowed by service-layer catch, causing silent redirect back to the list (same pattern as ISS-034) | High | Dev | **Resolved** | Replaced dynamic member access with `IDictionary<string, object>` + `TryGetValue` pattern |
| ISS-R16 | 2026-08-26 | 5 | `GitHub Copilot` | `QCNoteRepository.cs` | QC notes for antibody tests missing entirely from `EditQCNote`; blank notes showed empty instead of legacy's default Sender Ref/Histo Ref/Block Ref/Test table — `GetByIdAsync` only called `GetQCNoteHistStainTestInformation`, never `GetQCNoteAntibodiesInformation`, and only read the first row | High | Dev | **Resolved** | Added antibodies SP call (using `BatchType` from header row) concatenated with hist/stain rows; added `BuildDefaultNoteText` reproducing legacy `InsertSpaces` table |
| ISS-R17 | 2026-08-26 | 5 | `GitHub Copilot` | `QCNoteRepository.cs` | Created-by date missing next to username on `EditQCNote` — `DateCreated` returned as `dd/MM/yyyy` string, but dynamic/IDictionary reads bypass Dapper's `NullableDateTimeTypeHandler`, so `created is DateTime` always failed | Medium | Dev | **Resolved** | Added `ParseDate` helper mirroring `NullableDateTimeTypeHandler` (native passthrough + dd/MM/yyyy exact parse + general fallback) |
| ISS-R18 | 2026-08-27 | 5 | `GitHub Copilot` | `Histo.Histology` (`BlockTest`/`IBlockTestRepository`) | Per-block test selection (EO/H&E/IHC PrP/IHC Other/Special Stain) at block-creation time has no migrated equivalent — `BlockService.AddBlockAsync` does not create any `BlockTest` rows; `EditBlockHistology`/`EditBlockAntibodies`/`EditBlockStain` SPs only edit an existing row. Unverified whether an "Add" equivalent SP exists in the DB | Medium | Dev | Open | Deliberately not implemented — added a link from `SubmissionDetailsBlock` to the existing `QC/QualityData` page instead of guessing a stored procedure name |
| ISS-R19 | 2026-08-27 | 5 | `GitHub Copilot` | Submission wizard pages (`AddSample`, `SubmissionDetails`, `Cassetted`) | Route/query-based batch access state (Phase 0–2 of the TSE/NON-TSE redesign) was not extended to these three pages — they only use `BatchSubmissionID`/`AnimalID`/have no batch yet, so there was no unguarded access path to close | Low | Dev | Open | Deferred — no security or usability payoff identified for converting these specific pages |
| ISS-R20 | 2026-09-02 | 5 | `GitHub Copilot` | `Submissions/SampleSummary.cshtml` | **Copy sample is inverted vs legacy.** `BatchSummary.aspx.vb`/`BatchBlockSummary.aspx.vb` `btnCopySubmission_Click` removed `SV_SenderRef` (forcing a new Sender Ref) and then `AddSample.aspx` copied the source animal's tissues (`CopyBatchSubmissionWithAnimalID`) or blocks (`CopySampleBlocks`/`CopySamplePreBookedBlocks`). The migrated "Copy sample" link pre-fills the Sender Ref — the one thing legacy cleared — and copies no tissues or blocks at all | High | Dev | Open | — |
| ISS-R21 | 2026-09-02 | 5 | `GitHub Copilot` | `Batches/BatchDetails.cshtml(.cs)` | **Submission-date rule reversed.** Legacy `btnSave_Click` validated `eValidateEarliest` with "Must be today or later" for new batches. Create form renders `max="@DateTime.Today"` (today or earlier) and performs no server-side date-range check | Medium | Dev | Open | — |
| ISS-R22 | 2026-09-02 | 5 | `GitHub Copilot` | `Batches/BatchDetails.cshtml(.cs)` | **"At least one Block/Sample" Finish gate missing.** Legacy `ValidateData()` blocked Finish with "Atleast one Block/Sample must be added to the Submission." and only then set `BatchStatus = 1`. The migrated flow persists the batch as Submitted on "Create submission" before any sample exists, and "Finish" is only a link to `/Index` | High | Dev | Open | — |
| ISS-R23 | 2026-09-02 | 5 | `GitHub Copilot` | `Submissions/SubmissionDetailsBlock.cshtml(.cs)` | **View Submission mode not enforced on the block journey.** `SubmissionDetails` honours `Session.IsViewSubmissionMode` but `SubmissionDetailsBlock` has no equivalent gate — Save details, Add block, Add tissue and Delete are all available in the read-only View Submission journey | High | Dev | Open | — |
| ISS-R24 | 2026-09-02 | 5 | `GitHub Copilot` | `Batches/BatchDetails.cshtml.cs`, `Batches/EditBatch.cshtml.cs` | **Contacts and Projects no longer area-filtered.** Legacy bound `ddlContactName`/`ddlProjectCode` via `GetContactsByArea(sUserArea)` / `GetProjectsByArea(sUserArea)`. Both pages now call unfiltered `GetLookupDataAsync(18)` / `GetLookupDataAsync(19)`, exposing every area's values | Medium | Dev | Open | — |
| ISS-R25 | 2026-09-02 | 5 | `GitHub Copilot` | `Submissions/SubmissionDetails.cshtml(.cs)`, `Submissions/SubmissionDetailsBlock.cshtml(.cs)` | **Histology ref validation lost.** Legacy `BatchBlockSummary` validated inline edits for `NN/NNNNN` format, year not in the future, no duplicate on submission or in DB, and "not higher than or equal to the current next Histology Ref for the selected area". Histology Ref is now free text with no validation | Medium | Dev | Open | — |
| ISS-R26 | 2026-09-02 | 5 | `GitHub Copilot` | `Submissions/AddSubmission.cshtml(.cs)` | **Add sample reduced to Sender Ref + Neuropath checkbox.** Missing from legacy `AddSample.aspx`/`AddSubmission.aspx`: duplicate sender-ref check ("The specified Sender Reference is already present on the Submission"), `SenderRef1.CheckSenderRef()` format validation, the `cbUseValidation` TB Diagnostics override, mouse-number range bulk add (`MouseNumber1`/`MouseNumber2`), correlation file upload (`corrFile`/`btnUpload`), Neuropath PG-number TSE Daybook lookup (`GetAnimalFromDayBook` → species/project auto-population), and pre-booked block validation for cassetted batches (`GetPreBookedBlocks`/`ValidateAnimalBlocks`) | High | Dev | Open | — |
| ISS-R27 | 2026-09-02 | 5 | `GitHub Copilot` | `Batches/BatchDetails.cshtml`, `Batches/EditBatch.cshtml` | **Minor field-level parity gaps.** `SafeToHandle` rendered as a 2-state checkbox where legacy `ddlSafeToHandle` was a mandatory 3-state dropdown (`--`/Yes/No, `rfvSafeToHandle` with `InitialValue="-1"`) so "No" cannot be distinguished from unanswered; `SampleSameProjects` ("Same project code for all samples?", legacy `cbSampleOverride`) has no UI on either form; Cancel has no confirmation interruption (legacy `PromptBeforeSaveScript`); PM date is unvalidated free text | Low | Dev | Open | — |
| ISS-R28 | 2026-09-02 | 5 | `GitHub Copilot` | `Submissions/SampleSummary.cshtml.cs`, `Submissions/AddSubmission.cshtml.cs` | **Wet Tissue `SubmittedAs` code needs data verification.** Both pages route Wet Tissue on `submittedAsCode == "4"`, while `ValidationHelpers.IsBatchPreCassetted` uses `"5"` for Pre-Cassetted (confirmed). No `luSubmittedAs` seed data is present in the repo to confirm `"4"`; if Wet Tissue is actually `"3"` every Wet Tissue submission is routed to the block journey instead of `SubmissionDetails` | High | Dev | Open | — |

---

## Assumptions

- All SP names for TC codes (`AddHistologyTCCode`, `DeleteHistologyTCCode`, etc.) are sourced from `clsCheckBoxData.vb` in the legacy library and assumed to exist in the current database. Verify before running QC workflow end to end.
- `LOOKUP_PREMIUM_CHARGES = 17` confirmed from `Common.vb`.
- TC code result sets in `GetBatchBlocksByID`: set 7 = Stain TC, set 8 = Antibodies TC, set 9 = Histology TC (derived from `clsBatch.vb` BLOCK_*_TCCODES constants offset by 6).
- Security group option A (Enterprise Application assignment gate) is the correct approach for authentication; `tblUser` remains the authorisation gate.

---

## Session 2026-08-26 (15:23–16:53) — Submission creation flow + Edit Submission + Pick List fixes

| # | Fix area | Issue | Fix applied |
|---|---|---|---|
| 1 | `Cassetted.cshtml` / `BatchDetails.cshtml` | Submission creation fields all on Cassetted — does not match legacy two-step flow | Stripped Cassetted to type-selection only (BatchType + SubmittedAs); moved full creation form (Project, Pathologist, Species, Date, Histology/Antibody/Stain checkboxes) to `BatchDetails.cshtml?mode=create` |
| 2 | `BatchDetails.cshtml.cs` | Histology/Antibody/Stain checkboxes missing from create mode | Added `Create_Selected*Codes` bind properties, lookup loading, validation (mirrors `CheckHistology()`), and `SaveBatchTestSelectionsAsync` call after batch creation |
| 3 | `BatchDetails.cshtml` | "Batch not found. }" rendered as page text | `@if (!Model.IsCreateMode)` was only wrapping the LoadError section; `else if (Batch is null)` and main `else` were at top level — fixed to use inner `if/else if/else` inside outer `@if` block |
| 4 | `Cassetted.cshtml.cs` | Selections lost when user navigates back from BatchDetails | Added `OnGetAsync` pre-population: `BatchType` from `Session.BatchType`, `SubmittedAs` from `TempData["CreateSubmittedAsId"]` with `.Keep()` |
| 5 | `Cassetted.cshtml` | SubmittedAs defaulting to "Pre Cassetted Tissue" instead of blank | `asp-for="SubmittedAs"` on `<select>` caused SelectTagHelper to bypass blank placeholder for `int?` null; replaced with `name="SubmittedAs"` |
| 6 | `SearchSubmissions.cshtml.cs` | `CanEditSubmission` conditions inverted vs legacy — enabled for Received/OnHold/InProgress, disabled for Submitted/Rejected | Fixed to `Submitted \|\| Rejected` matching `ViewSubmissions` and legacy `ViewSubmissions.aspx.vb` |
| 7 | `BatchesForEditing.cshtml.cs` | `Session.ReturnPage` not set on select — EditBatch had no return context | Added `Session.ReturnPage = "/Batches/BatchesForEditing"` in `OnPostSelect` |
| 8 | `EditBatch.cshtml.cs` | Back link hardcoded to BatchDetails; Cancel/save always to BatchesForEditing | Added `ReturnPage` property using `Session.ReturnPage`; wired to back link, Cancel, and post-save redirect |
| 9 | `EditBatch.cshtml` | Back link and Cancel hardcoded to wrong pages | Changed to `Model.ReturnPage` |
| 10 | `EditLookupItem.cshtml.cs` | Area column shows numeric code instead of name for Contacts/Projects (tables 18/19) | Loaded `GetUserAreasAsync()` when `ShowAreaColumn`, built `AreaNameById` dictionary, table now displays name |
| 11 | `EditLookupItem.cshtml` | Area dropdown missing in Add/Edit form for tables 18/19 | Added Area `<select>` bound to `Model.UserAreas`; pre-populated on edit from `item.Area`; used in `OnPostAsync` for new items |
| 12 | `LookupRepository.cs` | `AddluContacts` error: "@ID was not supplied" on insert for ID-keyed tables | Added `else parameters.Add("ID", item.ID)` branch in `CreateLookupItemAsync` — mirrors legacy `BuildParamListID` which always passed `@ID` |
| 13 | `QualityData.cshtml.cs` | Test name resolution used `GetLookupDataAsync(histId)` which returns integer IDs not string codes; histology codes like "3","4" never matched | Switched to `GetHistologyTypesAsync()` which returns the string codes used in `tblBatchBlockHistology.Code` |

---

## Session 2026-09-02 — Create / Edit / View Submission workflow review + Pick List Management integration

Commit `91deb3f` — "Implement return URL handling in user and batch management forms".

### Implemented

| # | Area | Change |
|---|---|---|
| 1 | `BatchDetails.cshtml` (create mode) | Added "Manage submitted by list", "Manage project / contract codes" and "Manage pathologists" secondary buttons beneath the three fields — replaces legacy `btnNewSubmittedBy` / `btnNewProject` / `btnNewContact` |
| 2 | `BatchDetails.cshtml.cs` | `OnPostManagePickList(field)` handler + `CreateDraft` record; serialises every entered create-form value (including histology / antibody / stain code lists) to `TempData` before redirecting, and `RestoreCreateDraft()` re-applies it on return |
| 3 | `EditBatch.cshtml` / `.cs` | Same three buttons plus `EditDraft` save/restore covering all editable header fields, status and status comments |
| 4 | `PickListUserArea.cshtml` / `.cs` | `ReturnUrl` bind property, `SafeReturnUrl` (`Url.IsLocalUrl` guarded), context-aware back link, inset guidance text, "Return to submission" button, and `returnUrl` carried through the row Edit links and the post/redirect cycle. Removed the hard-coded `Done → /Batches/BatchDetails` dead-end |
| 5 | `UserMaintenance.cshtml` / `.cs` | `ReturnUrl` + `SafeReturnUrl`, context-aware back link, inset guidance, "Return to submission" button, `returnUrl` propagated to the Add user / Change links and the "Show deactivated items" GET form |
| 6 | `AddUser` / `EditUser` | `ReturnUrl` + `SafeReturnUrl`, hidden field in the form, and `returnUrl` preserved on every `RedirectToPage("/Admin/UserMaintenance")` |

**Routing:** Project → `PickListUserArea/19` (`LOOKUP_PROJECTS`), Pathologist → `PickListUserArea/18` (`LOOKUP_CONTACTS`), Submitted by → `UserMaintenance` (that list is user-backed, matching legacy `btnNewSubmittedBy`).

### Reviewed and confirmed correct

- Home → Cassetted (TSE / Non-TSE radio + SubmittedAs select) → `BatchDetails?mode=create` — functionally equivalent to legacy's two Home links plus `Cassetted.aspx`.
- Mandatory-field set on create matches legacy `ValidateMandatoryFields()`: Submitted By, Submitted Area, Project / Contract Code, Pathologist, Species, Submission Date.
- Histology / antibody / special-stain conditional rules match legacy `CheckHistology()` — at least one histology; antibodies required when IHC-PrP or IHC-Other selected; stains required when Special Stain selected.
- TSE / Non-TSE histology filtering matches legacy `HideOptions()` — Non-TSE removes IHC-PrP and H&E(BSE); TSE removes IHC-Other.
- Sample Summary retains Add / Edit / Delete / Copy / Done and the Bypass sort toggle; delete confirmations converted from browser `confirm()` to inline GOV.UK warning panels.
- Wet Tissue routes Edit sample to `SubmissionDetails`; block types route to `SubmissionDetailsBlock` — re-resolved server-side on POST rather than trusting a hidden field.
- `CanModifySamples` (Submitted / Rejected) correctly re-derives legacy `EnableDisableButtons` session-mode gating from batch status.

### Raised

ISS-R20 … ISS-R28 — see Open Issues above. Nine items: Copy sample inverted, submission-date rule reversed, missing "at least one sample" Finish gate, View mode unenforced on the block journey, Contacts/Projects area filtering lost, histology-ref validation lost, Add sample reduced to Sender Ref only, minor field-level gaps, and the unverified Wet Tissue `SubmittedAs` code.


