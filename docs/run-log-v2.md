# Histopathology System Migration Agent Run Journal version 2
---

## Run Log

> One row per agent invocation. Add rows chronologically.

| # | Date | Phase | Agent | Scope / Input | Status | Issues Found | Fixes Applied | Output / Linked Report |
|---|------|-------|-------|---------------|--------|--------------|---------------|------------------------|
| 1 | 2026-08-25 | 1 | `GitHub Copilot` | **Auth — Entra ID SAML scheme fix + dev environment config:** (1) `InvalidOperationException: No authentication handler for 'Cookies'` — root cause was `HistoPageModel.cs` issuing `ChallengeResult(CookieAuthenticationDefaults.AuthenticationScheme)` instead of `"saml2"`. Fixed. (2) `appsettings.Development.json` populated with real Entra ID tenant/app IDs, IdP metadata URL, ACS and SLO paths. Provided local test steps including Entra admin action to add `https://localhost:57879/Saml2/Acs` as a Reply URL. | **Done** | Entra admin must add localhost Reply URL before local testing works | `HistoPageModel.cs` ChallengeResult scheme fixed; `appsettings.Development.json` updated | [src/Histo.Web/Pages/HistoPageModel.cs](../src/Histo.Web/Pages/HistoPageModel.cs), [src/Histo.Web/appsettings.Development.json](../src/Histo.Web/appsettings.Development.json) |
| 2 | 2026-08-25 | 1 | `GitHub Copilot` | **AccessDenied page — GDS-compliant Entra ID version:** Legacy page used `Layout = null` (no GDS chrome), Windows login messaging (`Environment.UserDomainName`), and stale Windows-era model. Replaced with shared `_Layout.cshtml`, Entra ID-appropriate messaging, and email resolved from SAML claims (`ClaimTypes.Email` / `preferred_username`). | **Done** | — | Removed inline HTML boilerplate; model changed `Login` → `Email?` from SAML claims | [src/Histo.Web/Pages/AccessDenied.cshtml](../src/Histo.Web/Pages/AccessDenied.cshtml), [src/Histo.Web/Pages/AccessDenied.cshtml.cs](../src/Histo.Web/Pages/AccessDenied.cshtml.cs) |
| 3 | 2026-08-25 | 5 | `GitHub Copilot` | **GDS "There is a problem with the service" page (ServiceProblem):** Created `Pages/Errors/ServiceProblem.cshtml(.cs)` per GDS mandatory content rules (title, h1, body copy). Fixed `Program.cs` middleware from non-existent `/Error` to `UseExceptionHandler("/Errors/ServiceProblem")` + `UseStatusCodePagesWithReExecute`. `[ResponseCache(NoStore)]` applied; `TraceIdentifier` logged, never surfaced to user. | **Done** | `UseStatusCodePagesWithReExecute` currently catches 404s too — add `/Errors/PageNotFound` page when 404 pattern is needed | Created `ServiceProblem.cshtml(.cs)`; fixed `Program.cs` middleware | [src/Histo.Web/Pages/Errors/ServiceProblem.cshtml](../src/Histo.Web/Pages/Errors/ServiceProblem.cshtml), [src/Histo.Web/Program.cs](../src/Histo.Web/Program.cs) |
| 4 | 2026-08-25 | 1 | `GitHub Copilot` | **Entra ID security group analysis — authentication vs authorisation split:** Confirmed no Entra ID security group is implemented. Analysed two options: (A) security group as pre-auth gate on Enterprise Application (`Assignment required = Yes`) — no code changes; (B) Entra group claims mapped to app roles — requires backend changes. Recommended Option A. Provided step-by-step portal instructions for creating `SG-HistopathologySystem-Users` and assigning it to the Enterprise Application. | **Done** | Entra admin action required (portal-only, no code) | Analysis only — no code changes | — |
| 5 | 2026-08-25 | 5 | `GitHub Copilot` | **Quality Data session review + regression prevention (session-metrics + migration-run-journal analysis):** Reviewed documented anti-patterns. Applied fixes to `BatchesForDispatch.cshtml`, `QualityData.cshtml`, `EditQualityDataTest.cshtml`: (1) error summary `<li>` changed to `<a href="#QuickGoId">` link; (2) `"Batch No"` → `"Batch no"` sentence case; (3) `QualityData` "Done" destination corrected to `BatchesForDispatch` (was `/Index`, legacy goes back to dispatch list); (4) `EditQualityDataTest` `<form method="post">` given `asp-page` attribute (Run #83 antiforgery pattern); (5) QCNote and Dispatched checkboxes wrapped in proper `govuk-form-group > govuk-checkboxes` structure. Build: 0 errors. | **Done** | — | 5 targeted fixes applied | [src/Histo.Web/Pages/Batches/BatchesForDispatch.cshtml](../src/Histo.Web/Pages/Batches/BatchesForDispatch.cshtml), [src/Histo.Web/Pages/QC/QualityData.cshtml](../src/Histo.Web/Pages/QC/QualityData.cshtml), [src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml](../src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml) |
| 6 | 2026-08-25 | 5 | `GitHub Copilot` | **Quality Data full gap analysis (BatchesForDispatch / QualityData / EditQualityDataTest) + parity fixes:** Gap analysis of 3 pages against legacy. Implemented: (1) `BatchesForDispatch` columns renamed to legacy labels (Submission Number, Project Code, Submitted Date), Completed Date and Customer Ref removed; (2) `QualityData` batch summary expanded to all 10 legacy fields (Project/Contract Code, Pathologist, Entered By/Area, Submitted By/Area, Submission Date, Received Date, Species — all resolved via ILookupService/IUserService lookups); (3) `QualityData` grid gained dedicated On Hold column + separate Failed/Passed columns matching legacy; (4) `EditQualityDataTest` gained Not Tested radio option, QC Note Ref read-only link, Charges checkbox section (LOOKUP_PREMIUM_CHARGES = 17), JS show/hide for Dispatched fields and QC Note conditional on result, `type="number"` on Number of slides, missing validations (DispatchedBy/To required when dispatched, RemedialAction always required, archive date ↔ location cross-check). Build: 0 errors. | **Done** | TC Codes save pending at time of gap analysis (completed in Run #7) | Column renames, summary fields, validation gaps, JS conditionals, Charges UI | [src/Histo.Web/Pages/Batches/BatchesForDispatch.cshtml](../src/Histo.Web/Pages/Batches/BatchesForDispatch.cshtml), [src/Histo.Web/Pages/QC/QualityData.cshtml](../src/Histo.Web/Pages/QC/QualityData.cshtml), [src/Histo.Web/Pages/QC/QualityData.cshtml.cs](../src/Histo.Web/Pages/QC/QualityData.cshtml.cs), [src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml](../src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml), [src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml.cs](../src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml.cs) |
| 7 | 2026-08-25 | 5 | `GitHub Copilot` | **TC Codes (Premium Charges) — full stack implementation:** Investigated legacy `clsCheckBoxData.vb` and confirmed SP names: `AddHistologyTCCode` / `DeleteHistologyTCCode`, `AddAntibodiesTCCode` / `DeleteAntibodiesTCCode`, `AddSpecialStainTCCode` / `DeleteSpecialStainTCCode`. Confirmed `LOOKUP_PREMIUM_CHARGES = 17` from `Common.vb`. Confirmed TC code data is already fetched in `GetBatchBlocksByID` result sets 7–9 (previously skipped). **Full stack implemented:** (1) `BlockTest.cs` — added `TcCode(int Id, string Code)` record and `TCCodes` property; (2) `BlockTestRepository` — reads result sets 7/8/9, builds per-type TC code lookups, populates `TCCodes` on each `BlockTest`; added `SaveTCCodesAsync` delta method (delete removed → insert added); (3) `IBlockTestRepository` / `IBlockTestService` / `BlockTestService` — `SaveTCCodesAsync` added at each layer; (4) `EditQualityDataTest.cshtml.cs` — `SelectedCharges` populated from `Test.TCCodes` on GET; `SaveTCCodesAsync` called after `UpdateAsync` on POST. Build: 0 errors. | **Done** | SP existence in DB should be verified via `sys.procedures` query before testing | Full TC code read + delta save wired end to end | [src/Histo.Histology/Models/BlockTest.cs](../src/Histo.Histology/Models/BlockTest.cs), [src/Histo.Histology/Repositories/BlockTestRepository.cs](../src/Histo.Histology/Repositories/BlockTestRepository.cs), [src/Histo.Histology/Interfaces/IBlockTestRepository.cs](../src/Histo.Histology/Interfaces/IBlockTestRepository.cs), [src/Histo.Histology/Interfaces/IBlockTestService.cs](../src/Histo.Histology/Interfaces/IBlockTestService.cs), [src/Histo.Histology/Services/BlockTestService.cs](../src/Histo.Histology/Services/BlockTestService.cs), [src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml.cs](../src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml.cs) |
| 8 | 2026-08-26 | 5 | `GitHub Copilot` | **Navigation, BatchesForEditing, and EditBatch legacy parity review:** Compared `Quality data` nav link (→`BatchesForEditing`) against `EditSubmissionStatus`/`EditBatch` and legacy `BatchesForEditing.aspx`/`EditBatch.aspx.vb`. Found and fixed 3 gaps: (1) `_NavPartial.cshtml` had a duplicate link — both `Edit submission status` and `Quality data` pointed to `/Batches/BatchesForEditing` — replaced `Quality data` with `Edit QC notes` → `/QC/QCNotes`; (2) `BatchesForEditingModel.OnPostSelect` redirected to `/Batches/BatchDetails` instead of `/Batches/EditBatch` — legacy `grdBatchesForEditing_SelectedIndexChanged` always redirects to `EditBatch.aspx` — fixed; (3) `BatchesForEditing.cshtml` grid headers/columns didn't match legacy (`Batch No`/`Project`/`Batch date` instead of `Submission Number`/`Project Code`/`Submitted Date`; extra `Received date`/`Customer ref` columns not present in legacy) — corrected to match legacy exactly and the sibling `BatchesForDispatch.cshtml` pattern. `EditBatch.cshtml(.cs)` reviewed against `EditBatch.aspx.vb` — status transition validation rules already match legacy correctly, no changes needed. Build: 0 CS errors (only pre-existing file-lock copy errors from a running dev process). | **Done** | — | Nav dedup, row-select redirect target, grid column/header parity | [src/Histo.Web/Pages/Shared/_NavPartial.cshtml](../src/Histo.Web/Pages/Shared/_NavPartial.cshtml), [src/Histo.Web/Pages/Batches/BatchesForEditing.cshtml.cs](../src/Histo.Web/Pages/Batches/BatchesForEditing.cshtml.cs), [src/Histo.Web/Pages/Batches/BatchesForEditing.cshtml](../src/Histo.Web/Pages/Batches/BatchesForEditing.cshtml) |
| 9 | 2026-08-26 | 5 | `GitHub Copilot` | **EditQCNote — restored missing QC Note Ref summary and Created by/Date created sections:** Compared `EditQCNote.cshtml` against legacy `EditQCNote.aspx`/`.aspx.vb`. Legacy top summary box (`QC Note Ref`, `Submission Number`, `Project`, `Species`, `Stain Ref`) and bottom box (`CreatedBy`/`DateCreated` labels, populated from `GetQCNoteTestInformation`'s `Name`/`DateCreated` columns) were entirely absent from the migrated page — only the note textarea remained. Added `CreatedBy`/`DateCreated` to the `QCNote` model, populated them in `QCNoteRepository.GetByIdAsync` from the already-fetched `detail` result, and added a `govuk-summary-list` (QC note ref/Submission number/Project/Species/Stain ref) plus a `govuk-body` "Created by X on <date>" line to `EditQCNote.cshtml`. | **Done** | — | Restored 2 legacy sections | [src/Histo.QualityControl/Models/QCNote.cs](../src/Histo.QualityControl/Models/QCNote.cs), [src/Histo.QualityControl/Repositories/QCNoteRepository.cs](../src/Histo.QualityControl/Repositories/QCNoteRepository.cs), [src/Histo.Web/Pages/QC/EditQCNote.cshtml](../src/Histo.Web/Pages/QC/EditQCNote.cshtml) |
| 10 | 2026-08-26 | 5 | `GitHub Copilot` | **QCNotes Edit button silently non-functional — RuntimeBinderException regression from Run #9:** Reported the `Edit` button on `QCNotes.cshtml` stopped working after the previous fix. Root cause: `QCNoteRepository.GetByIdAsync` used raw `dynamic` member access (`detail?.Name`, `detail?.DateCreated`) added in Run #9 — when the `GetQCNoteHistStainTestInformation` result set lacked those exact columns for a given note, dynamic dispatch threw `RuntimeBinderException`, silently swallowed by `QCNoteService`'s broad `catch (Exception)`, returning `null`; `EditQCNoteModel.OnGetAsync` treats a null `Note` as not-found and redirects straight back to `QCNotes` — the same failure pattern as `ISS-034`. Fixed by replacing dynamic access with the established `IDictionary<string, object>` + `TryGetValue` pattern (matching `LookupRepository.MapDescriptionIsActive`/`UserRepository.MapUser`), so a missing column returns a safe default instead of throwing. | **Done** | — | Dynamic access replaced with safe dictionary lookups | [src/Histo.QualityControl/Repositories/QCNoteRepository.cs](../src/Histo.QualityControl/Repositories/QCNoteRepository.cs) |
| 11 | 2026-08-26 | 5 | `GitHub Copilot` | **EditQCNote — notes missing entirely for antibody tests + blank notes not auto-populated:** Reported that some notes present in legacy do not appear in the current system. Root cause: legacy `clsQCNote.GetQCNoteTestInformation` calls **two** SPs — `GetQCNoteHistStainTestInformation` and `GetQCNoteAntibodiesInformation` — and imports both result sets into one table before reading the note; it also builds a default padded table of Sender Ref/Histo Ref/Block Ref/Test from **all** rows when `QCText` is blank (note never edited), rather than showing nothing. `QCNoteRepository.GetByIdAsync` only called the first SP and only read the first row, so antibody-test notes were dropped entirely and blank notes showed empty instead of the legacy default text. Fixed: added the `GetQCNoteAntibodiesInformation` call (using `BatchType` read from the header row as the `SubmissionType` param, concatenated with the hist/stain rows), and added `BuildDefaultNoteText` reproducing legacy's `InsertSpaces`-padded table. Build: 0 errors. | **Done** | — | Combined both SPs' result sets; reproduced legacy default-note-text generation | [src/Histo.QualityControl/Repositories/QCNoteRepository.cs](../src/Histo.QualityControl/Repositories/QCNoteRepository.cs), [src/Histo.QualityControl/Models/QCNote.cs](../src/Histo.QualityControl/Models/QCNote.cs) |
| 12 | 2026-08-26 | 5 | `GitHub Copilot` | **EditQCNote — Created by date missing after Run #11 fix:** Reported the date next to the username was missing (username alone displayed correctly). Root cause: the `DateCreated` column is returned by the SPs as a `dd/MM/yyyy` `VARCHAR` string (same pattern documented for `NullableDateTimeTypeHandler`); that type handler only runs for Dapper's **typed** query mapping, but `QCNoteRepository` reads these rows dynamically via `IDictionary<string, object>`, bypassing type handlers entirely — the raw string failed the `created is DateTime` check and always evaluated to `null`. Fixed: added a `ParseDate` helper mirroring `NullableDateTimeTypeHandler`'s logic (native `DateTime` passthrough, `dd/MM/yyyy`/`dd/MM/yyyy HH:mm:ss` exact parse, general parse fallback). Build: 0 errors. | **Done** | — | Added dd/MM/yyyy-aware date parsing for the dynamic-read code path | [src/Histo.QualityControl/Repositories/QCNoteRepository.cs](../src/Histo.QualityControl/Repositories/QCNoteRepository.cs) |
| 13 | 2026-08-27 | 5 | `GitHub Copilot` | **TSE/NON-TSE submission workflow GDS redesign + route-based state/access-guard rollout + New Submission Create/Edit flow fixes:** Produced `docs/TSE-NonTSE-Submission-Workflow-Redesign.md` (legacy-vs-current pain points, GDS-aligned target journey, Mermaid diagram). Consolidated block management onto `SubmissionDetailsBlock` (add/edit/delete/copy, per-block tissue assignment, pre-booked block ref + mandatory histology ref for pre-cassetted submissions, bulk "number of blocks"); replaced browser `confirm()` with inline GOV.UK confirmation panels; replaced an auto-submitting checkbox with an explicit Apply button. Added `HistoPageModel.CheckBatchAccessAsync` object-level access guard backed by a pure, unit-tested `BatchAccessDecision.IsAllowed` (`Histo.Core.Domain`), and rolled out route/query-based `BatchId`/`AnimalId` (with session fallback) across `BatchBlockSummary`, `SubmissionDetailsBlock`, `BatchDetails`, `Blocks/BlockDetails`, `CopyBlocks`, `CopySamples(Summary)`. Fixed the New Submission Create/Edit flow: restored Sender Ref search/select on `AddSubmission` via the existing `SearchSender` picker, redirect after adding a sample now goes to Sample Blocks instead of back to the list, and `BatchSubmissionID` is resolved/created instead of silently redirecting to Home when missing. Fixed `Cassetted`'s Submission Type dropdown defaulting to a stale previous selection. Changed `BatchDetails` Submission date to a native `type="date"` input. Investigated and fixed `BatchDetails` button visibility across Create/View Submission journeys (added `IsViewMode`/`CanPrint`; removed the redundant QC notes button and a duplicate Samples task-list row/link). Build: 0 errors. Tests: 144 passed, 1 pre-existing skip. | **Done** | Per-block test selection (EO/H&E/IHC/Special Stain) creation still needs DBA confirmation of the correct stored procedure before implementing — not guessed; task-list overview and full route-based-state rollout to remaining wizard pages (`AddSample`, `SubmissionDetails`, `Cassetted`) deferred as lower-value/higher-risk | Full list in commit — see `docs/TSE-NonTSE-Submission-Workflow-Redesign.md` and session prompts 56–66 in `User-Prompts-Log.md` | [docs/TSE-NonTSE-Submission-Workflow-Redesign.md](TSE-NonTSE-Submission-Workflow-Redesign.md), [src/Histo.Web/Pages/HistoPageModel.cs](../src/Histo.Web/Pages/HistoPageModel.cs), [src/Histo.Core/Domain/BatchAccessDecision.cs](../src/Histo.Core/Domain/BatchAccessDecision.cs), [src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml.cs](../src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml.cs), [src/Histo.Web/Pages/Submissions/AddSubmission.cshtml.cs](../src/Histo.Web/Pages/Submissions/AddSubmission.cshtml.cs), [src/Histo.Web/Pages/Batches/Cassetted.cshtml.cs](../src/Histo.Web/Pages/Batches/Cassetted.cshtml.cs), [src/Histo.Web/Pages/Batches/BatchDetails.cshtml.cs](../src/Histo.Web/Pages/Batches/BatchDetails.cshtml.cs) |
| 14 | 2026-08-27 | 5 | `GitHub Copilot` | **Submission scenario gating, BlockDetails consolidation, and AddAnimal stored-procedure fixes (22:47–23:50):** Investigated "Add animal" button not visible on `BatchBlockSummary` despite previously working, and clarified that Add/Edit/Copy sample buttons must be gated per submission journey (Create/View/Edit/Assign Tissues) rather than always shown — confirmed Edit Submission journey edits header fields on `EditBatch`, while sample/animal/block details are edited via `BatchBlockSummary`/`SubmissionDetailsBlock`. Investigated the Add sample flow redirecting to `AddSubmission` without showing the newly added sample, cross-checked against legacy behaviour. Reviewed the create journey's navigation complexity (New TSE Submission → Submission Samples → Sample Summary → Blocking → Block Details → Search Block Refs) and, on request, implemented the agreed consolidation: merged `Blocks/BlockDetails.cshtml` into `SubmissionDetailsBlock.cshtml` (dual-mode batch-wide/animal-scoped view) with an inline "check used block refs" panel, removing a full page hop. While verifying, found newly added animals still not appearing on the sample list — traced to `BatchBlockSummary`'s either/or animal-list fallback silently dropping block-less animals whenever another animal in the batch already had blocks; fixed via a `MergeAnimals` helper. Fixed two runtime exceptions raised during testing: (1) `System.NotSupportedException: The member PMDate of type System.DBNull cannot be used as a parameter value` in `SubmissionRepository.AddAnimalAsync` — Dapper's `DynamicParameters.Add` requires an explicit `dbType` when the value can be `DBNull.Value`; fixed by adding `dbType: DbType.String` to the `PMDate` parameter; (2) `SqlException: Procedure or function AddAnimal has too many arguments specified` — the migrated call passed extra/incorrect parameters; cross-referenced legacy `clsAnimal.vb` to confirm the real `AddAnimal` signature (`SenderRef, HistologyRef, NextBlockRef, PMDate, OnHold` + `@NewID` OUTPUT) and corrected the Dapper call to match exactly. | **Done** | — | `SubmissionRepository.AddAnimalAsync` parameter/type fixes; `BatchBlockSummary` animal-list merge; `BlockDetails.cshtml` consolidated into `SubmissionDetailsBlock.cshtml` | [src/Histo.Submissions/Repositories/SubmissionRepository.cs](../src/Histo.Submissions/Repositories/SubmissionRepository.cs), [src/Histo.Web/Pages/Submissions/BatchBlockSummary.cshtml.cs](../src/Histo.Web/Pages/Submissions/BatchBlockSummary.cshtml.cs), [src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml](../src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml) |
| 15 | 2026-08-28 | 5 | `GitHub Copilot` | **Submission-journey navigation verification + GDS button-alignment fixes:** Verified back-link/cancel/redirect targets across all 4 submission journeys (Assign Tissues to Blocks, Create/View/Edit Submission) against legacy — all correct, no dead links or wrong redirect targets found. Diagnosed and fixed 4 button-alignment defects caused by missing `govuk-button-group` wrappers or missing `data-module` attributes: (1) `EditBatch.cshtml` — "Samples" link moved inside a `govuk-button-group` (was floating outside any group, misaligned from Save/Cancel); (2) `SubmissionDetailsBlock.cshtml` — added missing `data-module="govuk-button"` to the "Manage sample" link (sibling "Delete" link already had it); (3) `AddSubmission.cshtml` — wrapped "Add sample"/"Cancel" in a `govuk-button-group`, and wrapped the separately-formed "Check historical data for this sender ref" button in its own `govuk-button-group` with top margin so it no longer renders detached; (4) `BatchBlockSummary.cshtml` — "Add sample" and "Done" buttons each wrapped in a `govuk-button-group` instead of ad-hoc margin utility classes. Build: 0 errors. | **Done** | — | 4 files — `govuk-button-group` wrapping + missing `data-module` attribute | [src/Histo.Web/Pages/Batches/EditBatch.cshtml](../src/Histo.Web/Pages/Batches/EditBatch.cshtml), [src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml](../src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml), [src/Histo.Web/Pages/Submissions/AddSubmission.cshtml](../src/Histo.Web/Pages/Submissions/AddSubmission.cshtml), [src/Histo.Web/Pages/Submissions/BatchBlockSummary.cshtml](../src/Histo.Web/Pages/Submissions/BatchBlockSummary.cshtml) |
| 16 | 2026-09-01 | 5 | `GitHub Copilot` | **Receive Submission page (`ReceiveBatch`) legacy-parity rebuild + `develop` merge reconciliation:** Rebuilt `ReceiveBatch.cshtml(.cs)` from a stub "Confirm receipt?" button into the full legacy workflow (status choice Received/Rejected, date/time/received-by capture, post-fixation checkboxes with "Other" free text, rejection reason, read-only repeat-blocks indicator, read-only view mode for already-actioned submissions). Extended `Batch` model (`TimeReceived`, `ReceivedBy`, `PostFixationOther`) and `IBatchRepository`/`BatchRepository`/`IBatchService`/`BatchService` with post-fixation get/save (legacy `AddPostFixation`/`DeletePostFixation` SPs). Merged `origin/develop` into the feature branch (58 commits ahead) and resolved 8 conflicting files: additive interface/service methods kept both sides; `Batch.cs` deduped overlapping fields; `BatchRepository.UpdateAsync` — adopted develop's more complete `EditBatch` parameter mapping; `ReceiveBatch.cshtml(.cs)` — kept this session's full-featured logic rebuilt on develop's exhaustive field carry-forward (develop's own change here had regressed back to a bare "Confirm receipt" stub). Post-merge fix: `Batch.SubmittedArea`/`OtherSubmittedArea` type changed `int?`→`string?` on develop; `ReceiveBatchModel.LoadLookupsAsync` updated to match. Build: 0 errors. Tests: 144 passed, 1 skipped. | **Done** | — | Merge conflict resolution across `Batch.cs`, `IBatchRepository.cs`, `IBatchService.cs`, `BatchRepository.cs`, `BatchService.cs`, `ReceiveBatch.cshtml(.cs)`, `.gitignore` | [src/Histo.Web/Pages/Batches/ReceiveBatch.cshtml.cs](../src/Histo.Web/Pages/Batches/ReceiveBatch.cshtml.cs), [src/Histo.Web/Pages/Batches/ReceiveBatch.cshtml](../src/Histo.Web/Pages/Batches/ReceiveBatch.cshtml), [src/Histo.Submissions/Models/Batch.cs](../src/Histo.Submissions/Models/Batch.cs), [src/Histo.Submissions/Repositories/BatchRepository.cs](../src/Histo.Submissions/Repositories/BatchRepository.cs) |
| 17 | 2026-09-02 | 5 | `GitHub Copilot` | **ReceiveBatch bug fixes + Copilot PR review comment:** Fixed a `dotnet build` error in `BatchService.GetPostFixationCodesAsync` — `_logger.LogError(ex, message, args)` call had exception/message arguments reversed vs `IAppLogger.LogError(string, Exception?, params)`'s real signature. Fixed the "Received" radio button rendering invisible on `ReceiveBatch.cshtml` — its `<label>` element was missing entirely (only "Not started"/"Rejected" had one) and its `aria-controls` attribute was misspelled `data-aria-controls`; same typo fixed on the post-fixation "Other" checkbox. Addressed a GitHub Copilot PR review comment (medium severity): `ReceiveBatchModel.OnPostSaveAsync` checked `if (!await _batches.UpdateAsync(...))` expecting a `false` return on failure, but `BatchService.UpdateAsync` actually rethrows on failure — the friendly error path was dead code. Fixed by wrapping the call in `try/catch`, matching the pattern already used by `EditBatch.cshtml.cs`; corrected the misleading `UpdateAsync` doc comments on `IBatchService`/`BatchService` to state it rethrows. Also investigated and fixed a local-only dev auth bypass in `HistoPageModel.cs` (uncommitted, gated by a gitignored `appsettings.Development.json` flag, not part of this branch's merge) — root cause of "nav/user missing after login" was the bypass only re-populating `ISessionService` when the auth-cookie claim was absent, not when the in-memory session itself was empty (session is wiped on every app restart independently of the cookie). Build: 0 errors. Tests: 144 passed, 1 skipped. | **Done** | — | `LogError` argument order; missing `<label>`/`aria-controls` typo; dead error-handling branch; doc-comment correction | [src/Histo.Submissions/Services/BatchService.cs](../src/Histo.Submissions/Services/BatchService.cs), [src/Histo.Web/Pages/Batches/ReceiveBatch.cshtml](../src/Histo.Web/Pages/Batches/ReceiveBatch.cshtml), [src/Histo.Web/Pages/Batches/ReceiveBatch.cshtml.cs](../src/Histo.Web/Pages/Batches/ReceiveBatch.cshtml.cs), [src/Histo.Submissions/Interfaces/IBatchService.cs](../src/Histo.Submissions/Interfaces/IBatchService.cs) |

| 16 | 2026-08-28 | 1 | `GitHub Copilot` | **Live Dev deployment debugging — Entra ID SAML 2.0 sign-in chain (4 fixes):** Reported symptom: `https://dev-cde.azure.defra.cloud` redirected to the raw App Service hostname `devcdewebaw1401.azurewebsites.net/Saml2/login?ReturnUrl=%2F`, which then failed with `ERR_CONNECTION_CLOSED`. Root-caused and fixed in sequence via live Log stream evidence: (1) `Program.cs` — added `UseForwardedHeaders()` (trusting `X-Forwarded-For`/`Proto`/`Host`, cleared `KnownNetworks`/`KnownProxies`) since `dev-cde.azure.defra.cloud` has no App Service custom domain binding and is proxied by an external edge to the App Service's default hostname; (2) confirmed via `git log`/`git diff` that the fix wasn't reaching Dev because it was on a feature branch not yet merged to `develop` — merged via PR, redeployed; (3) since the proxy still didn't yield the correct `Request.Host` after redeploy (confirmed via captured `Location` header), added `PostConfigure<CookieAuthenticationOptions>("saml2", ...)` overriding `OnRedirectToLogin`/`OnRedirectToAccessDenied` to force the redirect to a **relative** URI (`PathAndQuery` only), removing the dependency on `Request.Host` entirely; (4) after that fix, the sign-in flow reached Entra ID and back to `/Saml2/Acs` but failed with `Microsoft.IdentityModel.Tokens.SecurityTokenInvalidAudienceException: IDX10214: Audience validation failed` — `saml2Config.AllowedAudienceUris` was never populated, so audience validation failed unconditionally regardless of the assertion; fixed by adding `saml2Config.AllowedAudienceUris.Add(saml2Config.Issuer)`. Confirmed via Log stream: SAML assertion now validates successfully (`NameIdentifier: Sabari.Katari@defra.gov.uk`). Also fixed in the same session: `_Layout.cshtml` nav/user-context chrome was rendering for anonymous requests (e.g. on the `ServiceProblem` error page after hitting the decommissioned `/Login` route) because neither `_NavPartial.cshtml` nor `_UserContextPartial.cshtml` checked authentication state — wrapped both partial renders in `@if (User.Identity?.IsAuthenticated == true)`. Remaining blocker (infra, not code): `ResolveUserByEmailAsync` fails with `SqlException: Could not open a connection to SQL Server` — `ConnectionStrings:HistologyDb` Key Vault secret/SQL firewall not yet configured for Dev; raised as a separate infra/DBA ask. Build: 0 errors. | **Done** (SAML chain); DB connectivity **Open** | Missing `UseForwardedHeaders()`; absolute Host-derived cookie challenge redirect; empty `AllowedAudienceUris`; anonymous nav/user chrome leak; `HistologyDb` DB unreachable in Dev (infra) | [src/Histo.Web/Program.cs](../src/Histo.Web/Program.cs), [src/Histo.Web/Pages/Shared/_Layout.cshtml](../src/Histo.Web/Pages/Shared/_Layout.cshtml) |
| 17 | 2026-09-01 | 5 | `GitHub Copilot` | **Dev-only auth bypass (HistoPageModel) + submission-type routing analysis across all 5 types (Create/Edit/View/Copy):** **(A) Dev bypass** — implemented a `DevAuthBypass` flag in `HistoPageModel.OnPageHandlerExecutionAsync` reading from `appsettings.Development.json` (not committed): when `true`, unconditionally bakes a hardcoded `Maintenance`-group principal into `HttpContext.User` and calls `Session.PopulateFromClaims`, bypassing the Entra ID SAML round-trip for local testing without modifying auth infrastructure. Two bugs diagnosed in the initial implementation: `SignInAsync("saml2", ...)` threw (the `"saml2"` scheme has no `IAuthenticationSignInHandler`) causing a service-error page with no nav chrome; and the inner `if (GroupName is empty)` guard was being skipped by stale cookie state, leaving `Session.GroupName` empty and all home-page panels hidden. Fixed by removing `SignInAsync` and making the principal re-application unconditional. **Why not fixed during migration:** No Entra ID endpoint was available during migration — the ADR-006 NTLogin bridge served as the dev bypass until Entra ID was wired. That bridge was decommissioned on 2026-08-28, leaving a gap for developers without an Entra ID Reply URL for localhost. **(B) Submission-type routing analysis** — deep-read of legacy `Cassetted.aspx.vb`, `BatchBlockSummary.aspx.vb`, `BatchSummary.aspx.vb`, `SubmissionDetailsBlock.aspx.vb`, `AddSample.aspx.vb`; cross-referenced all 5 submission types (Wet Tissue code `"4"`, Pre-Cassetted code `"5"`, Wax Block/Stained Section/Unstained Section codes `"1"/"2"/"3"`). Three bugs found and fixed: (1) `BatchBlockSummary.cshtml.cs` `OnPostSelect` always routed Edit sample to `SubmissionDetailsBlock` regardless of type — Wet Tissue must route to `SubmissionDetails` (tissue list + PM date/Histology ref) per legacy `BatchSummary.aspx` behaviour; added `IsWetTissue` property (`GetSubmittedAsCodeAsync` code `"4"`) and branched `OnPostSelect` accordingly; (2) `SubmissionDetails.cshtml` back-link and "Back to samples" missing `BatchId` route parameter — returning from Wet Tissue edit lost batch context; added `asp-route-BatchId`, `[BindProperty(SupportsGet=true)] BatchId`, and propagated through `LoadAnimalAsync`; (3) `SubmissionDetails.cshtml` had no view-mode awareness — PM date/Histology ref form, Delete tissue, and Add tissue all rendered in View Submission mode; added `IsViewMode => Session.IsViewSubmissionMode` property and wrapped write controls with `@if (!Model.IsViewMode)` guards; also restored "Edit sample" visibility in View mode (`BatchBlockSummary.cshtml` had hidden it, but legacy `EnableDisableButtons` kept it enabled in `SV_ViewSubmission=True` mode). **Why not fixed during migration:** The two-page consolidation (`BatchSummary.aspx` + `BatchBlockSummary.aspx` → single `BatchBlockSummary.cshtml`) was completed in Run #80–#81; however, legacy `BatchSummary.aspx` (Wet Tissue variant) was initially misidentified as already covered by `ViewSamples.cshtml` — see Run #81 root-cause note. Once the consolidation was corrected, the per-type routing divergence existed but went unnoticed until live testing with different submission types. Build: 0 errors. | **Done** | `SignInAsync` on non-sign-in scheme; unconditional bypass guard skip; Edit sample routing per submission type; `SubmissionDetails` missing `BatchId`; `SubmissionDetails` no view-mode guard | [src/Histo.Web/Pages/HistoPageModel.cs](../src/Histo.Web/Pages/HistoPageModel.cs), [src/Histo.Web/Pages/Submissions/BatchBlockSummary.cshtml.cs](../src/Histo.Web/Pages/Submissions/BatchBlockSummary.cshtml.cs), [src/Histo.Web/Pages/Submissions/SubmissionDetails.cshtml.cs](../src/Histo.Web/Pages/Submissions/SubmissionDetails.cshtml.cs), [src/Histo.Web/Pages/Submissions/SubmissionDetails.cshtml](../src/Histo.Web/Pages/Submissions/SubmissionDetails.cshtml) |
| 18 | 2026-09-01 | 5 | `GitHub Copilot` | **Full TSE/Non-TSE workflow parity audit + GDS consolidation review + route rename + Edit-sample/Add-sample routing regression fix:** **(A) Parity audit** — comprehensive gap analysis of Create/View/Edit/Copy across all 5 submission types against `Migration-Plan.md`/`Intelligent-Migration-Plan.md`/`LLD.md`; confirmed 2 defects (Wet Tissue Edit-sample POST routing depended on a stale hidden-field `IsWetTissue` instead of re-resolving server-side; `SearchSubmissions` Edit-gating inverted vs legacy) — both fixed at the time (`OnPostSelect` now re-queries `GetSubmittedAsCodeAsync` on POST; `CanEditSubmission` corrected to Received/OnHold/InProgress). Registered 7 accepted-consolidation deviations (D-1..D-7) in `Parity-Audit-Report.md` §13 (inline histology-ref grid edit, grid paging, mouse-range bulk entry, Excel mouse-number upload, TSE/NonTSE match check on Copy samples, auto-generate histology ref on Copy blocks, per-block test-type checkboxes) that were previously undocumented code comments only. **(B) Module-to-Page Mapping nav audit** — found and fixed 2 orphaned pages with zero inbound links (`Batches/SubmissionsOnHold` — restored via a "Samples on hold" button on `EditBatch`; `Bookings/EditHistologyRef` — restored via a "Edit next histology ref" link on `BookingMenu`); refreshed the stale Module-to-Page Mapping table in `Migration-Plan.md` to reflect shipped/consolidated page names. **(C) GDS route rename** — `BatchBlockSummary` renamed to `SampleSummary` (`git mv`, class rename, 301 redirect registered in `Program.cs` for the old path) because "Block" is user-facing-URL-meaningless for Wet Tissue submissions; added a `ViewData["BatchNo"]` caption ("Wet Tissue" / "Wax Block / Cassetted") to the page so users can tell submission type without inspecting the URL, closing a usability gap the rename itself exposed. **(D) D-1 disposition downgrade** — reassessed inline histology-ref grid editing: rejected as a GDS anti-pattern (no editable-table Design System component; violates "one thing per page"); downgraded Medium→Low, disposition changed to "Resolved by design", reasoning tied to fix (E). **(E) Add-sample routing regression (found via user retest)** — `AddSubmissionModel.OnPostAsync` always redirected back to `SampleSummary` after adding a sample, forcing an extra "Edit sample" click for every sample on every submission type; legacy `AddSubmission.aspx.vb::btnNext_Click` redirects straight into the per-sample detail page (`SubmissionDetailsBlock`/`SubmissionDetails`) via `SV_AddSampleNextPage`. Fixed by injecting `IBatchService` and branching the post-add redirect on `GetSubmittedAsCodeAsync` exactly like `SampleSummary`/`BatchBlockSummary`. **User-reported false negative on first fix:** the initial fix compiled correctly but a stale `Histo.Web` process (holding the previous build's `.exe` locked, confirmed via `MSB3027` in the build log) was still serving pre-fix code during the user's live retest; `dotnet test` (run to "verify") does not rebuild `Histo.Web` at all, giving a false-positive verification signal. Root-caused, confirmed no process now holds the lock, rebuilt `Histo.Web` clean (0 errors), and flagged the `dotnet test`-does-not-rebuild-Histo.Web verification gap explicitly to the user. Build: 0 errors, 0 warnings (Histo.Web + full solution). Tests: 144 pass, 1 skipped. | **Done** | D-1 through D-7 consolidation deviations (see `Parity-Audit-Report.md` §13); stale build masking a verified fix during manual retest | Edit-sample POST re-resolves submission type server-side; `SearchSubmissions.CanEditSubmission` corrected; `SubmissionsOnHold`/`EditHistologyRef` nav links restored; `BatchBlockSummary`→`SampleSummary` rename + redirect + `BatchNo` caption; `AddSubmission` post-add redirect now type-aware | [docs/Parity-Audit-Report.md](Parity-Audit-Report.md), [docs/Migration-Plan.md](Migration-Plan.md), [src/Histo.Web/Pages/Submissions/SampleSummary.cshtml.cs](../src/Histo.Web/Pages/Submissions/SampleSummary.cshtml.cs), [src/Histo.Web/Pages/Submissions/AddSubmission.cshtml.cs](../src/Histo.Web/Pages/Submissions/AddSubmission.cshtml.cs), [src/Histo.Web/Program.cs](../src/Histo.Web/Program.cs), [src/Histo.Web/Pages/Batches/EditBatch.cshtml](../src/Histo.Web/Pages/Batches/EditBatch.cshtml), [src/Histo.Web/Pages/Bookings/BookingMenu.cshtml](../src/Histo.Web/Pages/Bookings/BookingMenu.cshtml) |
| 20 | 2026-09-02 | 5 | `GitHub Copilot` | **Wet Tissue Edit sample silent-redirect bug — root-caused via user-supplied browser Network tab trace, fixed to match the already-fixed `SubmissionDetailsBlock` twin:** User reported Edit sample "does nothing" for a Wet Tissue submission (batch 29398, animal 91478) after several earlier turns misattributed it to a stale build. A Network tab trace proved otherwise: `POST /Submissions/SampleSummary?handler=Select` correctly 302'd to `SubmissionDetails` (submission-type routing is correct), but `GET /Submissions/SubmissionDetails?batchId=29398&animalId=91478` itself returned a **302** instead of 200, bouncing straight back to `/Submissions/SampleSummary?batchId=29398` — indistinguishable from "nothing happened". Root cause: `SubmissionDetailsModel.LoadAnimalAsync` only queried the plain `GetAnimalsByBatchAsync` table and silently redirected to `SampleSummary` whenever the animal wasn't found there — the identical defect class already found and fixed in `SubmissionDetailsBlockModel` earlier this session, but never ported to this twin page. `SampleSummary`'s own animal list is a merge of the plain table and the block-animal table, so an animal visible/selectable there could still be invisible to `SubmissionDetails`. Fixed `LoadAnimalAsync` to check `GetBlockAnimalsByBatchAsync` first with a fallback to `GetAnimalsByBatchAsync` (mirrors `SampleSummaryModel`/`SubmissionDetailsBlockModel`), and removed the silent redirect on not-found so `Animal` stays `null` and the view's existing (previously unreachable) "Sample not found" `govuk-warning-text` branch renders instead. Added explicit `if (Animal is null)` guards to `OnGetAsync`, `OnPostSaveDetailsAsync` and `OnPostAddTissueAsync` (all previously dereferenced `Animal!` unguarded, relying on the redirect that no longer happens). Build: 0 errors, 3 pre-existing warnings. Tests: 145 pass, 1 skipped. | **Done** | `OnPostDeleteTissueAsync` still doesn't call `LoadAnimalAsync` for batch/animal scope validation before deleting (unlike the fixed `SubmissionDetailsBlockModel` equivalent) — left as-is, not part of the reported defect | `LoadAnimalAsync` block-animal-table merge + no-redirect-on-not-found; null guards added to 3 handlers | [src/Histo.Web/Pages/Submissions/SubmissionDetails.cshtml.cs](../src/Histo.Web/Pages/Submissions/SubmissionDetails.cshtml.cs) |
| 24 | 2026-09-04 | 5 | `GitHub Copilot` | **ViewSubmissions POST-based sort/pagination — colour mismatch then a real non-functional-sort bug, both found via user retest:** **(1) Colour mismatch:** `_SortableHeaderPost`/`_PaginationPost` styled their `<button>` elements with `govuk-link`/`govuk-pagination__link`, but GOV.UK Frontend's link colour/hover rules are written against the `:link`/`:visited` pseudo-classes, which only ever match real `<a href>` elements — applying those classes to a `<button>` has zero visual effect, so the buttons rendered in the browser's default text colour instead of GOV.UK blue. Fixed by rewriting `.app-link-button` in `Styles.css` to state the link colour/hover/focus states directly (no pseudo-class dependency) and removing the now-pointless `govuk-link` classes from both partials. **(2) Sorting icon still not appearing after the CSS fix — root-caused as a real functional bug, not styling:** the hidden carrier form on `ViewSubmissions.cshtml` had hidden `SortColumn`/`SortDesc`/`PageNumber` inputs, and ASP.NET Core's default composite value provider checks **Form body before Query string** — so the sort-header button's `formaction="?handler=Search&SortColumn=X&SortDesc=true"` query override was always shadowed by the stale hidden-field value from the form body, meaning the sort never actually changed server-side (same class of bug the page's own code had already documented and avoided for `SelectedBatchId`, but not applied to the new Sort/Page fields). Fixed by removing the three hidden inputs entirely and carrying current sort/page state on the Select button's own `formaction` instead (no competing hidden field to conflict with); the sort-header and pagination buttons already build a complete self-contained `formaction` so are unaffected. Build: 0 errors. Tests: 171 total, 170 pass, 1 skipped. | **Done** | — | `.app-link-button` CSS rewritten to state colours directly instead of via `govuk-link`; hidden `SortColumn`/`SortDesc`/`PageNumber` fields removed from the carrier form; Select button's `formaction` now carries current sort/page state | [src/Histo.Web/wwwroot/css/Styles.css](../src/Histo.Web/wwwroot/css/Styles.css), [src/Histo.Web/Pages/Shared/_SortableHeaderPost.cshtml](../src/Histo.Web/Pages/Shared/_SortableHeaderPost.cshtml), [src/Histo.Web/Pages/Shared/_PaginationPost.cshtml](../src/Histo.Web/Pages/Shared/_PaginationPost.cshtml), [src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml](../src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml) |
| 23 | 2026-09-04 | 5 | `GitHub Copilot` | **Create submission auto-populate + grid sorting/pagination rollout (BatchesNotReceived/ViewSubmissions/BatchesForDispatch/QCNotes/BatchesReceived):** **(A)** `BatchDetails.cshtml.cs` Create mode — on first visit (not overriding a restored draft), "Submitted by" now defaults to `Session.UserID` and "Submitted area" to `Session.UserAreaID`; both remain plain editable `<select>` dropdowns, no `disabled` attribute added or present. **(B)** Grid sorting/pagination audit against the `UserMaintenance.cshtml` reference pattern: `BatchesNotReceived`, `BatchesForDispatch`, `BatchesReceived` already fully implemented `GridPageModel`/`PagedEntries`/`_SortableHeader`/`_Pagination` — verified, no changes needed. `QCNotes.cshtml(.cs)` converted to `GridPageModel` with a `PagedEntries` property and sortable headers, same pattern as `UserMaintenance`. `ViewSubmissions.cshtml(.cs)` is POST-driven (all search criteria bound via `[BindProperty]`, not the query string), so the standard GET-based `_SortableHeader`/`_Pagination` partials would have silently dropped every search filter on sort/page — created two new POST-safe partials, `_SortableHeaderPost.cshtml` and `_PaginationPost.cshtml`, which submit the page's existing hidden carrier form instead of navigating via a GET link; added `SortColumn`/`SortDesc`/`PageNumber` bind properties and a `PagedResults` property to `ViewSubmissionsModel`. Fixed a Razor `RZ1010` parse error along the way (an inline `@{ }` block placed directly after `</table>`) by moving that `ViewData` assignment into the model's `PopulateGridViewData()`. Build: 0 errors. Tests: 171 total, 170 pass, 1 skipped. | **Done** | — | `BatchDetails.cshtml.cs` Submitted by/area defaults; `QCNotes` converted to `GridPageModel`; `ViewSubmissions` POST-safe sort/page via two new shared partials | [src/Histo.Web/Pages/Batches/BatchDetails.cshtml.cs](../src/Histo.Web/Pages/Batches/BatchDetails.cshtml.cs), [src/Histo.Web/Pages/QC/QCNotes.cshtml.cs](../src/Histo.Web/Pages/QC/QCNotes.cshtml.cs), [src/Histo.Web/Pages/QC/QCNotes.cshtml](../src/Histo.Web/Pages/QC/QCNotes.cshtml), [src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml.cs](../src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml.cs), [src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml](../src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml), [src/Histo.Web/Pages/Shared/_SortableHeaderPost.cshtml](../src/Histo.Web/Pages/Shared/_SortableHeaderPost.cshtml), [src/Histo.Web/Pages/Shared/_PaginationPost.cshtml](../src/Histo.Web/Pages/Shared/_PaginationPost.cshtml) |
| 22 | 2026-09-04 | 5 | `GitHub Copilot` | **QualityData.cshtml sorting/pagination (ISS-R06 groundwork):** User asked for the sortable/paginated grid pattern already shipped on `UserMaintenance.cshtml` to be applied elsewhere, referencing the deferred ISS-R06 note ("Pagination not implemented — needed for large batches"). Converted `QualityDataModel` from `HistoPageModel` to `GridPageModel`; added a `PagedEntries` property sorting the already-filtered `Tests` list (Histology ref, Block ref, Test, Result, Dispatched, Archived, On hold) and paging it; `OnGetAsync` now calls `PopulateGridViewData(Tests.Count)`. View: all 7 column headers converted to `_SortableHeader`; body loop switched to `Model.PagedEntries`; `_Pagination` partial added below the table. `EditQualityDataTest.cshtml` (also named in ISS-R06) has no grid — confirmed not applicable. Build: 0 errors. Tests: 171 total, 170 pass, 1 skipped (up from 164). | **Done** | — | `QualityDataModel` converted to `GridPageModel` + sortable/paginated `Tests` grid | [src/Histo.Web/Pages/QC/QualityData.cshtml.cs](../src/Histo.Web/Pages/QC/QualityData.cshtml.cs), [src/Histo.Web/Pages/QC/QualityData.cshtml](../src/Histo.Web/Pages/QC/QualityData.cshtml) |
| 21 | 2026-09-02 | 5 | `GitHub Copilot` | **ISS-R28 resolved — Wet Tissue routing decision was based on an unverified hardcoded code, not legacy's actual comparison method:** User restated the Block Types (Pre Cassetted Tissue/Stained Section/Unstained Section/Wax Block → Sample Blocks) vs Tissue Type (Wet Tissue → Sample Details) navigation rule as a spec to verify. Confirmed the rule was already implemented correctly in `SampleSummary.cshtml.cs`/`AddSubmission.cshtml.cs`, but while cross-checking against the actual legacy source (`clsBatch.vb`, `Common.vb`, `Cassetted.aspx.vb` — read directly, not from docs) found `Cassetted.aspx.vb::btnYes_Click` never compares a numeric `SubmittedAs` code for Wet Tissue at all — it compares the resolved lookup **description text** (`chkblSubmittedAs.SelectedItem.Text.ToString() = "Wet Tissue"`). Unlike `IsBatchPreCassetted` (legitimately code-based, `"5"`, confirmed directly in `Common.vb`), the migrated app's `submittedAsCode == "4"` check for Wet Tissue was an unverified guess with no legacy or DB confirmation — exactly the gap ISS-R28 flagged. Fixed by adding `ValidationHelpers.IsWetTissueDescription(string?)` (pure, testable) and resolving the code to its `LOOKUP_SUBMITTEDAS` (table 11) description via the already-injected `ILookupService` before comparing, in both `SampleSummaryModel` (`OnGetAsync` + `OnPostSelect`) and `AddSubmissionModel` (newly injected `ILookupService`). This now matches legacy's actual behaviour regardless of what the real DB code value is. Added 7 unit test cases for the new helper. Build: 0 errors, 4 warnings (pre-existing). Tests: 164 total, 163 pass, 1 skipped (up from 145). | **Done** | — | `ValidationHelpers.IsWetTissueDescription` added; `SampleSummaryModel`/`AddSubmissionModel` resolve description via `ILookupService.GetLookupDataAsync(11)` instead of comparing a hardcoded code | [src/Histo.Core/Domain/ValidationHelpers.cs](../src/Histo.Core/Domain/ValidationHelpers.cs), [src/Histo.Web/Pages/Submissions/SampleSummary.cshtml.cs](../src/Histo.Web/Pages/Submissions/SampleSummary.cshtml.cs), [src/Histo.Web/Pages/Submissions/AddSubmission.cshtml.cs](../src/Histo.Web/Pages/Submissions/AddSubmission.cshtml.cs), [tests/Histo.Tests/Unit/ValidationHelperTests.cs](../tests/Histo.Tests/Unit/ValidationHelperTests.cs) |
| 19 | 2026-09-02 | 5 | `GitHub Copilot` | **Create / Edit / View Submission workflow review vs legacy + Pick List Management integration:** End-to-end review of the migrated submission journey (Home → Cassetted → BatchDetails create → SampleSummary → AddSubmission → SubmissionDetails / SubmissionDetailsBlock) against legacy `BatchDetails.aspx`, `BatchSummary.aspx`, `BatchBlockSummary.aspx`, `AddSample.aspx`, `AddSubmission.aspx`, `SearchSender.aspx`. Validated navigation paths, button enable/disable states, mandatory-field rules, submission-type conditional behaviour, and the sample/block/tissue workflows. **Implemented** the previously-absent Pick List Management integration: "Manage submitted by list", "Manage project / contract codes" and "Manage pathologists" buttons on both the Create Submission form (`BatchDetails?mode=create`) and the Edit Submission form (`EditBatch`), replacing legacy `btnNewSubmittedBy` / `btnNewProject` / `btnNewContact`. Each button posts the form, serialises all entered values to `TempData` and redirects to the correct maintenance page (`PickListUserArea/19` for Projects, `PickListUserArea/18` for Contacts, `UserMaintenance` for the user-backed Submitted By list) with a validated local `returnUrl`; on return the draft is restored so no entered data is lost — reproducing legacy `UpdateSessionWithBatchDetails()`-before-redirect. `ReturnUrl` passthrough added to `PickListUserArea`, `UserMaintenance`, `AddUser` and `EditUser`, and the hard-coded `Done → /Batches/BatchDetails` dead-end on `PickListUserArea` removed. All return URLs guarded with `Url.IsLocalUrl` (open-redirect prevention). | Partial — pick list integration delivered; 11 parity defects/gaps reported for decision, not fixed | **Defects:** (1) Copy sample inverted — legacy cleared Sender Ref and copied tissues/blocks, new page pre-fills Sender Ref and copies nothing; (2) submission-date rule reversed — legacy "must be today or later", new form uses `max=today` with no server check; (3) "At least one Block/Sample" Finish gate missing — batch persists as Submitted before any sample exists and "Finish" is only a link to Home; (4) `SubmissionDetailsBlock` has no `IsViewMode` gate — Save details / Add block / Add tissue / Delete all live in read-only View Submission mode; (5) Contacts and Projects no longer area-filtered (`GetLookupDataAsync(18/19)` replaced `GetContactsByArea`/`GetProjectsByArea`); (6) histology-ref validation lost (format `NN/NNNNN`, year, duplicate, next-available checks). **Missing Add sample functions:** duplicate sender-ref check, sender-ref format validation + TB Diagnostics override, mouse-number range bulk add, correlation file upload, Neuropath PG-number TSE Daybook lookup, pre-booked block validation for cassetted batches. **Lower severity:** `SafeToHandle` 2-state checkbox vs legacy mandatory 3-state dropdown; `SampleSameProjects` has no UI; no Cancel confirmation interruption; PM date unvalidated. **Verify:** `SampleSummary` routes Wet Tissue on `SubmittedAs == "4"` — could not confirm against `luSubmittedAs` seed data. | Pick List Management buttons added to Create + Edit Submission; `ReturnUrl` + draft save/restore across `BatchDetails`, `EditBatch`, `PickListUserArea`, `UserMaintenance`, `AddUser`, `EditUser`. Build 0 errors; `dotnet test` 145 passed / 0 failed. | [src/Histo.Web/Pages/Batches/BatchDetails.cshtml](../src/Histo.Web/Pages/Batches/BatchDetails.cshtml), [src/Histo.Web/Pages/Batches/BatchDetails.cshtml.cs](../src/Histo.Web/Pages/Batches/BatchDetails.cshtml.cs), [src/Histo.Web/Pages/Batches/EditBatch.cshtml](../src/Histo.Web/Pages/Batches/EditBatch.cshtml), [src/Histo.Web/Pages/Batches/EditBatch.cshtml.cs](../src/Histo.Web/Pages/Batches/EditBatch.cshtml.cs), [src/Histo.Web/Pages/Admin/PickListUserArea.cshtml](../src/Histo.Web/Pages/Admin/PickListUserArea.cshtml), [src/Histo.Web/Pages/Admin/PickListUserArea.cshtml.cs](../src/Histo.Web/Pages/Admin/PickListUserArea.cshtml.cs), [src/Histo.Web/Pages/Admin/UserMaintenance.cshtml](../src/Histo.Web/Pages/Admin/UserMaintenance.cshtml), [src/Histo.Web/Pages/Admin/UserMaintenance.cshtml.cs](../src/Histo.Web/Pages/Admin/UserMaintenance.cshtml.cs), [src/Histo.Web/Pages/Admin/AddUser.cshtml](../src/Histo.Web/Pages/Admin/AddUser.cshtml), [src/Histo.Web/Pages/Admin/AddUser.cshtml.cs](../src/Histo.Web/Pages/Admin/AddUser.cshtml.cs), [src/Histo.Web/Pages/Admin/EditUser.cshtml](../src/Histo.Web/Pages/Admin/EditUser.cshtml), [src/Histo.Web/Pages/Admin/EditUser.cshtml.cs](../src/Histo.Web/Pages/Admin/EditUser.cshtml.cs) |

| 25 | 2026-09-04 | 5 | `GitHub Copilot` | **Three-part submission-workflow fix (AddSubmission search flow, SearchSender picker UX, SampleSummary caption):** (1) `AddSubmission.cshtml` — Sender Ref search/select flow restored via the existing `SearchSender` picker, passing the selected value straight back into the form. (2) `SearchSender.cshtml` — picker mode required a second, blank search before results appeared; fixed with GET-binding so the picker auto-searches and shows results immediately on first load. (3) `SampleSummary.cshtml` — the submission-type caption (`SubmittedAsDescription`) used a 2-way ternary collapsing all 4 distinct submission types into one generic label; corrected to the right per-type text. Build: 0 errors. Tests: 171 total, 170 pass, 1 skipped. | **Done** | — | Sender Ref passthrough on `AddSubmission`; auto-search on `SearchSender` picker mode; per-type caption fix on `SampleSummary` | [src/Histo.Web/Pages/Submissions/AddSubmission.cshtml.cs](../src/Histo.Web/Pages/Submissions/AddSubmission.cshtml.cs), [src/Histo.Web/Pages/Submissions/SearchSender.cshtml.cs](../src/Histo.Web/Pages/Submissions/SearchSender.cshtml.cs), [src/Histo.Web/Pages/Submissions/SampleSummary.cshtml.cs](../src/Histo.Web/Pages/Submissions/SampleSummary.cshtml.cs) |
| 26 | 2026-09-04 | 5 | `GitHub Copilot` | **Access Denied regression — `HistoPageModel.CheckBatchAccessAsync` had silently reverted to Histo-user-only:** User reported an Access Denied error recurring for a scenario that previously worked. Re-read the current file state fresh (rather than assuming an earlier session fix was still present) and found `CheckBatchAccessAsync` computed `isAreaUnrestricted` from `Session.IsHistoUser` only — the `|| Session.IsMaintenance` condition added earlier in the session (Run #87/#88 era) was no longer present, so Maintenance-role dev-bypass test users (`DevAuthBypass` hardcodes `GroupName = "Maintenance"`) were denied access to batches outside their hardcoded dev-area. Restored `|| Session.IsMaintenance` to the unrestricted-access check passed into `BatchAccessDecision.IsAllowed`. Build: 0 errors. | **Done** | Likely also explains earlier intermittent "Sample not found" reports for Maintenance-role test users on batches outside their dev-bypass area | Restored `Session.IsMaintenance` to the area-unrestricted check | [src/Histo.Web/Pages/HistoPageModel.cs](../src/Histo.Web/Pages/HistoPageModel.cs) |
| 27 | 2026-09-04 | 5 | `GitHub Copilot` | **Block management page un-merge — restored legacy's `SubmissionDetailsBlock.aspx`/`BlockDetails.aspx` two-page split:** User reported the Run #88 consolidation (`Blocks/BlockDetails.cshtml` merged into `SubmissionDetailsBlock.cshtml`) made the page cluttered and difficult to use, and requested Add/Edit block be separated back into a dedicated page matching legacy exactly — grid columns Block Ref/Tissue Details/Archive/EO/H&E/H&E(BSE)+IHC Prp (TSE) or IHC Other (Non-TSE)/Special Stain/Select; actions Add Block/Edit Block/Delete Block/Copy to Samples/Block Ref Search/Back/Done. Re-read legacy `SubmissionDetailsBlock.aspx(.vb)`/`BlockDetails.aspx(.vb)` directly to confirm exact fields, columns and navigation. Trimmed `SubmissionDetailsBlock.cshtml(.cs)` back to a pure grid page — removed the inline Add/Edit-block form, tissue-add form and per-block test panels; added `ConfirmDeleteBlockIds` (multi-select delete), `IsTse`/`HistologyCodeByName` helpers driving the read-only test-indicator columns, and `OnPostEditBlockSelectAsync` (validates exactly one row selected before redirecting). Recreated `Blocks/BlockDetails.cshtml(.cs)` as the dedicated Add/Edit page (block ref or pre-booked ref, customer ref, number of blocks, repeat, tissues, Histology/Antibody/Special-stain test checkboxes), reached via "Add block"/"Edit block" and returning to the grid on Done. Added `Block.Archived` to back the grid's read-only Archive column. Build: 0 errors. Tests: 171 total, 170 pass, 1 skipped. | **Done** | ISS-R25 (histology-ref validation) remains open and was out of scope for this task | Split page back into grid + dedicated details page across 4 files | [src/Histo.Histology/Models/Block.cs](../src/Histo.Histology/Models/Block.cs), [src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml.cs](../src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml.cs), [src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml](../src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml), [src/Histo.Web/Pages/Blocks/BlockDetails.cshtml.cs](../src/Histo.Web/Pages/Blocks/BlockDetails.cshtml.cs), [src/Histo.Web/Pages/Blocks/BlockDetails.cshtml](../src/Histo.Web/Pages/Blocks/BlockDetails.cshtml) |

| 28 | 2026-09-04 | 5 | `GitHub Copilot` | **SubmissionDetailsBlock/BlockDetails legacy-parity investigation — 2 systemic "wrong/nonexistent stored procedure" bugs found and fixed, plus full BlockDetails.aspx feature parity:** User reported the "Or Pick" histology-ref-type picker looked unfamiliar, the blocks grid showed "No blocks found" despite data existing, and the Add Block screen (Block Ref dropdown, tissue workflow, missing fields) didn't match legacy. Verified against legacy source (`SubmissionDetailsBlock.aspx.vb`, `BlockDetails.aspx.vb`, `clsBatchSummary.vb`) and the real local DB (`sqlcmd` against LocalDB `histo_bacpack_03Aug2026`) rather than guessing. **Findings:** (1) "Or Pick" dropdown genuinely exists in legacy (`ddlHistologyType`, AutoPostBack) — current explicit "Get next ref" button is a correct, already-documented GDS redesign; no change needed. (2) `BlockRepository.GetByBatchAsync` called a nonexistent SP `GetBlocksByBatchID` (real: `GetBatchBlockDetails`, @ID=BatchID) — silently swallowed by `BlockService`'s catch-all, producing "No blocks found" for real data; fixed and verified live against the reported batch/animal. Also fixed `GetPreBookedByAnimalAsync`'s nonexistent `GetPreBookedBlocksByAnimalID` (real: `GetAnimalPreBookedBlocks`, @AnimalID). Also fixed the grid's Archive/IHC Prp/IHC Other/Special Stain columns, which read from the wrong test-type table (Antibodies/Stain instead of Histology — DB-confirmed all 7 indicator flags are `luHistology` codes 1–7 per `clsBatchSummary.vb`); removed the dead `Block.Archived` property (no such DB column exists). Tissue Details now resolves the tissue code to its description. (3) Discovered the SAME nonexistent-SP bug class independently affects tissue loading everywhere: `GetTissuesByBlockAsync`/`GetTissuesBySubmissionAsync` called invented, never-existing SPs (`GetTissuesByBlockID`/`GetTissuesBySubmissionID`) — one repo doc comment admitted this was guessed rather than verified. Fixed using the confirmed real `GetBatchBlockTissues`/`GetBatchTissues` (both @ID=BatchID, filtered client-side), changing both methods' signatures to require batchId; updated all 6 call sites (`BlockDetails`, `CopyBlocks`, `CopySamples`, `CopyBatch`, `SubmissionDetails`, `SubmissionDetailsBlock`). This was silently broken across `BlockDetails`, `SubmissionDetailsBlock`, `SubmissionDetails` (Wet Tissue), `CopyBlocks`, `CopySamples` and `CopyBatch`. **BlockDetails feature parity:** fixed Block Ref to always be free text (never a dropdown) with a legacy-matching default; added the missing "Comments and details of other tests" field; added Additional Request enable/disable by `luSubmittedAs` code (DB-confirmed 1/3/5 disabled, 2/4 enabled); added Histology test-selection validation (EO/Archive mutual exclusivity, Special Stain requires a stain, IHC requires an antibody, at least one Histology code required); added "Use these tests for the next block?" + Next Block workflow; added "Use the entire tissue list?" toggle (defaults to tissues already used on the submission). Build: 0 errors. Tests: 171 total, 170 pass, 1 skipped. | **Done** | — | Root-caused via direct legacy source reads + live `sqlcmd` verification against LocalDB, not guessed | [src/Histo.Histology/Repositories/BlockRepository.cs](../src/Histo.Histology/Repositories/BlockRepository.cs), [src/Histo.Histology/Interfaces/IBlockRepository.cs](../src/Histo.Histology/Interfaces/IBlockRepository.cs), [src/Histo.Histology/Models/Block.cs](../src/Histo.Histology/Models/Block.cs), [src/Histo.Submissions/Models/BatchTestSelections.cs](../src/Histo.Submissions/Models/BatchTestSelections.cs), [src/Histo.Submissions/Repositories/SubmissionRepository.cs](../src/Histo.Submissions/Repositories/SubmissionRepository.cs), [src/Histo.Submissions/Services/SubmissionService.cs](../src/Histo.Submissions/Services/SubmissionService.cs), [src/Histo.Submissions/Interfaces/ISubmissionRepository.cs](../src/Histo.Submissions/Interfaces/ISubmissionRepository.cs), [src/Histo.Submissions/Interfaces/ISubmissionService.cs](../src/Histo.Submissions/Interfaces/ISubmissionService.cs), [src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml.cs](../src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml.cs), [src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml](../src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml), [src/Histo.Web/Pages/Blocks/BlockDetails.cshtml.cs](../src/Histo.Web/Pages/Blocks/BlockDetails.cshtml.cs), [src/Histo.Web/Pages/Blocks/BlockDetails.cshtml](../src/Histo.Web/Pages/Blocks/BlockDetails.cshtml), [src/Histo.Web/Pages/Blocks/CopyBlocks.cshtml.cs](../src/Histo.Web/Pages/Blocks/CopyBlocks.cshtml.cs), [src/Histo.Web/Pages/Blocks/CopySamples.cshtml.cs](../src/Histo.Web/Pages/Blocks/CopySamples.cshtml.cs), [src/Histo.Web/Pages/Batches/CopyBatch.cshtml.cs](../src/Histo.Web/Pages/Batches/CopyBatch.cshtml.cs), [src/Histo.Web/Pages/Submissions/SubmissionDetails.cshtml.cs](../src/Histo.Web/Pages/Submissions/SubmissionDetails.cshtml.cs) |

| 29 | 2026-09-04 | 5 | `GitHub Copilot` | **SubmissionDetails.cshtml full CRUD parity + BatchSubmissionID resolution bug (multi-round investigation):** User requested PM Date/Tissue Details load/Edit/Delete/Add parity vs legacy. Fixed PM Date from plain text to a native `type="date"` input via a new shared `DateFormatHelpers` (dd/MM/yyyy ⇄ yyyy-MM-dd, matching `CalendarDate.ascx.vb::FormattedDate`); also applied to the identical `SubmissionDetailsBlock.cshtml.cs` PMDate field per user follow-up (reusing the same helper, not a new implementation). Built the previously entirely-unwired Edit tissue feature (`UpdateTissueAsync` existed at every layer but had zero callers). Root-caused "Add tissue not working" to `AddTissueAsync` passing a nonexistent `@UserID` parameter (confirmed via `clsTissue.vb`'s `AddInsertParam` list — Add takes only `{keyField, TissueCode, NoPieces, Comment}`); fixed. **Multi-round "tissue still not loading" investigation** (user supplied live network-log evidence each round): round 1 — assumed `GetBlockAnimalsByBatchAsync`'s missing `BatchSubmissionID` could be patched from `GetAnimalsByBatchAsync`, but round 2 (DB-verified via `sqlcmd`, user approved the query after asking "will it be useful") proved `GetBatchAnimal` (backing `GetAnimalsByBatchAsync`) *also* never returns `BatchSubmissionID` — animal 53042 additionally has `BatchSubmission` rows under two different batches (30407 and 9804), so a global lookup would be ambiguous too. Correct fix: resolve via the already-existing, already-fixed `GetSubmissionsByBatchAsync` (batch-scoped `AnimalID`→`ID` mapping); added the missing `AnimalID` property to the `BatchSubmission` model (was silently discarding a real SP column). Verified live: batch 30407/animal 53042 → `BatchSubmissionID` 65912, 1 real tissue row. **Final round**: fixed Edit/Delete button style inconsistency (Edit now a button matching Delete); fixed Add-tissue form being silently pre-filled with Edit values (shared bind properties — split into dedicated `EditTissueCode`/`EditNoPieces`/`EditComment`); root-caused "Save tissue not working" to `UpdateTissueAsync` missing the `@BatchSubmissionID`/`@BlockID` keyfield parameter that `EditTissue`/`EditBlockTissue` require (same `AddUpdateParam` list omission class as the Add-side bug). Mid-session the user disabled the terminal/edit tools to raise a token-consumption concern; re-enabled after an explanation, and a repo-memory note was added instructing future sessions to check memory before re-deriving already-documented bug patterns. Build: 0 errors. Tests: 171 total, 170 pass, 1 skipped throughout. | **Done** | — | DB-verified via live `sqlcmd` at each round rather than guessing; user-driven multi-round debugging with network-log evidence | [src/Histo.Core/Domain/DateFormatHelpers.cs](../src/Histo.Core/Domain/DateFormatHelpers.cs), [src/Histo.Submissions/Models/BatchSubmission.cs](../src/Histo.Submissions/Models/BatchSubmission.cs), [src/Histo.Submissions/Repositories/SubmissionRepository.cs](../src/Histo.Submissions/Repositories/SubmissionRepository.cs), [src/Histo.Web/Pages/Submissions/SubmissionDetails.cshtml.cs](../src/Histo.Web/Pages/Submissions/SubmissionDetails.cshtml.cs), [src/Histo.Web/Pages/Submissions/SubmissionDetails.cshtml](../src/Histo.Web/Pages/Submissions/SubmissionDetails.cshtml), [src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml.cs](../src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml.cs), [src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml](../src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml) |

| 30 | 2026-09-07 | 5 | `GitHub Copilot` | **Edit Submission field accessibility + Submissions On Hold filtering + Edit Submission vs Edit Submission Status review:** User requested 3 fixes vs legacy. (1) `EditBatch.cshtml` was missing Entered By/Entered Area/Submitted As entirely (not just editable — absent) and had "Submitted area (external)" editable when legacy disables it on an existing submission; added the 3 read-only fields (resolved via already-loaded `AllUsers`/`UserAreas` + `GetSubmittedAsCodeAsync`+lookup 11) and made Submitted area read-only (disabled select + hidden input to preserve the posted value), leaving Submitted by (external) editable per spec. (2) Root-caused `SubmissionsOnHold.cshtml` via legacy `SubmissionsOnHold.aspx.vb`: it shows every sample of the CURRENT batch with a per-sample On Hold checkbox toggle (from `EditBatch.aspx`'s "Put Samples On Hold" button) — not a system-wide report of every on-hold batch, which is what the migrated page had reinvented (`IBatchService.GetOnHoldAsync()`). Rewrote `SubmissionsOnHoldModel`/`.cshtml` to load the current batch's animals (reusing `SampleSummary`'s `MergeAnimals` block/plain-animal union pattern, patching `OnHold` from the plain source since `GetBlockAnimalsByBatchAsync` doesn't reliably carry it either) with explicit checkboxes + Save, and a Done button back to Edit Submission Status. (3) Confirmed via `EditBatch.aspx.vb` that legacy's `EditBatch.aspx` genuinely IS "Edit Submission Status" (thin: read-only summary + Status + Reason + an "Edit Submission" button that redirects to `BatchDetails.aspx`, which is legacy's actual full-field edit screen reused in an edit context — there is no separate dedicated edit-fields page in legacy either). Assessed that literally splitting the current merged `EditBatch.cshtml` into two pages would require building a full new edit mode into `BatchDetails.cshtml` and updating ~6 navigation call sites — out of scope for this pass; per the user's own fallback option, added clear GDS section separation ("Submission details" / "Submission status" headings + section break) on the single page instead, and flagged the full two-page split as a follow-up if wanted. Build: 0 errors. Tests: 171 total, 170 pass, 1 skipped. | **Done** | Full BatchDetails-based two-page split (matching legacy's actual `BatchDetails.aspx` reuse for editing) not implemented this pass — flagged for a future task if wanted | Read-only field additions on `EditBatch`; `SubmissionsOnHold` rewritten from a global batch report to a per-batch per-sample toggle page | [src/Histo.Web/Pages/Batches/EditBatch.cshtml.cs](../src/Histo.Web/Pages/Batches/EditBatch.cshtml.cs), [src/Histo.Web/Pages/Batches/EditBatch.cshtml](../src/Histo.Web/Pages/Batches/EditBatch.cshtml), [src/Histo.Web/Pages/Batches/SubmissionsOnHold.cshtml.cs](../src/Histo.Web/Pages/Batches/SubmissionsOnHold.cshtml.cs), [src/Histo.Web/Pages/Batches/SubmissionsOnHold.cshtml](../src/Histo.Web/Pages/Batches/SubmissionsOnHold.cshtml) |
| 31 | 2026-09-07 | 5 | `GitHub Copilot` | **GDS pagination ellipsis truncation + out-of-range page clamping (all grids):** User reported that on larger screens the pagination component showed every page number instead of GDS-style ellipsis truncation, plus "other pagination issue and fix it". Fetched the official GOV.UK Design System pagination documentation to confirm the exact algorithm and worked examples (`1 [2] 3 … 100`, `1 … 4 [5] 6 … 100`, `1 … 99 [100]`, etc.) rather than guessing. Both `_Pagination.cshtml` (GET-based) and `_PaginationPost.cshtml` (POST-based, used by `ViewSubmissions` and other `[BindProperty]`-bound grids) previously rendered every page number with a naive `@for` loop. Created `PaginationHelpers.BuildPageItems` (`Histo.Core.Domain`) implementing the GDS rule (current page, first, last, ±1 neighbour, ellipses for gaps of 2+ skipped pages, showing the page directly instead of an ellipsis for a gap of exactly 1) and rewrote both partials to consume it. **Other pagination issue** (proactively identified in the same area): `GridPageModel.PopulateGridViewData` didn't clamp an out-of-range `PageNumber` (stale link, filtered-down results), silently rendering an empty grid — fixed by clamping to `[1, totalPages]`. Added `PaginationHelperTests.cs` covering the GDS worked examples plus an out-of-range-current-page case, which caught and fixed a bug in the helper itself (neighbour calculation used the raw unclamped `currentPage` instead of the clamped value). Build: 0 errors. Tests: 183 total, 182 pass, 1 skipped (up from 171/170). | **Done** | — | `PaginationHelpers.cs` created; both pagination partials rewritten; `GridPageModel` page-number clamp | [src/Histo.Core/Domain/PaginationHelpers.cs](../src/Histo.Core/Domain/PaginationHelpers.cs), [src/Histo.Web/Pages/Shared/_Pagination.cshtml](../src/Histo.Web/Pages/Shared/_Pagination.cshtml), [src/Histo.Web/Pages/Shared/_PaginationPost.cshtml](../src/Histo.Web/Pages/Shared/_PaginationPost.cshtml), [src/Histo.Web/Pages/GridPageModel.cs](../src/Histo.Web/Pages/GridPageModel.cs), [tests/Histo.Tests/Unit/PaginationHelperTests.cs](../tests/Histo.Tests/Unit/PaginationHelperTests.cs) |
| 32 | 2026-09-07 | 5 | `GitHub Copilot` | **Wet Tissue Create Submission journey — Tissue Details root cause (BatchSubmission↔Animal linkage never created):** User asked to validate whether the already-fixed Edit-journey Tissue Details bugs (data not loading, Add/Edit/Delete not working) also existed in the Create Submission journey (`SubmissionDetails.cshtml`/Add Sample workflow). Confirmed the Razor markup itself is identical/shared between both journeys (no gap there). Root-caused via legacy `clsBatchSubmission.vb::NewRecord(dtBatchSubmission, iBatchSubmissionID, iBatchID, iAnimalID)`: the real `AddBatchSubmission` SP genuinely accepts a per-animal `AnimalID`, confirming each Wet Tissue sample needs its own dedicated `BatchSubmission` row linked via `AnimalID`. `AddSubmissionModel.OnPostAsync` instead reused the first existing submission (or a hardcoded `AnimalID=0` placeholder) for every new animal and never updated its `AnimalID`; `SubmissionRepository.AddSubmissionAsync` also hardcoded `AnimalID=0` regardless of the model's real value. Since `SubmissionDetailsModel.LoadAnimalAsync` (Run #29's Edit-journey fix) resolves `Animal.BatchSubmissionID` by matching `submissions.FirstOrDefault(s => s.AnimalID == AnimalId)`, a freshly-created animal's owning submission never matched, leaving `BatchSubmissionID` at 0 and silently breaking Tissue Details load/Add/Edit/Delete for every new Wet Tissue sample — pre-existing seeded/production animals worked fine since their submissions already had real `AnimalID` values. Fixed: `AddSubmissionAsync` now sends `submission.AnimalID` instead of hardcoded `0`; `AddSubmissionModel.OnPostAsync` creates a dedicated new submission (`AnimalID = newAnimalId`) after each new Wet Tissue animal is added. Cassetted/block batches unaffected (Tissue there is Block-owned, not Submission-owned) — left unchanged. Build: 0 errors. Tests: 183 total, 182 pass, 1 skipped (unchanged). | **Done** | Same class of bug likely affects Copy Batch/Copy Samples' Wet Tissue flow (`CopyAnimalAsync`) — not yet fixed, flagged as follow-up | `AddSubmissionAsync` AnimalID param fix; `AddSubmissionModel.OnPostAsync` dedicated-submission-per-animal fix | [src/Histo.Submissions/Repositories/SubmissionRepository.cs](../src/Histo.Submissions/Repositories/SubmissionRepository.cs), [src/Histo.Web/Pages/Submissions/AddSubmission.cshtml.cs](../src/Histo.Web/Pages/Submissions/AddSubmission.cshtml.cs) |
| 33 | 2026-09-07 | 5 | `GitHub Copilot` | **CopyBatch Wet Tissue flow — same BatchSubmission↔Animal linkage bug fixed:** Follow-up to Run #32's flagged item. `CopyBatchModel.OnPostAsync` created the destination submission via `CopySubmissionAsync` BEFORE copying its animal via `CopyAnimalAsync` — chicken-and-egg, so the new submission's `AnimalID` could never be set to the newly copied animal, reproducing the exact same silent Tissue Details breakage on copied Wet Tissue batches. Added an optional `animalId` parameter to `ISubmissionService.CopySubmissionAsync`/`SubmissionService.CopySubmissionAsync` (threaded into the `BatchSubmission.AnimalID` field, defaults to 0 for backward compat) and restructured `OnPostAsync` to copy the animal FIRST (its `newBatchSubmissionId` parameter is inert at the SQL layer, confirmed via the AddSubmission fix, so a placeholder is safe), then create the destination submission linked to the real new animal ID, then copy tissues into it. Applies uniformly to both cassetted and non-cassetted copy paths — harmless for cassetted (Tissue there is Block-owned, unaffected). Build: 0 errors. Tests: 183 total, 182 pass, 1 skipped (unchanged). | **Done** | — | `ISubmissionService`/`SubmissionService.CopySubmissionAsync` animalId param; `CopyBatchModel.OnPostAsync` copy-order restructure | [src/Histo.Submissions/Interfaces/ISubmissionService.cs](../src/Histo.Submissions/Interfaces/ISubmissionService.cs), [src/Histo.Submissions/Services/SubmissionService.cs](../src/Histo.Submissions/Services/SubmissionService.cs), [src/Histo.Web/Pages/Batches/CopyBatch.cshtml.cs](../src/Histo.Web/Pages/Batches/CopyBatch.cshtml.cs) |
| 34 | 2026-09-07 | 5 | `GitHub Copilot` | **ViewSubmissions/Edit Submission 4-issue fix (sorting, category lock, dropdown values, alignment):** **(1)** `ViewSubmissions.cshtml`'s "Customer Received Date" column was a plain `<th>`, not wrapped in `_SortableHeaderPost` — confirmed sortable in legacy (`ViewSubmissions.aspx`'s `<asp:BoundColumn DataField="CustomerReceivedDate" SortExpression="CustomerReceivedDate">`); added the sortable header partial and a `CustomerReceivedDate` case to `PagedResults`' sort switch (already `DateTime?`-typed, so sorts as a date not text). **(2)** `EditBatch.cshtml`'s Submission category (TSE/Non-TSE) radios were fully editable — legacy's `Cassetted.aspx` type-selection step is a one-time Create-journey step never re-shown on Edit, so category can never change post-creation; changed the radios to `disabled` (visible, hidden input preserves the value, matching the established `OtherSubmittedAreaDisplay` pattern) and hardened `EditBatchModel.OnPostAsync` to always persist the original loaded `Batch.BatchType` rather than the posted value (defence in depth). **(3)** Root-caused Project/Contract Code and Pathologist dropdowns showing wrong values on Edit: `EditBatchModel.LoadLookupsAsync` called `GetLookupDataAsync` with the default `includeInactive: false`, so a submission's saved-but-since-deactivated project/pathologist silently had no matching `<option>`, and the browser defaulted to selecting the first item — added `includeInactive: true` (matching the established `EditLookupItem.cshtml.cs` pattern) for both. **(4)** Wrapped the Project/Contract Code, Pathologist, and Submitted-by-(external) `<select>` + "Manage …" button pairs in a new `.app-field-with-button` flex container (`Styles.css`) so they align on one row and wrap gracefully on narrow screens. Build: 0 errors. Tests: 183 total, 182 pass, 1 skipped (unchanged). | **Done** | — | `ViewSubmissions.cshtml(.cs)` sortable column; `EditBatch.cshtml(.cs)` disabled category radios + backend hardening + `includeInactive: true` lookups + button alignment; `Styles.css` new utility class | [src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml](../src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml), [src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml.cs](../src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml.cs), [src/Histo.Web/Pages/Batches/EditBatch.cshtml](../src/Histo.Web/Pages/Batches/EditBatch.cshtml), [src/Histo.Web/Pages/Batches/EditBatch.cshtml.cs](../src/Histo.Web/Pages/Batches/EditBatch.cshtml.cs), [src/Histo.Web/wwwroot/css/Styles.css](../src/Histo.Web/wwwroot/css/Styles.css) |
| 35 | 2026-09-07 | 5 | `GitHub Copilot` | **SubmissionDetailsBlock/BlockDetails full CRUD review — DBNull dynamic-cast crash root-caused, PM Date/Histology Ref made read-only, Edit tissue added:** **(1)** `SubmissionDetailsBlock.cshtml`'s PM date/Histology reference converted from editable inputs to read-only `govuk-summary-list` rows (matching Sender ref), per explicit user UX decision (legacy's own controls ARE editable there — confirmed via `DisableEnableControls`, so this is a deliberate deviation, not a legacy-parity fix). **(2)** Root-caused the empty Archive/EO/H&E/H&E(BSE)/IHC-Prp/Special Stain grid columns: `BlockTestRepository.Map`'s `row.QCNote is bool b ? b : row.QCNote is not null && (int)row.QCNote != 0` looked null-safe but wasn't — Dapper's dynamic rows return `DBNull.Value` (a real, non-null object) for NULL columns, so `is not null` was `true` and `(int)row.QCNote` threw whenever QCNote/Dispatched was NULL (very common for not-yet-QC'd/dispatched tests), silently wiping every indicator column for the WHOLE batch via `BlockTestService`'s catch-all. Rewrote `Map` to read every nullable column via `IDictionary<string, object>` + explicit DBNull checks, matching the established codebase-wide convention. **(3)** "Save details" removed as redundant now that PM Date/Histology Ref are read-only; "Or pick.../Get next ref" (confirmed genuinely exists in legacy) is now the sole way to change Histology Ref and persists immediately via `UpdateAnimalAsync` instead of requiring a separate manual save. **(4)** Confirmed "Or pick"/"Get next ref" and `BlockDetails`' "Number of blocks" field are both legacy-confirmed and already correctly implemented — not removed/added. **(5)** Added Edit tissue functionality to `BlockDetails.cshtml`'s Tissue table (previously Delete-only), using the established inline-edit-row + separately-named bind-property pattern from `SubmissionDetails.cshtml`; added a read-only Histology reference row matching Sender ref. Build: 0 errors. Tests: 183 total, 182 pass, 1 skipped (unchanged). | **Done** | — | `SubmissionDetailsBlock.cshtml(.cs)` read-only fields + Save-details removal + Get-next-ref persistence; `BlockTestRepository.cs` DBNull-safe rewrite; `BlockDetails.cshtml(.cs)` Histology ref display + Edit tissue | [src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml](../src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml), [src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml.cs](../src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml.cs), [src/Histo.Histology/Repositories/BlockTestRepository.cs](../src/Histo.Histology/Repositories/BlockTestRepository.cs), [src/Histo.Web/Pages/Blocks/BlockDetails.cshtml](../src/Histo.Web/Pages/Blocks/BlockDetails.cshtml), [src/Histo.Web/Pages/Blocks/BlockDetails.cshtml.cs](../src/Histo.Web/Pages/Blocks/BlockDetails.cshtml.cs) |
| 36 | 2026-09-07 | 5 | `GitHub Copilot` | **Context-sensitive Help navigation:** User reported that clicking "Help" from within a specific module (e.g. User Management) always opened `Help/Index.cshtml` at the top instead of the relevant section. Confirmed `Help/Index.cshtml` already had a `<section id="kebab-case">` anchor per topic, each with a secondary legacy-page-name `id` on its `<h2>` (e.g. `id="UserMaintenance"`) — a ready-made map, but cross-referenced against `docs/Functionality-Traceability-Matrix.md` since several legacy pages were renamed/consolidated during migration (e.g. `BatchSummary`+`BatchBlockSummary` → `SampleSummary`) so current page name ≠ legacy id in every case. Added `Histo.Core.Domain.HelpSectionMap` (pure, unit-tested static dictionary keyed by current Razor Page path → Help anchor). `_NavPartial.cshtml`'s single global "Help" link now resolves `ViewContext.RouteData.Values["page"]` through the map and appends `#anchor` when a mapping exists — a plain `<a href>` (no JS routing), so browser back/forward and direct access to `Help/Index` both keep working unchanged. Added a small `@section Scripts` snippet to `Help/Index.cshtml` that sets `tabindex="-1"` and calls `.focus()` on the URL-fragment target on load and on every `hashchange` (also benefits the page's own contents/"Back to top" links) — needed because scrolling to an anchor does not itself move keyboard/screen-reader focus, addressing the "correct section highlighted or focused" requirement. Build: 0 errors. Tests: 191 total, 190 pass, 1 skipped (up from 183/182, 8 new tests). | **Done** | Not every current page has a mapped Help section (e.g. pages with no direct legacy-era equivalent) — Help link falls back to opening at the top for those, which is the pre-existing behaviour, not a regression | `HelpSectionMap.cs` created; `_NavPartial.cshtml` context-sensitive Help href; `Help/Index.cshtml` focus-management script | [src/Histo.Core/Domain/HelpSectionMap.cs](../src/Histo.Core/Domain/HelpSectionMap.cs), [src/Histo.Web/Pages/Shared/_NavPartial.cshtml](../src/Histo.Web/Pages/Shared/_NavPartial.cshtml), [src/Histo.Web/Pages/Help/Index.cshtml](../src/Histo.Web/Pages/Help/Index.cshtml), [tests/Histo.Tests/Unit/HelpSectionMapTests.cs](../tests/Histo.Tests/Unit/HelpSectionMapTests.cs) |
| 37 | 2026-09-07 | 5 | `GitHub Copilot` | **PR review comment fix — unencoded SortColumn in ViewSubmissions Select button formaction:** Reviewer flagged that the row Select button's `formaction` embedded the user-controllable, POST-bound `Model.SortColumn` via raw string interpolation, allowing query-parameter injection if the value contained `&`. Fixed by moving the formaction to a new `SelectFormAction` property on `ViewSubmissionsModel`, built with `Uri.EscapeDataString` on `SortColumn` (matching the existing `_SortableHeaderPost.cshtml`/`_PaginationPost.cshtml` pattern). While fixing, hit and resolved two unrelated pre-existing regressions blocking the build (from prior uncommitted/external edits): (1) an inline `@{ }` block placed directly after `</form>` inside a nested `@if {}` in `ViewSubmissions.cshtml` caused Razor error RZ1010 — resolved by moving the computation into the PageModel property instead of inline Razor code (same fix pattern as Run #23); (2) `AddSubmissionModel.OnPostAsync` referenced a since-removed `IsNeuropath` bind property (`CS0103`) — the field/checkbox had been removed from `AddSubmission.cshtml` without updating the code-behind; fixed by deriving `isNeuropath` from `Session.UserArea == "Neuropath"`, matching legacy `AddSubmission.aspx.vb`'s actual logic (session user-area check, never a manual form control). Build: 0 errors. Tests: 191 total, 190 pass, 1 skipped (unchanged). | **Done** | — | `ViewSubmissions.cshtml(.cs)` SelectFormAction property + RZ1010 fix; `AddSubmission.cshtml.cs` IsNeuropath regression fix | [src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml](../src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml), [src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml.cs](../src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml.cs), [src/Histo.Web/Pages/Submissions/AddSubmission.cshtml.cs](../src/Histo.Web/Pages/Submissions/AddSubmission.cshtml.cs) |
| 38 | 2026-09-07 | 5 | `GitHub Copilot` | **"Assign Tissues to Blocks" journey — full legacy comparison + 3 gaps fixed:** Documented the legacy chain `BatchesReceived.aspx` (select) → `BatchBlocks.aspx` (batch-wide Add/Edit/Delete Sample, Copy From Prev. Submission, Done) → `FinalPrintBatch.aspx`, and confirmed the current equivalent chain is `BatchesReceived.cshtml` → `/Batches/BatchDetails` (deliberate hub-page consolidation, one extra click vs legacy) → "Assign blocks" → `/Submissions/SubmissionDetailsBlock` in batch-wide mode — verified via legacy's own `VLAHeader1.PageTitle = "Submission Block Summary"` matching the current Help section id `BatchBlocks`. Found and fixed 3 real gaps in the batch-wide grid: (1) missing Histology Ref/Tissue Details/7 test-indicator columns — added, reusing the existing `LoadSupportingDataAsync` (previously only invoked for the per-animal branch); (2) no "Add sample" button at this level (legacy's `btnAddSample`) — added directly, previously required an extra hop via `SampleSummary`; (3) no "Done" action at all — legacy's `btSubmit_Click` sets `IsBlocked=True`, transitions status to In progress, and records `AllTissuesAssigned`, none of which existed anywhere in the current app (confirmed via grep that these fields were only ever read/passed-through, never set) — added `IBatchService.CompleteBlockAssignmentAsync` (+ repository method) and a "Done" button/handler computing `AllTissuesAssigned` as "every animal has at least one block" (a documented simplification of legacy's per-tissue-piece "green star" indicator). `FinalPrintBatch.aspx` is confirmed genuinely unmigrated (Reporting phase, `docs/Migration-Plan.md`), so "Done" redirects to `/Batches/BatchesReceived` instead, matching legacy's own eventual post-print destination. Documented as an accepted, unchanged deviation: legacy's entire journey operates on an in-memory session DataSet with zero DB writes until Done (Cancel discards everything) — the current app's immediate-write-per-action architecture makes this impossible to replicate exactly, consistent with every other page migrated this session. Build: 0 errors. Tests: 191 total, 190 pass, 1 skipped (unchanged). | **Done** | `FinalPrintBatch.aspx` PDF report page remains unmigrated (Reporting phase, explicitly out of scope); `AllTissuesAssigned` heuristic is a simplification, not an exact legacy reproduction | `IBatchService`/`IBatchRepository`/`BatchService`/`BatchRepository` `CompleteBlockAssignmentAsync`; `SubmissionDetailsBlock.cshtml(.cs)` batch-wide grid columns + Add sample + Done | [src/Histo.Submissions/Interfaces/IBatchService.cs](../src/Histo.Submissions/Interfaces/IBatchService.cs), [src/Histo.Submissions/Interfaces/IBatchRepository.cs](../src/Histo.Submissions/Interfaces/IBatchRepository.cs), [src/Histo.Submissions/Services/BatchService.cs](../src/Histo.Submissions/Services/BatchService.cs), [src/Histo.Submissions/Repositories/BatchRepository.cs](../src/Histo.Submissions/Repositories/BatchRepository.cs), [src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml](../src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml), [src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml.cs](../src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml.cs) |
| 39 | 2026-09-07 | 5 | `GitHub Copilot` | **`Batches/BatchBlocks` page split — true legacy parity for the "Assign Tissues to Blocks" entry point:** After a design discussion on cross-journey button ambiguity, split the batch-wide branch out of `SubmissionDetailsBlock.cshtml`/`.cs` (which now ALWAYS requires `AnimalId`, redirecting to the new page otherwise) into a genuinely separate `Pages/Batches/BatchBlocks.cshtml(.cs)`, mirroring legacy's real page boundary (`BatchBlockSummary.aspx`→`SampleSummary.cshtml` vs `BatchBlocks.aspx`→new page, previously conflated). Repointed `BatchDetails.cshtml`'s "Assign blocks" button and all `CopyBlocks`/`CopySamples`/`CopySamplesSummary` cancel/done redirects from the removed batch-wide `SubmissionDetailsBlock` mode to `/Batches/BatchBlocks`. Renamed "Copy samples" → "Copy from a previous submission" to disambiguate from `SampleSummary`'s "Copy sample" (confirmed via legacy `SV_CopySample` grep that it's never actually read anywhere in legacy — that button never copied a tissue list, only pre-filled context, so the current app's "Copy sample" behaviour is a faithful reproduction, not a regression). Build: 0 errors. Tests: 190 passed, 1 skipped, 0 failed. | **Done** | — | New `Batches/BatchBlocks.cshtml(.cs)`; trimmed `Submissions/SubmissionDetailsBlock.cshtml(.cs)`; repointed 5 files | [src/Histo.Web/Pages/Batches/BatchBlocks.cshtml](../src/Histo.Web/Pages/Batches/BatchBlocks.cshtml), [src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml.cs](../src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml.cs) |
| 40 | 2026-09-07 | 5 | `GitHub Copilot` | **BatchesReceived grid columns + BatchBlocks action-button legacy parity:** (1) Trimmed `BatchesReceived.cshtml`'s grid to legacy's exact 6 columns (Submission number/Project code/Pathologist/Species/Submitted date/All tissues assigned), removing Received date/Customer ref which legacy's `grdBatches` never had. (2) Rebuilt `BatchBlocks.cshtml(.cs)`'s action row to match legacy's single shared button group exactly — Add sample/Edit sample/Delete sample/Copy from a previous submission/Cancel/Done — replacing the previous per-row "Manage sample"/"Delete" links; added `OnPostEditSampleSelectAsync`/`OnPostDeleteSampleSelectAsync`/`OnPostDeleteSampleAsync` (checkbox-selection based, requires exactly one row, mirrors the established `OnPostEditBlockSelectAsync` pattern), with "Delete sample" now calling `ISubmissionService.DeleteAnimalAsync` (whole sample) instead of a single-block delete, matching legacy's `btnDeleteSample_Click`/`Animal.RemoveSubmission`. Kept "Copy selected blocks" as an extra beyond legacy's 6 buttons since it filled a real, already-built gap. Cancel/back-link retargeted to `/Batches/BatchDetails` (the page's real entry point) instead of the incorrect carried-over `/Submissions/SampleSummary`. Build: 0 errors. Tests: 190 passed, 1 skipped, 0 failed. | **Done** | — | Grid column trim; button-group rebuild | [src/Histo.Web/Pages/Batches/BatchesReceived.cshtml](../src/Histo.Web/Pages/Batches/BatchesReceived.cshtml), [src/Histo.Web/Pages/Batches/BatchBlocks.cshtml](../src/Histo.Web/Pages/Batches/BatchBlocks.cshtml), [src/Histo.Web/Pages/Batches/BatchBlocks.cshtml.cs](../src/Histo.Web/Pages/Batches/BatchBlocks.cshtml.cs) |
| 41 | 2026-09-07 | 5 | `GitHub Copilot` | **BatchesReceived wrong stored procedure + BatchBlocks CRUD review (4 real bugs found and fixed):** Grepped the legacy `.aspx.vb` call site directly (rather than trusting the repository's own doc comment) and confirmed `BatchesReceived.aspx.vb::InitialiseBatchesGrid` calls SP `GetBatchesToBeBlocked` — NOT `GetReceivedBatches`, which exists in `clsBatch.vb` but is never called by any legacy page. Fixed `BatchRepository.GetReceivedAsync` accordingly (expected to also resolve the reported Species-showing-as-ID bug, same root cause as the earlier `BatchDetails` Species fix — not verified live, user declined both a DB query and a browser check). Fixed the default sort (legacy: `"ID DESC"` with no column clicked) and `OnPostGoAsync`/`OnPostSelect` to validate via `GetByIdAsync` + status check and redirect to `/Batches/BatchBlocks` (legacy's `btnGo_Click`/`grdBatches_SelectedIndexChanged`, both via `GetBatchWithStatus`), matching the `GoError` pattern already used on `BatchesNotReceived`. BatchBlocks CRUD review found 2 more real bugs: `LoadGridAsync`/`OnPostDoneAsync` only called `GetAnimalsByBatchAsync` (incomplete for cassetted batches — same known gotcha already fixed in `SampleSummaryModel`), fixed via a new `GetAllAnimalsAsync()` merge helper; and the main grid `<form method="post">` (plus `SubmissionDetailsBlock`'s sibling per-animal grid form, plus `BatchesReceived`'s row "View" form) had no `asp-*` attribute, so Razor's `FormTagHelper` never renders an antiforgery token — added `asp-page` to all three. Flagged (not fixed) that the identical bare-form pattern recurs in 32 files across the app — could not verify live whether it's actually broken in practice. Build: 0 errors. Tests: 190 passed, 1 skipped, 0 failed. | **Done** | Antiforgery pattern recurs in 32 files — flagged for a follow-up investigation, not mass-fixed | SP name fix, sort/redirect fix, animal-merge fix, 3 antiforgery form fixes | [src/Histo.Submissions/Repositories/BatchRepository.cs](../src/Histo.Submissions/Repositories/BatchRepository.cs), [src/Histo.Web/Pages/Batches/BatchesReceived.cshtml.cs](../src/Histo.Web/Pages/Batches/BatchesReceived.cshtml.cs), [src/Histo.Web/Pages/Batches/BatchBlocks.cshtml.cs](../src/Histo.Web/Pages/Batches/BatchBlocks.cshtml.cs) |
| 42 | 2026-09-07 | 5 | `GitHub Copilot` | **BlockDetails Edit block — Tissue type dropdown empty, root-caused and fixed:** `BlockDetailsModel.LoadAnimalAsync` resolves `Animal` via `GetBlockAnimalsByBatchAsync` first (for accurate Sender/Histology ref) — but that SP's result set (`BATCH_BLOCK_ANIMAL`) doesn't return a `BatchSubmissionID` column, so it stays 0. In Edit block mode (block already exists, so this path is almost always hit) `LoadSupportingDataAsync`'s default tissue-dropdown filter calls `GetTissuesBySubmissionAsync(batchId, 0)`, matching zero rows — the same known gotcha already documented and worked around in `SampleSummaryModel`, just not yet applied here. Fixed by patching `Animal.BatchSubmissionID` from the plain `GetAnimalsByBatchAsync` list whenever the block-animal source left it at 0. Build: 0 errors. Tests: 190 passed, 1 skipped, 0 failed. | **Done** | — | `Animal.BatchSubmissionID` patched from the plain animal list | [src/Histo.Web/Pages/Blocks/BlockDetails.cshtml.cs](../src/Histo.Web/Pages/Blocks/BlockDetails.cshtml.cs) |
| 43 | 2026-09-07 | 5 | `GitHub Copilot` | **Root-cause correction of Run #42 (invisible read-only indicator checkboxes) + BlockDetails "Number of blocks" gap:** Run #42's `BatchSubmissionID` patch was itself wrong — verified via `BlockDetails.aspx.vb::LoadLookupTypeList` that the tissue-dropdown filter is really keyed on BatchID+AnimalID (SP `GetBatchSampleTissues`), not any submission ID; fixed by cross-referencing this animal's own block IDs against `GetTissuesByBatchAsync` instead, and removed the now-unneeded patch. Then re-investigated "Archive/EO/H&E/IHC Other/Special Stain not populated" on `SubmissionDetailsBlock.cshtml`: a live DB check (user-approved sqlcmd) plus a throwaway xUnit test calling `BlockTestRepository`/`BlockRepository`/`LookupRepository` directly against LocalDB (bypassing the web app/Entra ID auth entirely) proved the data pipeline was 100% correct end to end. Real root cause: `govuk-frontend.min.css` renders `.govuk-checkboxes__input{opacity:0}` — the visible tick is drawn entirely via the adjacent `.govuk-checkboxes__label`; these 7 indicator checkboxes had no wrapper/label, so they were invisible regardless of their real checked state. Fixed in `SubmissionDetailsBlock.cshtml` and `BatchBlocks.cshtml` by adding the proper `govuk-checkboxes__item`+label structure. Also fixed "Number of blocks" incorrectly hidden on `BlockDetails.cshtml` for pre-cassetted Add mode (legacy always shows it — confirmed `txtNoBlocks.Text = "1"` is set unconditionally and `CreateMultiBlocks` explicitly supports `bIsPreCassetted`); bulk-creation now advances through `PreBookedBlockRefs` sequentially instead of the free-text auto-increment scheme when pre-cassetted. Confirmed the "Or pick a histology reference type"/"Get Next Ref" workflow (Issue 3) is an already-verified, correct GDS redesign from Run #28 — no change needed. Build: 0 errors. Tests: 190 passed, 1 skipped, 0 failed. | **Done** | — | Checkbox markup fix (2 files); `BlockDetails` Number of blocks fix; `BlockDetailsModel.LoadAnimalAsync`/`LoadSupportingDataAsync` corrected | [src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml](../src/Histo.Web/Pages/Submissions/SubmissionDetailsBlock.cshtml), [src/Histo.Web/Pages/Batches/BatchBlocks.cshtml](../src/Histo.Web/Pages/Batches/BatchBlocks.cshtml), [src/Histo.Web/Pages/Blocks/BlockDetails.cshtml.cs](../src/Histo.Web/Pages/Blocks/BlockDetails.cshtml.cs) |
| 44 | 2026-09-07 | 5 | `GitHub Copilot` | **ViewSubmissions pagination font colour + page slowness fixed:** Root-caused the pagination colour mismatch: GOV.UK's CSS gives the current-page pagination item a blue background with white link text via the `:link`/`:visited` pseudo-classes, which never match a `<button>` — `ViewSubmissions`'s POST-based pagination (`_PaginationPost.cshtml`) uses buttons, so the shared `.app-link-button` class's default blue text rendered blue-on-blue for the current page instead of white-on-blue. Fixed with a scoped `.govuk-pagination__item--current .app-link-button{color:#fff}` override in `Styles.css` (fixes the same latent bug on every other POST-paginated grid too, since it's a shared stylesheet rule). Root-caused the slowness: `ViewSubmissionsModel.LoadLookupsAsync` awaited 5 independent lookup calls (Users/Projects/Contacts/Species/Fixations) sequentially, and each POST handler then awaited the search query after that — 6 sequential DB round trips per request. Fixed via `Task.WhenAll`, running all lookups concurrently with each other and with the search query. Build: 0 errors. Tests: 190 passed, 1 skipped, 0 failed. | **Done** | — | `.govuk-pagination__item--current .app-link-button` colour override; `LoadLookupsAsync`/`OnPostSearchAsync`/`OnPostSelectAsync` parallelized | [src/Histo.Web/wwwroot/css/Styles.css](../src/Histo.Web/wwwroot/css/Styles.css), [src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml.cs](../src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml.cs) |
| 49 | 2026-09-08 | 5 | `GitHub Copilot` | **Pick List Maintenance — GDS "one thing per page" split (Add/Edit separated from List):** Analysed `PickListMaintenance.cshtml` → `EditLookupItem.cshtml` flow and confirmed `EditLookupItem.cshtml` combined three responsibilities on one page (item list, Add form, Edit form), violating GDS "one thing per page". Enumerated all 14 editable lookup tables' real field shapes via `GetEditableLookupProcs` + `sys.parameters` on every Add SP: confirmed only 2 variants exist — "Code-keyed" (12 tables: `@Code`/`@Description`/`@IsActive`) and "Area-scoped/ID-keyed" (Contacts 18, Projects 19: `@Area`/`@Description`/`@IsActive`/`@ID`, no `@Code`). Split into 3 pages: new `LookupItems.cshtml(.cs)` (read-only item list + "Show deactivated" filter + "Add item" button + per-row "Change" links), new `AddLookupItem.cshtml(.cs)` (dedicated Add form), and `EditLookupItem.cshtml(.cs)` (stripped down to Edit-only, redirects to `LookupItems` on success, returns a safe redirect instead of silently rendering blank if the item isn't found). Added `LookupTableSchema` (internal static class) with `ShowAreaColumn`/`HasCodes` so all 3 pages derive field shape identically instead of duplicating the logic. Updated `PickListMaintenance.cshtml`'s "Edit items" link and `HelpSectionMap.cs`. No existing test file existed for `EditLookupItemModel` before this change, so no test breakage risk; added 12 new tests across 3 new test files. Build: 0 errors. Tests: 267 passed, 4 pre-existing unrelated `BatchesReceivedModelTests` failures, 1 skipped. | **Done** | — | 3-page split (list/add/edit) + shared field-shape schema | [src/Histo.Web/Pages/Admin/LookupItems.cshtml.cs](../src/Histo.Web/Pages/Admin/LookupItems.cshtml.cs), [src/Histo.Web/Pages/Admin/AddLookupItem.cshtml.cs](../src/Histo.Web/Pages/Admin/AddLookupItem.cshtml.cs), [src/Histo.Web/Pages/Admin/EditLookupItem.cshtml.cs](../src/Histo.Web/Pages/Admin/EditLookupItem.cshtml.cs), [src/Histo.Web/Pages/Admin/LookupTableSchema.cs](../src/Histo.Web/Pages/Admin/LookupTableSchema.cs) |
| 48 | 2026-09-08 | 5 | `GitHub Copilot` | **EditQualityDataTest — error summary links not clickable (GDS violation):** User reported the error summary on `EditQualityDataTest.cshtml` rendered as plain `<li>@Model.Error</li>` with no way to click through to the offending field. Confirmed this app already has an established shared `_ErrorSummary.cshtml` partial (`IReadOnlyDictionary<string,string>` field-id→message, rendering `<a href="#FieldId">`), previously rolled out to the Search module, that this page had never adopted. Replaced the single `Error` string with a field-keyed `Errors` dictionary (`QCCode`/`DispatchedDate`/`DispatchedBy`/`DispatchedTo`/`RemedialAction`/`ArchiveLocation`/`ArchivedDate`), switched `OnPostAsync` to accumulate every validation failure in one pass instead of stopping at the first, added a separate non-linked `ConcurrencyError` for the optimistic-concurrency case (matching `EditQCNote`'s existing convention), and added GDS field-level error markup (`govuk-form-group--error`/`govuk-error-message`/`aria-describedby`) to all 7 fields. Added 5 new tests. Build: 0 errors. | **Done** | — | Field-keyed `Errors` dictionary + shared `_ErrorSummary` partial + field-level GDS error markup | [src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml.cs](../src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml.cs), [src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml](../src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml) |
| 47 | 2026-09-08 | 5 | `GitHub Copilot` | **BlockTestRepository.UpdateAsync — missing @Code/@EnteredBy/@PremiumCharge params on save:** User reported `Microsoft.Data.SqlClient.SqlException: Procedure or function 'EditBlockStain' expects parameter '@Code', which was not supplied` when saving a QC test. Confirmed via `sys.parameters` that the real `EditBlockHistology`/`EditBlockAntibodies`/`EditBlockStain` signatures all require `@Code`, `@EnteredBy` (varchar) and `@PremiumCharge` (varchar) in addition to the already-wired fields — none of the three were being sent. Confirmed via `GetBatchBlockHistology`/`Antibodies`/`Stain`'s real SELECT columns that `EnteredBy`/`PremiumCharge` exist on the read side too but were never mapped onto the `BlockTest` model. Added both as nullable properties, mapped in `BlockTestRepository.Map()`. Fixed `UpdateAsync` to send `@Code` (`test.Code`), `@EnteredBy` (always the current session user — legacy `QualityData.aspx.vb` always overwrites this on every save, never preserves the old value), and `@PremiumCharge` (round-tripped unchanged — legacy never edits this field from the QC screen). `EditQualityDataTest.cshtml.cs`'s `updated` object now sets `PremiumCharge = Test.PremiumCharge` so the round-trip doesn't null it out. Build: 0 errors. | **Done** | — | 2 new model properties + 3 missing SP params wired | [src/Histo.Histology/Models/BlockTest.cs](../src/Histo.Histology/Models/BlockTest.cs), [src/Histo.Histology/Repositories/BlockTestRepository.cs](../src/Histo.Histology/Repositories/BlockTestRepository.cs), [src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml.cs](../src/Histo.Web/Pages/QC/EditQualityDataTest.cshtml.cs) |
| 46 | 2026-09-08 | 5 | `GitHub Copilot` | **QualityData grid showing an extra "Special Stain" row vs legacy (4 rows instead of 3):** User reported submission 29399 shows 4 grid rows/an extra Test dropdown value where legacy shows 3. Read `clsBatchSummary.vb::CreateTestSummaryData`'s `BLOCK_HISTOLOGY` loop and confirmed via `grep -E "^\s*Case \d"` that only codes 1 (EO), 2 (H&E), 5 (H&E-BSE), 7 (Archive) get an explicit `Case` branch adding a worklist row — codes 3 (Special Stain), 4 (IHC-PrP), 6 (IHC-Other) fall through to `Case Else: 'Do nothing`, because those are gating/indicator flags only; the real per-test rows for those categories live in the separate `BLOCK_ANTIBODIES`/`BLOCK_STAIN` child tables, which already get their own unconditional rows. Confirmed live for submission 29399/block 317172: `BLOCK_HISTOLOGY` had 1 row (Code=3), `BLOCK_STAIN` had 3 real rows — legacy math is 0+0+3=3, current app was 1+0+3=4 (the redundant flag row). `BlockTestRepository.GetByBatchAsync` mapped every `BLOCK_HISTOLOGY` row unconditionally; fixed by filtering to `Code IN ("1","2","5","7")` before mapping, matching legacy's `Case Else` exclusion exactly. Same SP (`GetBatchBlocksByID`) on both sides — not a wrong-SP bug, purely a missing code-level filter. Build: 0 errors (`Histo.Histology` project). | **Done** | Code 7 "Archive" is still mapped with `TestType = BlockTestType.Histology` (a known minor discrepancy vs legacy's distinct "Archive" type) — deliberately not addressed to avoid scope creep into `BlockTestService`'s save/dispatch switch logic | Histology-code worklist filter added | [src/Histo.Histology/Repositories/BlockTestRepository.cs](../src/Histo.Histology/Repositories/BlockTestRepository.cs) |
| 45 | 2026-09-08 | 5 | `GitHub Copilot` | **Search module legacy-parity review + GDS remediation (7 screens) and search rationalisation:** Full comparison of the migrated Search module against the legacy Web Forms source (read directly from the supplied `Histopathology-2026-07-15\HistopathologySystem.2018` tree — no `.aspx` exists in this repo, so the legacy path was supplied by the user mid-session). Reviewed `SearchPMDates`, `SearchSubmissions`, `SearchMenu`, `SearchTest`, `SearchBlockRefs`, `SearchArchiveLocation`, `SearchUnUsedHistologyRefs`, `ViewImportedData`. **Corrected two mappings in the brief:** `ViewImportedData.aspx` maps to a real `Search/ViewImportedData.cshtml` (not `SearchSubmissions`), and `SearchMenu.aspx`/`SearchTest.aspx` are two distinct legacy pages, not one "Search Outputs Menu". **Fixes applied:** (1) **H1** — `SearchSubmissions` had downgraded 5 lookup-populated dropdowns (`ddlProject`/`ddlContact`/`ddlSpecies`/`ddlFixation`/`ddlUserArea`) to free-text boxes, so any typo silently returned zero rows; restored as `govuk-select` fed by `ILookupService` (Projects 19, Contacts 18, Species, Fixative 10, UserArea 13) following the `ViewSubmissions` ISS-030 pattern, Submitted area binding to the area **code** per `EditBatch`'s resolution logic; also fixed `SubmittedArea` bypassing `NullIfEmpty()`. (2) **H2** — `SearchTest` rendered Start/End date inputs that were bound but never reached the query; read the actual SP at `Support\1\GetTestRows.sql` and confirmed `GetTestRows` declares only `@ProjectContractDesc`/`@BatchType`, so date filtering is impossible without a DB change — removed the misleading controls rather than shipping a dead filter, and documented the constraint on `IBatchRepository`. (3) **H3** — restored legacy `IsDateRangeValid` guards (from ≤ to) on `SearchPMDates` and both `SearchSubmissions` ranges, plus the `revSubmissionNumber` `^[1-9]+[0-9]*$` rule. (4) **GDS** — only 3 of 11 search pages had an error summary and none used clickable links; added shared `_ErrorSummary.cshtml` rendering `<a href="#FieldId">` entries and rolled it out, with `govuk-form-group--error`/`govuk-error-message`/`aria-describedby` at field level. (5) **Sort/paging** — `SearchSubmissions` (default **ID DESC** per legacy `dvBatchesView.Sort`), `SearchPMDates` (both via the POST-safe partials + hidden carrier form) and `SearchUnUsedHistologyRefs` (converted to `GridPageModel`). (6) **CSV export** wired into the 4 pages missing it. (7) **`SearchBlockRefs` converted to a GET search** — restores legacy `Request.QueryString("SenderRef"/"HistologyRef")` deep-linking **and fixes a latent bug found while doing so**: the page already used the GET `_SortableHeader`/`_Pagination` partials against POST-bound criteria, so sorting or paging silently dropped the search terms and rendered an empty grid; ICC_Sub data link re-added (legacy `lbViewImportedData`). (8) **GDS date input** — replaced every native `<input type="date">` in the module with the three-field component: new `DateParts` (parts held as strings so invalid input survives the round-trip; rejects partial input, non-existent dates via `DaysInMonth`, and non-4-digit years) and `_DateInput.cshtml`; `SearchPMDates` dates are now blank-on-load and both mandatory, matching legacy. (9) **Rationalisation** — `SearchSample.cshtml(.cs)` deleted: it called the same `GetAnimalsBySenderRefAsync` and rendered the same 2 columns as `SearchSender`, with no picker callers and no tests; `SearchSender` **kept** (removal would break `CopyBatch`'s picker flow); `ViewSamples` confirmed correctly located at `/Search/ViewSamples` with no duplicate; `SearchMenu` gained the missing "View samples" and "View old ICC_Sub data" entries. Build: 0 errors. Tests: 217 passed, 1 skipped, 6 failed (**all 6 pre-existing — verified by stashing the working tree and re-running to establish a 199-passed/6-failed baseline**). | **Done** | (1) `GetTestRows.sql` in `Support\1\` selects `PROJECTCONTRACTCODE`/`SUBMITTEDAREA` but the code maps results to `TestItemRow{Description,Count}` — if the live SP matches that script, `SearchTest` renders blank descriptions and zero counts; needs DB verification, not fixed. (2) Empty-string vs NULL semantics: legacy passed `""` for unselected filters, the migrated code passes `NULL` via `NullIfEmpty` — needs a DB-side check that each SP tests `IS NOT NULL` rather than `<> ''`. (3) Non-clickable error summaries remain outside the Search module (`BatchBlocks`, `BlockDetails`, `SubmissionDetailsBlock`, `AddSubmission`, `BatchDetails`, plus ~25 files using `List<string>`); deliberately not swept because the error models differ per page and several are page-level (load/save failure) errors that GDS does not require to link anywhere. (4) `SearchTest` legacy cross-tab analytics and test-type checkbox filtering remain unported (accepted simplification, Parity Audit §2 row 54). | 5 lookup dropdowns restored; dead date filter removed; 3 date-range + 1 numeric validation restored; shared linked error summary; sort/paging on 3 grids; CSV export on 4 pages; GET deep-linking + latent sort-drops-criteria bug fixed; GOV.UK date input component; `SearchSample` removed | [src/Histo.Web/Pages/Search/SearchSubmissions.cshtml](../src/Histo.Web/Pages/Search/SearchSubmissions.cshtml), [src/Histo.Web/Pages/Search/SearchSubmissions.cshtml.cs](../src/Histo.Web/Pages/Search/SearchSubmissions.cshtml.cs), [src/Histo.Web/Pages/Search/SearchPMDates.cshtml](../src/Histo.Web/Pages/Search/SearchPMDates.cshtml), [src/Histo.Web/Pages/Search/SearchPMDates.cshtml.cs](../src/Histo.Web/Pages/Search/SearchPMDates.cshtml.cs), [src/Histo.Web/Pages/Search/SearchBlockRefs.cshtml](../src/Histo.Web/Pages/Search/SearchBlockRefs.cshtml), [src/Histo.Web/Pages/Search/SearchBlockRefs.cshtml.cs](../src/Histo.Web/Pages/Search/SearchBlockRefs.cshtml.cs), [src/Histo.Web/Pages/Search/SearchTest.cshtml](../src/Histo.Web/Pages/Search/SearchTest.cshtml), [src/Histo.Web/Pages/Search/SearchTest.cshtml.cs](../src/Histo.Web/Pages/Search/SearchTest.cshtml.cs), [src/Histo.Web/Pages/Search/SearchUnUsedHistologyRefs.cshtml](../src/Histo.Web/Pages/Search/SearchUnUsedHistologyRefs.cshtml), [src/Histo.Web/Pages/Search/SearchUnUsedHistologyRefs.cshtml.cs](../src/Histo.Web/Pages/Search/SearchUnUsedHistologyRefs.cshtml.cs), [src/Histo.Web/Pages/Search/SearchMenu.cshtml](../src/Histo.Web/Pages/Search/SearchMenu.cshtml), [src/Histo.Web/Pages/Shared/_ErrorSummary.cshtml](../src/Histo.Web/Pages/Shared/_ErrorSummary.cshtml), [src/Histo.Web/Pages/Shared/_DateInput.cshtml](../src/Histo.Web/Pages/Shared/_DateInput.cshtml), [src/Histo.Web/DateParts.cs](../src/Histo.Web/DateParts.cs), [tests/Histo.Tests/Unit/SearchValidationTests.cs](../tests/Histo.Tests/Unit/SearchValidationTests.cs) |

| 50 | 2026-09-09 | 5 | `GitHub Copilot` | **LookupItems pagination/sorting regression + BatchesForEditing/BatchStatus test-bug fixes:** `LookupItemsModel.OnGetAsync` had silently regressed to a stub that never populated `Items`, leaving the grid empty; restored `Items = await _lookups.GetLookupDataAsync(TableId, includeInactive: true)` + `LookupTableSchema.HasCodes(Items)` + `PopulateGridViewData`. Investigated "test cases failed" report: found 4 pre-existing failures in `BatchesReceivedModelTests.cs` — missing `await` on `async Task<IActionResult>` calls, and a stale redirect-target assertion (`/Batches/BatchDetails` expected, production code correctly redirects to `/Batches/BatchBlocks`); fixed all 4. Fixed `BatchesForEditingModel`'s default sort (no column clicked) always evaluating ascending regardless of `SortDesc`. Fixed `BatchesForArchiving.cshtml`'s back link — hardcoded to `/Archive/ArchiveMenu`, creating an infinite loop with `ArchiveMenu`'s own `Session.ReturnPage`-based back link; changed to `asp-page="/Index"` matching sibling list pages. Build: 0 errors. Tests: 274 passed, 1 skipped, 0 failed. | **Done** | — | Regression fix + 6 test/navigation bugs | [src/Histo.Web/Pages/Admin/LookupItems.cshtml.cs](../src/Histo.Web/Pages/Admin/LookupItems.cshtml.cs), [src/Histo.Web/Pages/Batches/BatchesForEditing.cshtml.cs](../src/Histo.Web/Pages/Batches/BatchesForEditing.cshtml.cs), [src/Histo.Web/Pages/Archive/BatchesForArchiving.cshtml](../src/Histo.Web/Pages/Archive/BatchesForArchiving.cshtml), [tests/Histo.Tests/Unit/BatchesReceivedModelTests.cs](../tests/Histo.Tests/Unit/BatchesReceivedModelTests.cs) |
| 51 | 2026-09-09 | 5 | `GitHub Copilot` | **BatchDetails/ArchiveBlocks/ArchiveTissues field-parity fixes + app-wide "Not received" status label:** `BatchDetails.cshtml.cs`'s Project/Contact lookups were missing `includeInactive: true`, dropping deactivated records from resolution. `ArchiveBlocks`/`ArchiveTissues` were missing the Archive location display name (raw code shown instead), sortable Block ref/Tissue columns, and 7 summary fields (Entered/Submitted by/area, Project/Contract code, Submission date, Pathologist, Species) — added via a new shared `BatchSummaryDisplayResolver` helper (factors out the resolution logic previously copy-pasted per page). `BatchStatus.DisplayName`/`NormalizeDisplayText` corrected — live DB (`luStatus`) confirmed Submitted(`"1"`) displays as "Not received", not "Not started" as a prior session had set it; applied across `ViewSubmissions.cshtml` (2 places) and `EditSubmissionStatus.cshtml`. Build: 0 errors. | **Done** | — | `BatchSummaryDisplayResolver` shared helper added; 3 pages + `BatchStatus.cs` fixed | [src/Histo.Web/Pages/BatchSummaryDisplayResolver.cs](../src/Histo.Web/Pages/BatchSummaryDisplayResolver.cs), [src/Histo.Web/Pages/Batches/BatchDetails.cshtml.cs](../src/Histo.Web/Pages/Batches/BatchDetails.cshtml.cs), [src/Histo.Web/Pages/Archive/ArchiveBlocks.cshtml.cs](../src/Histo.Web/Pages/Archive/ArchiveBlocks.cshtml.cs), [src/Histo.Web/Pages/Archive/ArchiveTissues.cshtml.cs](../src/Histo.Web/Pages/Archive/ArchiveTissues.cshtml.cs), [src/Histo.Core/Domain/BatchStatus.cs](../src/Histo.Core/Domain/BatchStatus.cs) |
| 52 | 2026-09-09 | 5 | `GitHub Copilot` | **EditBatch/BatchesForEditing/EditSubmissionStatus — button alignment, ReturnUrl/ReturnPageQuery context restore, missing summary fields:** `EditBatch.cshtml` "Samples" link moved into the same button-group as Save/Cancel per user request. Added `ISessionService.ReturnPageQuery` (carries the sort/page query string separately from the bare page-name `ReturnPage`, since `RedirectToPage` requires a bare name) and a `ParseReturnQuery()` helper on both `EditBatch`/`EditSubmissionStatus` so Cancel/Back/post-save redirects restore full list context (sort/page). `BatchesForEditing` now captures `Session.ReturnPageQuery` on row select. `EditSubmissionStatus` gained missing "Submitted by"/"Submitted area"/"Submission date" summary rows via `BatchSummaryDisplayResolver`. Build: 0 errors. | **Done** | — | `ISessionService`/`SessionService` extended; 3 pages updated | [src/Histo.Web/Services/ISessionService.cs](../src/Histo.Web/Services/ISessionService.cs), [src/Histo.Web/Pages/Batches/EditBatch.cshtml.cs](../src/Histo.Web/Pages/Batches/EditBatch.cshtml.cs), [src/Histo.Web/Pages/Batches/BatchesForEditing.cshtml.cs](../src/Histo.Web/Pages/Batches/BatchesForEditing.cshtml.cs), [src/Histo.Web/Pages/QC/EditSubmissionStatus.cshtml.cs](../src/Histo.Web/Pages/QC/EditSubmissionStatus.cshtml.cs) |
| 53 | 2026-09-09 | 5 | `GitHub Copilot` | **ViewSubmissions area-restriction false-positive fully reverted:** Hypothesised (based on `GetSearchBatchDetails` SP having no area-scoping logic and a `TOP 200` cap) that legacy restricts submission search results by the caller's user area, and implemented a `SubmittedArea` restriction. User then disproved this via direct observation — a Rejected-status record from a different VLA appeared in legacy's 2-row result for that filter, confirming legacy does **not** restrict by area. Fully reverted: removed `SubmittedArea`/`IsAreaRestricted`/`UserAreas` from `ViewSubmissionsModel`, the enforcement in `BuildCriteria()`, and the dropdown markup. Cross-checked `docs/migration-run-journal.md` — this exact fact (Submitted area not a legacy filter) was already established once before (Run #57/ISS-030); this was a reintroduced regression. Added 2 permanent regression-guard tests (`OnGetAsync_NeverAppliesAreaRestriction_RegardlessOfCallerRole`, reflection-based `HasNoSubmittedAreaOrAreaRestrictionProperty`). Build: 0 errors. | **Done** | — | Full revert + 2 regression-guard tests | [src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml.cs](../src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml.cs), [src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml](../src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml), [tests/Histo.Tests/Unit/ViewSubmissionsModelTests.cs](../tests/Histo.Tests/Unit/ViewSubmissionsModelTests.cs) |
| 54 | 2026-09-09 | 5 | `GitHub Copilot` | **AddSubmission legacy deep-analysis + "Check historical data" button reposition + Parity-Audit-Report closures:** Deep-read legacy `AddSubmission.aspx`/`.aspx.vb`; confirmed the "Search for a previously used sender reference" link already correctly replicates legacy's `lbLookup_Click` flow. Moved the "Check historical data for this sender ref" button out of a detached `<form method="get">` (illegally split from the main POST form) into the main form using HTML5 `formmethod="get" formaction=... formnovalidate`, repositioned under the sender-ref search link. Confirmed via `docs/TSE-NonTSE-Submission-Workflow-Redesign.md` this is a deliberate, previously-reviewed popup→page modernisation of legacy's "View Old ICC Sub Data" link. Closed `Parity-Audit-Report.md` D-3/D-4 (mouse-number bulk entry/Excel upload — Mouse Bioassay area being decommissioned) as won't-implement, and added D-8 (PG-Number/TSE Daybook auto-lookup, Neuropath-only) with the same disposition. Build: 0 errors. | **Done** | — | Form-nesting fix + 3 Parity-Audit-Report dispositions closed (docs-only) | [src/Histo.Web/Pages/Submissions/AddSubmission.cshtml](../src/Histo.Web/Pages/Submissions/AddSubmission.cshtml), [docs/Parity-Audit-Report.md](../docs/Parity-Audit-Report.md) |
| 55 | 2026-09-10 | 5 | `GitHub Copilot` | **Footer branding — GOV.UK crown replaced with APHA departmental logo:** Replaced the `govuk-footer__crown` SVG in `_Layout.cshtml` with the supplied `apha_icon.png`. Per GOV.UK Design System guidance (June 2025 brand refresh), the `govuk-header`/`govuk-footer` crown components are reserved for services on the gov.uk domain — since this is an internal APHA/Defra service, moved the logo out of the `govuk-footer__crown` inline slot into its own `govuk-footer__logo` row above the footer meta content instead of reusing that class. Corrected sizing from an initially-distorted `60×60` to the image's true `1140×1005` aspect ratio at `height="120" width="136"`, large enough to keep the "Animal & Plant Health Agency" wordmark legible (WCAG 1.4.5 — the asset is a full stacked lockup with baked-in text, not a small icon). Added a small `.govuk-footer__logo` margin rule to `Styles.css` (govuk-frontend has no built-in class for this slot). Build: 0 errors. | **Done** | — | Footer logo restructured + WCAG-compliant sizing | [src/Histo.Web/Pages/Shared/_Layout.cshtml](../src/Histo.Web/Pages/Shared/_Layout.cshtml), [src/Histo.Web/wwwroot/css/Styles.css](../src/Histo.Web/wwwroot/css/Styles.css), [src/Histo.Web/wwwroot/assets/images/apha_icon.png](../src/Histo.Web/wwwroot/assets/images/apha_icon.png) |
| 56 | 2026-09-10 | 5 | `GitHub Copilot` | **ViewSubmissions — Print submission notes gated on notes existing:** User requested "Print submission notes" only be enabled when the submission has recorded notes (`Submission comments` on `BatchDetails`). Found the existing precedent in `PrintSubmissionModel` (`HasNotes` mirroring legacy `EnableSubmissionNotes`, checking both `Batch.Comments` and `Batch.StatusComments`) and replicated it in `ViewSubmissionsModel`: added `HasNotes`, populated in `OnPostSelectAsync` via `_batches.GetByIdAsync(SelectedBatchId)`, and gated the button in `ViewSubmissions.cshtml` (disabled with a tooltip when false), matching `PrintSubmission.cshtml`'s existing disabled-button pattern exactly. Build: 0 errors. | **Done** | — | `HasNotes` gating added, matching existing `PrintSubmission` precedent | [src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml.cs](../src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml.cs), [src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml](../src/Histo.Web/Pages/Submissions/ViewSubmissions.cshtml) |

---

## Open Issues
 Root-caused and fixed in sequence via live Log stream evidence: (1) `Program.cs` — added `UseForwardedHeaders()` (trusting `X-Forwarded-For`/`Proto`/`Host`, cleared `KnownNetworks`/`KnownProxies`) since `dev-cde.azure.defra.cloud` has no App Service custom domain binding and is proxied by an external edge to the App Service's default hostname; (2) confirmed via `git log`/`git diff` that the fix wasn't reaching Dev because it was on a feature branch not yet merged to `develop` — merged via PR, redeployed; (3) since the proxy still didn't yield the correct `Request.Host` after redeploy (confirmed via captured `Location` header), added `PostConfigure<CookieAuthenticationOptions>("saml2", ...)` overriding `OnRedirectToLogin`/`OnRedirectToAccessDenied` to force the redirect to a **relative** URI (`PathAndQuery` only), removing the dependency on `Request.Host` entirely; (4) after that fix, the sign-in flow reached Entra ID and back to `/Saml2/Acs` but failed with `Microsoft.IdentityModel.Tokens.SecurityTokenInvalidAudienceException: IDX10214: Audience validation failed` — `saml2Config.AllowedAudienceUris` was never populated, so audience validation failed unconditionally regardless of the assertion; fixed by adding `saml2Config.AllowedAudienceUris.Add(saml2Config.Issuer)`. Confirmed via Log stream: SAML assertion now validates successfully (`NameIdentifier: dummy@defra.gov.uk`). Also fixed in the same session: `_Layout.cshtml` nav/user-context chrome was rendering for anonymous requests (e.g. on the `ServiceProblem` error page after hitting the decommissioned `/Login` route) because neither `_NavPartial.cshtml` nor `_UserContextPartial.cshtml` checked authentication state — wrapped both partial renders in `@if (User.Identity?.IsAuthenticated == true)`. Remaining blocker (infra, not code): `ResolveUserByEmailAsync` fails with `SqlException: Could not open a connection to SQL Server` — `ConnectionStrings:HistologyDb` Key Vault secret/SQL firewall not yet configured for Dev; raised as a separate infra/DBA ask. Build: 0 errors. | **Done** (SAML chain); DB connectivity **Open** | Missing `UseForwardedHeaders()`; absolute Host-derived cookie challenge redirect; empty `AllowedAudienceUris`; anonymous nav/user chrome leak; `HistologyDb` DB unreachable in Dev (infra) | [src/Histo.Web/Program.cs](../src/Histo.Web/Program.cs), [src/Histo.Web/Pages/Shared/_Layout.cshtml](../src/Histo.Web/Pages/Shared/_Layout.cshtml) |

| 57 | 2026-09-10 | 4 | `GitHub Copilot` | **New `Histo.WebJobs` project — annual histology reset WebJob + Application Insights + config alignment with `Histo.Web`:** Implemented per `docs/Azure-ManagedIdentity-EntraID-WebJob.md` §4 — a triggered WebJob (`Microsoft.Azure.WebJobs` SDK, `net10.0`) replacing the legacy SQL Server Agent job, executing `dbo.EditResetHistologyRef` on a `TimerTrigger` CRON `0 0 4 1 1 *` (04:00 UTC, 1 January annually) via `Microsoft.Data.SqlClient` with Managed Identity (`Authentication=Active Directory Managed Identity`, no secrets in code). Fixed 4 build errors while scaffolding: missing `Microsoft.Azure.WebJobs.Extensions` package (`TimerTrigger`/`TimerInfo` types unresolved), missing `<OutputType>Exe</OutputType>` (top-level statements require an executable), `Microsoft.Data.SqlClient` version mismatch vs `Histo.Infrastructure` (NU1605), and a silently no-op `Logging:LogLevel` section in `appsettings.json` (`ConfigureLogging` registered Console/Debug providers but never called `logging.AddConfiguration(context.Configuration.GetSection("Logging"))`, so level overrides never bound). Added Application Insights logging via `Microsoft.ApplicationInsights.WorkerService` + `services.AddApplicationInsightsTelemetryWorkerService(...)` (the correct pattern for a plain `HostBuilder` console host — two earlier attempts using `Microsoft.Azure.WebJobs.Logging.ApplicationInsights`/`ILoggingBuilder.AddApplicationInsights` failed to compile against the actual package APIs). Cross-checked `Histo.Web`'s real `appsettings.json`/`Program.cs` conventions and re-aligned WebJobs to match exactly: `APPLICATIONINSIGHTS_CONNECTION_STRING` moved to a top-level key (was nested under a non-functional doc-only `ApplicationInsightsConfig` section) using the identical `__APPINSIGHTS_CONNECTION_STRING__` placeholder Web uses; `ConnectionStrings:HistologyDb` changed to the same Managed-Identity connection string format Web has checked into its base `appsettings.json`; removed the non-functional `AzureStorageAccountConfig` doc block, replacing it with a real `AzureWebJobsStorage` placeholder. Removed `appsettings.Development.json` and the `.AddJsonFile($"appsettings.{EnvironmentName}.json", optional: true)` line entirely — confirmed `Histo.Web` has no code-level per-environment file loading for connection strings/App Insights either; both apps now rely purely on the base `appsettings.json` + Azure DevOps-injected App Service Application Settings (environment variables). Build: 0 errors, 1 pre-existing non-critical `NU1603` warning throughout. | **Done** | Infrastructure setup (Key Vault secrets, Managed Identity role assignment, SQL firewall rule, `CREATE USER ... FROM EXTERNAL PROVIDER` grant, CI/CD WebJob packaging step) not yet performed — documented in `src/Histo.WebJobs/WEBJOBS_IMPLEMENTATION.md`/`BICEP_INFRASTRUCTURE_REFERENCE.md`, awaiting Platform Engineering/DBA/DevOps action | Missing `Microsoft.Azure.WebJobs.Extensions` package; missing `OutputType=Exe`; `Microsoft.Data.SqlClient` version conflict; `Logging:LogLevel` not bound; App Insights wired via the wrong package/method twice before landing on `Microsoft.ApplicationInsights.WorkerService`; config layout misaligned with `Histo.Web`'s real conventions | [src/Histo.WebJobs/Histo.WebJobs.csproj](../src/Histo.WebJobs/Histo.WebJobs.csproj), [src/Histo.WebJobs/Program.cs](../src/Histo.WebJobs/Program.cs), [src/Histo.WebJobs/HistologyResetJob.cs](../src/Histo.WebJobs/HistologyResetJob.cs), [src/Histo.WebJobs/appsettings.json](../src/Histo.WebJobs/appsettings.json), [HistopathologySystem.slnx](../HistopathologySystem.slnx) |
---

## Open Issues

| ID | Date | Phase | Agent | File / Area | Issue | Severity | Owner | Status | Resolution |
|---|------|-------|-------|-------------|-------|----------|-------|--------|------------|
| ISS-R01 | 2026-08-25 | 1 | `GitHub Copilot` | Entra ID Portal | Entra admin must add `https://localhost:57879/Saml2/Acs` as a Reply URL for local dev testing | Medium | APHA ITU | Open | — |
| ISS-R02 | 2026-08-25 | 1 | `GitHub Copilot` | Entra ID Portal | `SG-HistopathologySystem-Users` security group not yet created; `Assignment required` not yet set to Yes on Enterprise Application | High | APHA ITU | Open | — |
| ISS-R03 | 2026-08-25 | 5 | `GitHub Copilot` | `Program.cs` | `UseStatusCodePagesWithReExecute` routes both 4xx and 5xx to ServiceProblem — a dedicated 404 page is needed | Low | Dev | Open | — |
| ISS-R04 | 2026-08-25 | 5 | `GitHub Copilot` | `EditQualityDataTest.cshtml.cs` | TC code SPs (`AddHistologyTCCode` etc.) not verified to exist in DB — run `SELECT name FROM sys.procedures WHERE name IN ('AddHistologyTCCode', ...)` | High | Dev | Open | — |
| ISS-R05 | 2026-08-25 | 5 | `GitHub Copilot` | `Admin/EditUser.cshtml` | `<form method="post">` still missing `asp-page` attribute (Run #83 fix was documented but not applied to file) — edit user may fail with 400 | High | Dev | Open | — |
| ISS-R06 | 2026-08-25 | 5 | `GitHub Copilot` | `QualityData.cshtml` + `EditQualityDataTest.cshtml` | Pagination not implemented on either page (deferred by user — needed for large batches) | Medium | Dev | Open | Deferred |
| ISS-R07 | 2026-08-25 | 5 | `GitHub Copilot` | `BatchesForDispatch.cshtml.cs` | Quick-Go validation used `GetByIdAsync` + status check; batch 29395 was in the grid but rejected because `GetCommonBatchTablesByID` returns a different status than `GetBatchesForDispatch` — fixed by validating against the already-loaded dispatch list | High | Dev | **Resolved** | Fixed: removed `GetByIdAsync` + status check; replaced with `Batches.FirstOrDefault(b => b.ID == id)` |
| ISS-R08 | 2026-08-25 | 5 | `GitHub Copilot` | `EditQualityDataTest.cshtml` | Custom JS (`@section Scripts`) used for show/hide of QCNote and Dispatched fields — not GDS-recommended pattern; replaced with native `govuk-radios__conditional` and `govuk-checkboxes__conditional` | Medium | Dev | **Resolved** | Fixed: removed Scripts block; added `data-aria-controls` + GDS conditional divs |
| ISS-R09 | 2026-08-25 | 5 | `GitHub Copilot` | `EditQualityDataTest.cshtml` | Structural div errors: stray `</div>` after QCCode form-group (leftover from QCNoteRow removal) + misaligned closing tags in dispatched conditional causing rendering break | High | Dev | **Resolved** | Fixed: removed stray div; re-indented dispatched conditional to correct nesting level |
| ISS-R10 | 2026-08-25 | 5 | `GitHub Copilot` | `QualityData.cshtml` | Test column showing raw lookup codes (e.g. "3", "4") — `BlockTest.TestDetails` always null; SP returns code not description | High | Dev | **Resolved** | Fixed: added `ResolveTestNamesAsync` to `QualityDataModel`; uses `GetHistologyTypesAsync()` + `GetLookupDataAsync` for antibodies/stain; `GetTestName(t)` called in view |
| ISS-R11 | 2026-08-25 | 5 | `GitHub Copilot` | `EditQualityDataTest.cshtml` | QC Note Ref only rendered when `QCNoteRef > 0` — hidden for new tests; user expected to always see it (legacy showed empty textbox) | Medium | Dev | **Resolved** | Fixed: always renders "QC note ref: None" or clickable link; removed conditional wrapper |
| ISS-R12 | 2026-08-25 | 5 | `GitHub Copilot` | `QualityData.cshtml` | QC note column removed (showed ref link inline in grid); row CSS colour classes (`qd-row--*`) removed — not a GDS documented pattern | Low | Dev | **Resolved** | Removed column and CSS; retained `govuk-tag` in Result column as the sole GDS-compliant status indicator |
| ISS-R13 | 2026-08-26 | 5 | `GitHub Copilot` | `_NavPartial.cshtml`, `BatchesForEditing.cshtml(.cs)` | Duplicate `Quality data`/`Edit submission status` nav links both pointing to `BatchesForEditing`; row-select redirected to `BatchDetails` instead of legacy-matching `EditBatch`; grid columns/headers didn't match legacy (`BatchesForEditing.aspx`) | High | Dev | **Resolved** | Nav link replaced with `Edit QC notes` → `/QC/QCNotes`; `OnPostSelect` redirect target fixed to `/Batches/EditBatch`; grid headers/columns corrected to legacy parity |
| ISS-R14 | 2026-08-26 | 5 | `GitHub Copilot` | `EditQCNote.cshtml(.cs)`, `QCNote.cs`, `QCNoteRepository.cs` | Legacy QC Note Ref summary box and Created by/Date created footer missing from migrated `EditQCNote` page | Medium | Dev | **Resolved** | Added `CreatedBy`/`DateCreated` to `QCNote` model + repository mapping; restored both sections in the Razor view |
| ISS-R15 | 2026-08-26 | 5 | `GitHub Copilot` | `QCNoteRepository.cs` | `Edit` button on `QCNotes.cshtml` silently stopped working — `RuntimeBinderException` from dynamic access to possibly-absent `Name`/`DateCreated` columns (introduced by ISS-R14 fix), swallowed by service-layer catch, causing silent redirect back to the list (same pattern as ISS-034) | High | Dev | **Resolved** | Replaced dynamic member access with `IDictionary<string, object>` + `TryGetValue` pattern |
| ISS-R16 | 2026-08-26 | 5 | `GitHub Copilot` | `QCNoteRepository.cs` | QC notes for antibody tests missing entirely from `EditQCNote`; blank notes showed empty instead of legacy's default Sender Ref/Histo Ref/Block Ref/Test table — `GetByIdAsync` only called `GetQCNoteHistStainTestInformation`, never `GetQCNoteAntibodiesInformation`, and only read the first row | High | Dev | **Resolved** | Added antibodies SP call (using `BatchType` from header row) concatenated with hist/stain rows; added `BuildDefaultNoteText` reproducing legacy `InsertSpaces` table |
| ISS-R17 | 2026-08-26 | 5 | `GitHub Copilot` | `QCNoteRepository.cs` | Created-by date missing next to username on `EditQCNote` — `DateCreated` returned as `dd/MM/yyyy` string, but dynamic/IDictionary reads bypass Dapper's `NullableDateTimeTypeHandler`, so `created is DateTime` always failed | Medium | Dev | **Resolved** | Added `ParseDate` helper mirroring `NullableDateTimeTypeHandler` (native passthrough + dd/MM/yyyy exact parse + general fallback) |
| ISS-R18 | 2026-08-27 | 5 | `GitHub Copilot` | `Histo.Histology` (`BlockTest`/`IBlockTestRepository`) | Per-block test selection (EO/H&E/IHC PrP/IHC Other/Special Stain) at block-creation time has no migrated equivalent — `BlockService.AddBlockAsync` does not create any `BlockTest` rows; `EditBlockHistology`/`EditBlockAntibodies`/`EditBlockStain` SPs only edit an existing row. Unverified whether an "Add" equivalent SP exists in the DB | Medium | Dev | Open | Deliberately not implemented — added a link from `SubmissionDetailsBlock` to the existing `QC/QualityData` page instead of guessing a stored procedure name |
| ISS-R19 | 2026-08-27 | 5 | `GitHub Copilot` | Submission wizard pages (`AddSample`, `SubmissionDetails`, `Cassetted`) | Route/query-based batch access state (Phase 0–2 of the TSE/NON-TSE redesign) was not extended to these three pages — they only use `BatchSubmissionID`/`AnimalID`/have no batch yet, so there was no unguarded access path to close | Low | Dev | Open | Deferred — no security or usability payoff identified for converting these specific pages |
| ISS-R20 | 2026-09-02 | 5 | `GitHub Copilot` | `Submissions/SampleSummary.cshtml` | **Copy sample is inverted vs legacy.** `BatchSummary.aspx.vb`/`BatchBlockSummary.aspx.vb` `btnCopySubmission_Click` removed `SV_SenderRef` (forcing a new Sender Ref) and then `AddSample.aspx` copied the source animal's tissues (`CopyBatchSubmissionWithAnimalID`) or blocks (`CopySampleBlocks`/`CopySamplePreBookedBlocks`). The migrated "Copy sample" link pre-fills the Sender Ref — the one thing legacy cleared — and copies no tissues or blocks at all | High | Dev | Open | — |
| ISS-R21 | 2026-09-02 | 5 | `GitHub Copilot` | `Batches/BatchDetails.cshtml(.cs)` | **Submission-date rule reversed.** Legacy `btnSave_Click` validated `eValidateEarliest` with "Must be today or later" for new batches. Create form renders `max="@DateTime.Today"` (today or earlier) and performs no server-side date-range check | Medium | Dev | Open | — |
| ISS-R22 | 2026-09-02 | 5 | `GitHub Copilot` | `Batches/BatchDetails.cshtml(.cs)` | **"At least one Block/Sample" Finish gate missing.** Legacy `ValidateData()` blocked Finish with "Atleast one Block/Sample must be added to the Submission." and only then set `BatchStatus = 1`. The migrated flow persists the batch as Submitted on "Create submission" before any sample exists, and "Finish" is only a link to `/Index` | High | Dev | Open | — |
| ISS-R23 | 2026-09-02 | 5 | `GitHub Copilot` | `Submissions/SubmissionDetailsBlock.cshtml(.cs)` | **View Submission mode not enforced on the block journey.** `SubmissionDetails` honours `Session.IsViewSubmissionMode` but `SubmissionDetailsBlock` has no equivalent gate — Save details, Add block, Add tissue and Delete are all available in the read-only View Submission journey | High | Dev | Open | — |
| ISS-R24 | 2026-09-02 | 5 | `GitHub Copilot` | `Batches/BatchDetails.cshtml.cs`, `Batches/EditBatch.cshtml.cs` | **Contacts and Projects no longer area-filtered.** Legacy bound `ddlContactName`/`ddlProjectCode` via `GetContactsByArea(sUserArea)` / `GetProjectsByArea(sUserArea)`. Both pages now call unfiltered `GetLookupDataAsync(18)` / `GetLookupDataAsync(19)`, exposing every area's values | Medium | Dev | Open | — |
| ISS-R25 | 2026-09-02 | 5 | `GitHub Copilot` | `Submissions/SubmissionDetails.cshtml(.cs)`, `Submissions/SubmissionDetailsBlock.cshtml(.cs)` | **Histology ref validation lost.** Legacy `BatchBlockSummary` validated inline edits for `NN/NNNNN` format, year not in the future, no duplicate on submission or in DB, and "not higher than or equal to the current next Histology Ref for the selected area". Histology Ref is now free text with no validation | Medium | Dev | Open | — |
| ISS-R26 | 2026-09-02 | 5 | `GitHub Copilot` | `Submissions/AddSubmission.cshtml(.cs)` | **Add sample reduced to Sender Ref + Neuropath checkbox.** Missing from legacy `AddSample.aspx`/`AddSubmission.aspx`: duplicate sender-ref check ("The specified Sender Reference is already present on the Submission"), `SenderRef1.CheckSenderRef()` format validation, the `cbUseValidation` TB Diagnostics override, mouse-number range bulk add (`MouseNumber1`/`MouseNumber2`), correlation file upload (`corrFile`/`btnUpload`), Neuropath PG-number TSE Daybook lookup (`GetAnimalFromDayBook` → species/project auto-population), and pre-booked block validation for cassetted batches (`GetPreBookedBlocks`/`ValidateAnimalBlocks`) | High | Dev | Open | — |
| ISS-R27 | 2026-09-02 | 5 | `GitHub Copilot` | `Batches/BatchDetails.cshtml`, `Batches/EditBatch.cshtml` | **Minor field-level parity gaps.** `SafeToHandle` rendered as a 2-state checkbox where legacy `ddlSafeToHandle` was a mandatory 3-state dropdown (`--`/Yes/No, `rfvSafeToHandle` with `InitialValue="-1"`) so "No" cannot be distinguished from unanswered; `SampleSameProjects` ("Same project code for all samples?", legacy `cbSampleOverride`) has no UI on either form; Cancel has no confirmation interruption (legacy `PromptBeforeSaveScript`); PM date is unvalidated free text | Low | Dev | Open | — |
| ISS-R28 | 2026-09-02 | 5 | `GitHub Copilot` | `Submissions/SampleSummary.cshtml.cs`, `Submissions/AddSubmission.cshtml.cs` | **Wet Tissue `SubmittedAs` code needs data verification.** Both pages route Wet Tissue on `submittedAsCode == "4"`, while `ValidationHelpers.IsBatchPreCassetted` uses `"5"` for Pre-Cassetted (confirmed). No `luSubmittedAs` seed data is present in the repo to confirm `"4"`; if Wet Tissue is actually `"3"` every Wet Tissue submission is routed to the block journey instead of `SubmissionDetails` | High | Dev | **Resolved** | `Cassetted.aspx.vb::btnYes_Click` (read directly) proved legacy never hardcodes a numeric code for Wet Tissue — it compares the resolved lookup *description* text ("Wet Tissue"). Replaced the guessed `== "4"` code check with `ValidationHelpers.IsWetTissueDescription`, resolving the code to its `LOOKUP_SUBMITTEDAS` (table 11) description first — correct regardless of the actual DB code value |

---

## Assumptions

- All SP names for TC codes (`AddHistologyTCCode`, `DeleteHistologyTCCode`, etc.) are sourced from `clsCheckBoxData.vb` in the legacy library and assumed to exist in the current database. Verify before running QC workflow end to end.
- `LOOKUP_PREMIUM_CHARGES = 17` confirmed from `Common.vb`.
- TC code result sets in `GetBatchBlocksByID`: set 7 = Stain TC, set 8 = Antibodies TC, set 9 = Histology TC (derived from `clsBatch.vb` BLOCK_*_TCCODES constants offset by 6).
- Security group option A (Enterprise Application assignment gate) is the correct approach for authentication; `tblUser` remains the authorisation gate.

---

## Session 2026-08-26 (15:23–16:53) — Submission creation flow + Edit Submission + Pick List fixes

| # | Fix area | Issue | Fix applied |
|---|---|---|---|
| 1 | `Cassetted.cshtml` / `BatchDetails.cshtml` | Submission creation fields all on Cassetted — does not match legacy two-step flow | Stripped Cassetted to type-selection only (BatchType + SubmittedAs); moved full creation form (Project, Pathologist, Species, Date, Histology/Antibody/Stain checkboxes) to `BatchDetails.cshtml?mode=create` |
| 2 | `BatchDetails.cshtml.cs` | Histology/Antibody/Stain checkboxes missing from create mode | Added `Create_Selected*Codes` bind properties, lookup loading, validation (mirrors `CheckHistology()`), and `SaveBatchTestSelectionsAsync` call after batch creation |
| 3 | `BatchDetails.cshtml` | "Batch not found. }" rendered as page text | `@if (!Model.IsCreateMode)` was only wrapping the LoadError section; `else if (Batch is null)` and main `else` were at top level — fixed to use inner `if/else if/else` inside outer `@if` block |
| 4 | `Cassetted.cshtml.cs` | Selections lost when user navigates back from BatchDetails | Added `OnGetAsync` pre-population: `BatchType` from `Session.BatchType`, `SubmittedAs` from `TempData["CreateSubmittedAsId"]` with `.Keep()` |
| 5 | `Cassetted.cshtml` | SubmittedAs defaulting to "Pre Cassetted Tissue" instead of blank | `asp-for="SubmittedAs"` on `<select>` caused SelectTagHelper to bypass blank placeholder for `int?` null; replaced with `name="SubmittedAs"` |
| 6 | `SearchSubmissions.cshtml.cs` | `CanEditSubmission` conditions inverted vs legacy — enabled for Received/OnHold/InProgress, disabled for Submitted/Rejected | Fixed to `Submitted \|\| Rejected` matching `ViewSubmissions` and legacy `ViewSubmissions.aspx.vb` |
| 7 | `BatchesForEditing.cshtml.cs` | `Session.ReturnPage` not set on select — EditBatch had no return context | Added `Session.ReturnPage = "/Batches/BatchesForEditing"` in `OnPostSelect` |
| 8 | `EditBatch.cshtml.cs` | Back link hardcoded to BatchDetails; Cancel/save always to BatchesForEditing | Added `ReturnPage` property using `Session.ReturnPage`; wired to back link, Cancel, and post-save redirect |
| 9 | `EditBatch.cshtml` | Back link and Cancel hardcoded to wrong pages | Changed to `Model.ReturnPage` |
| 10 | `EditLookupItem.cshtml.cs` | Area column shows numeric code instead of name for Contacts/Projects (tables 18/19) | Loaded `GetUserAreasAsync()` when `ShowAreaColumn`, built `AreaNameById` dictionary, table now displays name |
| 11 | `EditLookupItem.cshtml` | Area dropdown missing in Add/Edit form for tables 18/19 | Added Area `<select>` bound to `Model.UserAreas`; pre-populated on edit from `item.Area`; used in `OnPostAsync` for new items |
| 12 | `LookupRepository.cs` | `AddluContacts` error: "@ID was not supplied" on insert for ID-keyed tables | Added `else parameters.Add("ID", item.ID)` branch in `CreateLookupItemAsync` — mirrors legacy `BuildParamListID` which always passed `@ID` |
| 13 | `QualityData.cshtml.cs` | Test name resolution used `GetLookupDataAsync(histId)` which returns integer IDs not string codes; histology codes like "3","4" never matched | Switched to `GetHistologyTypesAsync()` which returns the string codes used in `tblBatchBlockHistology.Code` |

---

## Session 2026-09-02 — Create / Edit / View Submission workflow review + Pick List Management integration

Commit `91deb3f` — "Implement return URL handling in user and batch management forms".

### Implemented

| # | Area | Change |
|---|---|---|
| 1 | `BatchDetails.cshtml` (create mode) | Added "Manage submitted by list", "Manage project / contract codes" and "Manage pathologists" secondary buttons beneath the three fields — replaces legacy `btnNewSubmittedBy` / `btnNewProject` / `btnNewContact` |
| 2 | `BatchDetails.cshtml.cs` | `OnPostManagePickList(field)` handler + `CreateDraft` record; serialises every entered create-form value (including histology / antibody / stain code lists) to `TempData` before redirecting, and `RestoreCreateDraft()` re-applies it on return |
| 3 | `EditBatch.cshtml` / `.cs` | Same three buttons plus `EditDraft` save/restore covering all editable header fields, status and status comments |
| 4 | `PickListUserArea.cshtml` / `.cs` | `ReturnUrl` bind property, `SafeReturnUrl` (`Url.IsLocalUrl` guarded), context-aware back link, inset guidance text, "Return to submission" button, and `returnUrl` carried through the row Edit links and the post/redirect cycle. Removed the hard-coded `Done → /Batches/BatchDetails` dead-end |
| 5 | `UserMaintenance.cshtml` / `.cs` | `ReturnUrl` + `SafeReturnUrl`, context-aware back link, inset guidance, "Return to submission" button, `returnUrl` propagated to the Add user / Change links and the "Show deactivated items" GET form |
| 6 | `AddUser` / `EditUser` | `ReturnUrl` + `SafeReturnUrl`, hidden field in the form, and `returnUrl` preserved on every `RedirectToPage("/Admin/UserMaintenance")` |

**Routing:** Project → `PickListUserArea/19` (`LOOKUP_PROJECTS`), Pathologist → `PickListUserArea/18` (`LOOKUP_CONTACTS`), Submitted by → `UserMaintenance` (that list is user-backed, matching legacy `btnNewSubmittedBy`).

### Reviewed and confirmed correct

- Home → Cassetted (TSE / Non-TSE radio + SubmittedAs select) → `BatchDetails?mode=create` — functionally equivalent to legacy's two Home links plus `Cassetted.aspx`.
- Mandatory-field set on create matches legacy `ValidateMandatoryFields()`: Submitted By, Submitted Area, Project / Contract Code, Pathologist, Species, Submission Date.
- Histology / antibody / special-stain conditional rules match legacy `CheckHistology()` — at least one histology; antibodies required when IHC-PrP or IHC-Other selected; stains required when Special Stain selected.
- TSE / Non-TSE histology filtering matches legacy `HideOptions()` — Non-TSE removes IHC-PrP and H&E(BSE); TSE removes IHC-Other.
- Sample Summary retains Add / Edit / Delete / Copy / Done and the Bypass sort toggle; delete confirmations converted from browser `confirm()` to inline GOV.UK warning panels.
- Wet Tissue routes Edit sample to `SubmissionDetails`; block types route to `SubmissionDetailsBlock` — re-resolved server-side on POST rather than trusting a hidden field.
- `CanModifySamples` (Submitted / Rejected) correctly re-derives legacy `EnableDisableButtons` session-mode gating from batch status.

### Raised

ISS-R20 … ISS-R28 — see Open Issues above. Nine items: Copy sample inverted, submission-date rule reversed, missing "at least one sample" Finish gate, View mode unenforced on the block journey, Contacts/Projects area filtering lost, histology-ref validation lost, Add sample reduced to Sender Ref only, minor field-level gaps, and the unverified Wet Tissue `SubmittedAs` code.


